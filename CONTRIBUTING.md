# 贡献指南

感谢你愿意帮忙。这个项目最缺的不是代码，是**好的讲解**——把定义讲明白、把证明讲透。

---

## 目录

- [先跑起来](#先跑起来)
- [贡献一：改进某条讲解](#贡献一改进某条讲解)
- [贡献二：加题目](#贡献二加题目)
- [贡献三：加一整本书](#贡献三加一整本书)
- [贡献四：改代码 / 界面](#贡献四改代码--界面)
- [内容规范速查](#内容规范速查)
- [提交前自检](#提交前自检)
- [版权红线](#版权红线)

---

## 先跑起来

```bash
git clone <你的 fork>
cd First
npm start        # http://localhost:5173
```

没有依赖需要安装，Node 18+ 即可。

---

## 贡献一：改进某条讲解

最推荐、最容易被合并的贡献。

文件在 `data/books/*.js`。找到你想改的条目，重点看这几个字段：

```js
{
  id: 'thm-rolle',
  kind: 'theorem',                    // definition | theorem | formula | note
  name: '罗尔定理',
  statement: '……',                    // 严谨说法
  plain: '……',                        // 说人话（★ 最值钱）
  why: '……',                          // 为什么要这样想（★ 第二值钱）
  proof: '……',                        // 证明
  example: '……',                      // 最小例子
  pitfalls: ['……'],                   // 易错点
  tags: ['中值定理'],
  related: ['thm-lagrange', 'thm-fermat'],
}
```

判断 `plain` 写得够不够好的标准只有一个：

> **一个初二学生读完，能不能用自己的话复述出大意？**

反面例子（同义词换皮，等于没解释）：

> ❌ "极限就是当自变量无限趋近某点时，函数值无限趋近的那个值。"

正面例子（有画面、有比喻）：

> ✅ "你朝一堵墙走，每一步都走剩下距离的一半。你永远到不了墙，但你会越来越贴近它。
> 那个'你越来越贴近的那个位置'，就是极限。关键点是：你**不需要真的到达**。"

---

## 贡献二：加题目

1. 在 `data/questions/` 里新建（或加入）一个文件；
2. 在 `data/books/registry.js` 的 `QUESTION_FILES` 里登记路径；
3. 每道题的 `concepts` 填教材里的 item id（用 `data/concept-aliases.js` 里的别名也行）。

```js
{
  id: 'q-ch3-101',
  chapterId: 'ch3',
  concepts: ['thm-rolle'],
  tags: ['中值定理'],
  type: 'compute',        // choice | fill | judge | compute | proof
  difficulty: 2,          // 1 入门 / 2 基础 / 3 提高 / 4 挑战
  stem: '验证 <code>f(x)=x²-2x-3</code> 在 <code>[-1,3]</code> 上满足罗尔定理，并求 ξ。',
  answer: 'ξ = 1',
  solution: '思路：先验证三个条件……再令 f′(ξ)=0 解出 ξ。注意条件缺一不可。',
  source: 'original',
  license: 'CC-BY-4.0',
}
```

**难度怎么定**（组题组靠它做阶梯，请如实标）：

| 难度 | 标准 |
| --- | --- |
| 1 | 直接套定义 / 公式，一步出结果 |
| 2 | 需要一两步变形 |
| 3 | 要选对方法、多步推理 |
| 4 | 综合两三个知识点，或带思维陷阱 |

**解析要像人讲题**，建议结构：思路 → 步骤 → 结论 → 易错提醒。不要只写"选 B"。

**答案必须自己验算过**：积分题对结果求导、微分方程题代回原方程、面积体积题用已知公式校核。

---

## 贡献三：加一整本书

比如你想加线性代数：

1. 新建 `data/books/linalg-ch1.js`：

```js
export default {
  id: 'linalg-ch1',
  no: 1,
  title: '行列式',
  intro: '这一章在讲什么（2~3 句人话）',
  sections: [
    {
      id: 'linalg-ch1-1',
      no: '1.1',
      title: '二阶与三阶行列式',
      summary: '本节一句话概览',
      items: [ /* Item[] */ ],
    },
  ],
};
```

2. 在 `data/books/registry.js` 的 `BOOK_FILES` 里登记：

```js
{
  id: 'linalg',
  meta: {
    title: '线性代数',
    edition: '某某出版社 · 第 N 版',
    // ……
  },
  chapters: ['data/books/linalg-ch1.js'],
}
```

网站会自动出现这本教材的下拉选项，**不需要改任何 JS 逻辑**。

> 建议一个文件一个章节，多人协作时不容易冲突。

---

## 贡献四：改代码 / 界面

约束（有意为之，请尽量不要破坏）：

- **零依赖**：不引入 npm 包、CDN、构建工具。
- **零后端**：所有数据走 localStorage。
- **离线可用**：不依赖任何在线服务才能渲染。
- **图标**用内联 SVG（`js/icons.js`），不引入图标库。
- **公式**用 Unicode + `<code>`，**不要引入 LaTeX 渲染器**（会破坏离线与零依赖）。

换主题只需改 `styles/base.css` 顶部的 CSS 变量。

---

## 内容规范速查

完整规范见 [`docs/content-spec.md`](docs/content-spec.md)。最容易踩的三条：

### 1. 行内标记白名单

只允许：`<code>` `<b>` `<i>` `<sup>` `<sub>` `<br>`，以及块 `[[tip:文字]]`、`[[warn:文字]]`。

**不要用 Markdown**（`**粗体**`、`# 标题`、`$公式$` 都不会渲染，会原样显示成乱码观感）。
**不要用 LaTeX**（`\frac`、`\int`、`\lim` 等不会被渲染）。

### 2. 公式用 Unicode

```
ε δ → ∞ ∀ ∃ ∈ ≤ ≥ ≠ ≈ √ ∑ ± × ÷ ∫ ⁻¹ ² ³ ⁿ ₙ ′ ″ ξ η θ π Δ
```

例子：`<code>lim(x→0) sin x / x = 1</code>`、`<code>∫₀¹ x² dx = 1/3</code>`

### 3. 反引号陷阱

长文本建议用模板字符串（反引号）包裹，但**文本里绝对不能出现反引号或 `${`**，
否则会破坏文件。需要行内代码感就用 `<code>`。

---

## 提交前自检

```bash
npm run check
```

它会依次跑三件事，全部通过才返回 0：

### 1）`npm run validate` —— 内容校验

检查 id 唯一性、必填字段、`related` / `concepts` 引用是否存在、定理是否有 `proof`、
选择题是否有 `options`、难度是否在 1~4、行内标记是否配对、是否混入 LaTeX / Markdown。

- **error** → 退出码 1，必须修
- **warn** → 只是提示（例如"这个知识点配套题少于 3 道"）

### 2）`npm run smoke` —— 端到端冒烟测试

用 Node + 轻量 DOM 垫片真的把 `js/app.js` 跑一遍，44 项断言覆盖：
内容加载与索引、别名解析、组题难度阶梯、三个版块渲染、星标/错题本图标、
艾宾浩斯调度、答题闭环、导入导出。

### 3）`npm run verify-readme` —— README 事实核对

把 `README.md` 里所有**可验证的断言**拿真实代码与数据复算一遍：
条目计数、每章题目数与难度分布、第 5 节的题组 / 题目 / 复习示例、
导出 JSON 结构、文档里引用的每个文件路径是否存在。

> **如果你改了功能或内容，这个检查很可能会失败** —— 那是好事，说明 README 需要同步更新。
> 请一并改 `README.md`（或修正 `scripts/verify-readme.mjs` 里的期望值），
> 别让文档悄悄和代码脱节。

### 更严格

```bash
node scripts/validate-content.mjs --strict   # 把 warn 也当失败
```

---

## 版权红线

**这是唯一不能商量的一条。**

✅ 可以：

- 引用教材的**书名、版次、章节名、定义名、定理名**（事实性标题）
- 用你自己的话解释概念、重写证明、自编题目
- 引用定理的数学内容本身（数学不是版权客体）

❌ 不可以：

- 抄录教材、教辅、考研资料、网课的**原文段落**
- 抄录原书的例题、习题、解答原文
- 从付费题库抓取题目
- 上传扫描版书籍或答案

所有 `plain`、`why`、`proof`、`example`、`stem`、`solution` 字段都必须是**你自己写的**。
提交时默认你确认这一点。

发现侵权内容请开 issue，我们会立刻删除。

---

## 提交 PR

1. Fork → 建分支（`feat/rolle-plain-text` 之类）
2. 改内容或代码
3. `npm run check` 通过
4. PR 里说明：改了什么、为什么、如果是内容请说明来源（自己写的 / 哪本开放许可的书）

小而精的 PR 最好合并。一次只改一个知识点或一个功能，比一个大礼包好审得多。

PR 提交时会自动套用 [PR 模板](.github/PULL_REQUEST_TEMPLATE.md)，
里面的检查清单（尤其是版权确认那几条）请逐条勾选。

---

## 一起维护这个社区

- 参与本项目即表示你同意遵守 [行为准则（Contributor Covenant 3.0）](CODE_OF_CONDUCT.md)。
  一句话版本：**对人友善，对事严格。** 提问"这定理为什么这样"永远受欢迎，
  不要因为别人基础差就嘲讽；反过来，数学错误欢迎直接指出，越具体越好。
- 遇到骚扰、人身攻击、公开他人隐私等情况，请用
  [行为准则里的私密渠道](CODE_OF_CONDUCT.md#reporting-an-issue)举报，**不要用公开 issue**。
- 发现**安全问题**（例如注入、能执行脚本）请走 [`SECURITY.md`](SECURITY.md) 的私密渠道。
- 发现**数学错误或错别字**属于内容问题，用「内容纠错」issue 模板正常提即可。
