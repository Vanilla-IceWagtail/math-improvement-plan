# 统一换行符策略：仓库内一律使用 LF
#
# 为什么需要它：Windows 上常见的 core.autocrlf=true 会把工作区文件转成 CRLF，
# 而 macOS / Linux 贡献者克隆时可能又转回去，导致满屏"整文件改动"的假 diff。
# 在这里显式声明后，无论谁在什么系统上克隆，仓库内存储与检出行为都是一致的。

# 所有文本文件统一 LF
* text=auto eol=lf

# 明确这些是文本（便于 GitHub 正确统计语言、生成 diff）
*.js    text eol=lf
*.mjs   text eol=lf
*.json  text eol=lf
*.css   text eol=lf
*.html  text eol=lf
*.md    text eol=lf
*.txt   text eol=lf
.gitignore     text eol=lf
.gitattributes text eol=lf

# 明确这些是二进制，禁止任何换行转换
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.webp  binary
*.ico   binary
*.woff  binary
*.woff2 binary
*.ttf   binary
*.pdf   binary
*.zip   binary

# 语言统计：把 data/ 下的内容文件排除在"代码"之外，
# 避免这个项目的语言占比被 1MB 的教材数据文件带偏
data/books/*.js     linguist-documentation
data/questions/*.js linguist-documentation
docs/*.md           linguist-documentation

# 合并冲突时不要自动改动这些自动生成的锁文件
package-lock.json  -diff
pnpm-lock.yaml     -diff
