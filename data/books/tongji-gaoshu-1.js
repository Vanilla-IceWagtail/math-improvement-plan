// 同济《高等数学》上册（第八版）· 第1章 函数与极限
// 说明：本文件只登记章节名/定义名/定理名等事实性信息，全部讲解、通俗解释与证明均为本项目原创撰写。
export default {
  id: 'tongji-gaoshu-1',
  title: '高等数学（上册）',
  edition: '同济大学出版社 · 第八版',
  author: '同济大学数学科学学院',
  publisher: '高等教育出版社',
  subject: 'calculus',
  level: '大学本科一年级',
  tags: ['微积分', '一元函数'],
  licenseNote: '本书仅登记书名、章节名、定义名、定理名等事实性信息，全部讲解与证明均为本项目原创撰写，不抄录教材原文。',
  chapters: [
    {
      id: 'ch1',
      no: 1,
      title: '函数与极限',
      intro: '这一章做的事情只有两件：第一，把“函数”这个研究对象说清楚；第二，把“极限”这个工具装到手里。极限是整个微积分的地基——后面讲导数、积分、级数，全部都要靠它。所以这一章的定义看起来最啰嗦（一堆 ε、δ、N），但每一个符号都是在堵一个漏洞，读的时候别只背公式，要问自己：不这么写会出什么事？',
      sections: [
        {
          id: 'ch1-1',
          no: '1.1',
          title: '映射与函数',
          summary: '先把“对应关系”这件事讲精确：什么叫映射、什么叫函数、函数有哪些样子（单调、有界、奇偶、周期），以及哪些函数算“基本零件”。',
          items: [
            {
              id: 'def-mapping',
              kind: 'definition',
              name: '映射的定义',
              aka: ['映射', '单射', '满射', '一一映射'],
              statement: '设 <code>X</code>、<code>Y</code> 是两个非空集合。如果存在一个法则 <code>f</code>，使得对 <code>X</code> 中的<b>每一个</b>元素 <code>x</code>，按法则 <code>f</code> 在 <code>Y</code> 中都有<b>唯一确定</b>的元素 <code>y</code> 与之对应，就称 <code>f</code> 为从 <code>X</code> 到 <code>Y</code> 的映射，记作 <code>f : X → Y</code>，并记 <code>y = f(x)</code>。\n其中 <code>X</code> 叫定义域，记作 <code>D_f = X</code>；<code>y</code> 叫 <code>x</code> 的像，<code>x</code> 叫 <code>y</code> 的原像；所有像组成的集合 <code>R_f = { f(x) | x ∈ X }</code> 叫值域，显然 <code>R_f ⊆ Y</code>。\n若 <code>R_f = Y</code>，即 <code>Y</code> 中每个元素都至少被对应到一次，称 <code>f</code> 是<b>满射</b>；若 <code>X</code> 中不同元素的像一定不同（<code>x₁ ≠ x₂ ⟹ f(x₁) ≠ f(x₂)</code>），称 <code>f</code> 是<b>单射</b>；既单射又满射的映射叫<b>一一映射（双射）</b>。',
              plain: '映射就是一套“点名字”的规则。想象开班会：老师手里有全班名单（这就是集合 <code>X</code>），要往教室的座位上安排人（座位构成集合 <code>Y</code>）。规则有两条硬要求：\n1. 名单上<b>每个人</b>都必须被点到、被安排，不能有人漏掉；\n2. <b>每个人只能有一个座位</b>，不能张三同时坐 3 号和 5 号。\n满足这两条，这套安排就叫一个映射。\n至于“两个人都被安排到同一个座位”行不行？行——那只是不叫单射而已（这时座位有多余，叫“不满”）。反过来，如果有座位空着，就是“不满射”。如果人、座位正好一一配上、谁也不多谁也不少，那就是一一映射（双射），这是最“整齐”的一种。',
              why: '为什么要抠“每一个”和“唯一确定”这两个词？因为它们各堵一个漏洞。\n- 少了“每一个”：规则 <code>x ↦ 1/x</code> 在 <code>x = 0</code> 处没话说，那它就不是从全体实数出发的映射，必须把 0 从定义域里挖掉。这就是为什么讨论函数第一件事永远是看定义域。\n- 少了“唯一确定”：规则“<code>x</code> 对应到平方等于 <code>x</code> 的整数”在 <code>x = 4</code> 时会给出 2 和 -2 两个答案，这种“一对多”的东西不能当函数用，因为函数必须是“输入一个，输出一个”，否则后面算 <code>f(x) - f(x₀)</code> 时你都不知道该用哪个值。\n所以这两条不是数学家的洁癖，而是为了让“代入一个数、算出一个数”这件事永远可行。',
              example: '设 <code>X = {1, 2, 3}</code>，<code>Y = {a, b, c, d}</code>，规则是“1→a，2→b，3→b”。它满足“每个人都有像、每人只有一个像”，所以是映射；因为 <code>d</code> 没有原像，所以不是满射；因为 2 和 3 都指向 <code>b</code>，所以不是单射。\n再比如 <code>f(x) = x²</code>（<code>X = Y = ℝ</code>）：它是映射，但不是单射（<code>2</code> 和 <code>-2</code> 都映到 <code>4</code>），也不是满射（负数没有原像，因为实数的平方不会是负的）。',
              pitfalls: [
                '把“对应”和“映射”混为一谈：随便写一条关系（比如“<code>x</code> 对应到比 <code>x</code> 大的数”）不一定是映射，必须检查“每个都有、且只有一个”。',
                '以为值域 <code>R_f</code> 一定等于 <code>Y</code>。值域是“实际被对应到的像”的集合，可能只是 <code>Y</code> 的一小部分。',
                '写函数时不写定义域。同一个表达式 <code>y = x</code> 在 <code>x ∈ ℝ</code> 和 <code>x ∈ [0, 1]</code> 下是两个不同的函数。',
              ],
              tags: ['函数', '映射', '基础概念'],
              related: ['def-function', 'def-inverse-function', 'def-composite-function'],
            },
            {
              id: 'def-function',
              kind: 'definition',
              name: '函数的定义',
              aka: ['函数', '定义域', '值域', '自变量', '因变量'],
              statement: '设数集 <code>D ⊆ ℝ</code>。若存在对应法则 <code>f</code>，使得对每个 <code>x ∈ D</code>，都有唯一确定的实数 <code>y</code> 与之对应，则称 <code>f</code> 为定义在 <code>D</code> 上的<b>函数</b>，记作 <code>y = f(x)</code>，<code>x ∈ D</code>。\n<code>x</code> 叫自变量，<code>y</code> 叫因变量，<code>D</code> 叫定义域，函数值的集合 <code>f(D) = { y | y = f(x), x ∈ D }</code> 叫值域。\n本质上，函数就是从实数集 <code>D</code> 到实数集的一个映射，只是定义域和值域都换成了数集。',
              plain: '函数就是“投币机”：你塞进去一个数（自变量 <code>x</code>），它吐出来一个数（因变量 <code>y</code>）。同一台机器塞同样的币，吐出来的东西必须一样——这就是“唯一确定”。\n“定义域”就是“这台机器接受哪些币”：有的机器不收 0 元（因为要除以它），有的机器不收负数（因为要开平方）。\n“值域”就是“这台机器可能吐出来的所有东西的集合”。注意是“可能吐出的全部”，不是“今天吐出的”。',
              why: '初中把函数说成“一个 <code>x</code> 对应一个 <code>y</code>”，到了大学要补上“在哪个范围里对应”。原因是微积分里到处要做“在某个点附近”“在某个区间上”的讨论，范围一变，结论就变。\n举个扎心的例子：<code>f(x) = 1/x</code> 与 <code>g(x) = 1/x</code>，一个定义域是 <code>(0, +∞)</code>，一个是 <code>(-∞, 0)</code>，它们看起来一样，但在研究“<code>x → +∞</code> 时趋向多少”时给出的答案完全不同（一个是 0，一个是 0，但讨论 <code>x → 0⁺</code> 和 <code>x → 0⁻</code> 就一个 +∞ 一个 -∞）。所以“函数 = 表达式 + 定义域”必须捆在一起写。',
              example: '<code>f(x) = √(1 - x²)</code> 的自然定义域是 <code>[-1, 1]</code>（要让根号里非负），值域是 <code>[0, 1]</code>。\n<code>g(x) = (x² - 1)/(x - 1)</code> 的自然定义域是 <code>x ≠ 1</code>，虽然约分后是 <code>x + 1</code>，但它和 <code>h(x) = x + 1</code>（定义域为 <code>ℝ</code>）不是同一个函数，因为 <code>x = 1</code> 处前者没定义。',
              pitfalls: [
                '约分之后就把定义域也“约掉”了：<code>(x²-1)/(x-1)</code> 与 <code>x+1</code> 不是同一个函数。',
                '把值域当成定义域，或者求值域时只代几个点试。',
                '写“<code>y = f(x)</code> 的定义域是 ℝ”却不检查有没有除法、根号、对数。分母不为 0、偶次根号内非负、对数真数为正，这三条是最基本的检查清单。',
              ],
              tags: ['函数', '基础概念'],
              related: ['def-mapping', 'def-inverse-function', 'def-composite-function'],
            },
            {
              id: 'def-function-graph',
              kind: 'definition',
              name: '函数的图像',
              aka: ['图像', '函数图形'],
              statement: '设函数 <code>f</code> 的定义域为 <code>D</code>。点集 <code>{ (x, y) | y = f(x), x ∈ D }</code> 称为函数 <code>f</code> 的<b>图像</b>。',
              plain: '把定义域里每一个 <code>x</code> 和它对应的 <code>y</code> 当成一个点的坐标，全部画在坐标纸上，得到的图形就是函数图像。\n它有个一眼可辨的特征：<b>任意一条竖直线最多只能穿过图像一次</b>。因为同一个 <code>x</code> 只能有一个 <code>y</code>，如果一条竖线穿了两次，就说明这个 <code>x</code> 对应了两个值，那就不是函数了。',
              why: '图像的意义在于“把代数问题变成几何问题”。比如要比较 <code>f(x)</code> 和 <code>g(x)</code> 的大小，看谁在谁上面就行；要判断有没有零点，看图像有没有穿过 <code>x</code> 轴就行。\n后面讲介值定理时，我们会说“连续函数的图像是一条连绵不断的曲线，从 <code>x</code> 轴下方走到上方，必然穿过 <code>x</code> 轴”——这句话之所以成立，正是因为图像不会“跳”。',
              example: '<code>y = x²</code> 的图像是开口向上的抛物线，竖线穿过它最多两次？不对——穿过它<b>两次</b>的那是水平线（<code>y = 4</code> 与它交于 <code>x = ±2</code>）。竖线 <code>x = 2</code> 只穿过一次，交点只有一个 <code>(2, 4)</code>。\n反例：<code>x² + y² = 1</code> 的图像是单位圆，竖线 <code>x = 0</code> 穿过它两次（<code>y = ±1</code>），所以这个方程不表示 <code>y</code> 关于 <code>x</code> 的函数；但如果限制 <code>y ≥ 0</code>，即 <code>y = √(1 - x²)</code>，就变成函数了（上半圆）。',
              pitfalls: [
                '把“方程的图像”一律当成“函数的图像”，不去做竖线检验。',
                '画图时只看几个整数点就画，漏掉间断、尖点、空白区间。',
              ],
              tags: ['函数', '图像'],
              related: ['def-function', 'def-inverse-function'],
            },
            {
              id: 'def-monotone-function',
              kind: 'definition',
              name: '函数的单调性',
              aka: ['单调增加', '单调减少', '单调函数'],
              statement: '设函数 <code>f</code> 在区间 <code>I</code> 上有定义。若对 <code>I</code> 上任意两点 <code>x₁ < x₂</code>，恒有 <code>f(x₁) < f(x₂)</code>，则称 <code>f</code> 在 <code>I</code> 上<b>单调增加</b>；若恒有 <code>f(x₁) > f(x₂)</code>，则称 <code>f</code> 在 <code>I</code> 上<b>单调减少</b>。单调增加和单调减少的函数统称<b>单调函数</b>，<code>I</code> 称为单调区间。\n若把严格不等号 <code><</code>、<code>></code> 换成 <code>≤</code>、<code>≥</code>，则称为不减、不增（非严格单调）。',
              plain: '单调增加就是“<code>x</code> 往右走，<code>y</code> 就往上走”，一路只上不下（可以走平，但不能往回跌）；单调减少就是“<code>x</code> 往右走，<code>y</code> 一路往下”。\n它关心的不是某一点的高低，而是<b>整个区间上爬坡还是下坡</b>的趋势。就像爬山：你不能指着一块石头说“这里在上升”，必须说“这一段山路我一直在上升”。',
              why: '单调性是“能不能反推”的关键。因为如果 <code>f</code> 单调，那么知道 <code>f(x)</code> 的值就能唯一确定 <code>x</code>，函数就可以反解。这正是一一映射在函数语言里的样子，也是反函数存在的充分条件（见反函数的定义）。\n另外，后面证明“单调有界数列必有极限”时，单调性扮演的是“保证方向感”的角色：数列只会朝一个方向走，又被圈住了，那它就只能朝着某处“挤”出一个极限。',
              example: '<code>f(x) = x³</code> 在 <code>ℝ</code> 上单调增加（<code>x₁ < x₂ ⟹ x₁³ < x₂³</code>）。\n<code>f(x) = x²</code> 在 <code>(-∞, 0]</code> 上单调减少，在 <code>[0, +∞)</code> 上单调增加，但在整个 <code>ℝ</code> 上既不增也不减（<code>-1 < 1</code> 但 <code>f(-1) = f(1)</code>，不算严格增；<code>-2 < 1</code> 而 <code>f(-2) = 4 > f(1) = 1</code>，也不减）。\n常函数 <code>f(x) = 3</code> 是单调的（按非严格定义，既是不减也是不增），但不是严格单调的。',
              pitfalls: [
                '说“<code>f</code> 在 <code>x = k</code> 处单调”，单调是区间的性质，不是单个点的性质。',
                '把若干段单调区间用并集连起来。例如 <code>1/x</code> 在 <code>(0, +∞)</code> 减、在 <code>(-∞, 0)</code> 也减，但不能说它在 <code>(-∞,0) ∪ (0,+∞)</code> 上单调减少，因为 <code>-1 < 1</code> 而 <code>1/(-1) = -1 < 1 = 1/1</code> 其实是“增”的，跨过断点后比较失效。',
                '用特殊点代替证明：只算 <code>f(1) < f(2)</code> 就宣布单调增加。',
              ],
              tags: ['函数', '单调性'],
              related: ['def-bounded-function', 'def-inverse-function', 'thm-monotone-bounded'],
            },
            {
              id: 'def-bounded-function',
              kind: 'definition',
              name: '函数的有界性',
              aka: ['有界', '上界', '下界', '无界'],
              statement: '设函数 <code>f</code> 的定义域为 <code>D</code>，数集 <code>X ⊆ D</code>。\n若存在 <code>M > 0</code>，使得对一切 <code>x ∈ X</code> 都有 <code>|f(x)| ≤ M</code>，则称 <code>f</code> 在 <code>X</code> 上<b>有界</b>；若这样的 <code>M</code> 不存在，则称 <code>f</code> 在 <code>X</code> 上<b>无界</b>。\n等价的说法是：存在常数 <code>m</code>、<code>M</code>（不必同号），使对一切 <code>x ∈ X</code> 有 <code>m ≤ f(x) ≤ M</code>；这时 <code>M</code> 叫一个上界，<code>m</code> 叫一个下界。',
              plain: '有界就是“<code>y</code> 被关在两条水平线之间出不来”。比如 <code>f(x) = sin x</code>，永远在 <code>-1</code> 和 <code>1</code> 之间蹦跶，怎么都蹦不出去，所以有界。\n<code>f(x) = 1/x</code> 在 <code>(0, 1]</code> 上无界：<code>x</code> 越靠近 0，<code>y</code> 越大，你要多高它有多高，任何一条水平线都拦不住它。\n注意“有界”永远是相对于某个范围说的。同一个函数，在小范围内有界，在更大的范围内可能就无界了（<code>1/x</code> 在 <code>[1, 2]</code> 上有界，在 <code>(0, 1]</code> 上无界）。',
              why: '有界性值钱，是因为“有界”常常能升级成“极限存在”。最典型的例子是单调有界准则：一个数列如果只会朝一个方向走（单调）又被圈住了（有界），那它一定收敛。\n反过来，无界也能用来证“不收敛”：一个数列若有无界子列，它就不可能收敛（因为收敛数列一定有界）。所以有界性是把“看不出极限是多少”的难题变成“只要证明有界就够了”的桥梁。',
              example: '<code>f(x) = arctan x</code> 在 <code>ℝ</code> 上有界，因为 <code>-π/2 < arctan x < π/2</code>，取 <code>M = 2</code> 就够。\n<code>f(x) = x²</code> 在 <code>[-1, 1]</code> 上有界（<code>M = 1</code>），在 <code>ℝ</code> 上无界。\n数列 <code>aₙ = (-1)ⁿ</code> 有界（<code>|aₙ| = 1</code>），而 <code>aₙ = n</code> 无界。',
              pitfalls: [
                '把“有界”与“有极限”混为一谈：<code>aₙ = (-1)ⁿ</code> 有界但发散。有界只是收敛的必要条件，不是充分条件。',
                '说“<code>f</code> 有界”却不说明在哪个集合上。',
                '用“函数值看起来不大”代替证明，比如只算了几项就断定有界。',
              ],
              tags: ['函数', '有界性'],
              related: ['def-monotone-function', 'thm-conv-bounded', 'def-infinity-large'],
            },
            {
              id: 'def-odd-even-function',
              kind: 'definition',
              name: '函数的奇偶性',
              aka: ['奇函数', '偶函数'],
              statement: '设函数 <code>f</code> 的定义域 <code>D</code> 关于原点对称（即 <code>x ∈ D ⟹ -x ∈ D</code>）。\n若对一切 <code>x ∈ D</code> 有 <code>f(-x) = f(x)</code>，则称 <code>f</code> 为<b>偶函数</b>；\n若对一切 <code>x ∈ D</code> 有 <code>f(-x) = -f(x)</code>，则称 <code>f</code> 为<b>奇函数</b>。',
              plain: '偶函数就是“照镜子”：把图像沿 <code>y</code> 轴对折，左右两边完全重合。<code>x</code> 和 <code>-x</code> 的高度一样，比如 <code>f(x) = x²</code>：<code>2</code> 和 <code>-2</code> 都得到 <code>4</code>。\n奇函数是“转半圈”：把图像绕原点旋转 180 度，还是它自己。左边多高，右边就多低，比如 <code>f(x) = x³</code>：<code>f(2) = 8</code>，<code>f(-2) = -8</code>。\n有个好用的结论：奇函数的图像一定过原点（如果 0 在定义域里），因为 <code>f(0) = -f(0)</code> 逼出 <code>f(0) = 0</code>。',
              why: '定义里为什么必须先要求“定义域关于原点对称”？因为要写 <code>f(-x)</code>，得先保证 <code>-x</code> 也在这个函数的“受理范围”里。\n例如 <code>f(x) = x²</code>，<code>x ∈ [0, 2]</code>，此时 <code>f(-1)</code> 根本没定义，讨论奇偶性就是空谈。所以判断奇偶性第一步永远是先看定义域是否对称，不对称就直接结论：既非奇也非偶。',
              example: '<code>f(x) = x² + 1</code> 是偶函数；<code>f(x) = x³ - x</code> 是奇函数；\n<code>f(x) = x² + x</code> 既不是奇函数也不是偶函数（<code>f(1) = 2</code>，<code>f(-1) = 0</code>，既不等于 2 也不等于 -2）。\n<code>f(x) = 0</code>（定义域 <code>ℝ</code>）既是奇函数又是偶函数。',
              pitfalls: [
                '忘记先查定义域是否关于原点对称，就代入 <code>-x</code> 硬算。',
                '把 <code>f(-x) = -f(x)</code> 里的负号挪错位置，写成 <code>f(-x) = f(-x)</code> 之类的空话。',
                '认为奇函数一定无界或一定单调，都不对（<code>y = sin x</code> 是奇函数且有界，<code>y = sin x</code> 在 <code>ℝ</code> 上也不单调）。',
              ],
              tags: ['函数', '奇偶性'],
              related: ['def-function', 'def-function-graph'],
            },
            {
              id: 'def-periodic-function',
              kind: 'definition',
              name: '函数的周期性',
              aka: ['周期函数', '周期', '最小正周期'],
              statement: '设函数 <code>f</code> 的定义域为 <code>D</code>。若存在正数 <code>T</code>，使得对一切 <code>x ∈ D</code> 都有 <code>x + T ∈ D</code>，且 <code>f(x + T) = f(x)</code>，则称 <code>f</code> 为<b>周期函数</b>，<code>T</code> 称为 <code>f</code> 的<b>周期</b>。\n若在所有的周期中存在最小的正数，则称它为 <code>f</code> 的<b>最小正周期</b>。',
              plain: '周期函数就是“录像循环播放”：走过一段 <code>T</code> 之后，图像原样重复，一直重复下去。最熟悉的就是钟表上的指针位置、以及 <code>sin x</code>（每 <code>2π</code> 重复一次）。\n注意“<code>T</code> 是周期”不等于“最小正周期是 <code>T</code>”。比如常数函数 <code>f(x) = 1</code>，任何正数 <code>T</code> 都是它的周期，那就没有最小的正周期，所以它虽然是周期函数，却没有最小正周期。',
              why: '为什么定义里要加一句“对一切 <code>x ∈ D</code> 都有 <code>x + T ∈ D</code>”？因为如果定义域不够长，<code>f(x + T)</code> 可能压根没定义，等号就没意义。这句话实际上逼着周期函数的定义域向两边无限延伸（至少在正方向无限延伸）。\n所以像 <code>f(x) = sin x</code>，<code>x ∈ [0, 2π]</code> 这种被砍掉的定义域，就谈不上是周期函数。',
              example: '<code>sin x</code>、<code>cos x</code> 的最小正周期是 <code>2π</code>；<code>tan x</code> 的最小正周期是 <code>π</code>。\n<code>f(x) = sin(2x)</code> 的最小正周期是 <code>π</code>（因为 <code>sin(2(x + π)) = sin(2x + 2π) = sin(2x)</code>）。\n常数函数是周期函数但没有最小正周期。',
              pitfalls: [
                '以为周期函数的周期必须能取到最小正数。常数函数就是反例。',
                '求 <code>sin(ωx)</code> 的周期时忘记除以 <code>ω</code>：周期是 <code>2π/|ω|</code>，不是 <code>2π</code>。',
                '把“<code>f(x + T) = f(x)</code> 对某个 <code>x</code> 成立”当成周期，必须对定义域内所有 <code>x</code> 都成立。',
              ],
              tags: ['函数', '周期性'],
              related: ['def-function', 'def-elementary-functions'],
            },
            {
              id: 'def-inverse-function',
              kind: 'definition',
              name: '反函数',
              aka: ['反函数', '直接函数'],
              statement: '设函数 <code>f : D → f(D)</code> 是单射。对每个 <code>y ∈ f(D)</code>，存在唯一的 <code>x ∈ D</code> 使 <code>f(x) = y</code>，于是可以定义一个新函数 <code>f⁻¹ : f(D) → D</code>，<code>f⁻¹(y) = x</code>，称它为 <code>f</code> 的<b>反函数</b>。\n此时 <code>f</code> 的定义域是 <code>f⁻¹</code> 的值域，<code>f</code> 的值域是 <code>f⁻¹</code> 的定义域，并且 <code>f⁻¹(f(x)) = x</code>（<code>x ∈ D</code>），<code>f(f⁻¹(y)) = y</code>（<code>y ∈ f(D)</code>）。\n习惯上用 <code>x</code> 表示自变量，故常把 <code>y = f(x)</code> 的反函数写成 <code>y = f⁻¹(x)</code>。在同一坐标系中，<code>y = f(x)</code> 与 <code>y = f⁻¹(x)</code> 的图像关于直线 <code>y = x</code> 对称。',
              plain: '反函数就是“把机器的进出口对调”：原来给你原料出成品，现在给你成品问原料是什么。\n但这件事能做的前提是“成品不重复”——如果两台不同的原料给出同一个成品，那你拿到成品就不晓得该还哪个原料了。所以只有单射（一个原料对一个成品）才能反。<code>y = x²</code> 就不能整体反过来，因为成品 <code>4</code> 对应 <code>2</code> 和 <code>-2</code>；但如果限定 <code>x ≥ 0</code>，就变成 <code>x = √y</code>，可以反了。\n图像上，反函数就是把原图沿对角线 <code>y = x</code> 折过去（横纵坐标互换）。',
              why: '为什么“单射”是硬条件？因为函数的定义要求“一个输入只对应一个输出”。反函数把原来的输出当输入，如果同一个 <code>y</code> 有多个 <code>x</code>，那这个新规则就“一对多”，不满足函数定义。\n而“单调函数一定是单射”这个结论的用处就在这里：判断一个函数能不能反，往往不用逐个验证单射，只要看它在区间上是不是严格单调就行。所以求反函数前先看单调性，是标准套路。',
              example: '<code>y = 2x + 1</code>（<code>x ∈ ℝ</code>）的反函数：解出 <code>x = (y - 1)/2</code>，写成 <code>y = (x - 1)/2</code>。两者图像关于 <code>y = x</code> 对称。\n<code>y = eˣ</code> 的反函数是 <code>y = ln x</code>；<code>y = x³</code> 的反函数是 <code>y = ∛x</code>。\n<code>y = x²</code>（<code>x ∈ ℝ</code>）没有反函数；限制在 <code>[0, +∞)</code> 上后反函数为 <code>y = √x</code>。',
              pitfalls: [
                '不检查单射就直接“解出 <code>x</code>”，结果得到一对多的关系还以为那是反函数。',
                '把 <code>f⁻¹(x)</code> 理解成 <code>1/f(x)</code>。这两个完全无关：<code>f⁻¹</code> 是反函数，<code>1/f</code> 是倒数。',
                '求出 <code>x = g(y)</code> 就交卷，忘记把字母换回 <code>y = g(x)</code>（虽然只是记号，但考试和阅读习惯上都要求换）。',
              ],
              tags: ['函数', '反函数'],
              related: ['def-function', 'def-monotone-function', 'def-elementary-functions', 'thm-continuity-inverse'],
            },
            {
              id: 'def-composite-function',
              kind: 'definition',
              name: '复合函数',
              aka: ['复合函数', '中间变量'],
              statement: '设函数 <code>y = f(u)</code> 的定义域为 <code>D_f</code>，函数 <code>u = g(x)</code> 的定义域为 <code>D_g</code>，值域为 <code>R_g</code>。若 <code>R_g ∩ D_f ≠ ∅</code>，则对满足 <code>g(x) ∈ D_f</code> 的 <code>x</code>，可以确定一个函数 <code>y = f[g(x)]</code>，称为由 <code>g</code> 与 <code>f</code> 构成的<b>复合函数</b>，<code>u</code> 叫中间变量。\n这个复合函数的定义域是 <code>{ x | x ∈ D_g 且 g(x) ∈ D_f }</code>。',
              plain: '复合函数就是“流水线”：第一台机器把 <code>x</code> 加工成 <code>u</code>，第二台机器再把 <code>u</code> 加工成 <code>y</code>。\n关键在于“接口要对得上”：第二台机器只吃 <code>0</code> 到 <code>1</code> 之间的原料，而第一台吐出来的全在 <code>5</code> 以上，那这条流水线根本转不起来。\n比如 <code>f(u) = √u</code>（只吃非负数）与 <code>g(x) = -1 - x²</code>（只会吐出负数）：<code>g</code> 的输出永远是负的，<code>f</code> 永远不收，所以 <code>f[g(x)]</code> 这个复合函数无意义（定义域是空集）。',
              why: '定义里那句看似绕口的 <code>R_g ∩ D_f ≠ ∅</code>，就是在说“两台机器至少有一个能对上的口径”，否则复合函数的定义域是空集，等于白写。\n更实用的是后面那句：复合函数的定义域要<b>同时</b>满足两个条件——<code>x</code> 在 <code>g</code> 的定义域里，而且 <code>g(x)</code> 落在 <code>f</code> 的定义域里。很多同学求复合函数定义域时只查第一条，于是漏掉一半限制，这是最常见的失分点。',
              example: '设 <code>f(u) = √u</code>，<code>g(x) = 1 - x²</code>。则 <code>f[g(x)] = √(1 - x²)</code>，定义域要求 <code>1 - x² ≥ 0</code>，即 <code>x ∈ [-1, 1]</code>。\n再看 <code>f(u) = ln u</code>，<code>g(x) = x - 2</code>：<code>f[g(x)] = ln(x - 2)</code>，定义域 <code>x > 2</code>。\n反例：<code>f(u) = √(-u)</code>，<code>g(x) = x² + 1</code>，此时 <code>g(x) ≥ 1 > 0</code>，永远不能让 <code>-g(x) ≥ 0</code>，所以复合函数定义域为空。',
              pitfalls: [
                '求复合函数定义域时只看内层函数的定义域，忘记要求 <code>g(x)</code> 落在 <code>f</code> 的定义域内。',
                '把复合顺序搞反：<code>f[g(x)]</code> 与 <code>g[f(x)]</code> 通常完全不同，例如 <code>f(x) = x²</code>、<code>g(x) = x + 1</code> 时，前者是 <code>(x+1)²</code>，后者是 <code>x² + 1</code>。',
                '分解复合函数时拆得不彻底（比如把 <code>sin(2x + 1)</code> 拆成 <code>sin(2x)</code> 与 <code>+1</code>，正确拆法是 <code>y = sin u</code>，<code>u = 2x + 1</code>）。',
              ],
              tags: ['函数', '复合函数'],
              related: ['def-function', 'def-elementary-function', 'def-inverse-function'],
            },
            {
              id: 'def-elementary-functions',
              kind: 'definition',
              name: '基本初等函数',
              aka: ['幂函数', '指数函数', '对数函数', '三角函数', '反三角函数', '常数函数'],
              statement: '下列六类函数统称为<b>基本初等函数</b>：\n1. <b>常数函数</b>：<code>y = C</code>（<code>C</code> 为常数）；\n2. <b>幂函数</b>：<code>y = x^μ</code>（<code>μ</code> 为常数）；\n3. <b>指数函数</b>：<code>y = aˣ</code>（<code>a > 0</code>，<code>a ≠ 1</code>）；\n4. <b>对数函数</b>：<code>y = log_a x</code>（<code>a > 0</code>，<code>a ≠ 1</code>，<code>x > 0</code>），特别地 <code>y = ln x</code> 指以 <code>e</code> 为底的自然对数；\n5. <b>三角函数</b>：<code>y = sin x</code>、<code>y = cos x</code>、<code>y = tan x</code>、<code>y = cot x</code>、<code>y = sec x</code>、<code>y = csc x</code>；\n6. <b>反三角函数</b>：<code>y = arcsin x</code>、<code>y = arccos x</code>、<code>y = arctan x</code>、<code>y = arccot x</code>。',
              plain: '这六个家族是整个微积分的“乐高积木”。后面所有复杂的函数，都是用这些积木拼出来的（加减乘除、复合）。\n把它们记住的好处是：只要能算清楚积木的性质，拼出来的东西大多数性质也能跟着继承。比如这些积木在它们各自的定义区间上都是连续的，那么拼出来的初等函数在定义区间上也就天然连续——这就是后面“初等函数连续性”这条大结论的全部底气。',
              why: '为什么偏偏是这六类？因为它们各自对应一种最基本的“变化”方式：常数函数是“不变”，幂函数是“按次方放大”，指数函数是“按倍数增长”，对数函数是“指数的逆运算”，三角函数是“圆周运动的投影”，反三角函数是“由比值求角”。\n再加上三角函数是周期现象的通用语言，微积分里凡是要描述波动、振动、圆周运动，都绕不开它们。把六类定为“基本”，其实是在说：别的函数都由它们生成，所以我们只要研究这六类的性质，就等于掌握了全部初等函数。',
              example: '幂函数举例：<code>y = x</code>、<code>y = x²</code>、<code>y = √x = x^(1/2)</code>、<code>y = 1/x = x⁻¹</code>。\n指数函数 <code>y = 2ˣ</code> 过点 <code>(0, 1)</code>，<code>a > 1</code> 时单调增加；<code>y = (1/2)ˣ</code> 单调减少。\n反三角函数的值域要记牢：<code>arcsin x ∈ [-π/2, π/2]</code>，<code>arccos x ∈ [0, π]</code>，<code>arctan x ∈ (-π/2, π/2)</code>。正因为规定了值域，反三角函数才是单值的（否则 <code>sin x = 1/2</code> 有无数个解）。',
              pitfalls: [
                '把 <code>y = x⁰</code> 当成在整个 <code>ℝ</code> 上的常数函数 1：它在 <code>x = 0</code> 处无定义，定义域是 <code>x ≠ 0</code>。',
                '记错反三角函数的值域，写出 <code>arcsin(1/2) = 5π/6</code> 这类错误（正确的是 <code>π/6</code>）。',
                '把 <code>ln x²</code> 与 <code>(ln x)²</code> 混为一谈，前者定义域是 <code>x ≠ 0</code>，后者是 <code>x > 0</code>。',
              ],
              tags: ['函数', '基本初等函数'],
              related: ['def-elementary-function', 'def-inverse-function', 'thm-elementary-continuity'],
            },
            {
              id: 'def-elementary-function',
              kind: 'definition',
              name: '初等函数',
              aka: ['初等函数'],
              statement: '由<b>常数</b>和<b>基本初等函数</b>经过有限次的四则运算（加、减、乘、除）和有限次的复合所得到的、并能用一个式子表示的函数，称为<b>初等函数</b>。',
              plain: '初等函数就是“用积木搭出来的东西”，而且搭的步骤必须是有限步。\n比如 <code>y = sin(x²) + ln(x + 1)</code> 是初等函数：先幂、再正弦、再对数，最后相加，步骤数得清。\n反过来，如果某个函数需要“无穷多步”才能拼出来，或者要分段用不同的式子表达（像 <code>|x|</code> 用 <code>x ≥ 0</code> 与 <code>x < 0</code> 两段写），严格说它就不算初等函数——不过要注意，有些分段函数也能找到单一的初等表达式（比如 <code>|x| = √(x²)</code>）。',
              why: '为什么要专门圈出“初等函数”这个范围？因为教材后面会证明一条非常好用的大定理：<b>初等函数在其定义区间内是连续的</b>。这句话让你以后求极限时，遇到初等函数直接把点代进去就行，不用每次都回到 ε-δ 去抠。\n换句话说，这个定义是在给一条“免检通道”划范围：只有初等函数才能免检。像分段函数在分段点附近就必须单独检查连续性，不能直接代入。',
              example: '初等函数：<code>y = x³ + 2x - 1</code>、<code>y = e^(sin x)</code>、<code>y = (ln x)/(1 + x²)</code>、<code>y = arctan(1/x)</code>。\n非初等函数的典型例子：分段函数 <code>f(x) = x（x > 0），f(x) = -x（x ≤ 0）</code>，虽然在 <code>ℝ</code> 上连续，但用初等函数的定义去套时通常不把它归入初等函数（不过它等于 <code>|x| = √(x²)</code>，所以又可以写成初等函数的形式）。',
              pitfalls: [
                '以为“能求出导数的函数就是初等函数”，这是两回事。',
                '以为所有连续函数都是初等函数：连续和初等是两个独立的概念。',
                '看到分段函数就直接说“不是初等函数”，其实要看能不能合并成一个式子。',
              ],
              tags: ['函数', '初等函数'],
              related: ['def-elementary-functions', 'def-composite-function', 'thm-elementary-continuity'],
            },
          ],
        },
        {
          id: 'ch1-2',
          no: '1.2',
          title: '数列的极限',
          summary: '把“无限接近”这件事第一次写成可以严格检验的条件：ε-N 语言。并给出极限的唯一性、有界性、保号性等基本性质。',
          items: [
            {
              id: 'def-sequence',
              kind: 'definition',
              name: '数列的定义',
              aka: ['数列', '通项', '一般项'],
              statement: '按一定次序排列的一列无穷多个数 <code>a₁, a₂, a₃, …, aₙ, …</code> 称为<b>数列</b>，记作 <code>{aₙ}</code>，其中 <code>aₙ</code> 称为数列的<b>通项</b>（一般项）。\n本质上看，数列是定义在正整数集 <code>ℕ₊</code> 上的函数：<code>aₙ = f(n)</code>，<code>n ∈ ℕ₊</code>。当 <code>n</code> 依次取 1, 2, 3, … 时得到的函数值排成的序列就是这个数列。',
              plain: '数列就是一串排好队的数，第一个是 <code>a₁</code>，第二个是 <code>a₂</code>，一直排下去没完。\n因为无穷无尽，所以你没办把每一项都写出来（写到手酸也写不完），只能写个“通项公式”，告诉别人第 <code>n</code> 项长什么样。比如 <code>aₙ = 1/n</code>，你问第 100 项，它就告诉你 <code>1/100</code>。\n把数列看成“只有整数输入的函数”很有用：这样你就可以把它画成一堆孤立的点 <code>(1, a₁), (2, a₂), …</code>，一眼看出它是往高处爬还是往低处滑。',
              why: '为什么要强调“次序”？因为数列和集合不一样：集合 <code>{1, 2, 3}</code> 和 <code>{3, 1, 2}</code> 是同一个集合，但数列 <code>1, 2, 3, …</code> 和 <code>3, 1, 2, …</code> 是两个不同的数列。\n次序重要，是因为极限恰恰关心“越往后怎么样”。这里的“往后”必须有个明确的先后，才能说“当 <code>n</code> 足够大时”。这就是那条 <code>n → ∞</code> 背后的东西。',
              example: '<code>aₙ = 1/n</code>：写出来是 <code>1, 1/2, 1/3, 1/4, …</code>，越往后越小，越来越贴近 0。\n<code>aₙ = (-1)ⁿ</code>：写出来是 <code>-1, 1, -1, 1, …</code>，来回跳。\n<code>aₙ = n²</code>：<code>1, 4, 9, 16, …</code>，往无穷大冲。\n注意：数列不要求各项互不相同，也不要求有规律，甚至可以随便乱写；但有通项公式才方便研究。',
              pitfalls: [
                '把数列当成集合，忽略次序。',
                '以为数列的项数有限。数列一定是无穷多项的（这是与“有穷数列”区分的地方，本课程默认讨论无穷数列）。',
                '把 <code>{aₙ}</code> 与 <code>aₙ</code> 混用：前者是整串，后者是第 <code>n</code> 项这一个数。',
              ],
              tags: ['数列', '基础概念'],
              related: ['def-limit-sequence', 'def-function'],
            },
            {
              id: 'def-limit-sequence',
              kind: 'definition',
              name: '数列极限的定义',
              aka: ['ε-N 定义', '数列收敛', '数列极限'],
              statement: '设 <code>{aₙ}</code> 为一数列。若存在常数 <code>a</code>，使得对任意给定的正数 <code>ε</code>（不论它多么小），总存在正整数 <code>N</code>，使得当 <code>n > N</code> 时，恒有 <code>|aₙ - a| < ε</code> 成立，则称常数 <code>a</code> 是数列 <code>{aₙ}</code> 的<b>极限</b>，或称数列 <code>{aₙ}</code> <b>收敛于</b> <code>a</code>，记作\n<code>lim(n→∞) aₙ = a</code> 或 <code>aₙ → a (n → ∞)</code>。\n若这样的常数 <code>a</code> 不存在，则称数列 <code>{aₙ}</code> <b>发散</b>。\n用逻辑符号写就是：<code>lim aₙ = a ⟺ ∀ ε > 0, ∃ N ∈ ℕ₊, ∀ n > N : |aₙ - a| < ε</code>。',
              plain: '先把“无限接近”翻译成人话：<b>你随便报一个多小的距离 <code>ε</code>（比如 0.001），我都能告诉你从第几项开始，后面的所有项都落在 <code>a</code> 的 <code>ε</code> 范围里，再也跑不出去。</b>\n关键在“<b>所有</b>”：不是偶尔掉进去，而是从某一项之后<b>全部</b>都关在里面。\n打个比方：终点线是 <code>a</code>，你在跑道上跑。<code>ε</code> 是裁判随手画的一个小圈（圈可以画得任意小）。如果裁判每画一次圈，你都能指出“从第 <code>N</code> 步以后我一直在圈里”，那你就是收敛到 <code>a</code>。你可以永远不到达终点，但必须越来越离不开它。\n至于前面有限多项（前 <code>N</code> 项）在圈外？完全没关系，极限只看“足够靠后”的行为。',
              why: '这个定义为什么长这么绕？因为它想把“无限接近”这句口语变成<b>可以吵架时拿来核对的条件</b>。\n“接近”是个模糊词：0.1 算近吗？0.0001 算近吗？说 <code>aₙ</code> 接近 <code>a</code>，对手可以说“那只是偶尔接近”。而“<code>∀ ε > 0, ∃ N</code>”这个句式把主动权交给对手、把兑现义务留给自己：<b>对手先出题（给 ε），我后答题（给 N）</b>。对手的 ε 越小，我要给的 N 就越大，但只要我总有答案，就说明后段被彻底锁住了。\n再说那个容易让人困惑的不等号方向：为什么是 <code>|aₙ - a| < ε</code> 而不是 <code>≤ ε</code>？其实两种写法等价（因为 ε 可以取任意小，取 <code>ε/2</code> 就换回来了）。教材选严格小于，只是为了后面证明时书写方便。同理 <code>n > N</code> 与 <code>n ≥ N</code> 也只是习惯，不影响本质。',
              example: '证明 <code>lim(n→∞) 1/n = 0</code>：\n任给 <code>ε > 0</code>，要 <code>|1/n - 0| = 1/n < ε</code>，只需 <code>n > 1/ε</code>。于是取 <code>N = [1/ε]</code>（取整，保证 <code>N ≥ 1/ε</code> 即可，例如取 <code>N = [1/ε] + 1</code>）。则当 <code>n > N</code> 时必有 <code>1/n < ε</code>，故 <code>lim(n→∞) 1/n = 0</code>。\n直观图像：取 <code>ε = 0.01</code>，则从第 101 项起 <code>aₙ < 0.01</code>；取 <code>ε = 10⁻⁶</code>，则从第 <code>10⁶ + 1</code> 项起成立。<code>ε</code> 越刁钻，<code>N</code> 越大，但永远找得到。',
              pitfalls: [
                '把 <code>ε</code> 当成“某个很小的固定数”。<code>ε</code> 是“任意给定的”，证明里不能给它赋值（只能在例子中赋值说明）。',
                '把极限理解成“<code>aₙ</code> 能取到 <code>a</code>”。收敛数列可以永远不等于 <code>a</code>，<code>1/n</code> 就永远不为 0。',
                '以为“<code>aₙ</code> 越来越接近 <code>a</code>”就够了。光有“越来越近”不够：数列 <code>1, 1/2, 1/3, …</code> 越走越近 0 没问题；但“<code>aₙ</code> 与 <code>a</code> 的距离不断缩小”这种描述还必须加上“能任意小”，否则 <code>aₙ = 1 + 1/n</code> 距离 <code>0</code> 也在不断缩小，却不收敛于 0。',
                '做证明时只验证前面几项，或者只验证“存在无穷多项满足”，都不是定义要求。',
              ],
              tags: ['极限', '数列极限', 'ε-N'],
              related: ['def-limit-finite', 'thm-lim-unique', 'def-infinitesimal', 'thm-lim-sequence-operations'],
            },
            {
              id: 'thm-lim-unique',
              kind: 'theorem',
              name: '收敛数列极限的唯一性',
              aka: ['极限唯一性'],
              statement: '如果数列 <code>{aₙ}</code> 收敛，那么它的极限是唯一的。即若 <code>lim aₙ = a</code> 且 <code>lim aₙ = b</code>，则必有 <code>a = b</code>。',
              plain: '一个数列不可能同时稳定地贴在两个不同的数上。\n理由很直观：假设它既收敛到 3 又收敛到 5，那它迟早得既待在 3 旁边又待在 5 旁边；可 3 和 5 之间隔着距离 2，一个小圈根本没这么大，包不住两个地方。所以只能是一个。',
              why: '证明的思路只有一个词：<b>把两个圈画得足够小，让它们互不重叠</b>。\n具体怎么选圈的大小？两个候选极限之间的距离是 <code>|a - b|</code>。只要让每个圈的半径都小于这个距离的一半，两个圈就分开了。这就解释了证明里为什么要取 <code>ε = |a - b| / 2</code>——不是为了小，而是为了“分开”。\n最后用三角不等式把点“夹”出来：<code>aₙ</code> 同时在两个圈里，那两个圆心之间的距离就不可能超过两个半径之和。',
              proof: '<ol><li>反设极限不唯一，即存在两个不同的常数 <code>a</code>、<code>b</code>，使 <code>lim(n→∞) aₙ = a</code> 且 <code>lim(n→∞) aₙ = b</code>，于是 <code>|a - b| > 0</code>。</li><li>取正数 <code>ε = |a - b| / 2</code>。这个数只跟 <code>a</code>、<code>b</code> 有关，与 <code>n</code> 无关，所以它是一个合法的“给定正数”。</li><li>因为 <code>lim aₙ = a</code>，按定义，对此 <code>ε</code> 存在正整数 <code>N₁</code>，当 <code>n > N₁</code> 时 <code>|aₙ - a| < ε</code>。</li><li>又因为 <code>lim aₙ = b</code>，对此同一个 <code>ε</code> 存在正整数 <code>N₂</code>，当 <code>n > N₂</code> 时 <code>|aₙ - b| < ε</code>。</li><li>取 <code>N = max{ N₁, N₂ }</code>。当 <code>n > N</code> 时，两个不等式同时成立。于是由三角不等式\n<code>|a - b| = |(a - aₙ) + (aₙ - b)| ≤ |a - aₙ| + |aₙ - b| < ε + ε = |a - b|</code>。</li><li>这就得到 <code>|a - b| < |a - b|</code>，一个数不能严格小于自己，矛盾。</li><li>矛盾说明假设不成立，故 <code>a = b</code>，极限唯一。</li></ol>\n<b>过渡说明</b>：第 2 步是整个证明的“钥匙”，先钉死 ε；第 5 步是“合流”，把两条独立的控制合成一条不等式；第 6 步是收网。以后凡是遇到“证明某个量等于 0”的题，很多都是这个套路：先假设它大于 0，再推出它小于自己。',
              example: '用唯一性可以直接否定一些说法：数列 <code>aₙ = (-1)ⁿ</code> 不可能收敛，因为它的奇数项子列恒为 <code>-1</code>、偶数项子列恒为 <code>1</code>，若整体有极限 <code>a</code>，则这两条子列也得收敛到同一个 <code>a</code>，既要有 <code>a = -1</code> 又要有 <code>a = 1</code>，矛盾。\n（严格说这里用到了“收敛数列的子列收敛于同一极限”，见子数列的条目。）',
              pitfalls: [
                '把唯一性当成“显然”而不证明，考试中这是要写证明的。',
                '取 <code>ε = |a - b|</code> 而不是 <code>|a - b| / 2</code>：这样两个圈会相切甚至交叠，推不出矛盾。',
                '写 <code>N = max{N₁, N₂}</code> 时误写成 <code>min</code>，那样不能保证两个条件同时成立。',
              ],
              tags: ['极限', '数列极限', '证明'],
              related: ['def-limit-sequence', 'thm-conv-bounded', 'thm-subsequence-limit', 'thm-lim-sign-preserve'],
            },
            {
              id: 'thm-conv-bounded',
              kind: 'theorem',
              name: '收敛数列的有界性',
              aka: ['收敛必有界'],
              statement: '如果数列 <code>{aₙ}</code> 收敛，那么数列 <code>{aₙ}</code> 一定有界。即存在 <code>M > 0</code>，使得对一切 <code>n</code> 都有 <code>|aₙ| ≤ M</code>。',
              plain: '一个数列如果最终都挤在极限 <code>a</code> 附近，那它当然跑不到天边去。\n粗略地说：前面有限多项再怎么大，也就那么几个数，总有一个最大的；后面无穷多项都关在 <code>a</code> 附近的一个小圈里，也超不出 <code>|a| + 1</code>。两头都封住了，整个数列就有界了。\n注意反过来不成立：有界的数列不一定收敛，<code>(-1)ⁿ</code> 就是有界但不收敛的经典例子。所以“有界”是收敛的<b>必要条件</b>，不是充分条件。',
              why: '证明的诀窍是“<b>抓尾巴，再管头</b>”。尾巴是无穷多项，用极限定义控制；头是有限多项，用“有限个数必有最大”来控制。\n为什么 <code>ε</code> 直接取成 1？因为这里的 ε 是我们自己的工具，不是对手出的题，我们有权挑一个最省事的。取 <code>ε = 1</code> 就意味着“从第 <code>N</code> 项以后，<code>aₙ</code> 都在 <code>(a - 1, a + 1)</code> 里”，于是尾巴被封在 <code>|a| + 1</code> 以内。\n最后把“头”和“尾”的上界取最大，就得到全体的上界。这个“先处理无穷部分、再补上有限部分”的套路，在极限理论里会反复出现。',
              proof: '<ol><li>设 <code>lim(n→∞) aₙ = a</code>。</li><li>取 <code>ε = 1</code>（这是我们可以自由选择的一个正数）。由极限定义，存在正整数 <code>N</code>，当 <code>n > N</code> 时，<code>|aₙ - a| < 1</code>。</li><li>对这部分的项作估计：由 <code>|aₙ| = |(aₙ - a) + a| ≤ |aₙ - a| + |a| < 1 + |a|</code>，可见尾巴（<code>n > N</code> 的所有项）都被 <code>1 + |a|</code> 控制住了。</li><li>剩下的是前 <code>N</code> 项 <code>a₁, a₂, …, a_N</code>，这是有限个数（注意 N 是已经定下来的具体正整数）。取 <code>M₀ = max{ |a₁|, |a₂|, …, |a_N| }</code>，则 <code>n ≤ N</code> 时 <code>|aₙ| ≤ M₀</code>。</li><li>令 <code>M = max{ M₀, 1 + |a| }</code>。对任意 <code>n</code>：若 <code>n ≤ N</code>，则 <code>|aₙ| ≤ M₀ ≤ M</code>；若 <code>n > N</code>，则 <code>|aₙ| < 1 + |a| ≤ M</code>。两种情形都成立。</li><li>所以数列 <code>{aₙ}</code> 有界。</li></ol>\n<b>过渡说明</b>：第 3 步用了一次三角不等式（把 <code>aₙ</code> 与 <code>a</code> 的差转成大小估计），第 4 步承认“有限个数总能取最大”这一朴素事实，最后一步只是把两种情况合并。整个证明中 <code>N</code> 到头来只是一个普通常数，这是关键——正因为 N 有限，才能对它前面那几项取最大值。',
              example: '<code>aₙ = 1/n</code> 收敛于 0，显然有界（<code>|aₙ| ≤ 1</code>）。\n<code>aₙ = (2n + 1)/n = 2 + 1/n</code> 收敛于 2，有界（<code>2 < aₙ ≤ 3</code>）。\n逆命题不成立：<code>aₙ = (-1)ⁿ</code> 有界但不收敛。\n逆否命题很有用：无界的数列一定发散。例如 <code>aₙ = n</code> 无界，所以一定发散。',
              pitfalls: [
                '把定理反过来说成“有界必收敛”，这是最常见的误用。',
                '证明中把 <code>N</code> 当成可变的，导致“前 N 项”也跟着变，取最大值就失去意义。',
                '忘记 <code>M</code> 要与 <code>n</code> 无关。上界必须是固定常数，写成“<code>|aₙ| ≤ |aₙ|</code>”之类的废话不算证明。',
              ],
              tags: ['极限', '数列极限', '证明', '有界性'],
              related: ['def-limit-sequence', 'def-bounded-function', 'thm-monotone-bounded', 'thm-lim-unique'],
            },
            {
              id: 'thm-lim-sign-preserve',
              kind: 'theorem',
              name: '收敛数列的保号性',
              aka: ['保号性', '保序性'],
              statement: '（保号性）若 <code>lim(n→∞) aₙ = a</code> 且 <code>a > 0</code>（或 <code>a < 0</code>），则存在正整数 <code>N</code>，当 <code>n > N</code> 时，<code>aₙ > 0</code>（相应地 <code>aₙ < 0</code>）。\n（保序性）若 <code>lim aₙ = a</code>，<code>lim bₙ = b</code>，且存在正整数 <code>N₀</code>，当 <code>n > N₀</code> 时 <code>aₙ ≥ bₙ</code>，则 <code>a ≥ b</code>。',
              plain: '保号性的意思是：<b>极限是正的，那么数列从某一项往后就都是正的。</b>\n想象极限 <code>a = 3</code>，那 <code>aₙ</code> 迟早要挤在 3 附近，比如挤在 <code>(2, 4)</code> 里，这当然全是正数。要想让 <code>aₙ</code> 变负，它得先跨过 0，而一旦进了 3 附近的小圈就跨不过去了。\n保序性说的是：<b>如果后半段每一项都压着另一列，那么极限也压着。</b>但这只保证 <code>a ≥ b</code>，不能保证 <code>a > b</code>——因为取严格不等号时极限可能相等，比如 <code>aₙ = 1/n</code> 与 <code>bₙ = 0</code>，恒有 <code>aₙ > bₙ</code>，但极限都是 0。',
              why: '保号性证明的核心是“<b>留出安全距离</b>”。既然 <code>a > 0</code>，那么 <code>a/2</code> 也是正数，这是一个与 0 之间的距离。只要让 <code>aₙ</code> 落在 <code>a</code> 的 <code>a/2</code> 邻域内，就有 <code>aₙ > a - a/2 = a/2 > 0</code>。选 <code>ε = a/2</code>（也可以选 <code>a/3</code> 等等）就是为了“不碰到 0”。\n保序性为什么只能得到非严格不等号？用反证看：若 <code>a < b</code>，取 <code>ε = (b - a)/2</code>，则从某项起 <code>aₙ < a + ε = (a+b)/2</code> 且 <code>bₙ > b - ε = (a+b)/2</code>，于是 <code>aₙ < bₙ</code>，与题设 <code>aₙ ≥ bₙ</code> 矛盾。所以只能 <code>a ≥ b</code>。至于能不能加强成 <code>a > b</code>，<code>1/n</code> 与 <code>0</code> 的反例说明不能。',
              proof: '<ol><li><b>先证保号性（以 <code>a > 0</code> 为例）。</b>取 <code>ε = a/2 > 0</code>。由 <code>lim aₙ = a</code>，存在正整数 <code>N</code>，当 <code>n > N</code> 时 <code>|aₙ - a| < a/2</code>。</li><li>去掉绝对值即得 <code>-a/2 < aₙ - a < a/2</code>，左边这个不等式给出 <code>aₙ > a - a/2 = a/2 > 0</code>，所以从第 <code>N</code> 项之后数列各项都为正。<code>a < 0</code> 的情形完全对称：取 <code>ε = -a/2</code>，可得 <code>aₙ < a + (-a)/2 = a/2 < 0</code>。</li><li><b>再证保序性。</b>已知 <code>lim aₙ = a</code>，<code>lim bₙ = b</code>，且 <code>n > N₀</code> 时 <code>aₙ ≥ bₙ</code>。反设 <code>a < b</code>，令 <code>ε = (b - a)/2 > 0</code>。</li><li>由极限定义，存在 <code>N₁</code>，当 <code>n > N₁</code> 时 <code>aₙ < a + ε = (a + b)/2</code>；存在 <code>N₂</code>，当 <code>n > N₂</code> 时 <code>bₙ > b - ε = (a + b)/2</code>。</li><li>取 <code>N = max{ N₀, N₁, N₂ }</code>。则当 <code>n > N</code> 时同时有 <code>aₙ < (a + b)/2 < bₙ</code>，即 <code>aₙ < bₙ</code>，与 <code>aₙ ≥ bₙ</code> 矛盾。</li><li>矛盾说明 <code>a < b</code> 不成立，故 <code>a ≥ b</code>。</li></ol>\n<b>过渡说明</b>：第 1、2 步是“反推法”——想让 <code>aₙ > 0</code>，就找出一个能让它必然为正的邻域半径，这里 <code>a/2</code> 是最简单的选择。第 3 至 6 步再次使用“两个小圈分开”的技巧。',
              example: '数列 <code>aₙ = (3n + 1)/n = 3 + 1/n</code> 的极限是 3 > 0，果然从 <code>n ≥ 1</code> 起就全为正（实际上 <code>aₙ > 3</code>）。\n保序性的典型用法：已知 <code>0 ≤ aₙ ≤ 1/n</code>，若 <code>aₙ</code> 收敛，则夹逼得极限非负；进一步若还要说明极限为 0，需要夹逼准则一起用。\n反例（严格不等号不能传递到极限）：<code>aₙ = 1/n > 0 = bₙ</code> 对一切 <code>n</code> 成立，但两个极限都是 0。',
              pitfalls: [
                '把保号性记成“<code>aₙ > 0</code> 推出 <code>a > 0</code>”。反例：<code>aₙ = 1/n > 0</code> 但极限为 0。正确的传递方向是“极限正 ⟹ 后段正”，而且允许前面有限多项为负。',
                '把保序性的结论写成 <code>a > b</code>。',
                '忘记保号性里的“从某一项以后”，直接说“数列所有项都为正”。',
              ],
              tags: ['极限', '数列极限', '证明', '保号性'],
              related: ['def-limit-sequence', 'thm-lim-unique', 'thm-squeeze', 'thm-conv-bounded'],
            },
            {
              id: 'def-subsequence',
              kind: 'definition',
              name: '子数列',
              aka: ['子列', '子数列'],
              statement: '在数列 <code>{aₙ}</code> 中，保持原来的次序，任意抽取无穷多项而得到的一个新数列，称为 <code>{aₙ}</code> 的<b>子数列</b>，记作 <code>{a_{n_k}}</code>，其中 <code>n_k</code> 是抽取项在原数列中的下标，满足 <code>n₁ < n₂ < n₃ < …</code>。\n注意 <code>n_k ≥ k</code> 恒成立。',
              plain: '子数列就是“从原队列里挑人，但不许打乱排队顺序”。\n比如原数列 <code>1, 1/2, 1/3, 1/4, …</code>，把奇数位的人挑出来：<code>1, 1/3, 1/5, …</code>，这就是一个子列。你不许把第 5 位的和第 2 位的换个位置。\n为什么必须保持次序？因为极限关心“往后怎么样”。抽出来的人还得能排出个先后，才能谈“当 <code>k</code> 越来越大时”。',
              why: '子列这个工具的价值在“<b>反证</b>”。要证明一个数列发散，正面去否定极限定义（说“对每个 <code>a</code> 都存在某个 <code>ε</code>……”）很麻烦；但如果能找出两条子列跑向不同的地方，就立刻矛盾了，因为收敛数列的子列必须收敛到同一个极限。\n另外，<code>n_k ≥ k</code> 这条小性质在证明中很实用：它说明子列的下标跑得比 <code>k</code> 还快，所以当 <code>k → ∞</code> 时 <code>n_k → ∞</code>，这正是让极限定义能“传递”给子列的关键。',
              example: '<code>{aₙ} = {(-1)ⁿ}</code> 的偶数项子列是 <code>a₂, a₄, a₆, … = 1, 1, 1, …</code>（收敛于 1），奇数项子列是 <code>-1, -1, -1, …</code>（收敛于 -1）。两者极限不同，所以原数列发散。\n<code>{1/n}</code> 的子列 <code>{1/(2k)} = 1/2, 1/4, 1/6, …</code> 也收敛于 0——这符合定理：子列与母列同极限。\n注意：只取有限多项不叫子列，子列必须有<b>无穷</b>多项。',
              pitfalls: [
                '抽取时把次序打乱，或者允许重复取同一项无限次——那样就不是子列。',
                '以为子列的极限可以和原数列不同（若原数列收敛的话）。',
                '用“存在一条子列收敛于 <code>a</code>”来证明原数列收敛于 <code>a</code>：这是错误的，必须有<b>所有</b>子列都收敛于 <code>a</code>（或所有奇数项子列、偶数项子列都收敛于同一值）。',
              ],
              tags: ['数列', '子列', '基础概念'],
              related: ['thm-subsequence-limit', 'def-limit-sequence', 'thm-cauchy-criterion'],
            },
            {
              id: 'thm-subsequence-limit',
              kind: 'theorem',
              name: '收敛数列与其子数列的关系',
              aka: ['子列同极限'],
              statement: '若数列 <code>{aₙ}</code> 收敛于 <code>a</code>，则它的<b>任何</b>子数列 <code>{a_{n_k}}</code> 都收敛，且极限也是 <code>a</code>。\n（逆否形式）若存在两条子数列收敛于不同的极限，则原数列发散。',
              plain: '母数列最终都挤在 <code>a</code> 附近了，那你从里面随便挑出一部分人，他们当然也都在 <code>a</code> 附近。\n“随便挑”是关键：无论你怎么挑（挑奇数的、挑平方数的、挑素数的），只要挑无穷多个且保持次序，极限都一样。正因为如此，只要能找到两条“各奔东西”的子列，就能判原数列发散。',
              why: '证明的想法是“<b>把 N 借过来用</b>”：母数列给出一个 <code>N</code>（从第 <code>N</code> 项之后全在圈里），子列只需要证明“从某一项之后也全部在圈里”。\n那么子列要跳过多少项？只要子列的下标超过 <code>N</code> 就行。因为 <code>n_k</code> 是严格递增的正整数，它必然会越过 <code>N</code>；而由于 <code>n_k ≥ k</code>，取 <code>K = N</code> 就够了：<code>k > K</code> 时 <code>n_k ≥ k > N</code>，于是 <code>|a_{n_k} - a| < ε</code>。这条 <code>n_k ≥ k</code> 正是前面定义里那句提醒的用武之地。',
              proof: '<ol><li>设 <code>lim(n→∞) aₙ = a</code>，<code>{a_{n_k}}</code> 是它的任一子数列，其中 <code>n₁ < n₂ < … </code> 为正整数。</li><li>先说明一个基本事实：由严格递增知 <code>n_k ≥ k</code>（对 <code>k</code> 归纳：<code>n₁ ≥ 1</code>；若 <code>n_k ≥ k</code>，则 <code>n_{k+1} > n_k ≥ k</code>，所以 <code>n_{k+1} ≥ k + 1</code>）。</li><li>任给 <code>ε > 0</code>。由 <code>lim aₙ = a</code>，存在正整数 <code>N</code>，当 <code>n > N</code> 时 <code>|aₙ - a| < ε</code>。</li><li>取 <code>K = N</code>。当 <code>k > K</code> 时，由第 2 步 <code>n_k ≥ k > N</code>，于是第 3 步的不等式可以作用在 <code>n = n_k</code> 上，得到 <code>|a_{n_k} - a| < ε</code>。</li><li>这正是子列收敛于 <code>a</code> 的定义，于是 <code>lim(k→∞) a_{n_k} = a</code>。由子列的任意性，结论对一切子列成立。</li></ol>\n<b>过渡说明</b>：整个证明只有一句话的技术含量——把母列的“门槛 <code>N</code>”直接拿来当子列的“门槛 <code>K</code>”。能这么做，全靠 <code>n_k ≥ k</code> 保证子列下标不会“走得太慢”。\n<b>逆否命题</b>直接从原命题取逆否即可：若某子列不收敛于 <code>a</code>，则原数列不收敛于 <code>a</code>；因此出现两条极限不同的子列时，<code>a</code> 无论是谁都矛盾，故发散。',
              example: '<code>aₙ = 1 + 1/n</code> 收敛于 1。取子列 <code>a_{2k} = 1 + 1/(2k)</code>，也收敛于 1；取 <code>a_{k²} = 1 + 1/k²</code>，仍收敛于 1。\n反用：<code>aₙ = (-1)ⁿ + 1/n</code>。偶数项子列收敛于 1，奇数项子列收敛于 -1，故原数列发散。\n再如 <code>aₙ = 1 + (-1)ⁿ</code>：偶数项恒为 2，奇数项恒为 0，故发散。',
              pitfalls: [
                '误以为“存在一条子列收敛”就能推出母列收敛。比如 <code>(-1)ⁿ</code> 有收敛的子列（常数子列），但母列发散。',
                '用子列不收敛去推母列发散时，只找到一条不收敛的子列还不够——不过由原定理，一条不收敛的子列确实足以说明母列压根不收敛于那个值，配合对所有候选值讨论才完整。最省事的做法是找两条极限不同的子列。',
                '忘记子列必须保持次序，把重排后的数列当作子列使用。',
              ],
              tags: ['数列', '子列', '证明'],
              related: ['def-subsequence', 'def-limit-sequence', 'thm-lim-unique', 'thm-heine'],
            },
            {
              id: 'thm-cauchy-criterion',
              kind: 'theorem',
              name: '柯西收敛准则',
              aka: ['柯西准则', 'Cauchy 收敛准则', '数列收敛的充要条件'],
              statement: '数列 <code>{aₙ}</code> 收敛的<b>充分必要条件</b>是：对任意给定的正数 <code>ε</code>，总存在正整数 <code>N</code>，使得当 <code>n > N</code> 时，对一切正整数 <code>p</code>，都有\n<code>|a_{n+p} - aₙ| < ε</code>。\n用逻辑符号写就是：<code>{aₙ} 收敛 ⟺ ∀ ε > 0, ∃ N ∈ ℕ₊, ∀ n > N, ∀ p ∈ ℕ₊ : |a_{n+p} - aₙ| < ε</code>。\n满足这一条件的数列称为<b>柯西列</b>（基本列）。',
              plain: '柯西准则在说：<b>数列收敛，当且仅当“后面任意两项都挨得极近”。</b>\n这和极限定义的区别很关键。极限定义说“<code>aₙ</code> 靠近某个固定的 <code>a</code>”——可问题是，很多时候你根本不知道 <code>a</code> 是多少。柯西准则的好处在：它<b>完全不需要知道极限是什么</b>，只看数列自己内部“后段是否越来越拥挤”，就能判定它收敛。\n打个比方：一群人在操场上走，管理员要求“所有人最终聚到旗杆下”（这是极限定义，得知道旗杆在哪）；而柯西准则说的是“只要所有人最终互相靠得极近就行”——至于他们到底聚到哪儿，可以事后再说（事实上，实数系的完备性保证了他们一定聚到某个点）。',
              why: '这个定理为什么成立？两个方向的想法不同。\n<b>必要性</b>很自然：如果大家都往 <code>a</code> 身边挤，那任意两个人之间当然也近（用三角不等式：<code>|a_{n+p} - aₙ| ≤ |a_{n+p} - a| + |a - aₙ|</code>，两边各取 <code>ε/2</code> 就够了）。\n<b>充分性</b>才是这个定理的价值所在，思路分三步：先用柯西条件证明数列<b>有界</b>（取 <code>ε = 1</code>，后段都挤在 <code>a_{N+1}</code> 附近，前段有限，所以整体有界）；再由<b>致密性定理</b>（有界数列必有收敛子列，等价于波尔查诺-魏尔斯特拉斯定理）取出一条收敛子列 <code>a_{n_k} → a</code>；最后用柯西条件说明整列都收敛到同一个 <code>a</code>：因为 <code>|aₙ - a| ≤ |aₙ - a_{n_k}| + |a_{n_k} - a|</code>，第一项由柯西条件控制（只要 <code>n</code>、<code>n_k</code> 都够大），第二项由子列收敛控制，两项各取 <code>ε/2</code>。\n换句话说，<b>柯西准则 = 有界性 + 有收敛子列</b>这两件事的组合，而后者正是实数系“没有窟窿”的体现。这也是为什么柯西准则在实数系里成立，在有理数系里却会失效（比如 <code>1, 1.4, 1.41, 1.414, …</code> 逼近 <code>√2</code>，它是有理数中的柯西列，但在有理数里不收敛）。',
              proof: '<ol><li><b>必要性。</b>设 <code>lim aₙ = a</code>。任给 <code>ε > 0</code>，取 <code>ε/2 > 0</code>，存在 <code>N</code>，当 <code>n > N</code> 时 <code>|aₙ - a| < ε/2</code>。</li><li>于是当 <code>n > N</code> 且 <code>p</code> 为任意正整数时，<code>n + p > N</code>，故\n<code>|a_{n+p} - aₙ| = |(a_{n+p} - a) - (aₙ - a)| ≤ |a_{n+p} - a| + |aₙ - a| < ε/2 + ε/2 = ε</code>。必要条件得证。</li><li><b>充分性第 1 步：证明柯西列有界。</b>设 <code>{aₙ}</code> 满足柯西条件。取 <code>ε = 1</code>，存在 <code>N</code>，当 <code>n > N</code> 时对一切 <code>p</code> 有 <code>|a_{n+p} - aₙ| < 1</code>。特别地，固定 <code>n = N + 1</code>，则对一切 <code>m > N + 1</code>（取 <code>p = m - N - 1</code>）有 <code>|a_m - a_{N+1}| < 1</code>，即 <code>|a_m| < 1 + |a_{N+1}|</code>。而前 <code>N + 1</code> 项有限，取 <code>M = max{ |a₁|, …, |a_{N+1}|, 1 + |a_{N+1}| }</code>，则对一切 <code>n</code> 有 <code>|aₙ| ≤ M</code>。</li><li><b>充分性第 2 步：取出收敛子列。</b>由第 3 步，<code>{aₙ}</code> 是有界数列。由致密性定理（有界数列必有收敛子列），存在子数列 <code>{a_{n_k}}</code> 及实数 <code>a</code>，使 <code>lim(k→∞) a_{n_k} = a</code>。</li><li><b>充分性第 3 步：证明全列收敛于 a。</b>任给 <code>ε > 0</code>。由柯西条件，存在 <code>N₁</code>，当 <code>n > N₁</code> 时对一切 <code>p</code> 有 <code>|a_{n+p} - aₙ| < ε/2</code>。由子列收敛，存在 <code>K</code>，当 <code>k > K</code> 时 <code>|a_{n_k} - a| < ε/2</code>。</li><li>现取 <code>n > N₁</code>，并取一个 <code>k > K</code> 使得 <code>n_k > N₁</code>（这样的 <code>k</code> 一定存在，因为 <code>n_k → ∞</code>）。令 <code>p = n_k - n</code>；若 <code>n_k < n</code>，则交换两者位置，下面的三角不等式关于两项对称，结论不变。于是\n<code>|aₙ - a| ≤ |aₙ - a_{n_k}| + |a_{n_k} - a| < ε/2 + ε/2 = ε</code>。</li><li>这就证明了 <code>lim(n→∞) aₙ = a</code>，即柯西列一定收敛。结合必要性与充分性，定理成立。</li></ol>\n<b>过渡说明</b>：第 3 步用的仍是“抓尾巴管头”的老套路；第 4 步用到实数系的一条深刻性质（有界数列必有收敛子列），这是整个充分性证明的支点；第 5、6 步用三角不等式把“两项互相近”和“子列靠近 <code>a</code>”焊接起来，注意这里必须让 <code>n_k</code> 也足够大，这就是为什么要强调取 <code>k</code> 使 <code>n_k > N₁</code>。',
              example: '用柯西准则证明调和型数列 <code>aₙ = 1 + 1/2 + 1/3 + … + 1/n</code> 发散：取 <code>p = n</code>，则 <code>a_{2n} - aₙ = 1/(n+1) + … + 1/(2n) ≥ n · 1/(2n) = 1/2</code>。也就是说，无论 <code>N</code> 多大，总能取到 <code>n > N</code> 使 <code>|a_{2n} - aₙ| ≥ 1/2</code>，取 <code>ε = 1/2</code> 就违反柯西条件，故发散。\n再看一个收敛的：<code>aₙ = 1 - 1/n</code>，取 <code>p</code> 任意，<code>|a_{n+p} - aₙ| = |1/(n+p) - 1/n| < 1/n</code>，取 <code>N = [1/ε]</code> 即可，满足柯西条件。',
              pitfalls: [
                '把柯西条件中的 <code>p</code> 固定为某个具体数（比如只验证 <code>p = 1</code>）。必须对<b>一切</b>正整数 <code>p</code> 成立，也就是“任意两项”都近。',
                '把柯西准则当成“比较容易验证的条件”，实际上它在理论上极其重要，但在具体题目里验证起来常常比直接求极限更麻烦。',
                '误以为“柯西列必收敛”在任何数集里都成立。它在实数系、复数系里成立（完备性），但在有理数系里不成立。',
              ],
              tags: ['极限', '数列极限', '证明', '完备性'],
              related: ['def-limit-sequence', 'def-subsequence', 'thm-monotone-bounded', 'thm-lim-unique'],
            },
          ],
        },
        {
          id: 'ch1-3',
          no: '1.3',
          title: '函数的极限',
          summary: '把数列极限的思想搬到函数上。分两种情形：自变量趋向无穷、自变量趋向有限点；后者还要分左右两侧，并给出极限与左右极限的关系。',
          items: [
            {
              id: 'def-limit-infinite',
              kind: 'definition',
              name: '自变量趋于无穷大时函数的极限',
              aka: ['x 趋于无穷的极限', 'ε-X 定义'],
              statement: '设函数 <code>f</code> 在 <code>|x|</code> 大于某个正数时有定义。若存在常数 <code>A</code>，使得对任意给定的正数 <code>ε</code>，总存在正数 <code>X</code>，使得当 <code>|x| > X</code> 时，恒有 <code>|f(x) - A| < ε</code>，则称常数 <code>A</code> 为函数 <code>f(x)</code> 当 <code>x → ∞</code> 时的<b>极限</b>，记作\n<code>lim(x→∞) f(x) = A</code> 或 <code>f(x) → A (x → ∞)</code>。\n类似地：\n若把 <code>|x| > X</code> 换成 <code>x > X</code>，则得到 <code>lim(x→+∞) f(x) = A</code> 的定义；\n若换成 <code>x < -X</code>，则得到 <code>lim(x→-∞) f(x) = A</code> 的定义。',
              plain: '先看 <code>x → ∞</code>：意思是“让 <code>x</code> 往左右两头都跑得远远的”。<code>|x| > X</code> 就是说“<code>x</code> 跑到 <code>-X</code> 左边，或者 <code>X</code> 右边”。\n这个定义的结构和数列几乎一样，只是把“从第 <code>N</code> 项之后”换成了“在 <code>-X</code> 左边或 <code>X</code> 右边”。你可以这样读：<b>你随便给一个多窄的横带（高 <code>2ε</code>，上下各离 <code>A</code> 有 <code>ε</code>），我都能找到一个位置 <code>X</code>，只要 <code>x</code> 跑得比 <code>X</code> 还远，函数图像就钻进这条横带里，再也不出来。</b>\n类比：数列是“往后看都进圈”，函数是“往两头远看都进带”。\n而 <code>x → +∞</code> 只看右边那半头，<code>x → -∞</code> 只看左边那半头。所以可以有 <code>lim(x→+∞) arctan x = π/2</code>、<code>lim(x→-∞) arctan x = -π/2</code>，而 <code>lim(x→∞) arctan x</code> 不存在（两头去的地方不同）。',
              why: '为什么用 <code>|x| > X</code> 而不是 <code>x > X</code>？因为“<code>x → ∞</code>”在数学里约定是“<code>x</code> 的绝对值无限增大”，也就是两头都算。这是一个记号上的约定，很多初学者第一次见会愣一下，记牢就行。\n至于为什么 <code>x → ∞</code> 的极限存在等价于“<code>x → +∞</code> 与 <code>x → -∞</code> 的极限都存在且相等”：因为 <code>|x| > X</code> 这个区域恰好是 <code>x > X</code> 与 <code>x < -X</code> 两块的并集。两块合起来性质一致，当且仅当两块各自的性质一致且结论相同。',
              example: '<code>lim(x→∞) 1/x = 0</code>：任给 <code>ε > 0</code>，要 <code>|1/x - 0| = 1/|x| < ε</code>，只需 <code>|x| > 1/ε</code>，故取 <code>X = 1/ε</code> 即可。\n<code>lim(x→+∞) e⁻ˣ = 0</code>；而 <code>lim(x→-∞) e⁻ˣ = +∞</code>，所以在 <code>x → ∞</code> 意义下这个极限不存在。\n<code>lim(x→∞) (1/x) sin x = 0</code>：因为 <code>|(1/x) sin x| ≤ 1/|x|</code>，取 <code>X = 1/ε</code> 即可。这种“有界量乘无穷小”的估计是标准手法。',
              pitfalls: [
                '把 <code>x → ∞</code> 只理解成 <code>x → +∞</code>，于是错误地认为 <code>lim(x→∞) arctan x = π/2</code>。',
                '证明中把 <code>X</code> 与 <code>x</code> 混淆：<code>X</code> 是一个固定的界限（由 <code>ε</code> 定出），<code>x</code> 是自变量。',
                '误以为函数在 <code>x = ∞</code> 处有定义或有函数值。极限与函数在那里取值毫无关系，事实上无穷远点不在定义域内。',
              ],
              tags: ['极限', '函数极限', 'ε-X'],
              related: ['def-limit-sequence', 'def-limit-finite', 'def-infinitesimal', 'thm-lim-finite-independent'],
            },
            {
              id: 'def-limit-finite',
              kind: 'definition',
              name: '自变量趋于有限值时函数的极限',
              aka: ['x 趋于 x0 的极限', 'ε-δ 定义', '函数极限的定义'],
              statement: '设函数 <code>f</code> 在点 <code>x₀</code> 的某个去心邻域内有定义（即存在 <code>δ₀ > 0</code>，使 <code>f</code> 在 <code>0 < |x - x₀| < δ₀</code> 内有定义）。若存在常数 <code>A</code>，使得对任意给定的正数 <code>ε</code>，总存在正数 <code>δ</code>，使得当 <code>0 < |x - x₀| < δ</code> 时，恒有 <code>|f(x) - A| < ε</code>，则称常数 <code>A</code> 为函数 <code>f(x)</code> 当 <code>x → x₀</code> 时的<b>极限</b>，记作\n<code>lim(x→x₀) f(x) = A</code> 或 <code>f(x) → A (x → x₀)</code>。\n用逻辑符号写：<code>∀ ε > 0, ∃ δ > 0, ∀ x : 0 < |x - x₀| < δ ⟹ |f(x) - A| < ε</code>。',
              plain: '这次是“<code>x</code> 从两边慢慢凑近 <code>x₀</code>，看 <code>f(x)</code> 会不会凑近某个固定的数 <code>A</code>”。\n读法是：<b>你随便给一条很窄的横带（离 <code>A</code> 不超过 <code>ε</code>），我总能找一个很小的左右范围 <code>δ</code>，只要 <code>x</code> 落在 <code>x₀</code> 的 <code>δ</code> 范围里（但不要等于 <code>x₀</code>），那么 <code>f(x)</code> 一定落进你给的横带里。</b>\n打个比方：<code>x₀</code> 是靶心，<code>A</code> 是记分牌上的目标分。你说“我要打中 <code>A</code> 分上下 <code>ε</code> 以内”，我就能告诉你“你只要瞄准靶心 <code>δ</code> 以内就一定能行”。<code>ε</code> 提得越苛刻，我给的 <code>δ</code> 就越小。\n最值得注意的是那个 <code>0 < |x - x₀|</code>：<b>它明确排除了 <code>x = x₀</code> 这一点。</b>',
              why: '为什么非要把 <code>x = x₀</code> 挖掉？这是整个定义最需要想通的地方。\n原因：极限要研究的是“<b>靠近 <code>x₀</code> 的过程中</b> <code>f</code> 的表现”，而不是“在 <code>x₀</code> 这一点上 <code>f</code> 等于多少”。这么做有三个好处：\n1. 让极限存在与否和 <code>f(x₀)</code> 有没有定义、等于多少<b>完全无关</b>。例如 <code>f(x) = (x² - 1)/(x - 1)</code> 在 <code>x = 1</code> 处根本没定义，但 <code>lim(x→1) f(x) = 2</code> 完全说得通。\n2. 让极限可以在“分段点”“间断点”这种 <code>f(x₀)</code> 取值古怪的地方正常讨论。\n3. 为了后面定义导数。导数就是 <code>lim(Δx→0) Δy/Δx</code>，而 <code>Δx = 0</code> 时那个商式没有意义（分母为 0），正是靠“去心”这层保护，导数定义才成立。\n所以 <code>0 < |x - x₀|</code> 不是刻意刁难，而是为了让极限成为比“函数值”更强大、更灵活的工具。',
              example: '证明 <code>lim(x→1) (2x + 1) = 3</code>：\n任给 <code>ε > 0</code>，要 <code>|(2x + 1) - 3| = 2|x - 1| < ε</code>，只需 <code>|x - 1| < ε/2</code>。故取 <code>δ = ε/2</code>。则当 <code>0 < |x - 1| < δ</code> 时 <code>|f(x) - 3| < ε</code>，结论成立。\n再算一个“<code>x₀</code> 处无定义”的：<code>lim(x→1) (x² - 1)/(x - 1)</code>。当 <code>x ≠ 1</code> 时 <code>(x² - 1)/(x - 1) = x + 1</code>，故极限为 2。注意整个推导都依赖 <code>x ≠ 1</code> 才能约分——这正是去心条件的实际用途。',
              pitfalls: [
                '把 <code>0 < |x - x₀| < δ</code> 写成 <code>|x - x₀| < δ</code>，那样就把 <code>x = x₀</code> 也算进去了，定义就错了。',
                '以为求 <code>lim(x→x₀) f(x)</code> 必须先知道 <code>f(x₀)</code>，或者要先验证 <code>f</code> 在 <code>x₀</code> 有定义。',
                '搞错 δ 与 ε 的从属关系：<code>δ</code> 是“对给定的 <code>ε</code>”找到的，可以依赖 <code>ε</code>，但<b>绝不能依赖 <code>x</code></b>。',
                '在证明中写“取 <code>δ = ε/2</code>，显然成立”而不把 <code>|f(x) - A|</code> 的估计写出来，步骤缺失会丢分。',
              ],
              tags: ['极限', '函数极限', 'ε-δ'],
              related: ['def-limit-sequence', 'def-limit-one-sided', 'def-limit-infinite', 'def-continuity-point', 'thm-lim-finite-independent'],
            },
            {
              id: 'def-limit-one-sided',
              kind: 'definition',
              name: '左极限与右极限',
              aka: ['单侧极限', '左极限', '右极限'],
              statement: '设函数 <code>f</code> 在 <code>x₀</code> 的某个左邻域（或右邻域）内有定义。\n若对任意给定 <code>ε > 0</code>，存在 <code>δ > 0</code>，当 <code>x₀ - δ < x < x₀</code> 时恒有 <code>|f(x) - A| < ε</code>，则称 <code>A</code> 为 <code>f(x)</code> 当 <code>x → x₀</code> 时的<b>左极限</b>，记作 <code>f(x₀⁻) = lim(x→x₀⁻) f(x) = A</code>；\n若把条件换成 <code>x₀ < x < x₀ + δ</code>，则称 <code>A</code> 为<b>右极限</b>，记作 <code>f(x₀⁺) = lim(x→x₀⁺) f(x) = A</code>。\n左极限与右极限统称<b>单侧极限</b>。',
              plain: '左极限是“从左边贴着走过去的看法”，右极限是“从右边贴着走过去的看法”。\n为什么需要分左右？因为有些函数从两边凑近时去的地方不一样。最典型的是 <code>f(x) = x/|x|</code>（即 <code>x > 0</code> 时为 1，<code>x < 0</code> 时为 -1）：从右边过来一直是 1，从左边过来一直是 -1，两边“谈不拢”。\n再比如 <code>f(x) = 1/x</code> 在 <code>x → 0</code> 时：从右边看，<code>x</code> 是个小正数，<code>1/x</code> 冲到 <code>+∞</code>；从左边看，<code>x</code> 是个小负数，<code>1/x</code> 掉到 <code>-∞</code>。\n所以“左右极限”是给这种“两面派”行为准备的两把尺子。',
              why: '有了双侧极限，为什么还要单侧？因为<b>定义域未必两边都有</b>。\n考虑 <code>f(x) = √x</code> 在 <code>x = 0</code> 处：<code>x < 0</code> 时函数根本没定义，所以 <code>lim(x→0) √x</code> 按双侧定义压根无法讨论（去心邻域里有一半是空的）。但如果只说右极限 <code>lim(x→0⁺) √x = 0</code>，就完全合理。\n另一个用途是处理分段函数在分段点的情况，以及判断间断点类型。这时用单侧极限能把“从哪边过来”的差别精确地记下来。',
              example: '设 <code>f(x) = x - 1 (x < 0)</code>，<code>f(x) = 0 (x = 0)</code>，<code>f(x) = x + 1 (x > 0)</code>。则\n<code>lim(x→0⁻) f(x) = -1</code>，<code>lim(x→0⁺) f(x) = 1</code>。左右极限都存在但不相等，所以 <code>lim(x→0) f(x)</code> 不存在（这是典型的跳跃间断点）。\n再如 <code>f(x) = sin x / |x|</code>（<code>x ≠ 0</code>）：<code>lim(x→0⁺) = 1</code>，<code>lim(x→0⁻) = -1</code>。',
              pitfalls: [
                '把左极限写成 <code>lim(x→x₀⁻)</code> 却在推导中用了 <code>x > x₀</code> 的范围，符号与条件不一致。',
                '以为单侧极限存在就一定有双侧极限。必须左右都存在<b>且相等</b>才行。',
                '在定义域只有一边时硬谈双侧极限（如 <code>√x</code> 在 <code>x = 0</code>；严格地讲，由于 <code>x = 0</code> 的某个去心邻域内不全是定义域内的点，双侧极限的定义不适用）。',
              ],
              tags: ['极限', '函数极限', '单侧极限'],
              related: ['def-limit-finite', 'thm-limit-two-sided', 'def-discontinuity-types'],
            },
            {
              id: 'thm-limit-two-sided',
              kind: 'theorem',
              name: '函数极限与左右极限的关系',
              aka: ['左右极限判定定理'],
              statement: '函数 <code>f(x)</code> 当 <code>x → x₀</code> 时极限存在的<b>充分必要条件</b>是：左极限与右极限都存在并且相等。即\n<code>lim(x→x₀) f(x) = A ⟺ lim(x→x₀⁻) f(x) = lim(x→x₀⁺) f(x) = A</code>。\n（把 <code>x₀</code> 换成 <code>∞</code> 有类似的结论：<code>lim(x→∞) f(x) = A ⟺ lim(x→+∞) f(x) = lim(x→-∞) f(x) = A</code>。）',
              plain: '一句话：<b>两边都到同一个地方，才叫真的到了；只有一边到，或者两边到的不是同一个地方，都不算。</b>\n这就像两个人分别从路的左右两头走向同一座房子：只有两人最后都进了同一座房子，才说明“大家的目的地是这座房子”。如果一个人进了 A 屋、另一个进了 B 屋，那说明根本没有共同目的地。\n这条定理最大的实用价值在<b>判定极限不存在</b>：只要算出左右极限不相等（或者有一侧不存在），就能一口断定双侧极限不存在，非常省事。',
              why: '这个结论几乎是定义的“重新排版”，没有深刻技巧，但用处极大。\n证明思路就是把“去心邻域”拆成“左半”和“右半”：<code>0 < |x - x₀| < δ</code> 这句话本来就等价于 <code>x₀ - δ < x < x₀</code> 或 <code>x₀ < x < x₀ + δ</code>。所以如果双侧极限存在，取同一个 <code>δ</code>，两个单侧条件自然满足；反过来，如果左右极限都用 <code>δ</code> 满足，那把两个 <code>δ</code> 取小的那个，就能同时管住两边，合并成双侧条件。\n关键的技术细节是：<b>最后必须取 <code>δ = min{δ₁, δ₂}</code></b>，因为两个单侧极限各自给的 δ 未必一样，要让它们同时生效只能取更严的那个。',
              proof: '<ol><li><b>必要性。</b>设 <code>lim(x→x₀) f(x) = A</code>。任给 <code>ε > 0</code>，存在 <code>δ > 0</code>，当 <code>0 < |x - x₀| < δ</code> 时 <code>|f(x) - A| < ε</code>。</li><li>若 <code>x₀ - δ < x < x₀</code>，则当然有 <code>0 < |x - x₀| < δ</code>，故 <code>|f(x) - A| < ε</code>，这说明左极限为 <code>A</code>。同理若 <code>x₀ < x < x₀ + δ</code>，也有 <code>0 < |x - x₀| < δ</code>，故右极限也为 <code>A</code>。必要性得证。</li><li><b>充分性。</b>设 <code>lim(x→x₀⁻) f(x) = A</code> 且 <code>lim(x→x₀⁺) f(x) = A</code>。任给 <code>ε > 0</code>，由左极限，存在 <code>δ₁ > 0</code>，当 <code>x₀ - δ₁ < x < x₀</code> 时 <code>|f(x) - A| < ε</code>；由右极限，存在 <code>δ₂ > 0</code>，当 <code>x₀ < x < x₀ + δ₂</code> 时 <code>|f(x) - A| < ε</code>。</li><li>取 <code>δ = min{ δ₁, δ₂ } > 0</code>。则当 <code>0 < |x - x₀| < δ</code> 时，或者 <code>x₀ - δ < x < x₀</code>（此时因 <code>δ ≤ δ₁</code> 落在左邻域内），或者 <code>x₀ < x < x₀ + δ</code>（此时因 <code>δ ≤ δ₂</code> 落在右邻域内），两种情形都推出 <code>|f(x) - A| < ε</code>。</li><li>按定义 <code>lim(x→x₀) f(x) = A</code>。充分性得证。综合两方向，定理成立。</li></ol>\n<b>过渡说明</b>：整个证明的骨架就是“拆区间”和“取较小者”。注意第 4 步把两种情形都覆盖到了，才可以合并成 <code>0 < |x - x₀| < δ</code> 这一个条件。',
              example: '判断 <code>f(x) = |x| / x</code> 在 <code>x = 0</code> 处的极限：<code>lim(x→0⁺) f(x) = 1</code>，<code>lim(x→0⁻) f(x) = -1</code>，不相等，故 <code>lim(x→0) f(x)</code> 不存在。\n再如分段函数 <code>f(x) = x² (x ≤ 1)</code>，<code>f(x) = 2x - 1 (x > 1)</code>：<code>lim(x→1⁻) f(x) = 1</code>，<code>lim(x→1⁺) f(x) = 1</code>，左右相等，故 <code>lim(x→1) f(x) = 1</code>（顺便可见 <code>f</code> 在 <code>x = 1</code> 连续）。',
              pitfalls: [
                '只算了一侧的极限就下结论。必须两侧都算。',
                '算出左右极限相等，却忘记它们是<b>有限</b>数（如果两侧都趋向 <code>+∞</code>，那不是极限存在，而是极限为无穷大，仍属“极限不存在”的范畴）。',
                '充分性证明中忘记取 <code>δ = min{δ₁, δ₂}</code>，直接说“取 δ 即可”。',
              ],
              tags: ['极限', '函数极限', '单侧极限', '证明'],
              related: ['def-limit-one-sided', 'def-limit-finite', 'def-discontinuity-types', 'def-continuity-point'],
            },
            {
              id: 'thm-lim-finite-local-bounded',
              kind: 'theorem',
              name: '函数极限的局部有界性',
              aka: ['局部有界性'],
              statement: '若 <code>lim(x→x₀) f(x) = A</code>，则存在 <code>δ₀ > 0</code> 和 <code>M > 0</code>，使得当 <code>0 < |x - x₀| < δ₀</code> 时，<code>|f(x)| ≤ M</code>。\n（对 <code>x → ∞</code> 的情形有类似结论：存在 <code>X > 0</code> 与 <code>M > 0</code>，当 <code>|x| > X</code> 时 <code>|f(x)| ≤ M</code>。）',
              plain: '“局部有界”的意思是：<b>函数在 <code>x₀</code> 附近那一小片地方出不了圈。</b>\n为什么只能保证“局部”？看 <code>f(x) = 1/x</code> 在 <code>x = 1</code> 附近：<code>lim(x→1) 1/x = 1</code>，在 <code>x = 1</code> 附近 <code>f</code> 当然有界（大约在 0.9 到 1.1 之间）。但如果你把范围放宽到整个 <code>(0, +∞)</code>，它马上就无界了。\n所以极限只承诺“离 <code>x₀</code> 足够近的地方安分”，不承诺“整个定义域上都安分”。',
              why: '证明方法和“收敛数列的有界性”一模一样，只是把 <code>ε</code> 取成 <code>1</code>。\n想法是：既然 <code>f(x)</code> 最终都落在 <code>(A - 1, A + 1)</code> 里，那么这个邻域内的函数值最大不超过 <code>|A| + 1</code>。用三角不等式 <code>|f(x)| = |f(x) - A + A| ≤ |f(x) - A| + |A| < 1 + |A|</code> 就完成了。\n这也说明一条经验：<b>凡是“局部”性质，几乎都是把 ε 取成一个方便的常数（通常是 1）后得到的。</b>因为局部性质只谈“某个小范围内”，恰好适合用极限定义直接框住。',
              proof: '<ol><li>设 <code>lim(x→x₀) f(x) = A</code>。</li><li>取 <code>ε = 1</code>。由极限定义，存在 <code>δ₀ > 0</code>，当 <code>0 < |x - x₀| < δ₀</code> 时 <code>|f(x) - A| < 1</code>。</li><li>于是对这范围内的 <code>x</code>，由三角不等式\n<code>|f(x)| = |(f(x) - A) + A| ≤ |f(x) - A| + |A| < 1 + |A|</code>。</li><li>取 <code>M = 1 + |A| > 0</code>，则当 <code>0 < |x - x₀| < δ₀</code> 时 <code>|f(x)| < M</code>，特别地 <code>|f(x)| ≤ M</code>。定理得证。</li></ol>\n<b>过渡说明</b>：这里的 <code>δ₀</code> 就是极限定义中由 <code>ε = 1</code> 所确定的那一个，一步到位，不需要取 min。',
              example: '<code>lim(x→0) sin(1/x) · x = 0</code>，所以存在 <code>δ₀</code> 使 <code>x</code> 在 <code>(-δ₀, δ₀)</code> 内、<code>x ≠ 0</code> 时 <code>|x sin(1/x)| ≤ 1</code>。事实上这个不等式对一切 <code>x ≠ 0</code> 都对（因为 <code>|sin(1/x)| ≤ 1</code>），但定理只承诺局部成立，这已经够用。\n反例提醒：<code>f(x) = 1/x</code> 在 <code>x = 0</code> 附近无界，恰好是因为 <code>lim(x→0) 1/x</code> 不存在（不收敛于任何有限数），定理的前提不满足。',
              pitfalls: [
                '把“局部有界”误用成“整个定义域有界”，进而错误地认为“有极限就一定有界”。',
                '搞错 δ₀ 的地位：这里的 δ₀ 是定理保证存在的，不必求出具体数值。',
                '忘记 <code>0 < |x - x₀|</code>，把 <code>x = x₀</code> 也纳入有界的范围。其实加上也无妨（只是多一个点），但逻辑上必须从定义出发。',
              ],
              tags: ['极限', '函数极限', '有界性', '证明'],
              related: ['def-limit-finite', 'thm-conv-bounded', 'thm-lim-finite-sign-preserve', 'thm-continuity-local'],
            },
            {
              id: 'thm-lim-finite-sign-preserve',
              kind: 'theorem',
              name: '函数极限的局部保号性',
              aka: ['局部保号性'],
              statement: '若 <code>lim(x→x₀) f(x) = A</code> 且 <code>A > 0</code>（或 <code>A < 0</code>），则存在 <code>δ > 0</code>，使得当 <code>0 < |x - x₀| < δ</code> 时，<code>f(x) > 0</code>（相应地 <code>f(x) < 0</code>）。\n更一般地：若 <code>A > 0</code>，则对任何满足 <code>0 < r < A</code> 的 <code>r</code>，存在 <code>δ > 0</code>，使该邻域内 <code>f(x) > r > 0</code>。',
              plain: '极限是正的（比如 5），那函数在 <code>x₀</code> 附近就都是正的。\n理由和数列保号性一样：函数迟早要挤到 5 附近，比如挤进 <code>(4.5, 5.5)</code>，这当然全是正的。要想冒出负数，它得先掉到 0 以下，可它根本走不出这个圈。\n“局部”两个字要留意：它只说“<code>x₀</code> 附近的这一小段”，远处怎么样完全不管。比如 <code>f(x) = x</code> 在 <code>x = 1</code> 附近是正的，但 <code>x = -5</code> 处就是负的。',
              why: '证明的关键是“<b>留一半安全距离</b>”：既然 <code>A > 0</code>，那么 <code>A/2</code> 就是个正数，它和 0 之间还有一段距离。取 <code>ε = A/2</code>，就能保证 <code>f(x) > A - A/2 = A/2 > 0</code>。\n为什么不能取 <code>ε = A</code>？那样得到的是 <code>f(x) > A - A = 0</code>，虽然看起来也对，但推导过程中会用到严格不等号，取一半更稳妥，也方便推广成 <code>f(x) > r</code> 的形式。这个“取一半”的技巧在极限证明里出现频率极高，值得记住。\n这条定理最重要的用途，是证明<b>连续函数在零点两侧变号</b>（零点定理）和证明<b>极限的保序性</b>，都是下一节的工具。',
              proof: '<ol><li>设 <code>lim(x→x₀) f(x) = A</code> 且 <code>A > 0</code>。取 <code>ε = A/2 > 0</code>。</li><li>由极限定义，存在 <code>δ > 0</code>，当 <code>0 < |x - x₀| < δ</code> 时 <code>|f(x) - A| < A/2</code>。</li><li>展开绝对值：<code>-A/2 < f(x) - A < A/2</code>。取左边那半边不等式得 <code>f(x) > A - A/2 = A/2 > 0</code>。</li><li>故该去心邻域内 <code>f(x) > 0</code>。<code>A < 0</code> 的情形对称处理：取 <code>ε = -A/2</code>，得 <code>f(x) < A + (-A/2) = A/2 < 0</code>。</li><li>至于加强形式：任取 <code>0 < r < A</code>，则 <code>A - r > 0</code>，取 <code>ε = A - r</code>（这是一个正数），存在 <code>δ > 0</code> 使该邻域内 <code>f(x) > A - (A - r) = r > 0</code>。定理证毕。</li></ol>\n<b>过渡说明</b>：第 1 步的 <code>A/2</code> 是“安全余量”，第 3 步只取半边不等式（我们只要正性），第 5 步说明这个技巧可以推广到任意正下界。',
              example: '<code>lim(x→2) (3x - 1) = 5 > 0</code>，所以在 <code>x = 2</code> 附近 <code>3x - 1 > 0</code>（事实上 <code>x > 1/3</code> 即可，但定理只保证某个邻域内成立）。\n用反证的方式使用它：若已知在某去心邻域内恒有 <code>f(x) ≤ 0</code> 且 <code>lim f(x) = A</code>，则必有 <code>A ≤ 0</code>（否则与保号性矛盾）。这一形式在做题时最常用。',
              pitfalls: [
                '把结论反过来用：“<code>f(x) > 0</code> 在附近成立 ⟹ <code>A > 0</code>”。反例：<code>f(x) = x²</code> 在 <code>x = 0</code> 附近恒 <code>≥ 0</code> 且 <code>> 0</code>（<code>x ≠ 0</code>），但极限是 0，不是正的。正确结论只能是 <code>A ≥ 0</code>。',
                '忽略“局部”二字，把结论推广到整个定义域。',
                '在证明中写 <code>ε = A</code> 然后说 <code>f(x) > 0</code>，虽然结论对，但推理不严谨（严格不等号会被削弱）。',
              ],
              tags: ['极限', '函数极限', '保号性', '证明'],
              related: ['def-limit-finite', 'thm-lim-sign-preserve', 'thm-zero-point', 'thm-intermediate-value'],
            },
            {
              id: 'thm-lim-finite-independent',
              kind: 'theorem',
              name: '函数极限与函数在一点处的值无关',
              aka: ['极限的无关性', '去心邻域的意义'],
              statement: '函数 <code>f(x)</code> 当 <code>x → x₀</code> 时的极限是否存在、等于多少，与 <code>f</code> 在 <code>x₀</code> 处是否有定义、取值多少<b>无关</b>。\n更精确地说：若在 <code>x₀</code> 的某个去心邻域内 <code>f(x) = g(x)</code>，则 <code>lim(x→x₀) f(x)</code> 与 <code>lim(x→x₀) g(x)</code> 要么同时存在且相等，要么同时不存在。',
              plain: '极限看的是“<b>走到 <code>x₀</code> 门口之前</b>的行为”，不是你“进门之后坐在哪里”。\n所以：<code>f(x₀)</code> 有没有定义、等于 7 还是 -100 万，对 <code>lim(x→x₀) f(x)</code> 完全没有影响。\n两个极端例子最能说明问题：\n<code>f(x) = (x²-1)/(x-1)</code> 在 <code>x = 1</code> 处没定义，但极限是 2；\n<code>g(x) = x + 1 (x ≠ 1)</code>，<code>g(1) = 999</code>，极限还是 2。\n所以“极限值”和“函数值”是两个独立的东西，这一点现在是关键，等到讲连续时，我们会说：<b>连续就是这两者恰好相等。</b>',
              why: '这条结论其实是极限定义里那个 <code>0 < |x - x₀|</code> 的直接后果——定义中根本就没出现 <code>f(x₀)</code>，自然与它无关。\n为什么要专门把它拎出来讲一条？因为它是后面一切内容的分水岭：\n- 求极限时可以放心地约分、通分、有理化，只要这些操作在去心邻域内合法（比如约掉的分母不为 0）。这正是“<code>lim(x→1)(x²-1)/(x-1) = lim(x→1)(x+1) = 2</code>”合法的依据。\n- 反过来，连续的定义就是在这条“无关”的基础上加一条约束，把两者接上。\n- 导数也是靠它才定义得出来（<code>Δx ≠ 0</code> 时才有商式，但 <code>Δx → 0</code> 时讨论的是商的极限）。',
              proof: '<ol><li>先确认 <code>lim(x→x₀) f(x)</code> 的定义：<code>∀ ε > 0, ∃ δ > 0, ∀ x (0 < |x - x₀| < δ ⟹ |f(x) - A| < ε)</code>。</li><li>观察：这个定义中只出现了 <code>x ≠ x₀</code> 的取值 <code>f(x)</code>，<b>完全没有出现 <code>f(x₀)</code></b>，也没有出现“<code>f</code> 在 <code>x₀</code> 有定义”这一要求。</li><li>因此，如果 <code>f</code> 在 <code>x₀</code> 处没有定义，或把 <code>f(x₀)</code> 改成一个别的数，都不改变上述命题的真假。所以极限的存在性与值不受 <code>f(x₀)</code> 影响。</li><li>对于第二个断言：设在 <code>0 < |x - x₀| < δ₀</code> 内 <code>f(x) = g(x)</code>。若 <code>lim(x→x₀) f(x) = A</code>，任给 <code>ε > 0</code>，取极限定义中的 <code>δ₁ > 0</code>，再令 <code>δ = min{ δ₀, δ₁ }</code>，则当 <code>0 < |x - x₀| < δ</code> 时既有 <code>g(x) = f(x)</code> 又有 <code>|f(x) - A| < ε</code>，故 <code>|g(x) - A| < ε</code>，即 <code>lim g(x) = A</code>。把 <code>f</code>、<code>g</code> 互换即得另一方向。</li></ol>\n<b>过渡说明</b>：第 2、3 步是“读定义”——把定义里出现过和没出现过的符号都点明，这类“对定义本身的观察”在数学里是非常正当的论证方式。',
              example: '<code>lim(x→1) (x² - 1)/(x - 1)</code>：在 <code>x ≠ 1</code> 时 <code>(x²-1)/(x-1) = x + 1</code>，由本定理两者极限相同，故为 2，尽管原式在 <code>x = 1</code> 无定义。\n再如 <code>f(x) = sin x / x</code>（<code>x ≠ 0</code>），<code>f(0)</code> 可以随便补充定义为任何值，都不影响 <code>lim(x→0) sin x / x = 1</code>。',
              pitfalls: [
                '在做题时把 <code>x = x₀</code> 直接代进去（比如把 <code>(x²-1)/(x-1)</code> 在 <code>x = 1</code> 代成 <code>0/0</code> 然后说没极限）。应当先做在去心邻域内合法的化简。',
                '反过来又误以为“既然无关，那求极限就随便代入”。代入法只在函数连续时才成立，那时候才可以用 <code>f(x₀)</code> 当极限。',
                '把这条定理与连续的定义搞混：连续恰恰要求 <code>lim f(x) = f(x₀)</code>，也就是要求“有关”。',
              ],
              tags: ['极限', '函数极限', '连续'],
              related: ['def-limit-finite', 'def-continuity-point', 'def-discontinuity-types', 'thm-lim-finite-sign-preserve'],
            },
            {
              id: 'thm-heine',
              kind: 'theorem',
              name: '海涅定理（函数极限与数列极限的关系）',
              aka: ['归结原则', 'Heine 定理', '函数极限与数列极限的关系'],
              statement: '设函数 <code>f</code> 在 <code>x₀</code> 的某个去心邻域内有定义，则\n<code>lim(x→x₀) f(x) = A</code>\n的<b>充分必要条件</b>是：对任何满足 <code>xₙ ≠ x₀</code> 且 <code>lim(n→∞) xₙ = x₀</code> 的数列 <code>{xₙ}</code>，都有 <code>lim(n→∞) f(xₙ) = A</code>。',
              plain: '海涅定理在说：<b>“函数极限”和“把所有的路径都试一遍的数列极限”是一回事。</b>\n函数极限的说法是“只要 <code>x</code> 靠近 <code>x₀</code>，<code>f(x)</code> 就靠近 <code>A</code>”——它一口气管住了所有靠近方式（左边来的、右边来的、跳着来的、乱着来的）。\n海涅定理告诉你，这件事等价于：随便你挑一条通往 <code>x₀</code> 的“路线”（用一个数列 <code>xₙ → x₀</code> 表示），沿这条路走 <code>f</code> 都会走向 <code>A</code>。\n它的两大用途：\n1. <b>证极限不存在</b>：只要找到两条通往 <code>x₀</code> 的路，<code>f</code> 沿它们走向不同的地方，就说明函数极限不存在。最经典的是 <code>lim(x→0) sin(1/x)</code>：取 <code>xₙ = 1/(nπ)</code> 得 <code>f(xₙ) = 0</code>，取 <code>xₙ = 1/(2nπ + π/2)</code> 得 <code>f(xₙ) = 1</code>，两个极限不同，故不存在。\n2. <b>把函数极限的结论搬给数列</b>，或者反过来。',
              why: '证明的两个方向各有各的想法。\n<b>必要性（函数极限 ⟹ 所有数列极限）</b>很直接：函数极限给了你一个 <code>δ</code>；既然 <code>xₙ → x₀</code>，那从某项起 <code>0 < |xₙ - x₀| < δ</code>，于是 <code>|f(xₙ) - A| < ε</code>。也就是说，把函数的 <code>δ</code> 当作数列的“门槛”，一步到位。这里要特别用上 <code>xₙ ≠ x₀</code>，否则可能碰到 <code>xₙ = x₀</code> 而 <code>f(x₀)</code> 无定义。\n<b>充分性（所有数列极限 ⟹ 函数极限）</b>通常用<b>反证法</b>：如果 <code>lim(x→x₀) f(x) ≠ A</code>，那么按定义，存在某个 <code>ε₀ > 0</code>，使得无论 <code>δ</code> 取多小，总能在一个 <code>δ</code> 邻域里找到一个点 <code>x</code>，使 <code>|f(x) - A| ≥ ε₀</code>。于是我们依次取 <code>δ = 1, 1/2, 1/3, …</code>，每次挑出一个“坏点” <code>xₙ</code>，满足 <code>0 < |xₙ - x₀| < 1/n</code> 但 <code>|f(xₙ) - A| ≥ ε₀</code>。这样造出的数列 <code>{xₙ}</code> 明明满足 <code>xₙ → x₀</code>、<code>xₙ ≠ x₀</code>，却让 <code>f(xₙ)</code> 永远离 <code>A</code> 至少 <code>ε₀</code>，与假设矛盾。\n这个“用 <code>1/n</code> 当 <code>δ</code> 构造反例数列”的手法非常经典，是分析学中构造反例的万能钥匙。',
              proof: '<ol><li><b>必要性。</b>设 <code>lim(x→x₀) f(x) = A</code>。任给 <code>ε > 0</code>，存在 <code>δ > 0</code>，当 <code>0 < |x - x₀| < δ</code> 时 <code>|f(x) - A| < ε</code>。</li><li>设 <code>{xₙ}</code> 满足 <code>xₙ ≠ x₀</code> 且 <code>lim(n→∞) xₙ = x₀</code>。对这同一个 <code>δ</code>，存在 <code>N</code>，当 <code>n > N</code> 时 <code>|xₙ - x₀| < δ</code>；又因 <code>xₙ ≠ x₀</code>，故此时 <code>0 < |xₙ - x₀| < δ</code>，于是 <code>|f(xₙ) - A| < ε</code>。这正是 <code>lim(n→∞) f(xₙ) = A</code>。</li><li><b>充分性（反证）。</b>设对一切满足条件的数列 <code>{xₙ}</code> 都有 <code>f(xₙ) → A</code>，但假设 <code>lim(x→x₀) f(x)</code> 不成立或不等于 <code>A</code>。</li><li>那么按极限定义的否定，存在某个 <code>ε₀ > 0</code>，使得对<b>任意</b> <code>δ > 0</code>，都存在 <code>x</code> 满足 <code>0 < |x - x₀| < δ</code> 但 <code>|f(x) - A| ≥ ε₀</code>。</li><li>对每个正整数 <code>n</code>，取 <code>δ = 1/n</code>，由上一句存在 <code>xₙ</code> 使 <code>0 < |xₙ - x₀| < 1/n</code> 且 <code>|f(xₙ) - A| ≥ ε₀</code>。</li><li>于是 <code>{xₙ}</code> 满足 <code>xₙ ≠ x₀</code>；又由 <code>|xₙ - x₀| < 1/n</code> 及 <code>1/n → 0</code>，对任意 <code>ε > 0</code> 取 <code>N = [1/ε] + 1</code>，当 <code>n > N</code> 时 <code>|xₙ - x₀| < 1/n < ε</code>，所以 <code>xₙ → x₀</code>。按假设应有 <code>f(xₙ) → A</code>，即存在 <code>N′</code> 使 <code>n > N′</code> 时 <code>|f(xₙ) - A| < ε₀</code>。</li><li>取 <code>n > max{ N, N′ }</code>，则同时有 <code>|f(xₙ) - A| ≥ ε₀</code> 与 <code>|f(xₙ) - A| < ε₀</code>，矛盾。故 <code>lim(x→x₀) f(x) = A</code> 成立。</li></ol>\n<b>过渡说明</b>：第 4 步是把“极限存在”的定义<b>取否定</b>，这是本证明的技术核心，尤其注意量词翻转后 <code>∃ ε₀</code> 会提到最前面、<code>δ</code> 变成“任意”。第 5 步用 <code>1/n</code> 把“任意小的 δ 都能找到坏点”这件事榨成一个具体的数列，这一步是构造的精髓。',
              example: '<b>证极限不存在：</b><code>lim(x→0) sin(1/x)</code>。取 <code>xₙ = 1/(nπ) → 0</code>，则 <code>sin(1/xₙ) = sin(nπ) = 0 → 0</code>；取 <code>yₙ = 1/(2nπ + π/2) → 0</code>，则 <code>sin(1/yₙ) = sin(2nπ + π/2) = 1 → 1</code>。两条路径极限不同，故函数极限不存在。\n<b>转移结论：</b>由 <code>lim(x→0) sin x / x = 1</code>，立刻可得 <code>lim(n→∞) n·sin(1/n) = 1</code>（令 <code>x = 1/n</code>）。这类“数列极限化归为函数极限”的用法在计算中极常见。',
              pitfalls: [
                '构造 <code>{xₙ}</code> 时忘记要求 <code>xₙ ≠ x₀</code>。如果允许 <code>xₙ = x₀</code>，取常数列 <code>xₙ ≡ x₀</code> 时会用到 <code>f(x₀)</code>，与极限无关，定理就不成立了。',
                '用海涅定理证“存在”时只验证了<b>一条</b>数列。必要性方向要求“任意数列”，只验一条不足以证明函数极限存在。',
                '误把定理记成“若某条数列 <code>f(xₙ)</code> 收敛则函数极限存在”。这是把充分性错记成了“存在某个数列即可”。',
              ],
              tags: ['极限', '函数极限', '数列极限', '证明'],
              related: ['def-limit-finite', 'def-limit-sequence', 'def-subsequence', 'thm-lim-unique'],
            },
          ],
        },
        {
          id: 'ch1-4',
          no: '1.4',
          title: '无穷小与无穷大',
          summary: '把“极限为 0”单独起个名字叫无穷小，把“越跑越大”叫做无穷大。无穷小是极限论里最活跃的角色，它的性质（有界量乘无穷小仍是无穷小）是后面算极限的主力工具。',
          items: [
            {
              id: 'def-infinitesimal',
              kind: 'definition',
              name: '无穷小',
              aka: ['无穷小', '无穷小量'],
              statement: '如果函数 <code>f(x)</code> 当 <code>x → x₀</code>（或 <code>x → ∞</code>）时的极限为零，即\n<code>lim(x→x₀) f(x) = 0</code>，\n那么就称函数 <code>f(x)</code> 为当 <code>x → x₀</code>（或 <code>x → ∞</code>）时的<b>无穷小</b>。\n对数列也类似：若 <code>lim(n→∞) aₙ = 0</code>，则称数列 <code>{aₙ}</code> 为 <code>n → ∞</code> 时的无穷小。',
              plain: '无穷小就是“极限为 0 的量”。注意它不是一个很小的数，而是一个<b>过程</b>：\n<code>1/1000</code> 不是无穷小，它是一个普普通通的小正数；<code>1/n</code>（当 <code>n → ∞</code>）才是无穷小，因为它会趋于 0。\n所以判断“谁是无穷小”必须先问“在什么过程中”。<code>x</code> 当 <code>x → 0</code> 时是无穷小，但当 <code>x → 1</code> 时它的极限是 1，就不是无穷小。\n最常见的四种无穷小：<code>x → 0</code> 时的 <code>x</code>、<code>sin x</code>、<code>1 - cos x</code>、<code>tan x</code>；<code>x → ∞</code> 时的 <code>1/x</code>。',
              why: '为什么要把“极限为 0”单独拿出来命名？因为它是极限运算里的“零元”，作用相当于算术里的 0。\n有了这个名字，很多结论可以说得非常短。比如“<code>lim f(x) = A</code>”等价于“<code>f(x) = A + α</code>，其中 <code>α</code> 是无穷小”。这句换语把“极限”问题转化成了“误差有多小”的问题，处理起来灵活得多。\n另外，无穷小之间还可以比“快慢”（谁趋于 0 更快），这就是下一节“无穷小的比较”，它直接催生了等价无穷小替换这个最实用的计算技巧。',
              example: '<code>x → 0</code> 时，<code>x</code>、<code>x²</code>、<code>sin x</code>、<code>ln(1 + x)</code> 都是无穷小。\n<code>x → ∞</code> 时，<code>1/x</code>、<code>1/x²</code>、<code>e⁻ˣ</code> 都是无穷小。\n不是无穷小的例子：常数 <code>0</code> 是无穷小（极限恒为 0）；常数 <code>0.000001</code> 不是无穷小（它的极限是它自己，不为 0）；<code>x → 0</code> 时 <code>x + 1</code> 不是无穷小（极限为 1）。',
              pitfalls: [
                '把无穷小当成“很小很小的数”。无穷小是一个变量（或函数、数列），描述的是极限行为。',
                '不说明过程就说“<code>x</code> 是无穷小”。必须写清 <code>x → 0</code> 还是 <code>x → ∞</code>。',
                '认为“无穷小 + 无穷小一定还是无穷小”却忽略项数。有限个无穷小的和仍是无穷小，<b>无穷多个</b>就不一定了：<code>1/n</code> 加 <code>n</code> 次等于 1，不是无穷小。',
              ],
              tags: ['无穷小', '极限', '基础概念'],
              related: ['def-limit-finite', 'thm-infinitesimal-operations', 'thm-infinitesimal-bounded-product', 'def-infinitesimal-order', 'thm-limit-infinitesimal-relation'],
            },
            {
              id: 'thm-limit-infinitesimal-relation',
              kind: 'theorem',
              name: '函数极限与无穷小的关系',
              aka: ['极限=常数+无穷小'],
              statement: '在同一个极限过程下，<code>lim f(x) = A</code> 的<b>充分必要条件</b>是\n<code>f(x) = A + α</code>，\n其中 <code>α</code> 是同一极限过程中的无穷小（即 <code>lim α = 0</code>）。',
              plain: '这句话在说：<b>“<code>f</code> 的极限是 <code>A</code>”和“<code>f</code> 等于 <code>A</code> 加上一个趋于 0 的误差”完全是一回事。</b>\n比如 <code>f(x) = 2 + 1/x</code>（<code>x → ∞</code>）：它的极限是 2，而它确实等于 2 加上 <code>1/x</code>，而 <code>1/x</code> 是无穷小。\n反过来也行：如果 <code>f(x) = 3 + α</code> 而 <code>α → 0</code>，那 <code>f</code> 的极限就是 3。\n这个转换的价值在于：把“求极限”翻译成“做加法并控制一个余项”，很多估计问题就变成了“证明余项趋于 0”，写起来更灵活。',
              why: '这条结论几乎是定义的直接改写，但它是无穷小理论的地基。\n推导思路：<code>|f(x) - A| < ε</code> 这句话，正好就是 <code>|α| < ε</code> 其中 <code>α = f(x) - A</code>。所以“<code>f(x) → A</code>”和“<code>f(x) - A → 0</code>”是同义反复。\n正因为如此，<b>任何极限问题都可以改写成“无穷小问题”</b>，而无穷小最重要的一条性质就是“可以忽略高阶部分”，这为后面的等价无穷小替换、泰勒展开铺平了路。',
              proof: '<ol><li>记 <code>α(x) = f(x) - A</code>。</li><li><b>必要性：</b>若 <code>lim f(x) = A</code>，任给 <code>ε > 0</code>，存在 <code>δ > 0</code>，当 <code>0 < |x - x₀| < δ</code> 时 <code>|f(x) - A| < ε</code>，即 <code>|α(x)| < ε</code>。按定义这正是 <code>lim α(x) = 0</code>，故 <code>α</code> 是无穷小。</li><li><b>充分性：</b>若 <code>f(x) = A + α(x)</code> 且 <code>lim α(x) = 0</code>，任给 <code>ε > 0</code>，存在 <code>δ > 0</code>，当 <code>0 < |x - x₀| < δ</code> 时 <code>|α(x)| < ε</code>，即 <code>|f(x) - A| < ε</code>。按定义 <code>lim f(x) = A</code>。</li><li>两个方向都成立，故结论成立。</li></ol>\n<b>过渡说明</b>：证明的关键在于“看出定义的左右两边是同一句话”，不需要任何技巧。这种“定义换语言”的定理在教材里很多，读的时候要习惯它们。',
              example: '<code>lim(x→∞) (3 + 2/x) = 3</code>，因为 <code>3 + 2/x = 3 + α</code> 且 <code>α = 2/x → 0</code>。\n<code>lim(x→0) sin x / x = 1</code> 可以写成 <code>sin x / x = 1 + α</code>，其中 <code>α = sin x/x - 1 → 0</code>。这个写法在估计 <code>sin x</code> 与 <code>x</code> 的误差时非常有用。\n反过来用：要证 <code>lim f(x) = 0</code>，可以把它拆成两个趋于 0 的部分之和。',
              pitfalls: [
                '把 <code>α</code> 当成“随便一个小量”而不是“趋于 0 的变量”。',
                '写成 <code>f(x) = A + α</code> 后忘记说明 <code>α</code> 是在<b>同一个</b>极限过程下趋于 0。',
                '误以为 <code>A</code> 也可以是无穷小或无穷大。这里 <code>A</code> 必须是有限常数。',
              ],
              tags: ['无穷小', '极限', '证明'],
              related: ['def-infinitesimal', 'def-limit-finite', 'thm-lim-operations', 'def-infinitesimal-order'],
            },
            {
              id: 'thm-infinitesimal-operations',
              kind: 'theorem',
              name: '无穷小的运算性质',
              aka: ['有限个无穷小的和', '无穷小的乘积'],
              statement: '在同一个极限过程中：\n1. <b>有限个</b>无穷小的代数和仍是无穷小；\n2. <b>有限个</b>无穷小的乘积仍是无穷小；\n3. 常数与无穷小的乘积仍是无穷小。',
              plain: '三条都很好理解，但每条里都藏着一个“有限”的限制。\n1. 几个都趋于 0 的东西加起来，当然还是趋于 0。但如果是<b>无穷多个</b>，就不一定了：<code>1/n</code> 加 <code>n</code> 次等于 <code>1</code>，一点不趋于 0。\n2. 几个趋于 0 的东西相乘，只会更快地趋于 0。<code>1/n · 1/n = 1/n²</code>，跑得更快。\n3. 常数乘无穷小，只是把趋于 0 的速度按比例缩放一下，还是趋于 0。',
              why: '证明的关键工具还是三角不等式，以及一个很实用的技巧：<b>用 ε/2 和 ε/2 分给两个无穷小</b>。\n如果你直接对 <code>α + β</code> 用定义，会得到 <code>|α + β| < ε + ε = 2ε</code>，常数 2 碍眼。所以标准写法是：先定好要证的是 <code>|α + β| < ε</code>，于是对 <code>α</code> 用 <code>ε/2</code>、对 <code>β</code> 也用 <code>ε/2</code>，加起来正好是 <code>ε</code>。\n这个“<b>分半法</b>”是极限证明中最高频的技巧，一定要养成习惯：<b>看到 ε 就想能不能分成 ε/2 + ε/2</b>。\n至于乘积那条，还要额外用一次“局部有界性”：其中一个无穷小在局部是有界的（因为收敛必有局部界），用它当系数去压另一个。这就是为什么“有限个”这个限制重要——无穷多个因子相乘时，界会失控。',
              proof: '<ol><li><b>证 1（两个无穷小的和）。</b>设 <code>lim α(x) = 0</code>，<code>lim β(x) = 0</code>（同一极限过程）。任给 <code>ε > 0</code>，则 <code>ε/2 > 0</code>。</li><li>对 <code>ε/2</code>，存在 <code>δ₁ > 0</code>，当 <code>0 < |x - x₀| < δ₁</code> 时 <code>|α(x)| < ε/2</code>；存在 <code>δ₂ > 0</code>，当 <code>0 < |x - x₀| < δ₂</code> 时 <code>|β(x)| < ε/2</code>。</li><li>取 <code>δ = min{ δ₁, δ₂ }</code>，则当 <code>0 < |x - x₀| < δ</code> 时，由三角不等式\n<code>|α(x) + β(x)| ≤ |α(x)| + |β(x)| < ε/2 + ε/2 = ε</code>。故 <code>lim (α + β) = 0</code>。有限多个的情形对它反复使用即可（对项数作归纳）。</li><li><b>证 3（常数乘无穷小）。</b>设 <code>|c|</code> 为常数，<code>lim α(x) = 0</code>。若 <code>c = 0</code>，则 <code>cα ≡ 0</code>，结论显然。若 <code>c ≠ 0</code>，任给 <code>ε > 0</code>，取 <code>ε/|c| > 0</code>，存在 <code>δ > 0</code>，当 <code>0 < |x - x₀| < δ</code> 时 <code>|α(x)| < ε/|c|</code>，于是 <code>|c α(x)| = |c| · |α(x)| < ε</code>。故 <code>lim cα = 0</code>。</li><li><b>证 2（两个无穷小的积）。</b>设 <code>lim α(x) = 0</code>。由局部有界性，取 <code>ε = 1</code> 可得存在 <code>δ₁ > 0</code> 与 <code>M > 0</code>，使 <code>0 < |x - x₀| < δ₁</code> 时 <code>|α(x)| ≤ M</code>。</li><li>任给 <code>ε > 0</code>，对 <code>ε/M > 0</code>（<code>M > 0</code>），存在 <code>δ₂ > 0</code>，当 <code>0 < |x - x₀| < δ₂</code> 时 <code>|β(x)| < ε/M</code>。取 <code>δ = min{ δ₁, δ₂ }</code>，则当 <code>0 < |x - x₀| < δ</code> 时\n<code>|α(x) β(x)| = |α(x)| · |β(x)| ≤ M · (ε/M) = ε</code>。故 <code>lim αβ = 0</code>，即无穷小乘无穷小仍是无穷小。</li><li>有限多个无穷小相乘，对上述结论反复使用即可。</li></ol>\n<b>过渡说明</b>：第 1 至 3 步是“分半法”的样板。第 5 步先把 <code>α</code> 关进一个有界的笼子（这一步是关键，因为 <code>α</code> 本身趋于 0，但我们需要一个<b>与 x 无关</b>的界），再用 <code>β</code> 的小去压整个乘积。',
              example: '<code>x → 0</code> 时 <code>x + x² + x³</code> 是无穷小（三个无穷小之和）。\n<code>x → 0</code> 时 <code>x · sin x</code> 是无穷小（两个无穷小之积），而且它比 <code>x</code> 更快地趋于 0（<code>x sin x ~ x²</code>）。\n<code>x → ∞</code> 时 <code>5/x²</code> 是无穷小（常数乘无穷小）。\n反例（无穷多个）：<code>n</code> 个 <code>1/n²</code> 相加等于 <code>1/n</code>，确实还是无穷小；但 <code>n</code> 个 <code>1/n</code> 相加等于 <code>1</code>，不再是无穷小。可见“有限”这个条件不能删。',
              pitfalls: [
                '忽略“有限个”这一条件，把结论用到无穷多项上，这是级数理论中大量错误的来源。',
                '证明乘积时忘记先证局部有界，直接写 <code>|αβ| < ε · ε</code>，这样得到的只是 <code>< ε²</code>，逻辑上不能直接给出 <code>< ε</code>。',
                '把“无穷小的商仍是无穷小”也当成性质。这是错的：<code>x²/x = x</code> 是无穷小，但 <code>x/x² = 1/x</code> 是无穷大，取决于谁更快。',
              ],
              tags: ['无穷小', '极限', '证明'],
              related: ['def-infinitesimal', 'thm-infinitesimal-bounded-product', 'thm-lim-operations', 'def-infinitesimal-order'],
            },
            {
              id: 'thm-infinitesimal-bounded-product',
              kind: 'theorem',
              name: '有界函数与无穷小的乘积',
              aka: ['有界量乘无穷小'],
              statement: '在同一极限过程中，如果 <code>α</code> 是无穷小，而函数 <code>g</code> 在该极限过程的某个局部范围内<b>有界</b>（即 <code>|g(x)| ≤ M</code> 在某个去心邻域内成立），则乘积 <code>g(x) · α(x)</code> 仍是无穷小。',
              plain: '“有界量乘无穷小，结果还是无穷小”。\n这条性质的用处大到不可替代，因为它能一次性解决“<b>震荡因子</b>”带来的麻烦。最经典的例子：\n<code>lim(x→0) x · sin(1/x) = 0</code>。当 <code>x → 0</code> 时，<code>sin(1/x)</code> 疯狂震荡、根本没有极限，但没关系——它被牢牢关在 <code>[-1, 1]</code> 里。既然它跳不出这个范围，那 <code>x</code> 趋于 0 时就会把整个乘积一起拉向 0。\n一句话概括：<b>一个是小的，一个是有界的，小的说了算。</b>',
              why: '证明非常简单，只有一行估计：<code>|g(x)α(x)| ≤ M|α(x)|</code>，然后让 <code>|α(x)| < ε/M</code> 就行。\n但它值得单独列一条，因为它把“震荡”和“有界”这两件事理清了：极限不存在的原因往往不是函数值太大，而是它不收敛（比如 <code>sin(1/x)</code>）；但只要它<b>有界</b>，再被一个无穷小一乘，问题就消失了。\n要注意适用范围：一定要有一个因子趋于 0。如果两个因子都只是有界（比如 <code>sin(1/x) · cos(1/x)</code>），那乘积不一定有极限。',
              proof: '<ol><li>设在该极限过程的某个去心邻域内 <code>|g(x)| ≤ M</code>（<code>M > 0</code> 为常数），且 <code>lim α(x) = 0</code>。</li><li>若 <code>M = 0</code>，则 <code>g ≡ 0</code>，乘积恒为 0，结论显然。以下设 <code>M > 0</code>。</li><li>任给 <code>ε > 0</code>，则 <code>ε/M > 0</code>。由 <code>lim α(x) = 0</code>，存在 <code>δ₁ > 0</code>，当 <code>0 < |x - x₀| < δ₁</code> 时 <code>|α(x)| < ε/M</code>。</li><li>又由有界性有 <code>δ₂ > 0</code>，当 <code>0 < |x - x₀| < δ₂</code> 时 <code>|g(x)| ≤ M</code>。取 <code>δ = min{ δ₁, δ₂ }</code>。</li><li>当 <code>0 < |x - x₀| < δ</code> 时，<code>|g(x) α(x)| = |g(x)| · |α(x)| ≤ M · (ε/M) = ε</code>。</li><li>按定义 <code>lim g(x)α(x) = 0</code>，即乘积是无穷小。定理得证。</li></ol>\n<b>过渡说明</b>：证明的关键是第 5 步那个乘法估计——把“有界”的界 <code>M</code> 和“无穷小”的精度 <code>ε/M</code> 配成一对，恰好凑出 <code>ε</code>。这与“无穷小乘无穷小”的证明思路完全一致，区别只是这里不需要先证局部有界，因为它是假设。',
              example: '<code>lim(x→0) x sin(1/x) = 0</code>：<code>x</code> 是无穷小，<code>|sin(1/x)| ≤ 1</code> 有界，故乘积为无穷小。\n<code>lim(x→∞) (sin x)/x = 0</code>：<code>1/x</code> 是无穷小，<code>sin x</code> 有界，结论成立。\n<code>lim(x→0) x² · arctan(1/x) = 0</code>：<code>|arctan(1/x)| < π/2</code> 有界。注意反例：<code>lim(x→0) (1/x) sin(1/x)</code> 不能用这条定理（<code>1/x</code> 不是无穷小而是无穷大），事实上该极限不存在。',
              pitfalls: [
                '把“有界”与“有极限”混起来当条件用。定理只要求有界，不需要有极限——这正是它的威力所在。',
                '忘记检查“有界”是否在<b>该极限过程的局部</b>成立。<code>1/x</code> 在 <code>x → 0</code> 附近无界，不能当有界量用。',
                '误以为“两个有界量之积必收敛”，这是错的。',
              ],
              tags: ['无穷小', '极限', '证明'],
              related: ['def-infinitesimal', 'thm-infinitesimal-operations', 'def-bounded-function', 'thm-squeeze'],
            },
            {
              id: 'def-infinity-large',
              kind: 'definition',
              name: '无穷大',
              aka: ['无穷大', '无穷大量', '正无穷大', '负无穷大'],
              statement: '设函数 <code>f(x)</code> 在 <code>x₀</code> 的某个去心邻域内有定义。若对任意给定的正数 <code>M</code>（不论它多么大），总存在正数 <code>δ</code>，使得当 <code>0 < |x - x₀| < δ</code> 时，恒有 <code>|f(x)| > M</code>，则称函数 <code>f(x)</code> 当 <code>x → x₀</code> 时为<b>无穷大</b>，记作\n<code>lim(x→x₀) f(x) = ∞</code>。\n若把条件换成 <code>f(x) > M</code>，则记作 <code>lim(x→x₀) f(x) = +∞</code>；换成 <code>f(x) < -M</code>，则记作 <code>lim(x→x₀) f(x) = -∞</code>。\n类似地可以定义 <code>x → ∞</code> 时的无穷大。',
              plain: '无穷大的意思是“<b>要多大有多大</b>”：你说一个数 <code>M</code>（比如一百万），我都能指出从多近开始，函数值的绝对值就超过 <code>M</code> 了。<code>M</code> 提得越大，我能给的范围就越小，但总给得出来。\n两个必须记住的要点：\n1. <b>无穷大不是数，是一个记号</b>。它表示“极限不存在，但以一种特殊的方式不存在（越跑越大）”。所以在正式表述里，<code>lim f(x) = ∞</code> 属于“极限不存在”的一种情形，不能拿去参与算术运算。\n2. <b>无穷大与无界不是一回事</b>。无穷大是“最终所有值都很大”；无界只是“总能找到很大的值”，中间可以夹杂小的值。反例：<code>f(x) = x sin x</code> 当 <code>x → ∞</code> 时无界（<code>x = 2kπ + π/2</code> 处取到 $x$ 那么大），但 <code>x = kπ</code> 处函数值恒为 0，所以它不是无穷大。\n3. <b>无穷大也分正负</b>。<code>1/x</code> 当 <code>x → 0⁺</code> 时是 <code>+∞</code>，当 <code>x → 0⁻</code> 时是 <code>-∞</code>，而当 <code>x → 0</code> 时只能说 <code>|f| → ∞</code>，写作 <code>lim(x→0) 1/x = ∞</code>。',
              why: '为什么无穷大要用 <code>|f(x)| > M</code> 这种“绝对值很大”来定义，而不是干脆定义成某个新数？\n因为在实数系里，<code>∞</code> 不是一个实数：它不满足“任意两个数都能比较大小并做四则运算”这些实数该有的性质。比如 <code>∞ - ∞</code>、<code>∞ / ∞</code> 都没有唯一合理的结果。如果硬把它当数，推导立刻出乱子。\n所以数学家采取的办法是：<b>把 <code>∞</code> 当作一种“极限行为”的描述</b>。“<code>f(x) = ∞</code>”读作“<code>f</code> 的绝对值最终超过任意给定的正数”，而不是“<code>f</code> 等于某个叫无穷大的东西”。\n理解了这一点，就明白为什么 <code>lim f = ∞</code> 属于“极限不存在”——因为我们从来没定义过“以 ∞ 为极限的收敛”，它只是发散中特别规整的一类。',
              example: '<code>lim(x→0) 1/x² = +∞</code>：任给 <code>M > 0</code>，取 <code>δ = 1/√M</code>，则 <code>0 < |x| < δ</code> 时 <code>1/x² > M</code>。\n<code>lim(x→∞) x³ = +∞</code>；<code>lim(x→∞) -x² = -∞</code>；<code>lim(x→∞) (-1)ⁿ</code> 型的震荡不算无穷大。\n<code>lim(x→∞) x sin x</code> 既不是有限极限，也不是无穷大，只能说它无界。这个例子最值得琢磨。',
              pitfalls: [
                '把无穷大当成数来运算，写出 <code>∞ - ∞ = 0</code>、<code>∞ / ∞ = 1</code> 这类错误结论。',
                '把“无界”当成“无穷大”，用 <code>x sin x</code> 的反例就能打掉。',
                '把 <code>lim f(x) = ∞</code> 说成“极限存在，极限是 ∞”。严格的说法是“极限不存在（但趋向无穷大）”，考试答题时最好按教材口径表述。',
                '忘却正负号：<code>lim(x→0) 1/x</code> 不能写成 <code>+∞</code>，两侧符号不同，只能说为 <code>∞</code>。',
              ],
              tags: ['无穷大', '极限', '基础概念'],
              related: ['def-infinitesimal', 'thm-infinitesimal-infinity-relation', 'def-bounded-function', 'def-limit-infinite'],
            },
            {
              id: 'thm-infinitesimal-infinity-relation',
              kind: 'theorem',
              name: '无穷小与无穷大的关系',
              aka: ['无穷大的倒数'],
              statement: '在同一个极限过程中：\n1. 若 <code>lim f(x) = ∞</code>，则 <code>lim 1/f(x) = 0</code>（即无穷大的倒数是无穷小）；\n2. 若 <code>lim f(x) = 0</code> 且 <code>f(x) ≠ 0</code>，则 <code>lim 1/f(x) = ∞</code>（即（非零）无穷小的倒数是无穷大）。',
              plain: '这条定理很符合直觉：<b>一个东西越跑越大，它的倒数就越贴 0；一个东西越贴 0，它的倒数就越跑越大。</b>\n比如 <code>x → 0⁺</code> 时 <code>x → 0</code>（无穷小），而 <code>1/x → +∞</code>（无穷大）；反过来 <code>x → ∞</code> 时 <code>x → ∞</code>，而 <code>1/x → 0</code>。\n注意第 2 条里的条件“<code>f(x) ≠ 0</code>”不能省。如果 <code>f</code> 会取到 0（哪怕只是偶尔），<code>1/f</code> 就没定义了。比如 <code>f(x) = x sin(1/x)</code> 在 <code>x → 0</code> 时趋于 0，但在无数个点上等于 0（<code>x = 1/(nπ)</code>），所以 <code>1/f</code> 根本不是一个在去心邻域上处处有定义的函数，谈不上求极限。',
              why: '证明的核心是一次不等式的“取倒数”操作，而要取倒数就必须处理分子分母的大小关系。\n以第 1 条为例：目标是要证 <code>|1/f(x)| < ε</code>，这等价于 <code>|f(x)| > 1/ε</code>。而这个形式恰好就是无穷大定义的内容！所以只要在无穷大的定义里把 <code>M</code> 取成 <code>1/ε</code>，就一步到位。\n这就是为什么两个定义长得如此对称：<b>无穷小定义里的 <code>&lt; ε</code> 和无穷大定义里的 <code>&gt; M</code> 互为倒数关系</b>。教材把它们放在同一节，正是因为这种对偶性。',
              proof: '<ol><li><b>证 1。</b>设 <code>lim(x→x₀) f(x) = ∞</code>，即在某个去心邻域内 <code>f(x) ≠ 0</code>（因为 <code>|f(x)|</code> 最终大于 1），于是 <code>1/f(x)</code> 有意义。</li><li>任给 <code>ε > 0</code>，令 <code>M = 1/ε > 0</code>。由无穷大定义，存在 <code>δ > 0</code>，当 <code>0 < |x - x₀| < δ</code> 时 <code>|f(x)| > M = 1/ε</code>。</li><li>于是 <code>|1/f(x) - 0| = 1/|f(x)| < ε</code>。按定义 <code>lim 1/f(x) = 0</code>。</li><li><b>证 2。</b>设 <code>lim(x→x₀) f(x) = 0</code> 且在某去心邻域内 <code>f(x) ≠ 0</code>，于是 <code>1/f(x)</code> 有意义。</li><li>任给 <code>M > 0</code>，令 <code>ε = 1/M > 0</code>。由 <code>lim f(x) = 0</code>，存在 <code>δ > 0</code>，当 <code>0 < |x - x₀| < δ</code> 时 <code>|f(x)| < ε = 1/M</code>。</li><li>于是 <code>|1/f(x)| = 1/|f(x)| > M</code>。按定义 <code>lim 1/f(x) = ∞</code>。定理得证。</li></ol>\n<b>过渡说明</b>：两个方向的证明完全对称，都是“把 <code>ε</code> 和 <code>M</code> 用倒数互换”，这正是两条定义互为镜像的体现。第 1 步和第 4 步都要先确认 <code>1/f</code> 在邻域内有定义，这一步常被忽略但逻辑上必需。',
              example: '<code>lim(x→0) 1/x² = +∞</code>，故 <code>lim(x→0) x² = 0</code>（其实后者更显然）。\n<code>lim(x→∞) (x² + 1) = ∞</code>，故 <code>lim(x→∞) 1/(x² + 1) = 0</code>。\n<code>lim(x→1) (x - 1) = 0</code>，故 <code>lim(x→1) 1/(x - 1) = ∞</code>（这是一个典型的无穷型间断点）。\n注意 <code>lim(x→0) x sin(1/x) = 0</code>，但因为 <code>x sin(1/x)</code> 在 <code>x = 1/(nπ)</code> 处等于 0，所以不能说 <code>1/(x sin(1/x))</code> 趋向无穷大。',
              pitfalls: [
                '忽略“<code>f(x) ≠ 0</code>”的条件，对会取零值的函数乱用倒数关系。',
                '把 <code>lim f = ∞</code> 与 <code>lim f</code> 不存在当成两件不同的事。按教材口径，前者是后者的特殊情形。',
                '以为“无穷小的倒数一定是无穷大”而不用管符号：注意 <code>1/x</code> 在 <code>x → 0⁺</code> 是 <code>+∞</code>，在 <code>x → 0⁻</code> 是 <code>-∞</code>，双侧只能说 <code>∞</code>。',
              ],
              tags: ['无穷小', '无穷大', '证明'],
              related: ['def-infinitesimal', 'def-infinity-large', 'thm-lim-operations'],
            },
          ],
        },
        {
          id: 'ch1-5',
          no: '1.5',
          title: '极限运算法则',
          summary: '给出极限的四则运算法则与复合函数的极限法则。这一节是“算术”，但要牢牢记住每条法则的前提：每个极限都得先存在。',
          items: [
            {
              id: 'thm-lim-operations',
              kind: 'theorem',
              name: '极限的四则运算法则',
              aka: ['极限的和差积商', '极限运算法则'],
              statement: '设在同一极限过程中 <code>lim f(x) = A</code>，<code>lim g(x) = B</code>（<code>A</code>、<code>B</code> 均为有限常数），则：\n1. <code>lim [f(x) ± g(x)] = A ± B</code>；\n2. <code>lim [f(x) · g(x)] = A · B</code>；\n3. 若又有 <code>B ≠ 0</code>，则 <code>lim [f(x) / g(x)] = A / B</code>。\n推论：<code>lim [c · f(x)] = cA</code>（<code>c</code> 为常数），<code>lim [f(x)]ⁿ = Aⁿ</code>（<code>n</code> 为正整数）。',
              plain: '这三条法则就是“极限可以拆开算”的许可证：加法、减法、乘法可以直接各算各的再合起来，除法也一样——但<b>分母的极限不能是 0</b>。\n为什么除法要额外加条件？因为除以一个趋于 0 的东西，结果可能爆掉或者乱飘。比如 <code>lim(x→0) x/x = 1</code>，但 <code>lim(x→0) x/x²</code> 是无穷大，<code>lim(x→0) x²/x = 0</code>——分子分母都趋于 0，结果却可以是任意值，这就是所谓“不定式”，不能直接套法则。\n记住最关键的一条使用纪律：<b>用之前先确认每个部分的极限都存在且有限。</b>',
              why: '证明的基本套路是：把“函数”改写成“极限 + 无穷小”，然后做代数运算，最后再把结果收回成“极限 + 无穷小”。\n比如乘法那条：设 <code>f = A + α</code>、<code>g = B + β</code>（<code>α</code>、<code>β</code> 为无穷小），则 <code>fg = AB + Aβ + Bα + αβ</code>。只要证明后面三项之和是无穷小，就得到 <code>lim fg = AB</code>。而这三项都是“常数乘无穷小”或“无穷小乘无穷小”，前面已经证过它们仍是无穷小。\n这就是为什么要把“无穷小”单独定义一节：<b>有它当工具，四则运算法则的证明就变成了简单的代数展开。</b>如果直接回到 ε-δ 定义去硬算乘法，那就要处理 <code>|fg - AB| = |(f-A)g + A(g-B)|</code> 这类估计，需要用到 <code>g</code> 的局部有界性，麻烦得多。\n至于除法，思路是先证“<code>lim 1/g = 1/B</code>”，再用乘法法则合起来。而证明 <code>1/g → 1/B</code> 时，关键的第一步是先说明<b><code>g</code> 在局部不会靠近 0</b>（因为 <code>B ≠ 0</code>，由保号性可知局部 <code>|g(x)| > |B|/2</code>），这样才能给出 <code>|1/g|</code> 的上界估计。',
              proof: '<ol><li><b>准备。</b>由“极限与无穷小的关系”，在同一个极限过程下可写 <code>f(x) = A + α(x)</code>，<code>g(x) = B + β(x)</code>，其中 <code>α → 0</code>，<code>β → 0</code>。</li><li><b>证 1（和差）。</b><code>f ± g = (A ± B) + (α ± β)</code>。由“有限个无穷小的代数和仍是无穷小”，<code>α ± β → 0</code>，故 <code>f ± g = (A ± B) + 无穷小</code>，即 <code>lim (f ± g) = A ± B</code>。</li><li><b>证 2（乘积）。</b><code>f g = (A + α)(B + β) = AB + Aβ + Bα + αβ</code>。这里 <code>Aβ</code>、<code>Bα</code> 是“常数乘无穷小”，<code>αβ</code> 是“无穷小乘无穷小”，由无穷小的运算性质，三者之和仍是无穷小。故 <code>fg = AB + 无穷小</code>，即 <code>lim fg = AB</code>。</li><li><b>证 3 的第一步：<code>g</code> 在局部与 0 分离。</b>设 <code>B ≠ 0</code>，取 <code>ε = |B|/2 > 0</code>，则存在 <code>δ₁ > 0</code>，当 <code>0 < |x - x₀| < δ₁</code> 时 <code>|g(x) - B| < |B|/2</code>，于是 <code>|g(x)| ≥ |B| - |g(x) - B| > |B| - |B|/2 = |B|/2 > 0</code>。这说明在该范围内 <code>1/g</code> 有意义，且 <code>|1/g(x)| < 2/|B|</code>。</li><li><b>证 3 的第二步：<code>1/g → 1/B</code>。</b>估计\n<code>|1/g(x) - 1/B| = |B - g(x)| / (|B| · |g(x)|) < (2/|B|²) · |g(x) - B|</code>。<br>任给 <code>ε > 0</code>，取 <code>η = ε|B|²/2 > 0</code>，由 <code>g → B</code> 存在 <code>δ₂ > 0</code>，当 <code>0 < |x - x₀| < δ₂</code> 时 <code>|g(x) - B| < η</code>。取 <code>δ = min{ δ₁, δ₂ }</code>，则在该范围内 <code>|1/g - 1/B| < (2/|B|²)·(ε|B|²/2) = ε</code>，故 <code>lim 1/g = 1/B</code>。</li><li><b>证 3 的结论。</b>由乘积法则与上一步，<code>lim f/g = lim (f · 1/g) = A · (1/B) = A/B</code>。定理证毕。</li></ol>\n<b>过渡说明</b>：本证明全程使用“无穷小语言”，避免了 ε 的反复纠缠；唯一的 ε 估计出现在第 4、5 步，而第 4 步“先把分母推离 0”是除法法则的技术核心——没有这一步，<code>1/g</code> 连定义都成问题。',
              example: '<code>lim(x→2) (3x² - 2x + 1) = 3·4 - 4 + 1 = 9</code>：这是多项式，直接拆开算再合并。\n<code>lim(x→1) (x² - 1)/(x + 3) = 0/4 = 0</code>：分母极限 4 ≠ 0，可以直除。\n<code>lim(x→∞) (2x² + 1)/(3x² - x) = 2/3</code>：分子分母都趋于无穷，不能直接套除法法则，要先同除 <code>x²</code>，化成 <code>(2 + 1/x²)/(3 - 1/x) → 2/3</code>。\n<code>lim(x→1) (x² - 1)/(x - 1) = lim(x→1)(x + 1) = 2</code>：约分之后再用法则。注意约分这一步之所以合法，是因为在 <code>x ≠ 1</code> 的去心邻域内两个表达式相等。',
              pitfalls: [
                '不检查存在性就拆：<code>lim(x→∞) (x - x) = 0</code> 是对的，但如果写成 <code>lim x - lim x = ∞ - ∞</code> 就错了——两个分项的极限都不存在，法则不适用。',
                '在分母极限为 0 时直接写 <code>A/B</code>。例如 <code>lim(x→0) (sin x)/x</code>，分母极限是 0，绝不能写成 <code>0/0</code> 或 <code>1</code>，必须用别的方法（这里是重要极限）。',
                '把法则用在“部分项”上：<code>lim(x→∞) [x · (1/x + 1)]</code> 如果只对括号外的 <code>x</code> 和括号内的一项分别取极限就乱了，必须先整体化简。',
                '以为“极限可以随便拆”是等价的，实际只是<b>单向</b>：各项极限存在 ⟹ 和差的极限等于极限的和差；反过来不成立。',
              ],
              tags: ['极限', '运算法则', '证明'],
              related: ['thm-limit-infinitesimal-relation', 'def-infinitesimal', 'thm-squeeze', 'thm-limit-composite'],
            },
            {
              id: 'thm-lim-sequence-operations',
              kind: 'theorem',
              name: '数列极限的四则运算法则',
              aka: ['数列极限的运算'],
              statement: '设 <code>lim(n→∞) aₙ = A</code>，<code>lim(n→∞) bₙ = B</code>（<code>A</code>、<code>B</code> 为有限常数），则：\n1. <code>lim(n→∞) (aₙ ± bₙ) = A ± B</code>；\n2. <code>lim(n→∞) (aₙ · bₙ) = A · B</code>；\n3. 若 <code>bₙ ≠ 0</code>（<code>n</code> 充分大时）且 <code>B ≠ 0</code>，则 <code>lim(n→∞) aₙ / bₙ = A / B</code>。',
              plain: '这和函数极限的四则运算法则几乎一字不差，只是把“<code>x → x₀</code>”换成了“<code>n → ∞</code>”。\n有了它，求很多数列极限就变成了“拆成几个熟悉的块”：<code>(3n + 1)/(n - 2)</code> 上下同除 <code>n</code>，变成 <code>(3 + 1/n)/(1 - 2/n)</code>，而 <code>1/n → 0</code>，于是结果是 3。\n注意除法那条多了一个条件“<code>bₙ ≠ 0</code>（<code>n</code> 充分大时）”。这是因为数列里可能出现零项，那样 <code>aₙ/bₙ</code> 就没有定义；但因为极限 <code>B ≠ 0</code>，由保号性保证后面不会再有 0。',
              why: '证明完全可以把数列看成“定义在正整数上的函数”，于是函数极限的四则运算法则直接搬过来用。教材单独列出，主要是为了使用方便。\n唯一值得留意的是除法里那个“<code>n</code> 充分大时 <code>bₙ ≠ 0</code>”。它来自保号性：既然 <code>B ≠ 0</code>，那么从某项以后 <code>|bₙ| > |B|/2 > 0</code>，所以后面的项都不会是 0。前面有限项里有 0 没关系，反正极限只看后面。\n这也解释了为什么在做题时遇到 <code>bₙ</code> 有零项的数列，仍然可以放心地讨论比值极限——只要指明“从某项起”。',
              proof: '<ol><li>把数列看作函数 <code>f(n) = aₙ</code>、<code>g(n) = bₙ</code>（<code>n ∈ ℕ₊</code>），按函数极限（<code>n → +∞</code> 情形）的运算法则，结论 1、2 直接成立。</li><li>对结论 3：由 <code>B ≠ 0</code> 及保号性，存在 <code>N₀</code>，当 <code>n > N₀</code> 时 <code>|bₙ| > |B|/2 > 0</code>，故从第 <code>N₀ + 1</code> 项起商 <code>aₙ/bₙ</code> 有定义。</li><li>在这之后，令 <code>aₙ = A + αₙ</code>，<code>bₙ = B + βₙ</code>（<code>αₙ → 0</code>，<code>βₙ → 0</code>）。则\n<code>aₙ/bₙ - A/B = (A + αₙ)/(B + βₙ) - A/B = (B αₙ - A βₙ) / [B(B + βₙ)]</code>。</li><li>分子 <code>Bαₙ - Aβₙ → 0</code>（常数乘无穷小、无穷小的和）；分母 <code>B(B + βₙ) → B² ≠ 0</code>，且当 <code>n</code> 充分大时 <code>|B(B + βₙ)| ≥ |B|·|B|/2 = B²/2 > 0</code>。</li><li>于是 <code>|aₙ/bₙ - A/B| ≤ |Bαₙ - Aβₙ| / (B²/2) → 0</code>，即 <code>lim aₙ/bₙ = A/B</code>。结论得证。</li></ol>\n<b>过渡说明</b>：第 1 步把数列归入函数框架，省去重复劳动。第 2 步先用保号性保证商有意义，这是容易被忽略但不可省的一步。第 3、4 步是标准的“通分 + 估计分母远离 0”套路，与函数情形完全一致。',
              example: '<code>lim(n→∞) (2n² + 3n - 1)/(n² - n + 5) = 2</code>：上下同除 <code>n²</code> 得 <code>(2 + 3/n - 1/n²)/(1 - 1/n + 5/n²) → 2/1 = 2</code>。\n<code>lim(n→∞) (√(n+1) - √n) = lim(n→∞) 1/(√(n+1) + √n) = 0</code>：先有理化再用法则。\n<code>lim(n→∞) (1 + 2 + … + n)/n² = lim(n→∞) [n(n+1)/2]/n² = 1/2</code>：先求和再拆分。',
              pitfalls: [
                '对 <code>∞ - ∞</code> 型直接用法则：<code>lim(√(n²+n) - n)</code> 不能拆成 <code>∞ - ∞</code>，必须有理化得 <code>lim n/(√(n²+n) + n) = 1/2</code>。',
                '把“分子分母同时除以最高次幂”和“分别求极限再相除”混在一起写，中间步骤出现 <code>∞/∞</code>。',
                '忽略数列的项数依赖 <code>n</code>：像 <code>(1 + 1/n)ⁿ</code> 这种既不能拆底也不能拆指数，因为项数也在变。',
              ],
              tags: ['极限', '数列极限', '运算法则', '证明'],
              related: ['thm-lim-operations', 'def-limit-sequence', 'thm-cauchy-criterion', 'thm-important-limit-two'],
            },
            {
              id: 'thm-limit-composite',
              kind: 'theorem',
              name: '复合函数的极限运算法则',
              aka: ['复合函数极限', '变量代换法则'],
              statement: '设函数 <code>y = f[g(x)]</code> 在点 <code>x₀</code> 的某个去心邻域内有定义。若\n1. <code>lim(x→x₀) g(x) = u₀</code>，且存在 <code>δ₀ > 0</code>，当 <code>0 < |x - x₀| < δ₀</code> 时 <code>g(x) ≠ u₀</code>；\n2. <code>lim(u→u₀) f(u) = A</code>，\n则 <code>lim(x→x₀) f[g(x)] = A</code>。',
              plain: '这条法则说的是“<b>可以换元</b>”：想求 <code>x → x₀</code> 时复合函数的极限，可以让 <code>u = g(x)</code>，于是只要 <code>x → x₀</code> 时 <code>u → u₀</code>，问题就变成求 <code>u → u₀</code> 时 <code>f(u)</code> 的极限。\n用法上就是那句熟悉的“令 <code>u = g(x)</code>，则当 <code>x → x₀</code> 时 <code>u → u₀</code>”。\n那个看起来奇怪的附加条件“<code>g(x) ≠ u₀</code>”是在堵一个漏洞：如果 <code>g</code> 在靠近 <code>x₀</code> 的路上会恰好取到 <code>u₀</code>，而我们又只保证 <code>f</code> 在 <code>u₀</code> 的<b>去心</b>邻域里趋向 <code>A</code>（<code>f(u₀)</code> 可能是别的值甚至没有定义），那就会出错。\n好在<b>如果 <code>f</code> 在 <code>u₀</code> 连续</b>，这个条件就可以去掉，因为此时 <code>f(u₀) = A</code>，路上撞到 <code>u₀</code> 也无妨。这是后面复合函数连续性定理的来源。',
              why: '证明直接使用 ε-δ 语言，思路是把 <code>f</code> 的 <code>δ</code> 当作目标，去找 <code>g</code> 的 <code>δ</code>。\n具体地：任给 <code>ε > 0</code>，由 <code>lim(u→u₀) f(u) = A</code>，存在 <code>η > 0</code>，使得 <code>0 < |u - u₀| < η</code> 时 <code>|f(u) - A| < ε</code>。注意这里是<b>去心</b>条件，所以我们希望 <code>g(x)</code> 满足 <code>0 < |g(x) - u₀| < η</code>：前半部分 <code>g(x) ≠ u₀</code> 需要额外假设（这就是定理里那个条件的作用），后半部分 <code>|g(x) - u₀| < η</code> 由 <code>g</code> 的极限保证（取 <code>ε′ = η</code>）。\n把两部分的 <code>δ</code> 取 min 之后，就能顺利接上。可见这个定理的“附加条件”不是多余的技术性条款，而是逻辑上的必需。',
              proof: '<ol><li>任给 <code>ε > 0</code>。由 <code>lim(u→u₀) f(u) = A</code>，存在 <code>η > 0</code>，使得当 <code>0 < |u - u₀| < η</code> 时 <code>|f(u) - A| < ε</code>。</li><li>由 <code>lim(x→x₀) g(x) = u₀</code>，对正数 <code>η</code>（把它当作极限定义里的“<code>ε</code>”来用），存在 <code>δ₁ > 0</code>，当 <code>0 < |x - x₀| < δ₁</code> 时 <code>|g(x) - u₀| < η</code>。</li><li>由定理条件 1，存在 <code>δ₀ > 0</code>，当 <code>0 < |x - x₀| < δ₀</code> 时 <code>g(x) ≠ u₀</code>。</li><li>取 <code>δ = min{ δ₀, δ₁ } > 0</code>。当 <code>0 < |x - x₀| < δ</code> 时，同时有 <code>g(x) ≠ u₀</code> 与 <code>|g(x) - u₀| < η</code>，即 <code>0 < |g(x) - u₀| < η</code>。</li><li>把 <code>u = g(x)</code> 代入第 1 步的结论，得 <code>|f[g(x)] - A| < ε</code>。</li><li>按定义 <code>lim(x→x₀) f[g(x)] = A</code>。定理得证。</li></ol>\n<b>过渡说明</b>：证明的结构是“<b>套娃</b>”：把 <code>g</code> 的极限定义中的 <code>ε</code> 位置换成 <code>f</code> 的极限定义中的 <code>δ</code>（这里是 <code>η</code>），两个定义就接上了。这种“定义套定义”的推理是分析学的基本功，多练几次就会形成条件反射。',
              example: '<b>换元的典型用法：</b>求 <code>lim(x→0) sin(x²)/x²</code>。令 <code>u = x²</code>，当 <code>x → 0</code> 时 <code>u → 0</code>，且 <code>x ≠ 0</code> 时 <code>u > 0</code> 故 <code>u ≠ 0</code>，满足条件。于是原式 <code>= lim(u→0) sin u/u = 1</code>。\n<b>更常见的用法：</b>求 <code>lim(x→0) sin(2x)/x</code>。令 <code>u = 2x</code>，则 <code>x = u/2</code>，原式 <code>= lim(u→0) [sin u/(u/2)] = 2 lim(u→0) sin u/u = 2</code>。\n<b>为什么需要“<code>g(x) ≠ u₀</code>”？</b>考虑 <code>f(u) = 1（u ≠ 0），f(0) = 0</code>，<code>g(x) ≡ 0</code>。则 <code>lim(u→0) f(u) = 1</code>，但 <code>f[g(x)] ≡ f(0) = 0</code>，极限为 0，不等于 1。原因正是 <code>g(x) ≡ 0 = u₀</code>，违反了条件。',
              pitfalls: [
                '换元时忘记说明“<code>u → u₀</code>”，或者忘记 <code>x ≠ x₀</code> ⟹ <code>u ≠ u₀</code> 的检查。',
                '在 <code>f</code> 于 <code>u₀</code> 处不连续时随意换元，导致错误结论（见上面的反例）。',
                '把 <code>lim(x→x₀) f[g(x)] = f[lim(x→x₀) g(x)]</code> 当成恒等式使用。它只在 <code>f</code> 连续时才无条件成立。',
              ],
              tags: ['极限', '复合函数', '证明'],
              related: ['thm-continuity-composite', 'def-composite-function', 'thm-lim-operations', 'thm-elementary-continuity'],
            },
            {
              id: 'note-indeterminate-forms',
              kind: 'note',
              name: '未定式（不定式）',
              aka: ['不定式', '未定式', '0/0', '∞/∞'],
              statement: '下列七种形式的极限称为<b>未定式</b>：\n<code>0/0</code>、<code>∞/∞</code>、<code>∞ - ∞</code>、<code>0 · ∞</code>、<code>1^∞</code>、<code>0⁰</code>、<code>∞⁰</code>。\n它们的共同特点是：<b>不能由各部分极限直接确定结果</b>，结果依赖于各部分趋于极限的“快慢”。',
              plain: '未定式的意思就是“<b>看形式定不了答案，必须动手算</b>”。\n比如 <code>0/0</code>：<code>lim(x→0) x/x = 1</code>，<code>lim(x→0) x²/x = 0</code>，<code>lim(x→0) x/x² = ∞</code>。分子分母都趋于 0，但结果可以是 1、0、无穷大——什么都可能。所以“<code>0/0</code>”不是答案，而是“待办事项”。\n再比如 <code>∞ - ∞</code>：<code>lim(x→∞) (x - x) = 0</code>，<code>lim(x→∞) (x² - x) = ∞</code>，<code>lim(x→∞) (x - x²) = -∞</code>。同样“什么都可能”。\n而 <code>0 · ∞</code> 也是：<code>lim(x→0⁺) x · (1/x) = 1</code>，但 <code>lim(x→0⁺) x · (1/x²) = ∞</code>。',
              why: '为什么这些形式“定不了”？因为它们都涉及两个量的<b>竞争</b>：一个想把它拉大，一个想把它拉小，最后谁赢取决于两者趋于 0 或 ∞ 的相对速度。\n这也解释了为什么下一节要专门研究“无穷小的比较”：如果知道 <code>x</code> 比 <code>x²</code> 慢得多（<code>x²</code> 是 <code>x</code> 的高阶无穷小），那 <code>x²/x → 0</code> 就一目了然了。\n再往后，洛必达法则和泰勒公式就是把所有未定式系统化处理的工具——但那是后面章节的内容。本章能做的是：对 <code>0/0</code> 型用因式分解、有理化、等价无穷小替换；对 <code>∞/∞</code> 型用除以最高次幂；对 <code>∞ - ∞</code> 型先通分或有理化；对 <code>1^∞</code> 型用重要极限。\n[[tip:看到未定式的第一反应应该是“怎么变形”，而不是“答案是多少”。变形到不再是未定式，才能用运算法则。]]',
              example: '<code>lim(x→1) (x² - 1)/(x - 1)</code> 是 <code>0/0</code> 型，因式分解后得 2。\n<code>lim(x→∞) (3x² + 2)/(x² - 1)</code> 是 <code>∞/∞</code> 型，同除 <code>x²</code> 得 3。\n<code>lim(x→+∞) (√(x² + x) - x)</code> 是 <code>∞ - ∞</code> 型，有理化得 <code>lim x/(√(x²+x) + x) = 1/2</code>。\n<code>lim(x→0⁺) x ln x</code> 是 <code>0 · ∞</code> 型，改写为 <code>lim ln x / (1/x)</code> 变成 <code>∞/∞</code> 型（用洛必达法则可算得 0，此处仅示意变形思路）。\n<code>lim(x→0) (1 + x)^(1/x) = e</code> 是 <code>1^∞</code> 型，正是第二个重要极限。',
              pitfalls: [
                '把 <code>lim f/g</code> 在 <code>0/0</code> 型时直接写 <code>= 1</code>，或在 <code>∞/∞</code> 型时直接写 <code>= 1</code>。',
                '把 <code>∞ - ∞</code> 直接写成 0。这是最经典的错误，反例到处都是。',
                '把 <code>0 · ∞</code> 写成 0，把 <code>1^∞</code> 写成 1：<code>lim(x→0)(1+x)^(1/x) = e ≠ 1</code>。',
                '以为“不是未定式”的形式（如 <code>0/0</code> 之外的一切）都能直接套运算法则，其实还要先确认各部分的极限都存在。',
              ],
              tags: ['极限', '未定式', '计算'],
              related: ['thm-lim-operations', 'def-infinitesimal-order', 'thm-important-limit-two', 'thm-equivalent-substitution'],
            },
          ],
        },
        {
          id: 'ch1-6',
          no: '1.6',
          title: '极限存在准则 两个重要极限',
          summary: '本节回答“怎么在不求出极限值的情况下判断极限存在”。两大准则——夹逼准则与单调有界准则——是整章最有用的工具，两个重要极限则是计算极限的两块基石。',
          items: [
            {
              id: 'thm-squeeze',
              kind: 'theorem',
              name: '夹逼准则（数列与函数两种形式）',
              aka: ['夹逼定理', '两边夹法则', '迫敛性'],
              statement: '<b>数列形式：</b>若数列 <code>{xₙ}</code>、<code>{yₙ}</code>、<code>{zₙ}</code> 满足\n1. 从某项起（即存在 <code>N₀</code>，当 <code>n > N₀</code> 时）<code>yₙ ≤ xₙ ≤ zₙ</code>；\n2. <code>lim(n→∞) yₙ = lim(n→∞) zₙ = a</code>，\n则数列 <code>{xₙ}</code> 收敛，且 <code>lim(n→∞) xₙ = a</code>。\n<b>函数形式：</b>若在 <code>x₀</code> 的某个去心邻域内（或 <code>|x| > X</code> 时）有 <code>g(x) ≤ f(x) ≤ h(x)</code>，且 <code>lim g(x) = lim h(x) = A</code>，则 <code>lim f(x) = A</code>。',
              plain: '夹逼准则就是“<b>被两个人一左一右夹着走，只能跟到同一个地方</b>”。\n想象你和两个朋友一起走路：朋友甲一直在你左边，朋友乙一直在你右边。如果甲和乙最后都走到了同一个门口，那你（被夹在中间）也必然只能进那个门——你既不能跑到甲的左边，也不能跑到乙的右边，无处可逃。\n最经典的用法是求 <code>lim(n→∞) (sin n)/n</code>。这个数列本身看不出极限，但我们知道 <code>-1/n ≤ (sin n)/n ≤ 1/n</code>，而两边都趋于 0，所以中间也趋于 0。\n另一个经典用法就是证明第一个重要极限 <code>lim(x→0) sin x/x = 1</code>——那里的“左右两个朋友”是 <code>cos x</code> 和 <code>1</code>。',
              why: '证明的想法极其简单：<b>用 <code>N = max{N₁, N₂}</code> 把两条控制线同时打开</b>。\n因为 <code>yₙ → a</code> 和 <code>zₙ → a</code> 各自有一个门槛 <code>N₁</code>、<code>N₂</code>，取两者中较大的那个 <code>N</code>，就能保证 <code>n > N</code> 时两条控制同时生效：<code>a - ε < yₙ</code> 且 <code>zₙ < a + ε</code>。\n而 <code>xₙ</code> 被夹在中间，于是 <code>a - ε < yₙ ≤ xₙ ≤ zₙ < a + ε</code>，直接得到 <code>|xₙ - a| < ε</code>。\n整个过程没有新的技巧，关键只是“取 max”。但它的威力巨大：<b>它允许我们在不知道 <code>xₙ</code> 的具体表达式的情况下求出它的极限</b>，只要能用两个好算的量把它夹住。这就是为什么它常被用来处理“震荡”或“复杂组合”的数列。',
              proof: '<ol><li>设存在 <code>N₀</code>，当 <code>n > N₀</code> 时 <code>yₙ ≤ xₙ ≤ zₙ</code>；又设 <code>lim yₙ = a</code>，<code>lim zₙ = a</code>。</li><li>任给 <code>ε > 0</code>。由 <code>yₙ → a</code>，存在 <code>N₁</code>，当 <code>n > N₁</code> 时 <code>|yₙ - a| < ε</code>，即 <code>a - ε < yₙ</code>。</li><li>由 <code>zₙ → a</code>，存在 <code>N₂</code>，当 <code>n > N₂</code> 时 <code>|zₙ - a| < ε</code>，即 <code>zₙ < a + ε</code>。</li><li>取 <code>N = max{ N₀, N₁, N₂ }</code>。当 <code>n > N</code> 时三个条件同时成立，于是\n<code>a - ε < yₙ ≤ xₙ ≤ zₙ < a + ε</code>。</li><li>这给出 <code>a - ε < xₙ < a + ε</code>，即 <code>|xₙ - a| < ε</code>。按定义 <code>lim xₙ = a</code>。</li><li>函数形式完全类似：把 <code>N</code> 换成 <code>δ</code>，把“当 <code>n > N</code>”换成“当 <code>0 < |x - x₀| < δ</code>”，同样的推理得到 <code>|f(x) - A| < ε</code>。</li></ol>\n<b>过渡说明</b>：第 4 步的“取三者中最大”是把三个来自不同来源的条件同时满足的标准操作。第 5 步把双边不等式压缩成绝对值不等式，是收尾的固定动作。',
              example: '<b>例 1：</b>求 <code>lim(n→∞) (sin n)/n</code>。因为 <code>-1/n ≤ (sin n)/n ≤ 1/n</code>，而 <code>±1/n → 0</code>，故极限为 0。\n<b>例 2：</b>求 <code>lim(n→∞) (1/2ⁿ + 1/3ⁿ + … )</code> 类问题常可夹逼。又如 <code>lim(n→∞) ⁿ√(2ⁿ + 3ⁿ)</code>：因为 <code>3 = ⁿ√(3ⁿ) ≤ ⁿ√(2ⁿ + 3ⁿ) ≤ ⁿ√(2 · 3ⁿ) = 3 · ⁿ√2</code>，而 <code>ⁿ√2 → 1</code>，故极限为 3。\n<b>例 3：</b>求 <code>lim(x→0⁺) x · [1/x]</code>（<code>[ ]</code> 为取整）。由 <code>1/x - 1 < [1/x] ≤ 1/x</code> 得 <code>1 - x < x[1/x] ≤ 1</code>，令 <code>x → 0⁺</code> 由夹逼得极限为 1。',
              pitfalls: [
                '只夹住“大部分项”而不是“从某项以后的全部项”。必须写明“当 <code>n > N₀</code> 时”。',
                '两边极限不相等时仍然下结论。例如 <code>-1 ≤ sin n ≤ 1</code> 中两边极限不存在（也不是同一个数），完全用不上夹逼。',
                '忘记 <code>xₙ</code> 必须被夹住：如果只有 <code>xₙ ≤ zₙ</code> 而没有下界，就得不到结论。',
                '在函数形式中，把左右夹逼写成“<code>x → x₀</code> 时 <code>g ≤ f ≤ h</code>”却不说明是否包括 <code>x₀</code> 本身（通常只要求去心邻域）。',
              ],
              tags: ['极限', '夹逼准则', '存在准则', '证明'],
              related: ['thm-important-limit-one', 'thm-monotone-bounded', 'def-limit-sequence', 'thm-infinitesimal-bounded-product'],
            },
            {
              id: 'thm-monotone-bounded',
              kind: 'theorem',
              name: '单调有界准则',
              aka: ['单调有界定理', '单调有界数列必有极限'],
              statement: '单调有界数列必有极限。更精确地说：\n1. 若数列 <code>{aₙ}</code> 单调增加且有上界（即存在 <code>M</code> 使 <code>aₙ ≤ M</code> 对一切 <code>n</code> 成立），则 <code>{aₙ}</code> 收敛，且 <code>lim(n→∞) aₙ = sup{ aₙ }</code>（上确界）；\n2. 若数列 <code>{aₙ}</code> 单调减少且有下界，则 <code>{aₙ}</code> 收敛，且 <code>lim(n→∞) aₙ = inf{ aₙ }</code>（下确界）。',
              plain: '这条准则说：<b>一个数列如果只会朝一个方向走，又被一堵墙挡住了，那它一定会在墙前停在一个地方。</b>\n为什么？因为它一直往一个方向走（比如只增不减），又没有越界，那么它“往上顶”的动作会越来越慢，最后稳定在某个高度上。\n粗想可能觉得奇怪：为什么不能一直往上但又不超过墙？因为它被要求“上确界”存在——这正是实数系的一个基本性质：<b>有上界的非空数集必有上确界</b>。\n它的实用价值巨大：<b>很多时候我们根本算不出极限是多少，但只要能证明数列单调且有界，就能断定极限存在。</b>例如 <code>a₁ = √2</code>，<code>a_{n+1} = √(2 + aₙ)</code>，可以证明它单调增加且有上界 2，所以极限存在；然后设极限为 <code>L</code>，对递推式两边取极限得 <code>L = √(2 + L)</code>，解得 <code>L = 2</code>。这就是“<b>先证存在，再代入求值</b>”的标准套路。',
              why: '这个定理用到了实数系的一条核心性质（确界原理），也是它区分于有理数系的地方。证明思路如下：\n把 <code>S = { aₙ }</code> 看成数集。由有上界，<code>S</code> 有上确界 <code>a = sup S</code>。由上确界的定义，<code>a</code> 有以下两条性质：\n1. <code>a</code> 是上界：对一切 <code>n</code>，<code>aₙ ≤ a</code>；\n2. <code>a</code> 是最小的上界：对任意 <code>ε > 0</code>，<code>a - ε</code> 不再是上界，即存在某一项 <code>a_N > a - ε</code>。\n现在把单调性用上：因为数列单调增加，当 <code>n > N</code> 时 <code>aₙ ≥ a_N > a - ε</code>。又由第 1 条 <code>aₙ ≤ a < a + ε</code>。两者合起来就是 <code>|aₙ - a| < ε</code>。\n<b>看出关键了吗？</b>单调性在这里的唯一作用是“<b>把某一项的高度传染给后面所有的项</b>”。如果没有单调性，<code>a_N > a - ε</code> 只是那一个点的事，后面的项可能又掉回很低，就推不出收敛。\n再补一句：为什么用上确界而不是“最大的项”？因为数列可能没有最大项（比如 <code>1 - 1/n</code> 不断逼近 1 但永远取不到 1），上确界的存在性比“最大项存在”更普遍，这正是实数完备性的体现。',
              proof: '<ol><li>设 <code>{aₙ}</code> 单调增加且有上界，即存在 <code>M</code> 使 <code>aₙ ≤ M</code> 对一切 <code>n</code> 成立。令 <code>S = { aₙ | n ∈ ℕ₊ }</code>，则 <code>S</code> 非空且有上界。</li><li>由确界原理，<code>S</code> 有上确界，记 <code>a = sup S</code>（<code>a</code> 是有限实数）。于是对一切 <code>n</code>，<code>aₙ ≤ a</code>；且对任意 <code>ε > 0</code>，<code>a - ε</code> 不是 <code>S</code> 的上界，故存在某个 <code>N</code> 使 <code>a_N > a - ε</code>。</li><li>任给 <code>ε > 0</code>。取第 2 步中的那个 <code>N</code>。当 <code>n > N</code> 时，由单调增加有 <code>aₙ ≥ a_N > a - ε</code>；又由 <code>a</code> 是上界有 <code>aₙ ≤ a < a + ε</code>。</li><li>于是 <code>a - ε < aₙ < a + ε</code>，即 <code>|aₙ - a| < ε</code>。按定义 <code>lim(n→∞) aₙ = a = sup{ aₙ }</code>。第 1 部分得证。</li><li>第 2 部分（单调减少有下界）完全对称：考虑 <code>bₙ = -aₙ</code>，则 <code>{bₙ}</code> 单调增加且有上界 <code>-inf{ aₙ }</code>，由第 1 部分 <code>bₙ → sup{ bₙ } = -inf{ aₙ }</code>，于是 <code>aₙ = -bₙ → inf{ aₙ }</code>。</li><li>定理证毕。</li></ol>\n<b>过渡说明</b>：第 2 步把“上确界”的两条性质（是上界、是最小上界）分别翻译成“<code>aₙ ≤ a</code>”和“存在一项超过 <code>a - ε</code>”，这是全证明的关键翻译。第 3 步用单调性把这个“一项的高”扩散成“后面所有项的高”，这正是前面 why 里点出的要害。第 5 步用 <code>-aₙ</code> 把减少情形化归为增加情形，是处理“减少”问题的通用技巧。',
              example: '<b>例 1（求 √2 的迭代逼近）：</b><code>a₁ = √2</code>，<code>a_{n+1} = √(2 + aₙ)</code>。\n先证有界：用归纳法，若 <code>aₙ < 2</code>，则 <code>a_{n+1} = √(2 + aₙ) < √4 = 2</code>；又 <code>a₁ = √2 < 2</code>，故一切 <code>aₙ < 2</code>。\n再证单调增：<code>a_{n+1} - aₙ = √(2 + aₙ) - aₙ</code>，当 <code>aₙ < 2</code> 时 <code>2 + aₙ > aₙ²</code>（因为 <code>aₙ² - aₙ - 2 = (aₙ - 2)(aₙ + 1) < 0</code>），故 <code>√(2+aₙ) > aₙ</code>，即 <code>a_{n+1} > aₙ</code>。\n于是由单调有界准则，极限 <code>L</code> 存在。对 <code>a_{n+1} = √(2 + aₙ)</code> 两边取极限（由复合函数极限法则与根号函数的连续性）：<code>L = √(2 + L)</code>，平方得 <code>L² - L - 2 = 0</code>，即 <code>(L - 2)(L + 1) = 0</code>。由 <code>L ≥ a₁ = √2 > 0</code> 舍去 <code>L = -1</code>，得 <code>L = 2</code>。\n<b>例 2：</b>数列 <code>aₙ = 1 + 1/2² + 1/3² + … + 1/n²</code> 单调增加，且可证有上界 2，故收敛（其极限为 <code>π²/6</code>，这是后续内容）。',
              pitfalls: [
                '把“单调有界”拆开用：单调但无界（<code>aₙ = n</code>）、有界但不单调（<code>aₙ = (-1)ⁿ</code>）都不保证收敛。两个条件缺一不可。',
                '只证了有界忘了证单调（或反之）。两个都要写清楚。',
                '证出极限存在后忘记回代求值，或者回代时算错方程。',
                '回代取极限时忽略“根号函数连续”这一前提（在本题中根号在 <code>2 + L ≥ 0</code> 处连续，没问题，但要养成检查的习惯）。',
                '误以为这个定理能算出极限的<b>值</b>。它只保证存在，值要靠另外的办法（如回代解方程）求。',
              ],
              tags: ['极限', '存在准则', '单调有界', '证明'],
              related: ['def-monotone-function', 'def-bounded-function', 'thm-squeeze', 'thm-important-limit-two', 'thm-cauchy-criterion'],
            },
            {
              id: 'thm-important-limit-one',
              kind: 'theorem',
              name: '第一个重要极限',
              aka: ['重要极限 sin x/x', 'lim sin x / x = 1', '第一个重要极限'],
              statement: '<code>lim(x→0) sin x / x = 1</code>。\n更一般地，若在同一极限过程中 <code>φ(x) → 0</code> 且 <code>φ(x) ≠ 0</code>，则 <code>lim sin φ(x) / φ(x) = 1</code>。',
              plain: '这条极限说的是：<b>当角度很小的时候，<code>sin x</code> 和 <code>x</code> 几乎一样大。</b>\n为什么？回忆弧度的定义：<code>sin x</code> 是单位圆上角 <code>x</code> 所对的“弦的一半”的两倍（竖直边），而 <code>x</code> 是那段“弧长”。当角度很小时，弧和对应的竖直边几乎贴合在了一起，所以两者之比趋于 1。\n可用数字感受一下：<code>x = 0.1</code> 时 <code>sin 0.1 / 0.1 ≈ 0.99833</code>；<code>x = 0.01</code> 时约为 <code>0.99998</code>。<code>x</code> 越小，比值越接近 1。\n[[warn:这个公式只在弧度制下成立！如果用角度制，<code>lim(x→0) sin x°/x = π/180</code>，公式就不对了。所以微积分里三角函数的自变量一律用弧度。]]',
              why: '证明的思路是“用夹逼准则把 <code>sin x/x</code> 夹住”，而夹逼的关键是找一个几何不等式。\n考虑单位圆中 <code>0 < x < π/2</code> 的情形：设圆心为 <code>O</code>，射线与圆交于 <code>A</code>，作 <code>OB</code> 使 <code>∠AOB = x</code>。<code>B</code> 处作圆的切线与 <code>OA</code> 延长线交于 <code>D</code>。于是可以得到三个面积关系：\n<code>△OAB</code> 的面积 <code>= (1/2) sin x</code>；\n扇形 <code>OAB</code> 的面积 <code>= x/2</code>；\n<code>△OAD</code> 的面积 <code>= (1/2) tan x</code>。\n因为三角形套在扇形里、扇形又套在更大的三角形里，所以\n<code>(1/2) sin x < x/2 < (1/2) tan x</code>。\n两边乘 2 再除以 <code>sin x > 0</code>，得 <code>1 < x/sin x < 1/cos x</code>，取倒数得\n<code>cos x < sin x / x < 1</code>。\n现在看两边：<code>1</code> 的极限当然是 1；<code>cos x</code> 的极限是 <code>cos 0 = 1</code>（这需要用到余弦的连续性，或直接由 <code>0 ≤ 1 - cos x = 2sin²(x/2) ≤ x²/2 → 0</code> 推出）。两边夹住同一个数 1，中间只能是 1。\n<b>为什么 <code>x < 0</code> 时也对？</b>因为 <code>sin x/x</code> 是偶函数：<code>sin(-x)/(-x) = sin x / x</code>。左右对称，所以只要正半轴成立，负半轴自动成立。这就是为什么要先设 <code>0 < x < π/2</code>。\n<b>为什么这个证明“值得”这么绕？</b>因为 <code>sin x/x</code> 在 <code>x = 0</code> 处是 <code>0/0</code> 型，运算法则用不上；而它又是后面证明 <code>(sin x)′ = cos x</code> 的必要工具，所以必须有一个不依赖导数（否则循环论证）的证明。几何面积法正好满足这个要求。',
              proof: '<ol><li><b>先处理 <code>0 < x < π/2</code>。</b>在单位圆中，取圆心 <code>O</code>，<code>A</code>、<code>B</code> 为单位圆上的点且 <code>∠AOB = x</code>（弧度）。过 <code>A</code> 作圆的切线，与 <code>OB</code> 的延长线交于点 <code>D</code>。</li><li>比较三块图形的面积：<code>△OAB</code> 的面积是 <code>(1/2)·1·1·sin x = (1/2) sin x</code>；扇形 <code>OAB</code> 的面积是 <code>(1/2)·1²·x = x/2</code>；<code>△OAD</code> 的面积是 <code>(1/2)·1·tan x = (1/2) tan x</code>。</li><li>由图可见 <code>△OAB</code> 含于扇形 <code>OAB</code>，扇形 <code>OAB</code> 又含于 <code>△OAD</code>，故面积满足\n<code>(1/2) sin x < x/2 < (1/2) tan x</code>。</li><li>三边同乘 2：<code>sin x < x < tan x</code>。因为 <code>0 < x < π/2</code> 时 <code>sin x > 0</code>，两边同除 <code>sin x</code> 得 <code>1 < x/sin x < 1/cos x</code>。</li><li>取倒数（各边均为正，不等号方向反向）：<code>cos x < sin x / x < 1</code>。</li><li>估计左端：<code>0 ≤ 1 - cos x = 2 sin²(x/2) ≤ 2·(x/2)² = x²/2</code>（这里用了 <code>|sin t| ≤ |t|</code>，而它可由本步之前的不等式 <code>sin t < t</code> 对 <code>t > 0</code> 直接得到）。由夹逼 <code>lim(x→0⁺) cos x = 1</code>。</li><li>于是 <code>lim(x→0⁺) sin x/x</code> 被 <code>cos x</code> 与常数 1 夹住，两边极限都是 1，由夹逼准则得 <code>lim(x→0⁺) sin x/x = 1</code>。</li><li><b>再处理 <code>x < 0</code>。</b>注意 <code>sin(-x)/(-x) = sin x / x</code>，即 <code>sin x/x</code> 是偶函数，故 <code>lim(x→0⁻) sin x/x = lim(t→0⁺) sin t/t = 1</code>。</li><li>左右极限都存在且相等，由“函数极限与左右极限的关系”得 <code>lim(x→0) sin x/x = 1</code>。定理证毕。</li></ol>\n<b>过渡说明</b>：第 2、3 步是几何直观的核心——用三块图形的面积大小建立不等式，这是“看得见”的推理。第 6 步用倍角公式把 <code>cos x → 1</code> 也算出来，这样夹逼的两端都有了极限。第 8 步用偶函数省掉一半工作，是常见技巧。（当 <code>x ∈ (-π/2, 0)</code> 时，也可直接由不等式的对称性得到。）',
              example: '<code>lim(x→0) sin 3x / x = lim(x→0) 3 · sin 3x/(3x) = 3 · 1 = 3</code>。\n<code>lim(x→0) tan x / x = lim(x→0) (sin x/x) · (1/cos x) = 1 · 1 = 1</code>。\n<code>lim(x→0) (1 - cos x)/x² = lim(x→0) 2sin²(x/2)/x² = lim(x→0) (1/2)·[sin(x/2)/(x/2)]² = 1/2</code>。\n<code>lim(x→0) arcsin x / x = 1</code>：令 <code>t = arcsin x</code>，则 <code>x = sin t</code>，当 <code>x → 0</code> 时 <code>t → 0</code>，故原式 <code>= lim(t→0) t/sin t = 1</code>。',
              pitfalls: [
                '把 <code>lim(x→0) sin x/x</code> 当成“<code>0/0</code> 所以等于 0 或 1”——等于 1 是结论不是形式推论，必须证明或引用。',
                '把 <code>lim(x→0) sin x/x = 1</code> 误记成 <code>lim(x→∞) sin x/x = 1</code>。后者其实是 0（<code>|sin x| ≤ 1</code>，<code>1/x → 0</code>）。',
                '使用角度制（如计算器上的 DEG 模式）时套用这个公式。',
                '在 <code>lim(x→0) sin(2x)/sin(3x)</code> 中只把分子凑成重要极限，忘记分母也要凑：应写成 <code>[sin(2x)/(2x)]·[(3x)/sin(3x)]·(2/3) → 2/3</code>。',
              ],
              tags: ['极限', '重要极限', '证明'],
              related: ['thm-squeeze', 'thm-equivalent-substitution', 'thm-important-limit-two', 'def-infinitesimal-order'],
            },
            {
              id: 'thm-important-limit-two',
              kind: 'theorem',
              name: '第二个重要极限',
              aka: ['重要极限 e', 'lim (1+1/x)^x = e', '第二个重要极限'],
              statement: '<code>lim(x→∞) (1 + 1/x)^x = e</code>，其中 <code>e</code> 是一个无理数，<code>e ≈ 2.718281828…</code>。\n等价形式：<code>lim(x→0) (1 + x)^(1/x) = e</code>；\n更一般地：<code>lim(x→∞) (1 + 1/x)^(x + c) = e</code>（<code>c</code> 为任意常数），且若 <code>φ(x) → 0</code> 且 <code>φ(x) ≠ 0</code>，则 <code>lim (1 + φ(x))^(1/φ(x)) = e</code>。',
              plain: '这条极限回答了一个很实际的问题：<b>如果年利率 100%，但把一年分成 <code>n</code> 次结算（每次利率 <code>1/n</code>），那一年的收益会无限增长吗？</b>\n答案是：不会。它会稳定在 <code>e ≈ 2.71828</code> 倍左右。\n具体算一下：\n<code>n = 1</code>：<code>(1+1)¹ = 2</code>\n<code>n = 2</code>：<code>1.5² = 2.25</code>\n<code>n = 12</code>：约 <code>2.613</code>\n<code>n = 365</code>：约 <code>2.7146</code>\n<code>n = 10000</code>：约 <code>2.71815</code>\n可以看到它一直在往上爬，但爬得越来越慢，最终顶到 <code>e</code> 就上不去了。这个数就是自然对数的底。\n为什么它不是 1，也不是无穷大？底数 <code>1 + 1/x</code> 确实在向 1 靠（趋于 1），但指数 <code>x</code> 在向无穷大跑（趋于 ∞）。一个往 1 拉、一个往 ∞ 拉，结果由它们的“竞争”决定，最终定格在 <code>e</code>。这就是典型的 <code>1^∞</code> 型未定式。',
              why: '证明的标准路线是：<b>先用单调有界准则证明数列 <code>aₙ = (1 + 1/n)ⁿ</code> 收敛，把这个极限定义为 <code>e</code>；再推广到 <code>x → ∞</code> 的连续情形。</b>\n第一步关键在于证明 <code>{aₙ}</code> 单调增加且有上界。有两个经典办法：\n<b>办法一（二项式展开）：</b>把 <code>(1 + 1/n)ⁿ</code> 展开成\n<code>1 + 1 + (1/2!)(1 - 1/n) + (1/3!)(1 - 1/n)(1 - 2/n) + … + (1/n!)(1 - 1/n)…(1 - (n-1)/n)</code>。<br>与 <code>a_{n+1}</code> 的对应展开逐项比较：<code>a_{n+1}</code> 不但多出一项（正数），而且每一项里的因子 <code>(1 - k/n)</code> 都小于 <code>(1 - k/(n+1))</code>，所以每一项都更大。于是 <code>a_{n+1} > aₙ</code>，单调增加。\n上界估计同样从展开式来：注意到 <code>(1 - 1/n)(1 - 2/n)… < 1</code>，故\n<code>aₙ < 1 + 1 + 1/2! + 1/3! + … + 1/n! < 1 + 1 + 1/2 + 1/2² + … + 1/2^{n-1} < 3</code>。<br>所以 <code>{aₙ}</code> 单调增加且有上界，由单调有界准则收敛。把它的极限记作 <code>e</code>。\n<b>办法二（不等式估计）：</b>先用平均值不等式等方法证明 <code>aₙ</code> 单调增加、<code>bₙ = (1 + 1/n)^{n+1}</code> 单调减少，而 <code>aₙ < bₙ</code>，于是两者都有极限，且可以证明这两个极限相等（都等于 <code>e</code>）。\n第二步推广到实数 <code>x</code>：对任意 <code>x > 1</code>，取 <code>n = [x]</code>，则 <code>n ≤ x < n + 1</code>，于是\n<code>(1 + 1/(n+1))^n < (1 + 1/x)^x < (1 + 1/n)^{n+1}</code>。<br>两边当 <code>x → +∞</code>（从而 <code>n → ∞</code>）时都趋于 <code>e</code>，用夹逼准则即得 <code>lim(x→+∞) (1 + 1/x)^x = e</code>。对 <code>x → -∞</code> 令 <code>t = -x</code> 可化为同一结论。最后，令 <code>t = 1/x</code> 即得 <code>lim(t→0)(1+t)^{1/t} = e</code>。',
              proof: '<ol><li><b>第一步：证明数列 <code>aₙ = (1 + 1/n)ⁿ</code> 单调增加。</b>由二项式定理，\n<code>aₙ = 1 + n·(1/n) + [n(n-1)/(2!)]·(1/n²) + … + (1/nⁿ)</code>\n<code>= 1 + 1 + (1/2!)(1 - 1/n) + (1/3!)(1 - 1/n)(1 - 2/n) + … + (1/n!)(1 - 1/n)(1 - 2/n)…(1 - (n-1)/n)</code>。</li><li>同理把 <code>a_{n+1}</code> 展开，它共有 <code>n + 2</code> 项（比 <code>aₙ</code> 多一项，且多出的那项为正）。逐项比较第 <code>k + 1</code> 项（<code>k = 0, 1, …, n</code>）：<code>aₙ</code> 中对应项是 <code>(1/k!)(1 - 1/n)(1 - 2/n)…(1 - (k-1)/n)</code>，<code>a_{n+1}</code> 中是 <code>(1/k!)(1 - 1/(n+1))…(1 - (k-1)/(n+1))</code>，后者每个因子都更大，故后者更大。</li><li>因此 <code>a_{n+1} > aₙ</code>，即 <code>{aₙ}</code> 单调增加。</li><li><b>第二步：证明有上界。</b>在展开式中，每个括号因子 <code>(1 - j/n) < 1</code>，故 <code>aₙ < 1 + 1 + 1/2! + 1/3! + … + 1/n!</code>。又 <code>k! ≥ 2^{k-1}</code>（<code>k ≥ 1</code>），故\n<code>aₙ < 1 + 1 + 1/2 + 1/2² + … + 1/2^{n-1} < 1 + 1/(1 - 1/2) = 3</code>。\n所以 <code>{aₙ}</code> 有上界 3（事实上用更精细的估计可得 <code>aₙ < 3</code> 且极限约 2.718）。</li><li><b>第三步：定义 <code>e</code>。</b>由单调有界准则，<code>{aₙ}</code> 收敛。把这个极限定义为 <code>e = lim(n→∞) (1 + 1/n)ⁿ</code>。</li><li><b>第四步：推广到实数 <code>x → +∞</code>。</b>设 <code>x > 1</code>，令 <code>n = [x]</code>（取整），则 <code>n ≤ x < n + 1</code>，从而 <code>1/(n+1) < 1/x ≤ 1/n</code>。由幂函数的单调性（底数 <code>> 1</code> 时递增），\n<code>(1 + 1/(n+1))^n < (1 + 1/x)^x < (1 + 1/n)^{n+1}</code>。</li><li>计算两端的极限：左端 <code>(1 + 1/(n+1))^n = (1 + 1/(n+1))^{n+1} / (1 + 1/(n+1)) → e/1 = e</code>；右端 <code>(1 + 1/n)^{n+1} = (1 + 1/n)^n · (1 + 1/n) → e · 1 = e</code>。两端都趋于 <code>e</code>。</li><li>由夹逼准则，<code>lim(x→+∞) (1 + 1/x)^x = e</code>。</li><li><b>第五步：<code>x → -∞</code>。</b>令 <code>x = -t</code>（<code>t > 0</code>），则\n<code>(1 + 1/x)^x = (1 - 1/t)^{-t} = [(t/(t-1))]^{t} = [(1 + 1/(t-1))]^{t-1} · (1 + 1/(t-1)) → e · 1 = e</code>，故 <code>lim(x→-∞) (1 + 1/x)^x = e</code>。</li><li><b>第六步：得到 <code>x → 0</code> 的形式。</b>令 <code>x = 1/t</code>，则 <code>x → 0</code>（<code>x ≠ 0</code>）对应 <code>|t| → ∞</code>，由复合函数极限法则与第四、五步，<code>lim(x→0)(1 + x)^{1/x} = e</code>。定理证毕。</li></ol>\n<b>过渡说明</b>：第 1 至 3 步是本证明的技术核心，靠“二项式展开后逐项比较”建立单调性，这一步很多人第一次看会觉得繁琐，但它是唯一不借助导数就能证明 <code>aₙ</code> 单调的办法。第 4 步的上界估计用了“<code>k! ≥ 2^{k-1}</code>”这个简单的放缩，把陌生级数化成熟悉的等比级数。第 6、7 步把整数下标换成连续变量，用的是取整 + 夹逼。',
              example: '<code>lim(x→∞) (1 + 2/x)^x = lim(x→∞) [(1 + 2/x)^(x/2)]² = e²</code>：这里用了 <code>φ = 2/x → 0</code> 的推广形式。\n<code>lim(x→0) (1 - x)^{1/x} = lim(x→0) [(1 + (-x))^{1/(-x)}]^{-1} = e⁻¹</code>。\n<code>lim(x→∞) (1 + 1/x)^{2x} = e²</code>。\n<code>lim(x→∞) (1 + 1/x)^{x + 3} = e · lim(x→∞)(1 + 1/x)³ = e</code>：说明常数偏移不影响结果。\n<b>常见变形技巧：</b>遇到 <code>1^∞</code> 型，标准手法是写成 <code>e^{ln}</code>：<code>f^g = e^{g ln f}</code>，于是问题化为求 <code>lim g ln f</code>，通常是 <code>0 · ∞</code> 型，再变形处理。',
              pitfalls: [
                '把 <code>lim(1 + 1/x)^x</code> 与 <code>lim(1 + 1/x)</code> 混起来，误以为底数趋于 1 所以整体趋于 1。',
                '写成 <code>lim(x→∞) (1 + 1/x)^x = 1^∞ = 1</code>。这是把未定式当成了确定值。',
                '在 <code>lim(x→∞) (1 + 2/x)^x</code> 中忘记把指数也配成 <code>x/2</code>，得出错误的 <code>e</code>；正确结果是 <code>e²</code>。',
                '把 <code>e</code> 与 <code>lim(x→∞)(1 + 1/x)^x</code> 的收敛性当成“显然”，其实它需要单调有界准则来保证。',
              ],
              tags: ['极限', '重要极限', 'e', '证明'],
              related: ['thm-monotone-bounded', 'thm-squeeze', 'def-elementary-functions', 'thm-important-limit-one', 'note-indeterminate-forms'],
            },
            {
              id: 'thm-nested-intervals',
              kind: 'theorem',
              name: '区间套定理（数列形式的柯西准则与单调有界准则的补充）',
              aka: ['区间套定理', '闭区间套定理'],
              statement: '设 <code>{ [aₙ, bₙ] }</code> 是一列闭区间，满足\n1.（嵌套）<code>[a₁, b₁] ⊇ [a₂, b₂] ⊇ … ⊇ [aₙ, bₙ] ⊇ …</code>，即 <code>a₁ ≤ a₂ ≤ … ≤ aₙ ≤ … ≤ bₙ ≤ … ≤ b₂ ≤ b₁</code>；\n2.（长度趋于零）<code>lim(n→∞) (bₙ - aₙ) = 0</code>，\n则存在<b>唯一</b>的实数 <code>ξ</code> 属于所有这些闭区间，即 <code>ξ ∈ ⋂(n=1..∞) [aₙ, bₙ]</code>，并且 <code>lim aₙ = lim bₙ = ξ</code>。',
              plain: '区间套定理是“<b>不断缩小的套娃最终夹出一个点</b>”的严格表述。\n想象你在一根绳子上不断画越来越短的一段，后画的必须包含在前画的里面，而且长度越来越小趋于 0。那么最后所有线段共同拥有的那个点，有且只有一个。\n唯一性从哪里来？因为长度趋于 0。如果公共点有两个，比如 <code>ξ ≠ η</code>，那它们之间的距离 <code>|ξ - η| > 0</code>，而每段区间的长度都比这个距离还小的时候，就装不下这两个点了，矛盾。',
              why: '证明只需用一次单调有界准则，非常优雅。\n观察两端：左端点列 <code>{aₙ}</code> 单调增加（因为区间在往里缩），而且 <code>aₙ ≤ b₁</code> 有上界；右端点列 <code>{bₙ}</code> 单调减少，且 <code>bₙ ≥ a₁</code> 有下界。所以两列都收敛，设 <code>aₙ → ξ</code>、<code>bₙ → η</code>。\n再算差值：<code>η - ξ = lim(bₙ - aₙ) = 0</code>，所以 <code>ξ = η</code>，公共点存在。\n唯一性用反证：若有 <code>ξη</code> 两个不同公共点，则对一切 <code>n</code> 都有 <code>bₙ - aₙ ≥ |ξ - η| > 0</code>，与长度趋于 0 矛盾。（这里 <code>ξ</code>、<code>η</code> 都在 <code>[aₙ, bₙ]</code> 中，故它们的距离不超过区间长度。）\n这条定理的价值在于它是一个“<b>定位工具</b>”：二分法求方程的根、证明中间值定理、构造实数，都要靠它。',
              proof: '<ol><li>由条件 1，左端点列满足 <code>a₁ ≤ a₂ ≤ a₃ ≤ …</code>，即 <code>{aₙ}</code> 单调增加，且对一切 <code>n</code> 有 <code>aₙ ≤ bₙ ≤ b₁</code>，故有上界。</li><li>由单调有界准则，<code>{aₙ}</code> 收敛，设 <code>lim aₙ = ξ</code>。</li><li>同理，<code>b₁ ≥ b₂ ≥ …</code> 单调减少且 <code>bₙ ≥ aₙ ≥ a₁</code> 有下界，故 <code>{bₙ}</code> 收敛，设 <code>lim bₙ = η</code>。</li><li>由极限的差法则，<code>η - ξ = lim(bₙ - aₙ) = 0</code>，故 <code>ξ = η</code>。</li><li>证明这个公共值 <code>ξ</code> 属于每个区间：对任意固定的 <code>n</code>，当 <code>m > n</code> 时 <code>aₙ ≤ a_m ≤ b_m ≤ bₙ</code>，令 <code>m → ∞</code> 得 <code>aₙ ≤ ξ ≤ bₙ</code>。因 <code>n</code> 任意，<code>ξ ∈ [aₙ, bₙ]</code> 对一切 <code>n</code> 成立。</li><li>证明唯一性：设另有 <code>ξ′</code> 也属于一切 <code>[aₙ, bₙ]</code>。则对每个 <code>n</code>，<code>ξ</code>、<code>ξ′</code> 都在 <code>[aₙ, bₙ]</code> 中，故 <code>|ξ - ξ′| ≤ bₙ - aₙ</code>。令 <code>n → ∞</code>，右端趋于 0，得 <code>|ξ - ξ′| ≤ 0</code>，即 <code>ξ = ξ′</code>。</li><li>由第 5 步与 <code>ξ = η</code>，<code>lim aₙ = lim bₙ = ξ</code>。定理证毕。</li></ol>\n<b>过渡说明</b>：第 1、3 步分别对左右端点用单调有界准则，这是证明的骨架。第 4 步用 “<code>lim(bₙ - aₙ) = lim bₙ - lim aₙ</code>” 把长度条件转化为两端点极限相等，这里用到两个极限都已存在。第 6 步的唯一性是标准的“两个候选点距离任意小 ⟹ 相等”的论证。',
              example: '<b>二分法求根：</b>求 <code>f(x) = x³ - x - 1</code> 的根。取 <code>[1, 2]</code>，<code>f(1) < 0 < f(2)</code>。每次取中点 <code>c</code>，保留使函数值异号的那一半，得到一个区间套，长度是 <code>1/2ⁿ → 0</code>，由区间套定理存在唯一公共点 <code>ξ</code>，它就是方程的根（约 1.3247）。\n<b>与实数完备性的联系：</b>用区间套可以定义实数——有理数的区间套的公共点可能就是无理数（如用 <code>[1, 1.4] ⊃ [1.41, 1.42] ⊃ …</code> 套出 <code>√2</code>）。这说明实数系没有“窟窿”。',
              pitfalls: [
                '忘记“长度趋于 0”这个条件。只嵌套不缩到 0，公共点可能是一整段区间（如 <code>[aₙ, bₙ] ≡ [0, 1]</code>），不唯一。',
                '忘记要求闭区间。开区间套 <code>(0, 1/n)</code> 的长度也趋于 0，但它们的交集是空集，公共点不存在。这是“闭”这个字的必要性所在。',
                '证明唯一性时直接说“显然只有一个”，没有给出论证。',
              ],
              tags: ['极限', '区间套', '存在准则', '证明'],
              related: ['thm-monotone-bounded', 'thm-cauchy-criterion', 'thm-zero-point', 'thm-intermediate-value'],
            },
          ],
        },
        {
          id: 'ch1-7',
          no: '1.7',
          title: '无穷小的比较',
          summary: '无穷小都趋于 0，但趋于 0 的“速度”差别巨大。本节给快慢排序（高阶、同阶、等价），并给出最实用的计算武器：等价无穷小替换。',
          items: [
            {
              id: 'def-infinitesimal-order',
              kind: 'definition',
              name: '无穷小的比较（高阶、同阶、等价、k 阶）',
              aka: ['高阶无穷小', '同阶无穷小', '等价无穷小', 'k 阶无穷小'],
              statement: '设 <code>α</code> 与 <code>β</code> 都是同一极限过程中的无穷小，且 <code>β ≠ 0</code>。考察极限 <code>lim α/β</code>：\n1. 若 <code>lim α/β = 0</code>，则称 <code>α</code> 是比 <code>β</code> <b>高阶</b>的无穷小，记作 <code>α = o(β)</code>；\n2. 若 <code>lim α/β = ∞</code>，则称 <code>α</code> 是比 <code>β</code> <b>低阶</b>的无穷小；\n3. 若 <code>lim α/β = c ≠ 0</code>（<code>c</code> 为常数），则称 <code>α</code> 与 <code>β</code> 是<b>同阶</b>无穷小；\n4. 特别地，若 <code>lim α/β = 1</code>，则称 <code>α</code> 与 <code>β</code> 是<b>等价</b>无穷小，记作 <code>α ~ β</code>；\n5. 若 <code>lim α/β^k = c ≠ 0</code>（<code>k > 0</code>），则称 <code>α</code> 是关于 <code>β</code> 的 <b>k 阶</b>无穷小。',
              plain: '所有无穷小最后都到 0，但“到 0 的速度”可以差很多。<b>比较速度的办法就是除一下看比值。</b>\n- <code>x → 0</code> 时，<code>x²</code> 比 <code>x</code> 快得多：<code>x²/x = x → 0</code>，所以 <code>x²</code> 是 <code>x</code> 的<b>高阶</b>无穷小（跑得更快、更小）。\n- <code>2x</code> 和 <code>x</code> 速度差不多：<code>2x/x = 2</code>（常数），所以是<b>同阶</b>。\n- <code>sin x</code> 和 <code>x</code> 速度几乎完全一样：<code>sin x/x → 1</code>，所以是<b>等价</b>。\n- <code>1 - cos x</code> 和 <code>x²</code> 速度一样：<code>(1 - cos x)/x² → 1/2</code>，所以 <code>1 - cos x</code> 是关于 <code>x</code> 的<b>2 阶</b>无穷小。\n“等价”是最亲密的关系：它意味着两者在极限计算中可以互相替代（这就是等价无穷小替换）。',
              why: '为什么要用“比值”来定义快慢？因为两个量都趋于 0，直接相减（<code>α - β</code>）没有意义——都趋于 0，差也趋于 0，分不出高下。只有相除才能看出“谁相对谁更小”。\n可以这样理解：如果 <code>α/β → 0</code>，那么在 <code>β</code> 还很“大”（相对而言）的时候，<code>α</code> 已经小得可以忽略，所以 <code>α</code> 是 <code>β</code> 的高阶部分。\n<b>为什么“等价”这么重要？</b>因为 <code>α ~ β</code> 的含义是 <code>α = β + o(β)</code>，也就是“<code>α</code> 和 <code>β</code> 的主部相同，差别只在高阶小量”。在求极限时，高阶小量常常被整体消掉，所以主部可以互相替换。这就是下一节等价无穷小替换定理的直观来源。\n另外注意 <code>lim α/β</code> 可能根本不存在（比如 <code>α = x sin(1/x)</code>、<code>β = x</code>，比值为 <code>sin(1/x)</code> 无极限），这时两个无穷小<b>不可比较</b>。这不是定义漏了什么，而是客观事实。',
              example: '<code>x → 0</code> 时：<code>x³ = o(x²)</code>；<code>x²</code> 与 <code>2x²</code> 同阶；<code>sin x ~ x</code>；<code>tan x ~ x</code>；<code>1 - cos x ~ x²/2</code>；<code>ln(1 + x) ~ x</code>。\n<code>x → ∞</code> 时：<code>1/x²</code> 是比 <code>1/x</code> 高阶的无穷小；<code>1/x</code> 与 <code>2/x</code> 同阶。\n不可比较的例子：<code>α = x sin(1/x)</code>，<code>β = x</code>（<code>x → 0</code>），<code>α/β = sin(1/x)</code> 无极限，故两者无法比较高阶/同阶。',
              pitfalls: [
                '把 <code>α = o(β)</code> 理解成“<code>α</code> 等于某个叫 <code>o(β)</code> 的东西”。它只是一个<b>记号</b>，表示比值的极限为 0。',
                '混淆“高阶”与“低阶”：<b>高阶 = 更快趋于 0 = 更小</b>。可以记成“阶数越高，越小”。',
                '认为任何两个无穷小都能比较。如上例，比值无极限时不可比较。',
                '在同阶的定义里允许 <code>c = 0</code>，那不是同阶而是高阶。',
              ],
              tags: ['无穷小', '阶', '基础概念'],
              related: ['def-infinitesimal', 'thm-equivalent-substitution', 'thm-important-limit-one', 'thm-limit-infinitesimal-relation'],
            },
            {
              id: 'thm-equivalent-substitution',
              kind: 'theorem',
              name: '等价无穷小替换定理',
              aka: ['等价无穷小替换', '等价代换'],
              statement: '设在同一极限过程中 <code>α ~ α′</code>，<code>β ~ β′</code>，且 <code>lim α′/β′</code> 存在（或为 ∞），则\n<code>lim α/β = lim α′/β′</code>。\n特别地，若 <code>α ~ β</code>，则在乘积或商中可以用 <code>β</code> 替换 <code>α</code> 而极限不变：<code>lim (α · γ) = lim (β · γ)</code>，<code>lim (α/γ) = lim (β/γ)</code>。',
              plain: '这条定理是<b>算极限最省力的武器</b>。它说：在乘除法里，可以把一个无穷小换成与它等价的另一个，结果不变。\n为什么可以换？因为 <code>α ~ α′</code> 意味着 <code>α/α′ → 1</code>，也就是两者“主部相同”。写成\n<code>α/β = (α/α′) · (α′/β′) · (β′/β)</code>，\n第一个因子和第三个因子都趋于 1，乘上去等于没乘，所以极限就等于 <code>lim α′/β′</code>。\n比如求 <code>lim(x→0) sin 3x / (2x)</code>：直接用 <code>sin 3x ~ 3x</code> 换掉分子，得 <code>lim 3x/(2x) = 3/2</code>，一步到位。\n[[warn:替换只能用在<b>乘除</b>里，绝对不能用在<b>加减</b>里！]] 例如 <code>lim(x→0) (tan x - sin x)/x³</code>，如果把 <code>tan x</code> 和 <code>sin x</code> 都换成 <code>x</code>，得到 <code>(x - x)/x³ = 0</code>，但正确答案是 <code>1/2</code>。错在哪里？因为替换定理的前提是“整个乘除式”的替换，而这里是两个等价量相减，相减时它们的差异（高阶部分）恰恰是决定结果的东西。',
              why: '定理的证明短得惊人，但理解它为什么“只能用于乘除”才是关键。\n<b>证明：</b>因为 <code>α ~ α′</code> 即 <code>lim α/α′ = 1</code>，<code>β ~ β′</code> 即 <code>lim β/β′ = 1</code>，于是\n<code>α/β = (α/α′)(α′/β′)(β′/β)</code>，\n其中 <code>(α/α′) → 1</code>、<code>(β′/β) → 1</code>，由极限的乘积法则（在 <code>lim α′/β′</code> 存在的前提下）得 <code>lim α/β = 1 · lim α′/β′ · 1 = lim α′/β′</code>。\n<b>为什么加减不行？</b>看 <code>α - β</code>。若 <code>α ~ α′</code>、<code>β ~ β′</code>，则 <code>α - β = (α′ + o(α′)) - (β′ + o(β′)) = (α′ - β′) + o(...)</code>。当 <code>α′ - β′</code> 与 <code>α′</code> 同量级时，替换没问题；但当 <code>α′ ~ β′</code>（两者几乎相等，差是更高阶的小量）时，<code>α′ - β′</code> 本身就是个高阶小量，而我们把两个量各自替换时<b>丢掉的正是这些高阶小量</b>，于是主部被丢光，结果就错了。\n更直白地说：<b>等价替换是在保留“主部”，而在减法中，主部恰好被减掉了，剩下的全是我们丢掉的东西。</b>所以减法里必须换更精细的工具（如泰勒展开，或者先做恒等变形再替换）。',
              proof: '<ol><li>设 <code>α ~ α′</code>，<code>β ~ β′</code>，即 <code>lim α/α′ = 1</code>，<code>lim β/β′ = 1</code>（这意味着 <code>α′ ≠ 0</code>、<code>β′ ≠ 0</code> 在局部成立）。</li><li>把比值做恒等变形：<code>α/β = [α/α′] · [α′/β′] · [β′/β]</code>。</li><li>其中 <code>lim α/α′ = 1</code>，<code>lim β′/β = 1</code>；又由假设 <code>lim α′/β′</code> 存在（或为 ∞）。</li><li>由极限的乘积法则（当 <code>lim α′/β′</code> 为有限数时）得 <code>lim α/β = 1 · lim α′/β′ · 1 = lim α′/β′</code>。</li><li>当 <code>lim α′/β′ = ∞</code> 时，不能直接用有限情形的乘积法则；但可以反过来看：先由第 2 步写出 <code>α′/β′ = (α′/α)(α/β)(β/β′)</code>，其中 <code>α′/α → 1</code>、<code>β/β′ → 1</code>，故若 <code>lim α/β</code> 存在有限，则 <code>lim α′/β′</code> 也存在有限且相等。于是若 <code>lim α′/β′ = ∞</code>，则 <code>lim α/β</code> 也不可能收敛到有限值（否则矛盾），从而 <code>lim α/β = ∞</code>，结论仍成立。</li><li>特别地取 <code>γ ≡ 1</code> 或把 <code>γ</code> 并入 <code>β</code>、<code>β′</code>，即得乘积与商情形的替换规则。定理证毕。</li></ol>\n<b>过渡说明</b>：第 2 步是证明的全部内容——把 <code>α/β</code> 拆成三个比值相乘，让“趋于 1”的因子自动消失。第 5 步处理了 <code>∞</code> 的情形，用的是“反向书写”的方法，这是处理无穷极限时的常用替代手段。',
              example: '<b>正确用法（乘除）：</b><code>lim(x→0) sin 3x / tan 5x = lim 3x/(5x) = 3/5</code>。\n<code>lim(x→0) ln(1 + x)/x = 1</code>（用 <code>ln(1+x) ~ x</code>）。\n<code>lim(x→0) (1 - cos x)/(x sin x) = lim (x²/2)/(x · x) = 1/2</code>。\n<b>错误用法（加减）：</b><code>lim(x→0) (tan x - sin x)/x³</code>。若替换成 <code>(x - x)/x³ = 0</code>，错误！正确做法是先变形：\n<code>tan x - sin x = sin x (1/cos x - 1) = sin x · (1 - cos x)/cos x</code>，\n于是原式 <code>= lim [sin x/x] · [(1 - cos x)/x²] · (1/cos x) = 1 · (1/2) · 1 = 1/2</code>。\n（这里每一步替换都发生在乘除结构中，所以合法。）',
              pitfalls: [
                '在加减法里做等价替换。这是本章最高频的错误，务必牢记“只有乘除可以换”。',
                '替换时只换其中一个因子而忘了整体结构。例如 <code>lim(x→0) (sin x - x)/x³</code> 中把 <code>sin x</code> 换成 <code>x</code>，得到 0，而正确结果是 <code>-1/6</code>。',
                '把 <code>α ~ β</code> 当成 <code>α = β</code> 使用，然后做减法。',
                '记错常见的等价无穷小（如把 <code>1 - cos x ~ x²</code> 记成 <code>x²</code>，实际是 <code>x²/2</code>）。',
              ],
              tags: ['无穷小', '等价替换', '计算', '证明'],
              related: ['def-infinitesimal-order', 'thm-important-limit-one', 'def-elementary-continuity', 'thm-lim-operations'],
            },
            {
              id: 'def-elementary-continuity',
              kind: 'definition',
              name: '常用等价无穷小（当 x → 0 时）',
              aka: ['等价无穷小表', '常用等价无穷小'],
              statement: '当 <code>x → 0</code> 时，下列各对无穷小等价：\n1. <code>sin x ~ x</code>；\n2. <code>tan x ~ x</code>；\n3. <code>arcsin x ~ x</code>；\n4. <code>arctan x ~ x</code>；\n5. <code>1 - cos x ~ x²/2</code>；\n6. <code>ln(1 + x) ~ x</code>；\n7. <code>eˣ - 1 ~ x</code>；\n8. <code>(1 + x)^a - 1 ~ a x</code>（<code>a</code> 为常数，<code>a ≠ 0</code>）；\n9. <code>aˣ - 1 ~ x ln a</code>（<code>a > 0</code>，<code>a ≠ 1</code>）；\n10. <code>1 - cos^a x ~ (a/2) x²</code> 类可自行推广（<code>a ≠ 0</code>）。\n把上面各式中的 <code>x</code> 整体换成任意满足 <code>φ(x) → 0</code>、<code>φ(x) ≠ 0</code> 的函数 <code>φ(x)</code>，对应的等价关系仍然成立。',
              plain: '这张表是<b>微积分计算的乘法口诀</b>，必须背下来。它说的是“<code>x</code> 很小时，这些复杂的函数和某个简单的幂函数几乎一样”。\n记忆窍门：\n- 三角函数家族（<code>sin x</code>、<code>tan x</code>、<code>arcsin x</code>、<code>arctan x</code>）全部都和 <code>x</code> 等价——角度很小时，各种“弦”“切”“反”都退化成角本身。\n- 指数、对数家族（<code>ln(1+x)</code>、<code>eˣ - 1</code>）也都和 <code>x</code> 等价——<code>eˣ</code> 在 0 附近上升的初始速度恰好是 1。\n- <code>1 - cos x</code> 是“二阶”的，等价于 <code>x²/2</code>——因为余弦在 0 处是极值点，一阶项为零，所以更平缓，差值是二阶小量。\n- 一般幂 <code>(1+x)^a - 1 ~ ax</code> 是万能公式：令 <code>a = 1/2</code> 得 <code>√(1+x) - 1 ~ x/2</code>；令 <code>a = -1</code> 得 <code>1/(1+x) - 1 ~ -x</code>。',
              why: '这些等价关系都不是新定理，而是前面结论的直接推论。\n- <code>sin x ~ x</code> 就是第一个重要极限。\n- <code>tan x ~ x</code> 由 <code>tan x = sin x / cos x ~ x / 1 = x</code> 得到。\n- <code>arcsin x ~ x</code>：令 <code>t = arcsin x</code>，则 <code>x = sin t ~ t</code>，故 <code>x/t → 1</code>，即 <code>t/x → 1</code>。\n- <code>1 - cos x ~ x²/2</code>：用倍角公式 <code>1 - cos x = 2 sin²(x/2)</code>，再对 <code>sin(x/2) ~ x/2</code> 平方即得。\n- <code>ln(1 + x) ~ x</code>：令 <code>t = ln(1 + x)</code>，则 <code>x = eᵗ - 1</code>，而由第二个重要极限或指数函数的性质可得 <code>(eᵗ - 1)/t → 1</code>，于是 <code>t/x → 1</code>。\n- <code>eˣ - 1 ~ x</code>：令 <code>t = eˣ - 1</code>，则 <code>x = ln(1 + t)</code>，由上一条反推。\n- <code>(1+x)^a - 1 ~ ax</code>：这是最一般的一条，可以写成 <code>[(1+x)^a - 1]/x → a</code>，用换底 <code>(1+x)^a = e^{a ln(1+x)}</code> 和 <code>eᵘ - 1 ~ u</code>、<code>ln(1+x) ~ x</code> 复合得到。\n换句话说，<b>整张表的核心只有两个重要极限</b>，其它都是它们经过变量代换搅出来的。所以真正要记住的是那两个极限，这张表是可以现推的。',
              example: '<code>lim(x→0) (e^(2x) - 1)/x = lim (2x)/x = 2</code>（用 <code>eᵘ - 1 ~ u</code>，<code>u = 2x</code>）。\n<code>lim(x→0) ln(1 + 3x)/sin 2x = lim (3x)/(2x) = 3/2</code>。\n<code>lim(x→0) (√(1 + x) - 1)/x = lim (x/2)/x = 1/2</code>（用 <code>a = 1/2</code> 的公式）。\n<code>lim(x→0) (1 - cos 2x)/x² = lim ( (2x)²/2 )/x² = lim 2x²/x² = 2</code>。',
              pitfalls: [
                '把 <code>1 - cos x</code> 记成 <code>~ x²</code>（漏掉 1/2）。这在选择题里是常见陷阱。',
                '在加减法中套用这张表（如表中所说，只是为了方便记忆而列，不代表可以在减法里直接用）。',
                '把 <code>x</code> 换成 <code>φ(x)</code> 时忘记验证 <code>φ(x) → 0</code>。例如 <code>x → 1</code> 时 <code>ln(1 + x)</code> 显然不能等价为 <code>x</code>。',
                '混淆 <code>eˣ - 1 ~ x</code> 与 <code>eˣ ~ x</code>。后者是错的（<code>eˣ → 1</code>，不是无穷小）。',
              ],
              tags: ['无穷小', '等价无穷小', '计算', '公式'],
              related: ['thm-equivalent-substitution', 'thm-important-limit-one', 'thm-important-limit-two', 'def-infinitesimal-order'],
            },
            {
              id: 'thm-infinity-order',
              kind: 'theorem',
              name: '无穷大量的比较',
              aka: ['无穷大的阶', '高阶无穷大'],
              statement: '设在同一极限过程中 <code>f</code> 与 <code>g</code> 都是无穷大（即 <code>|f| → ∞</code>，<code>|g| → ∞</code>）。\n1. 若 <code>lim f/g = 0</code>，则称 <code>f</code> 是比 <code>g</code> <b>低阶</b>的无穷大；\n2. 若 <code>lim f/g = c ≠ 0</code>，则称 <code>f</code> 与 <code>g</code> 是<b>同阶</b>无穷大；\n3. 若 <code>lim f/g = 1</code>，则称 <code>f</code> 与 <code>g</code> 是<b>等价</b>无穷大，记作 <code>f ~ g</code>；\n4. 若 <code>lim f/g^k = c ≠ 0</code>（<code>k > 0</code>），则称 <code>f</code> 是关于 <code>g</code> 的 <code>k</code> 阶无穷大。\n在常见的比较中，当 <code>x → +∞</code> 时有\n<code>ln x ≪ x^a ≪ eˣ</code>（<code>a > 0</code>），\n即对数增长最慢，幂函数次之，指数函数最快。',
              plain: '无穷大也分“大得快”和“大得慢”。\n速度排名（<code>x → +∞</code> 时，从慢到快）：\n<b>对数最慢 < 幂函数 < 指数函数 < 阶乘</b>。\n具体感受一下 <code>x = 1000</code>：<code>ln x ≈ 6.9</code>，<code>x = 1000</code>，<code>x² = 1000000</code>，<code>eˣ</code> 大约是 <code>10^434</code>——这个数比宇宙中所有原子数还多得多。\n所以虽然它们都是“要多大有多大”，但速度完全不在一个层次上。\n这个排序在做极限题时极其有用：<code>lim(x→∞) (x^100)/eˣ = 0</code>——哪怕分子是 100 次方，指数函数照样赢。反过来 <code>lim(x→∞) ln x / x^0.001 = 0</code>。\n[[tip:记忆口诀：“指数最大，对数最小，幂在中间”。做 <code>∞/∞</code> 型极限时先用这个排序预判答案，再用计算验证。]]',
              why: '这条结论与无穷小的比较是“互为倒数”的关系：<code>f</code> 是 <code>g</code> 的高阶无穷小，等价于 <code>1/f</code> 是 <code>1/g</code> 的高阶无穷大。所以两个理论是一套东西的两种说法。\n至于速度排序的证明，思路都是“取对数把幂化成乘法”：\n要证 <code>ln x / x^a → 0</code>（<code>x → +∞</code>，<code>a > 0</code>），令 <code>x = eᵗ</code>，则 <code>t → +∞</code>，式子变成 <code>t / e^{at} = t / (e^{at}) → 0</code>（可用洛必达法则或直接估计）。<br>要证 <code>x^a / eˣ → 0</code>，同样令 <code>x = eᵗ</code>，变成 <code>e^{at}/e^{e^t} → 0</code>，一目了然。<br>这两个证明都要等到后面学了洛必达法则才好写完整，这里先记住结论即可。\n这类比较在算法分析中也天天用到：它说明为什么“多项式时间算法”远优于“指数时间算法”。',
              proof: '<ol><li><b>性质匹配。</b>设 <code>x → x₀</code>（或 <code>x → ∞</code>）时 <code>|f(x)| → ∞</code>、<code>|g(x)| → ∞</code>，则在某个去心邻域内 <code>f(x) ≠ 0</code>、<code>g(x) ≠ 0</code>，于是 <code>α = 1/f</code>、<code>β = 1/g</code> 都是无穷小。</li><li>由“无穷小与无穷大的关系”，<code>lim α = 0</code>，<code>lim β = 0</code>。计算 <code>α/β = (1/f)/(1/g) = g/f</code>。</li><li>于是：<code>f/g → 0 ⟺ g/f → ∞ ⟺ β/α → ∞ ⟺ α = o(β)</code>。这就把无穷大的比较完全化归为无穷小的比较，结论 1、2、3 随之成立（同阶、等价也一一对应）。</li><li>对增长速度的排序，这里说明其中最关键的一条的证明思路：设 <code>a > 0</code>，要证 <code>lim(x→+∞) x^a / eˣ = 0</code>。作变量代换 <code>x = eᵗ</code>（<code>t = ln x → +∞</code>），则 <code>x^a / eˣ = e^{at} / e^{e^t} = e^{at - e^t}</code>。因为 <code>e^t</code> 比 <code>at</code> 增长得快得多，<code>at - e^t → -∞</code>，故 <code>e^{at - e^t} → 0</code>。</li><li>同理，令 <code>x = eᵗ</code> 可把 <code>ln x / x^a = t / e^{at}</code> 化为“幂除以指数”的形式，由同类估计得 <code>→ 0</code>。因此 <code>ln x ≪ x^a ≪ eˣ</code>。</li></ol>\n<b>过渡说明</b>：第 1 至 3 步说明“无穷大的比较”本质上就是“无穷小的比较”的倒过来写，因此不需要重新建立一套理论。第 4、5 步的变量代换 <code>x = eᵗ</code> 是处理“幂与指数比大小”的万能钥匙：它把幂函数转化成线性函数，把指数函数转化成更快的指数函数，于是比较就变得显然。',
              example: '<code>lim(x→∞) (2x³ + 100)/(x⁴ - 1) = 0</code>：分子的无穷大阶数（3 阶）低于分母（4 阶），故极限为 0。\n<code>lim(x→∞) (3x² + x)/(2x² - 5) = 3/2</code>：同阶，比较系数。\n<code>lim(x→∞) x^10 / 2ˣ = 0</code>：指数函数完胜。\n<code>lim(x→∞) (ln x)³ / x = 0</code>：对数最慢。\n<code>lim(n→∞) 2ⁿ / n! = 0</code>：阶乘比指数增长更快（这是级数收敛性判断中常用的结论）。',
              pitfalls: [
                '把 <code>∞ + ∞</code> 当成“更大阶的无穷大”。同阶无穷大相加还是同阶，不是更高阶。',
                '把 <code>f ~ g</code>（无穷大等价）用来做减法：<code>f - g</code> 的极限要靠更高阶的分析，不能直接说为 0。',
                '以为“阶数高的无穷大更大”对一切比较都成立。这里说的是同一极限过程中的比较，不同过程之间不能直接套用。',
                '把 <code>x ≪ eˣ</code> 误记成 <code>eˣ ≪ x</code>。可以代入 <code>x = 100</code> 感受一下：<code>e^100 ≈ 2.7 × 10^43</code>，远大于 100。',
              ],
              tags: ['无穷大', '阶', '计算'],
              related: ['def-infinitesimal-order', 'def-infinity-large', 'thm-equivalent-substitution', 'thm-infinitesimal-infinity-relation'],
            },
          ],
        },
        {
          id: 'ch1-8',
          no: '1.8',
          title: '函数的连续性与间断点',
          summary: '连续就是“极限值等于函数值”。本节把不连续的点分成两类（第一类、第二类），并给出连续函数的局部性质。',
          items: [
            {
              id: 'def-continuity-point',
              kind: 'definition',
              name: '函数在一点连续的定义',
              aka: ['连续性', '在一点连续', '连续点'],
              statement: '设函数 <code>f</code> 在点 <code>x₀</code> 的某个邻域内有定义。若\n<code>lim(x→x₀) f(x) = f(x₀)</code>，\n则称函数 <code>f</code> 在点 <code>x₀</code> 处<b>连续</b>，<code>x₀</code> 称为 <code>f</code> 的<b>连续点</b>。\n用 ε-δ 语言写：<code>∀ ε > 0, ∃ δ > 0, ∀ x : |x - x₀| < δ ⟹ |f(x) - f(x₀)| < ε</code>。\n用增量形式写：记 <code>Δx = x - x₀</code>，<code>Δy = f(x₀ + Δx) - f(x₀)</code>，则连续等价于\n<code>lim(Δx→0) Δy = 0</code>，\n即“自变量增量趋于 0 时，函数增量也趋于 0”。\n若 <code>f</code> 在开区间 <code>(a, b)</code> 内每一点都连续，则称 <code>f</code> 在 <code>(a, b)</code> 内连续；若又在 <code>a</code> 处右连续（<code>lim(x→a⁺) f(x) = f(a)</code>）、在 <code>b</code> 处左连续，则称 <code>f</code> 在闭区间 <code>[a, b]</code> 上连续，记作 <code>f ∈ C[a, b]</code>。',
              plain: '函数在一点连续，意思就是“<b>图像在这个点不断裂</b>”，用公式说是：<b>极限值 = 函数值</b>。\n为什么“极限值 = 函数值”就代表不断裂？回顾一下：极限描述的是“走到这个点门口时，函数想去哪里”；函数值描述的是“这个点上函数实际在哪里”。如果两者一致，说明函数走到这里时“接上了自己”，图像是一条连续的线；如果不一致，就出现了一个“洞”或“跳台”。\n用笔画来类比：连续函数的图像可以<b>一笔画出来，笔尖不用离纸</b>（在它连续的区间上）；不连续的地方，笔必须抬起来再落下去。\n增量形式的说法更贴近物理直觉：<code>Δx → 0</code> 时 <code>Δy → 0</code>，意思是“<b>输入变化很小时，输出变化也很小</b>”。如果输入只动了一丁点，输出却突然跳了一大截，那就不连续。',
              why: '为什么连续的定义要包含三件事？把它展开写就清楚了：\n1. <code>f</code> 在 <code>x₀</code> 有定义（<code>f(x₀)</code> 存在）；\n2. <code>lim(x→x₀) f(x)</code> 存在；\n3. 两者相等。\n三件事缺一不可。<code>f(x) = 1/x</code> 在 <code>x = 0</code> 处缺第 1 条（没定义）；<code>f(x) = sin(1/x)</code>（<code>x ≠ 0</code>）在 <code>x = 0</code> 处缺第 2 条（极限不存在）；分段函数 <code>f(x) = 0（x ≠ 0），f(0) = 1</code> 缺第 3 条（极限存在且为 0，但函数值是 1）。\n<b>为什么这个定义比极限定义“多一条”？</b>因为极限故意不看 <code>x₀</code> 那一点（去心），而连续恰恰要把那一点接上。可以说：<b>连续 = 极限的好行为 + 函数值恰好接上。</b>\n这也解释了为什么求初等函数在其定义区间内的极限可以直接代入：因为初等函数在其定义区间内连续，代入就相当于用了 <code>lim f(x) = f(x₀)</code>。',
              example: '<b>连续的例子：</b><code>f(x) = x²</code> 在 <code>x = 2</code> 连续，因为 <code>lim(x→2) x² = 4 = f(2)</code>。\n<b>不连续的例子：</b>\n1. <code>f(x) = 1/x</code> 在 <code>x = 0</code> 无定义，不连续（无穷间断点）。\n2. <code>f(x) = sin(1/x)</code>（<code>x ≠ 0</code>），<code>f(0) = 0</code>：极限不存在，不连续（振荡间断点）。\n3. 分段函数 <code>f(x) = x（x ≠ 0），f(0) = 1</code>：<code>lim(x→0) f(x) = 0 ≠ 1 = f(0)</code>，不连续（可去间断点）。\n4. <code>f(x) = |x|/x</code>（<code>x ≠ 0</code>），<code>f(0) = 0</code>：左右极限分别为 <code>-1</code> 和 <code>1</code>，不连续（跳跃间断点）。',
              pitfalls: [
                '把“连续”和“有极限”混为一谈。有极限只是连续的必要条件，还需要极限值等于函数值。',
                '把“连续”与“可导”混为一谈。<code>f(x) = |x|</code> 在 <code>x = 0</code> 处连续但不可导。正确的关系是：可导 ⟹ 连续，但连续 ⇏ 可导。',
                '讨论连续性时不检查 <code>f(x₀)</code> 是否有定义。',
                '在闭区间端点处讨论“连续”时忘记只能用单侧极限。',
              ],
              tags: ['连续', '基础概念'],
              related: ['def-limit-finite', 'thm-lim-finite-independent', 'def-discontinuity-types', 'thm-continuity-local', 'thm-elementary-continuity'],
            },
            {
              id: 'def-discontinuity-types',
              kind: 'definition',
              name: '间断点及其分类',
              aka: ['间断点', '第一类间断点', '第二类间断点', '可去间断点', '跳跃间断点', '无穷间断点', '振荡间断点'],
              statement: '设函数 <code>f</code> 在点 <code>x₀</code> 的某个去心邻域内有定义（但在 <code>x₀</code> 处不连续）。若 <code>f</code> 在 <code>x₀</code> 处不连续，则称 <code>x₀</code> 为 <code>f</code> 的<b>间断点</b>。分类如下：\n<b>第一类间断点：</b>左右极限 <code>f(x₀⁻)</code> 与 <code>f(x₀⁺)</code> 都存在。\n- 若 <code>f(x₀⁻) = f(x₀⁺) ≠ f(x₀)</code>（或 <code>f(x₀)</code> 无定义），称 <code>x₀</code> 为<b>可去间断点</b>；\n- 若 <code>f(x₀⁻) ≠ f(x₀⁺)</code>，称 <code>x₀</code> 为<b>跳跃间断点</b>。\n<b>第二类间断点：</b>左右极限中至少有一个不存在。\n- 若至少有一侧为无穷大，称<b>无穷间断点</b>；\n- 若函数在 <code>x₀</code> 附近无限次振荡而无极限，称<b>振荡间断点</b>。',
              plain: '把“不连续的点”分两类的标准只有一条：<b>左右极限是不是都存在。</b>\n- 两边都能算出一个有限的数 → <b>第一类</b>。这时又分两种：两边算出同一个数，但那个数不等于函数值（或者函数压根没定义）→ <b>可去间断点</b>（意思是“只要改一下这一点的函数值就能补好”，像衣服上一个小洞，补一针就完事）；两边算出不同的数 → <b>跳跃间断点</b>（像台阶，从左边走是一个高度，从右边走是另一个高度）。\n- 有哪一边算不出来（跑到无穷去了，或者在那里疯狂震荡） → <b>第二类</b>。\n“可去”这个名字很实在：<code>f(x) = (x²-1)/(x-1)</code> 在 <code>x = 1</code> 处极限是 2，我们只要补充定义 <code>f(1) = 2</code>，函数就在 <code>x = 1</code> 连续了。这个“洞”被填上了，所以叫“可去”。\n跳跃间断点填不上：你没法用一个数同时充当左边的 -1 和右边的 1。',
              why: '为什么用“左右极限是否存在”当分类标准，而不是用别的？因为这个标准正好对应“能不能通过修改函数在这一点的值来修复”。\n如果左右极限都存在且相等（比如都等于 2），那么把 <code>f(x₀)</code> 改成 2 就连续了——修复方案只要改一个点，所以“可去”。\n如果左右极限存在但不等，那无论 <code>f(x₀)</code> 取什么值，总有一边的极限跟它不等，改不干净——所以“跳跃”。\n如果有一侧极限不存在（比如无穷或者振荡），那更没法修，因为连“应该改成多少”都无从谈起。所以归入第二类。\n这套分类的意义在于：它把“不连续”这个笼统的说法细化成了四种具体形态，这样在判断函数性质（比如可积性）时就能精确处理。',
              example: '<b>可去间断点：</b><code>f(x) = sin x / x</code>（<code>x ≠ 0</code>），<code>f(0) = 0</code>。因为 <code>lim(x→0) sin x/x = 1 ≠ 0 = f(0)</code>，且左右极限都等于 1，所以 <code>x = 0</code> 是可去间断点。补充定义 <code>f(0) = 1</code> 后就连续了。\n<b>跳跃间断点：</b><code>f(x) = |x|/x</code>（<code>x ≠ 0</code>），<code>f(0) = 0</code>。<code>f(0⁻) = -1</code>，<code>f(0⁺) = 1</code>，跳跃间断点。\n<b>无穷间断点：</b><code>f(x) = 1/x</code> 在 <code>x = 0</code>（第二类）；<code>f(x) = tan x</code> 在 <code>x = π/2</code>（<code>lim(x→π/2⁻) tan x = +∞</code>）。\n<b>振荡间断点：</b><code>f(x) = sin(1/x)</code> 在 <code>x = 0</code>（第二类）。',
              pitfalls: [
                '把“第一类”理解成“可以修补”，其实只有可去间断点能修补，跳跃间断点也是第一类但补不了。',
                '把 <code>f(x₀)</code> 无定义的情况误判为第二类。只要左右极限都存在，即使 <code>f(x₀)</code> 没定义，也属于第一类（可去）。',
                '把无穷间断点当作第一类，因为“<code>∞</code> 也是一种极限”。要注意 <code>∞</code> 不是有限数，极限不算存在。',
                '只算出 <code>lim(x→x₀) f(x)</code> 不存在就断言是第二类，不去检查单侧极限。',
              ],
              tags: ['连续', '间断点', '分类'],
              related: ['def-continuity-point', 'def-limit-one-sided', 'thm-limit-two-sided', 'thm-continuity-local'],
            },
            {
              id: 'thm-continuity-local',
              kind: 'theorem',
              name: '连续函数的局部性质',
              aka: ['连续函数的局部有界性', '连续函数的局部保号性'],
              statement: '若函数 <code>f</code> 在点 <code>x₀</code> 处连续，则：\n1.<b>局部有界性：</b>存在 <code>δ > 0</code> 和 <code>M > 0</code>，使得当 <code>|x - x₀| < δ</code> 时 <code>|f(x)| ≤ M</code>；\n2.<b>局部保号性：</b>若 <code>f(x₀) > 0</code>（或 <code>< 0</code>），则存在 <code>δ > 0</code>，使得当 <code>|x - x₀| < δ</code> 时 <code>f(x) > 0</code>（相应地 <code>f(x) < 0</code>）。',
              plain: '这两条其实是上一节“函数极限的局部有界性/保号性”的翻版，只不过因为连续时极限就等于函数值，所以结论可以写得更强一点：<b>范围包含 <code>x₀</code> 本身，而且用的是普通邻域而不是去心邻域。</b>\n- 局部有界：函数在连续点附近不会突然爆掉。\n- 局部保号：函数在连续点处如果是正的，那它附近一整小片都是正的；如果是负的，附近一整小片都是负的。\n后一条是<b>零点定理</b>的基础：如果一个连续函数在某点为正、另一点为负，那么从正到负的路上必然经过 0，这就是下一章要证的结论。',
              why: '证明就是把“连续性”翻译成“极限”再用上面两条已知定理。\n因为 <code>f</code> 在 <code>x₀</code> 连续 ⟹ <code>lim(x→x₀) f(x) = f(x₀)</code>，而这个极限是存在的，所以可以直接套用“函数极限的局部有界性”和“局部保号性”。\n想让邻域包含 <code>x₀</code> 也不难：上面两条定理给的是 <code>0 < |x - x₀| < δ</code>，再加上 <code>x = x₀</code> 这一点。对于有界性，<code>|f(x₀)|</code> 本身是个固定数，把它并入 <code>M</code> 即可（取 <code>M = max{ 旧M, |f(x₀)| }</code>）；对于保号性，已知 <code>f(x₀) > 0</code>，所以 <code>x₀</code> 这一点本身当然满足，直接并进去即可。\n这个“把一点并进邻域”的小操作看起来不起眼，但是连续性与普通极限的差别所在。',
              proof: '<ol><li><b>局部有界性。</b>由 <code>f</code> 在 <code>x₀</code> 连续，<code>lim(x→x₀) f(x) = f(x₀)</code>。由函数极限的局部有界性，存在 <code>δ₁ > 0</code> 与 <code>M₁ > 0</code>，使 <code>0 < |x - x₀| < δ₁</code> 时 <code>|f(x)| ≤ M₁</code>。</li><li>取 <code>δ = δ₁</code>，<code>M = max{ M₁, |f(x₀)| }</code>。则当 <code>0 < |x - x₀| < δ</code> 时 <code>|f(x)| ≤ M₁ ≤ M</code>；当 <code>x = x₀</code> 时 <code>|f(x)| = |f(x₀)| ≤ M</code>。故当 <code>|x - x₀| < δ</code> 时 <code>|f(x)| ≤ M</code>。</li><li><b>局部保号性（以 <code>f(x₀) > 0</code> 为例）。</b>由 <code>lim(x→x₀) f(x) = f(x₀) > 0</code> 及函数极限的局部保号性，存在 <code>δ > 0</code>，使 <code>0 < |x - x₀| < δ</code> 时 <code>f(x) > 0</code>。</li><li>又 <code>f(x₀) > 0</code>，故对 <code>x = x₀</code> 也成立。于是当 <code>|x - x₀| < δ</code> 时 <code>f(x) > 0</code>。<code>f(x₀) < 0</code> 的情形同理。</li><li>定理证毕。</li></ol>\n<b>过渡说明</b>：本证明是“拿来主义”的典型：先引用极限的局部性质，再做一处小修补（把 <code>x₀</code> 这一点并进邻域）。要特别注意最后这一步修补的必要性：如果没有连续这个前提，<code>x₀</code> 处的值可能很差，就并不进去。',
              example: '<code>f(x) = ln x</code> 在 <code>x = 1</code> 连续且 <code>f(1) = 0</code>。局部保号性在这里退化（因为 <code>f(x₀) = 0</code> 不满足 "<code>> 0</code>" 或 "<code>< 0</code>"），事实上 <code>ln x</code> 在 <code>x = 1</code> 两侧变号——这不是反例，而是说明保号性确实需要 <code>f(x₀) ≠ 0</code>。\n<code>f(x) = x² + 1</code> 在 <code>x = 0</code> 连续，<code>f(0) = 1 > 0</code>，取 <code>δ = 1</code>，则 <code>|x| < 1</code> 时 <code>f(x) ≥ 1 > 0</code>。\n<code>f(x) = x - 3</code> 在 <code>x = 4</code> 连续且 <code>f(4) = 1 > 0</code>，则存在 <code>δ = 1</code> 使 <code>|x - 4| < 1</code> 时 <code>f(x) > 0</code>。',
              pitfalls: [
                '把局部性质推广成整体性质：连续函数在全定义域上不一定有界（<code>f(x) = 1/x</code> 在 <code>(0,1)</code> 上连续但无界）。',
                '在 <code>f(x₀) = 0</code> 时使用保号性。保号性要求 <code>f(x₀) ≠ 0</code>。',
                '认为保号性可以给出“多正”的下界。<code>f(x) > 0</code> 只是符号结论，具体下界要另外估计。',
                '把连续点处的邻域写成去心的。连续的好处正是可以把 <code>x₀</code> 本身包含进去。',
              ],
              tags: ['连续', '局部性质', '证明'],
              related: ['def-continuity-point', 'thm-lim-finite-local-bounded', 'thm-lim-finite-sign-preserve', 'thm-zero-point'],
            },
          ],
        },
        {
          id: 'ch1-9',
          no: '1.9',
          title: '连续函数的运算与初等函数的连续性',
          summary: '连续函数经过四则运算、复合、求反函数之后仍然连续，由此得到一个极其省事的大结论：初等函数在它的定义区间内处处连续。',
          items: [
            {
              id: 'thm-continuity-operations',
              kind: 'theorem',
              name: '连续函数的四则运算',
              aka: ['连续函数的和差积商'],
              statement: '设函数 <code>f</code> 与 <code>g</code> 都在点 <code>x₀</code> 处连续，则它们的和、差、积、商（在分母不为零处）在 <code>x₀</code> 处也连续：\n1. <code>f ± g</code> 在 <code>x₀</code> 连续；\n2. <code>f · g</code> 在 <code>x₀</code> 连续；\n3. 若 <code>g(x₀) ≠ 0</code>，则 <code>f/g</code> 在 <code>x₀</code> 连续。',
              plain: '这条定理完全可以“望文生义”：连续函数加连续函数还是连续的。\n为什么？因为如果两块图像在某个点都没断，那把它们叠起来（相加）、放大（相乘）或者在同一个点上相除（分母不为零），也不会突然断掉。\n比如 <code>sin x</code> 和 <code>cos x</code> 处处连续，所以 <code>sin x + cos x</code>、<code>sin x · cos x</code> 也处处连续；<code>tan x = sin x / cos x</code> 在 <code>cos x ≠ 0</code> 的地方连续，在 <code>cos x = 0</code> 处（<code>x = π/2 + kπ</code>）没有定义，那里是无穷间断点。',
              why: '证明就是引用极限的四则运算法则，非常直接。\n以加法为例：要证 <code>lim(f + g) = (f + g)(x₀)</code>，只需算\n<code>lim(f + g) = lim f + lim g = f(x₀) + g(x₀) = (f + g)(x₀)</code>。\n中间那一步用了极限的加法法则（前提是两个极限都存在，而连续恰好保证了这一点）。\n<b>除法那条为什么要 <code>g(x₀) ≠ 0</code>？</b>因为极限的除法法则要求分母极限不为 0。而且从概念上讲，如果 <code>g(x₀) = 0</code>，那么 <code>f/g</code> 在 <code>x₀</code> 处本来就没有定义，谈不上连续。\n顺便说一句：这个“<code>g(x₀) ≠ 0</code>”的条件还能自动保证在 <code>x₀</code> 附近 <code>g</code> 也不为零（由局部保号性），所以 <code>f/g</code> 确实在一个邻域内有定义，连续性讨论才有意义。',
              proof: '<ol><li>设 <code>f</code>、<code>g</code> 在 <code>x₀</code> 连续，即 <code>lim(x→x₀) f(x) = f(x₀)</code>，<code>lim(x→x₀) g(x) = g(x₀)</code>。</li><li><b>加法：</b>由极限的加法法则，<code>lim(f(x) + g(x)) = f(x₀) + g(x₀) = (f + g)(x₀)</code>，故 <code>f + g</code> 在 <code>x₀</code> 连续。减法同理。</li><li><b>乘法：</b>由极限的乘法法则，<code>lim(f(x)g(x)) = f(x₀)g(x₀) = (fg)(x₀)</code>，故 <code>fg</code> 在 <code>x₀</code> 连续。</li><li><b>除法：</b>设 <code>g(x₀) ≠ 0</code>。由局部保号性，存在 <code>δ > 0</code>，当 <code>|x - x₀| < δ</code> 时 <code>g(x) ≠ 0</code>，故 <code>f/g</code> 在该邻域内有定义。</li><li>由极限的除法法则（分母极限 <code>g(x₀) ≠ 0</code>），<code>lim [f(x)/g(x)] = f(x₀)/g(x₀) = (f/g)(x₀)</code>，故 <code>f/g</code> 在 <code>x₀</code> 连续。</li><li>定理证毕。</li></ol>\n<b>过渡说明</b>：整个证明就是把“连续”翻译成“极限等于函数值”，然后逐个应用极限的运算法则。第 4 步是唯一稍有技术含量的地方，它说明“分母不为零”这个前提不只是为了避免除零，还保证了这个商在附近处处有意义。',
              example: '多项式 <code>P(x) = aₙxⁿ + … + a₁x + a₀</code> 在 <code>ℝ</code> 上处处连续（由常数函数与 <code>y = x</code> 连续，经有限次加乘得到）。\n有理函数 <code>P(x)/Q(x)</code> 在 <code>Q(x) ≠ 0</code> 的一切点连续。\n<code>f(x) = x² + sin x</code> 处处连续；<code>g(x) = tan x</code> 在 <code>x ≠ π/2 + kπ</code> 处连续。',
              pitfalls: [
                '两个连续函数相加连续，但“两个不连续函数相加必不连续”是错的：<code>f(x) = 1（x 为有理数），0（x 为无理数）</code> 与 <code>g(x) = 0（x 为有理数），1（x 为无理数）</code> 都不连续，但 <code>f + g ≡ 1</code> 处处连续。',
                '商的情形忘记检查 <code>g(x₀) ≠ 0</code>。',
                '以为“连续函数的复合必连续”是这条定理的一部分。复合是下一条定理。',
              ],
              tags: ['连续', '运算法则', '证明'],
              related: ['def-continuity-point', 'thm-lim-operations', 'thm-continuity-composite', 'thm-elementary-continuity'],
            },
            {
              id: 'thm-continuity-inverse',
              kind: 'theorem',
              name: '反函数的连续性',
              aka: ['反函数连续性定理'],
              statement: '若函数 <code>y = f(x)</code> 在区间 <code>I</code> 上单调增加（或单调减少）且连续，则它的反函数 <code>x = f⁻¹(y)</code> 在对应的区间 <code>I* = f(I)</code> 上也单调增加（相应地单调减少）且连续。',
              plain: '这条定理说：<b>一条不断裂、一路上升（或下降）的曲线，横过来看（把 <code>x</code>、<code>y</code> 互换）仍然是一条不断裂的曲线。</b>\n这非常符合直觉：反函数的图像就是把原图沿对角线 <code>y = x</code> 翻折过去，而翻折不会把连续的线扯断。\n单调性也保留：原来 <code>x</code> 越大 <code>y</code> 越大，反过来 <code>y</code> 越大 <code>x</code> 就越大，还是增加。\n这条定理的实际用途是：<b>它保证了反三角函数是连续的。</b>因为 <code>sin x</code> 在 <code>[-π/2, π/2]</code> 上单调增加且连续，所以 <code>arcsin x</code> 在 <code>[-1, 1]</code> 上连续。这样一来，<code>lim(x→0) arcsin x = arcsin 0 = 0</code> 就直接成立了。',
              why: '这个定理的证明比看起来要讲究。关键难点在于：<b>“<code>y → y₀</code>”并不自动意味着“<code>x = f⁻¹(y) → x₀ = f⁻¹(y₀)</code>”，因为反函数的定义域是 <code>y</code> 轴上的区间，<code>y</code> 可以从左右任意方式靠近 <code>y₀</code>。</b>\n证明的标准思路是“<b>用夹逼</b>”：设 <code>f</code> 在 <code>I</code> 上单调增加且连续，<code>y₀ = f(x₀)</code>，要证 <code>lim(y→y₀) f⁻¹(y) = x₀</code>。\n任取 <code>x₀</code> 附近的两个点 <code>x₁ < x₀ < x₂</code>（<code>I</code> 的内点情形），令 <code>y₁ = f(x₁) < y₀ < y₂ = f(x₂)</code>。由 <code>f⁻¹</code> 的单调性，当 <code>y₁ < y < y₂</code> 时必有 <code>x₁ < f⁻¹(y) < x₂</code>。\n现在任给 <code>ε > 0</code>，只要取 <code>x₁</code>、<code>x₂</code> 满足 <code>x₀ - ε < x₁ < x₀ < x₂ < x₀ + ε</code>（由 <code>I</code> 是区间、<code>x₀</code> 是内点，这样的点存在），再令 <code>δ = min{ y₀ - y₁, y₂ - y₀ } > 0</code>，则 <code>|y - y₀| < δ</code> 时 <code>y₁ < y < y₂</code>，从而 <code>|f⁻¹(y) - x₀| < ε</code>。这就证明了连续性。\n看出来了吗？<b>夹逼的方向反过来了</b>：平时我们是从 <code>x</code> 的范围推出 <code>f(x)</code> 的范围；这里是从 <code>y</code> 的范围反推 <code>x</code> 的范围，靠的正是单调性。这就是为什么单调性是本定理不可缺少的条件——没有单调性，<code>y ∈ (y₁, y₂)</code> 就不能定位 <code>x</code> 的位置。',
              proof: '<ol><li>设 <code>f</code> 在区间 <code>I</code> 上单调增加且连续，<code>I* = f(I)</code>。先说明反函数存在：单调函数是单射，故 <code>f⁻¹ : I* → I</code> 存在，且也单调增加（若 <code>y₁ < y₂</code> 而 <code>f⁻¹(y₁) ≥ f⁻¹(y₂)</code>，则由 <code>f</code> 单调增加得 <code>y₁ = f(f⁻¹(y₁)) ≥ f(f⁻¹(y₂)) = y₂</code>，矛盾）。</li><li>取 <code>x₀ ∈ I</code> 为 <code>I</code> 的内点，记 <code>y₀ = f(x₀)</code>。任给 <code>ε > 0</code>。因为 <code>x₀</code> 是内点，可取 <code>x₁, x₂ ∈ I</code> 使 <code>x₀ - ε < x₁ < x₀ < x₂ < x₀ + ε</code>。</li><li>令 <code>y₁ = f(x₁)</code>，<code>y₂ = f(x₂)</code>。由 <code>f</code> 单调增加，<code>y₁ < y₀ < y₂</code>。取 <code>δ = min{ y₀ - y₁, y₂ - y₀ } > 0</code>。</li><li>当 <code>y ∈ I*</code> 且 <code>|y - y₀| < δ</code> 时，有 <code>y₁ < y < y₂</code>。对两边同时作用单调增加的 <code>f⁻¹</code>，得 <code>x₁ = f⁻¹(y₁) < f⁻¹(y) < f⁻¹(y₂) = x₂</code>。</li><li>结合 <code>x₀ - ε < x₁</code> 与 <code>x₂ < x₀ + ε</code>，得 <code>x₀ - ε < f⁻¹(y) < x₀ + ε</code>，即 <code>|f⁻¹(y) - x₀| < ε</code>。</li><li>按定义 <code>lim(y→y₀) f⁻¹(y) = x₀ = f⁻¹(y₀)</code>，故 <code>f⁻¹</code> 在 <code>y₀</code> 连续。由 <code>y₀</code> 的任意性，<code>f⁻¹</code> 在 <code>I*</code> 的内部连续。</li><li>端点处的连续性只需把上述论证改成单侧（例如 <code>x₀</code> 是左端点时只取 <code>x₂ > x₀</code>），同样得到右连续（或左连续）的结论。</li><li>单调减少的情形把上述推理中的不等号方向翻转即可，结论同样成立。</li></ol>\n<b>过渡说明</b>：第 2、3 步是“<b>用 ε 造出 ±ε 两点</b>”，这是本证明最有创意的地方——为了控制 <code>f⁻¹(y)</code>，我们不直接从 <code>y</code> 出发，而是先在 <code>x</code> 轴上架好 <code>x₀ ± ε</code> 两根“桩”，再把它们映射到 <code>y</code> 轴上得到 <code>y₁</code>、<code>y₂</code>，用它们之间的区域（长度为 <code>δ</code>）反过来限制 <code>y</code>。这就是所谓的“<b>反过来夹</b>”。',
              example: '<code>y = sin x</code> 在 <code>[-π/2, π/2]</code> 上单调增加且连续，值域为 <code>[-1, 1]</code>，故 <code>y = arcsin x</code> 在 <code>[-1, 1]</code> 上单调增加且连续。\n<code>y = eˣ</code> 在 <code>ℝ</code> 上单调增加且连续，值域为 <code>(0, +∞)</code>，故 <code>y = ln x</code> 在 <code>(0, +∞)</code> 上单调增加且连续。\n反例的必要性：<code>f(x) = sin(1/x)</code>（<code>x ≠ 0</code>）不单调，所以不能直接断言其（某个分支的）反函数连续；事实上这个函数根本没有反函数（不单射）。',
              pitfalls: [
                '忽略单调性条件。没有单调性就没有反函数，谈连续性无从谈起。',
                '误以为“连续函数的反函数一定连续”，其实要先有反函数（需单调或单射）。',
                '把结论推广到整个定义域时忘记检查值域的端点情形。',
              ],
              tags: ['连续', '反函数', '证明'],
              related: ['def-inverse-function', 'def-continuity-point', 'def-elementary-functions', 'thm-elementary-continuity'],
            },
            {
              id: 'thm-continuity-composite',
              kind: 'theorem',
              name: '复合函数的连续性',
              aka: ['复合函数连续性定理'],
              statement: '设函数 <code>u = g(x)</code> 在点 <code>x₀</code> 处连续，函数 <code>y = f(u)</code> 在点 <code>u₀ = g(x₀)</code> 处连续，且 <code>f[g(x)]</code> 在 <code>x₀</code> 的某个邻域内有定义，则复合函数 <code>f[g(x)]</code> 在 <code>x₀</code> 处连续，即\n<code>lim(x→x₀) f[g(x)] = f[lim(x→x₀) g(x)] = f[g(x₀)]</code>。',
              plain: '这条定理的意思是：<b>连续的流水线接上连续的流水线，整条线还是连续的。</b>\n它的实际价值在于那个等式：<code>lim f[g(x)] = f(lim g(x))</code>——<b>极限符号可以“搬进”连续函数里面去。</b>\n比如求 <code>lim(x→0) ln(1 + x)/x</code>……更简单的例子：<code>lim(x→0) √(1 + x) = √(lim(x→0)(1 + x)) = √1 = 1</code>。因为根号是连续函数，所以可以先把极限算出来再开方。\n这个“搬进去”的操作在计算中天天用，它是等价无穷小替换、换元法等技巧的合法性依据。',
              why: '证明可以直接引用“复合函数的极限运算法则”，但要注意一个技术细节：<b>那里要求 <code>g(x) ≠ u₀</code>，而这里 <code>g</code> 完全可能在 <code>x₀</code> 附近取到 <code>u₀</code>（比如 <code>g</code> 是常数函数）。</b>\n好在连续性恰好把这个麻烦消掉了：因为 <code>f</code> 在 <code>u₀</code> 连续，所以 <code>lim(u→u₀) f(u) = f(u₀)</code>，也就是说即使 <code>g(x) = u₀</code>，我们也能把 <code>f(u₀)</code> 这个值顺利接上，不受影响。\n具体的证明有两种写法：\n<b>写法一（用极限的复合运算法则）：</b>如果额外假设 <code>g(x) ≠ u₀</code>，就直接引用。若不做此假设，需要把极限定义中的 <code>0 < |u - u₀| < η</code> 改成 <code>|u - u₀| < η</code>，而这一步之所以合法，正是因为 <code>u = u₀</code> 时不等式 <code>|f(u₀) - f(u₀)| = 0 < ε</code> 自动成立。\n<b>写法二（直接用 ε-δ）：</b>任给 <code>ε > 0</code>。由 <code>f</code> 在 <code>u₀</code> 连续，存在 <code>η > 0</code>，当 <code>|u - u₀| < η</code> 时 <code>|f(u) - f(u₀)| < ε</code>（注意这里<b>没有去心</b>，这正是连续性的好处）。再由 <code>g</code> 在 <code>x₀</code> 连续，对这个 <code>η</code> 存在 <code>δ > 0</code>，当 <code>|x - x₀| < δ</code> 时 <code>|g(x) - g(x₀)| = |g(x) - u₀| < η</code>。代入即得 <code>|f[g(x)] - f[g(x₀)]| < ε</code>。\n可以看出，写法二里那两个“没有去心”的不等号正是连续性的全部贡献。',
              proof: '<ol><li>已知 <code>g</code> 在 <code>x₀</code> 连续：<code>lim(x→x₀) g(x) = g(x₀) = u₀</code>；<code>f</code> 在 <code>u₀</code> 连续：<code>lim(u→u₀) f(u) = f(u₀)</code>。</li><li>任给 <code>ε > 0</code>。由 <code>f</code> 在 <code>u₀</code> 连续，存在 <code>η > 0</code>，使得当 <code>|u - u₀| < η</code> 时 <code>|f(u) - f(u₀)| < ε</code>（注意此处无需 <code>u ≠ u₀</code>）。</li><li>由 <code>lim(x→x₀) g(x) = u₀</code>，对正数 <code>η</code> 存在 <code>δ > 0</code>，使得当 <code>0 < |x - x₀| < δ</code> 时 <code>|g(x) - u₀| < η</code>。又 <code>g</code> 在 <code>x₀</code> 连续，故 <code>x = x₀</code> 时 <code>g(x₀) = u₀</code>，<code>|g(x₀) - u₀| = 0 < η</code> 也成立，所以实际上 <code>|x - x₀| < δ</code> 时均有 <code>|g(x) - u₀| < η</code>。</li><li>于是对满足 <code>|x - x₀| < δ</code> 的 <code>x</code>，令 <code>u = g(x)</code>，则 <code>|u - u₀| < η</code>，由第 2 步得 <code>|f[g(x)] - f(u₀)| < ε</code>，即 <code>|f[g(x)] - f[g(x₀)]| < ε</code>。</li><li>按定义，<code>f[g(x)]</code> 在 <code>x₀</code> 连续，并且 <code>lim(x→x₀) f[g(x)] = f[g(x₀)] = f[lim(x→x₀) g(x)]</code>。定理证毕。</li></ol>\n<b>过渡说明</b>：本证明与“复合函数极限法则”的唯一区别在第 2、3 步末尾的两句补充说明——都是把“<b>去心</b>”改成“<b>不去心</b>”。这一步看似微小，却正是连续性带来的实质性简化，因此教材把它单列为一条定理。',
              example: '<code>lim(x→0) √(1 + x²) = √(lim(1 + x²)) = √1 = 1</code>。\n<code>lim(x→0) ln(cos x) = ln(cos 0) = ln 1 = 0</code>（因为 <code>cos</code> 连续、<code>ln</code> 在 <code>1</code> 处连续）。\n<code>lim(x→1) arcsin((x - 1)/(x + 1)) = arcsin(0) = 0</code>。\n<b>对比一下不连续的情形：</b>设 <code>f(u) = 1（u ≠ 0），f(0) = 0</code>，<code>g(x) ≡ 0</code>。此时 <code>f</code> 在 <code>u = 0</code> 不连续，<code>f[g(x)] ≡ 0</code>，而 <code>f(lim g) = f(0) = 0</code>——这个例子恰好还成立，但若把 <code>f(0)</code> 改成别的值就会失败。这说明连续性是这条定理的必要条件。',
              pitfalls: [
                '把 <code>lim f[g(x)] = f(lim g(x))</code> 当成对任何 <code>f</code> 都成立的公式。它只对连续的 <code>f</code> 成立。',
                '验证时只看内层或只看外层的连续性，忘记两个都要连续。',
                '把“内层连续”与“内层极限存在”混淆：<code>g</code> 只需在 <code>x₀</code> 有极限 <code>u₀</code> 且 <code>f</code> 在 <code>u₀</code> 连续，就有 <code>lim f[g(x)] = f(u₀)</code>（这时 <code>g</code> 在 <code>x₀</code> 不必连续）。这是一个常用的小加强。',
              ],
              tags: ['连续', '复合函数', '证明'],
              related: ['thm-limit-composite', 'def-continuity-point', 'thm-elementary-continuity', 'def-composite-function'],
            },
            {
              id: 'thm-elementary-continuity',
              kind: 'theorem',
              name: '初等函数的连续性',
              aka: ['初等函数在其定义区间内连续'],
              statement: '一切初等函数在其<b>定义区间</b>内都是连续的。\n因此，若 <code>f</code> 是初等函数，<code>x₀</code> 属于它的定义区间，则\n<code>lim(x→x₀) f(x) = f(x₀)</code>，\n即求初等函数在定义区间内某点的极限时，可以直接把 <code>x₀</code> 代入函数式。',
              plain: '这条定理是本章最省事的一条：<b>凡是初等函数，在它能正常取值的地方，图像都是一笔画的，求极限直接代入就行。</b>\n比如 <code>lim(x→2) (x³ + sin x)/(1 + x²) = (8 + sin 2)/5</code>——直接把 2 代进去，不用做任何 ε-δ 的证明。\n这就是为什么学这一章之后，绝大多数“求极限”的题目都可以先尝试代入。\n[[warn:注意两个前提：第一，必须是<b>初等函数</b>；第二，必须在<b>定义区间</b>内，而不是随便什么地方。比如 <code>lim(x→0) 1/x</code> 就不能代入（0 不在定义域里），<code>lim(x→0) sin x/x</code> 也不能代入（这是“可去间断点”型，要另想办法）。]]',
              why: '这条定理的证明思路是“<b>从积木到整栋楼</b>”：\n1. 先证明六类基本初等函数在各自的定义区间内连续。这一步要逐个来：<code>y = C</code>（显然）、<code>y = x</code>（显然）、<code>y = xⁿ</code>（由乘积的连续性），一般幂函数 <code>y = x^μ = e^{μ ln x}</code>（由指数、对数、复合的连续性）；<code>y = aˣ</code> 的连续性可由第二个重要极限推出；<code>y = log_a x</code> 是 <code>aˣ</code> 的反函数，由反函数连续性定理得连续；<code>sin x</code> 的连续性用 <code>|sin x - sin x₀| = 2|cos((x+x₀)/2)|·|sin((x-x₀)/2)| ≤ |x - x₀|</code> 即得；<code>cos x</code> 类似；<code>tan x</code>、<code>cot x</code>、<code>sec x</code>、<code>csc x</code> 由除法法则得；<code>arcsin x</code> 等由反函数连续性定理得。\n2. 然后证明“连续性在四则运算、复合、求反函数下保持”——这正是本节前三条定理。\n3. 因为初等函数是由基本初等函数经有限次四则运算与复合得到的，所以它的连续性可以由前面的一步步“继承”下来。\n换句话说，这条定理不是一个新东西，而是前面所有结论的“总账”。',
              proof: '<ol><li><b>第一步：基本初等函数的连续性。</b>（i）<code>y = C</code> 是常数函数，任给 <code>ε > 0</code>，取 <code>δ</code> 任意（如 <code>δ = 1</code>），则 <code>|C - C| = 0 < ε</code>，故处处连续。（ii）<code>y = x</code>：取 <code>δ = ε</code> 即得 <code>|x - x₀| < ε</code>，处处连续。（iii）<code>y = xⁿ</code>（<code>n</code> 为正整数）由 <code>y = x</code> 经 <code>n</code> 次乘法得到，由“连续函数的乘积连续”即得处处连续。一般幂函数 <code>x^μ = e^{μ ln x}</code>（<code>x > 0</code>）由指数、对数与复合的连续性可得。</li><li>（iv）<code>y = sin x</code>：对任意 <code>x₀ ∈ ℝ</code>，由和差化积公式\n<code>|sin x - sin x₀| = 2|cos((x + x₀)/2)| · |sin((x - x₀)/2)| ≤ 2 · 1 · |x - x₀|/2 = |x - x₀|</code>。<br>任给 <code>ε > 0</code>，取 <code>δ = ε</code>，则 <code>|x - x₀| < δ</code> 时 <code>|sin x - sin x₀| < ε</code>，故 <code>sin x</code> 处处连续。（v）<code>y = cos x</code> 同理（或用 <code>cos x = sin(x + π/2)</code> 与复合）。</li><li>（vi）<code>y = aˣ</code>（<code>a > 0</code>，<code>a ≠ 1</code>）：先证 <code>lim(x→0) aˣ = 1</code>。当 <code>a > 1</code> 时，令 <code>aˣ - 1 = t > 0</code>（<code>x > 0</code>），由 <code>aˣ = 1 + t</code> 及二项式/不等式估计可得 <code>0 < aˣ - 1 < a - 1</code>（当 <code>0 < x < 1</code>）从而夹逼得 <code>aˣ → 1</code>；<code>0 < a < 1</code> 时由 <code>aˣ = 1/(1/a)ˣ</code> 化归。再由 <code>|aˣ - a^{x₀}| = a^{x₀}|a^{x - x₀} - 1| → 0</code> 得 <code>aˣ</code> 处处连续。（vii）<code>y = log_a x</code> 是 <code>aˣ</code> 的反函数，由“反函数连续性定理”在 <code>(0, +∞)</code> 上连续。</li><li>（viii）<code>tan x = sin x / cos x</code>、<code>cot x</code>、<code>sec x</code>、<code>csc x</code> 由“连续函数的商连续”在分母不为零处连续。（ix）<code>arcsin x</code>、<code>arccos x</code>、<code>arctan x</code>、<code>arccot x</code> 分别是三角函数在相应单调区间上的反函数，由“反函数连续性定理”连续。</li><li><b>第二步：连续性的继承。</b>由本节已证的“连续函数的四则运算”与“复合函数的连续性”两条定理，连续函数经过有限次四则运算与复合之后仍连续。</li><li><b>第三步：结论。</b>初等函数定义为常数与基本初等函数经过有限次四则运算和复合所得、且能用一个式子表示的函数。由第一步，它的“积木”都连续；由第二步，搭积木的过程保持连续性。故初等函数在其定义区间内连续。</li><li>再由连续的定义 <code>lim(x→x₀) f(x) = f(x₀)</code>，即得“求极限可直接代入”的结论。</li></ol>\n<b>过渡说明</b>：本证明不是一次性的技巧，而是一个“<b>清点积木 + 检查装配工艺</b>”的工程性论证。它的可靠性完全建立在前面几条定理之上，所以如果前面某条定理（如反函数连续性）没证，这条就会悬空。这也是教材为什么把顺序安排成“先局部性质、再四则、再反函数与复合”的原因。',
              example: '<code>lim(x→1) (x² + 2x - 1)/(x³ + 1) = (1 + 2 - 1)/(1 + 1) = 1</code>：直接代入。\n<code>lim(x→0) arctan(1/(1 + x²)) = arctan 1 = π/4</code>。\n<code>lim(x→π/4) (sin x + cos x) = √2/2 + √2/2 = √2</code>。\n<b>不能代入的例子：</b><code>lim(x→0) sin x/x</code>（<code>x = 0</code> 不在定义域内，属可去间断点）；<code>lim(x→0) 1/x</code>（<code>0</code> 不在定义域内）；分段函数在分段点处（不一定连续，要单独用左右极限判断）。',
              pitfalls: [
                '把“定义区间”与“定义域”混为一谈。教材用“定义区间”是指定义域中的区间部分，像 <code>[0, 1] ∪ [3, 4]</code> 这样的定义域，每个区间内部都连续，但跨区间的点没有意义。',
                '遇到 <code>0/0</code> 型的初等函数极限仍然直接代入（得到无意义的 <code>0/0</code>），忘记此时该点根本不在定义区间内。',
                '以为“连续”就意味着“可导”。初等函数处处连续，但 <code>|x| = √(x²)</code> 在 <code>x = 0</code> 处连续却不可导。',
                '忘记检查分母、根号、对数的限制，在定义域之外套用连续性。',
              ],
              tags: ['连续', '初等函数', '证明', '极限计算'],
              related: ['def-elementary-function', 'def-elementary-functions', 'thm-continuity-operations', 'thm-continuity-composite', 'thm-continuity-inverse'],
            },
            {
              id: 'thm-limit-power-exp',
              kind: 'theorem',
              name: '幂指函数的极限',
              aka: ['幂指函数', 'u(x)^v(x) 的极限'],
              statement: '设 <code>lim u(x) = a > 0</code>，<code>lim v(x) = b</code>（<code>a</code>、<code>b</code> 为有限常数），且 <code>u(x) > 0</code>，则\n<code>lim u(x)^{v(x)} = a^b = [lim u(x)]^{lim v(x)}</code>。',
              plain: '幂指函数就是“底数和指数都是变量的函数”，像 <code>x^x</code>、<code>(1 + x)^{1/x}</code>。\n这条定理说：<b>只要底数的极限是正数、指数的极限是有限数，就可以分别求极限再合起来。</b>\n比如 <code>lim(x→0) x^x</code>：这里底数极限是 0，不是正数，所以不能直接用这条定理，得先写成 <code>e^{x ln x}</code> 再算（结果是 <code>e⁰ = 1</code>）。\n再看 <code>lim(x→1) x^{1/(x-1)}</code>：底数极限是 1 > 0，指数极限是 <code>∞</code>，不是有限数，也不能用——这是 <code>1^∞</code> 型，答案是 <code>e</code>。\n所以这条定理的适用范围是那些“不刺激”的情形：底数收敛到正数、指数收敛到有限数。它就是初等函数（幂指形式）连续性的具体体现。',
              why: '证明的关键是恒等变形：<b><code>u^v = e^{v ln u}</code></b>。\n这一步把幂指函数变成了指数函数与乘积的复合，而这两者都已经研究透了：\n<code>lim v(x) ln u(x) = b · ln a</code>（用乘积法则，因为 <code>ln</code> 连续，<code>ln u(x) → ln a</code>）；\n然后再用 <code>e^t</code> 的连续性，得 <code>e^{b ln a} = a^b</code>。\n<b>为什么要 <code>a > 0</code>？</b>因为 <code>ln u</code> 要求 <code>u > 0</code>。如果 <code>a < 0</code>，那 <code>u(x)^{v(x)}</code> 在实数范围内可能压根没定义（比如负数开平方），或者在负数附近反复变号。所以这个条件不是为了限制证明，而是为了保证表达式有意义。\n<b>为什么 <code>b</code> 要有限？</b>因为 <code>e^t</code> 的连续性只能处理有限的 <code>t</code>；如果 <code>v ln u → ∞</code>，那就属于未定式，需要另行处理（通常还是靠写成 <code>e^{...}</code> 再仔细算指数部分的极限）。',
              proof: '<ol><li>设 <code>lim u(x) = a > 0</code>，<code>lim v(x) = b</code>，且在某去心邻域内 <code>u(x) > 0</code>（由保号性，因为 <code>a > 0</code>，这样的邻域存在）。</li><li>作恒等变形：<code>u(x)^{v(x)} = e^{v(x) ln u(x)}</code>（因为 <code>u(x) > 0</code>，两边取对数后指数化是恒等式）。</li><li>由 <code>ln</code> 在 <code>a > 0</code> 处连续及复合函数连续性，<code>lim ln u(x) = ln a</code>。</li><li>由极限的乘积法则，<code>lim [v(x) ln u(x)] = b · ln a</code>（有限数）。</li><li>由 <code>e^t</code> 处处连续及复合函数连续性，<code>lim e^{v(x) ln u(x)} = e^{b ln a} = a^b</code>（<code>a > 0</code> 保证了 <code>a^b</code> 有定义）。</li><li>因此 <code>lim u(x)^{v(x)} = a^b</code>。定理证毕。</li></ol>\n<b>过渡说明</b>：第 2 步的变形是全部技巧所在，它把“幂指”这个不好处理的结构翻译成“指数 + 对数 + 乘积”这种已经掌握的结构。这个变形在整个微积分里反复出现（如求导、求极限、解微分方程），值得牢牢记住。第 3、4、5 步都是把已知的连续性/运算法则依次用上，注意每一步都要确认前一步的结果是有限的，否则法则不适用。',
              example: '<code>lim(x→0) (1 + x²)^{1/x} </code>：底数极限为 1 > 0，指数极限为 <code>∞</code>，不满足定理条件。改写为 <code>e^{ln(1 + x²)/x}</code>，而 <code>ln(1 + x²)/x ≈ x²/x = x → 0</code>，故极限为 <code>e⁰ = 1</code>。\n<code>lim(x→2) x^{x²} = 2⁴ = 16</code>：底数极限 2 > 0，指数极限 4，直接用定理。\n<code>lim(x→0⁺) x^x = e^{lim x ln x} = e⁰ = 1</code>（<code>x ln x → 0</code>，这需要另外计算，属 <code>0 · ∞</code> 型）。\n<code>lim(x→+∞) (1 + 1/x)^{x²}</code>：底数极限 1，指数极限 <code>∞</code>，不能用定理；改写为 <code>e^{x² ln(1 + 1/x)}</code>，而 <code>x² ln(1 + 1/x) ≈ x² · (1/x) = x → +∞</code>，故极限为 <code>+∞</code>。',
              pitfalls: [
                '在底数极限小于等于 0 时套用定理，或者忽略 <code>u(x) > 0</code> 的要求。',
                '把 <code>1^∞</code>、<code>0⁰</code>、<code>∞⁰</code> 这些未定式当成满足定理条件（它们的指数或底数极限不是有限正数组合），直接代出 1 或 0。',
                '写成 <code>lim u^v = (lim u)^{lim v}</code> 却不检查两个极限是否存在、是否为有限数。',
                '计算出 <code>e^{指数极限}</code> 时把指数极限的符号或数值算错（尤其是 <code>e^{-∞} = 0</code>、<code>e^{+∞} = +∞</code>）。',
              ],
              tags: ['极限', '幂指函数', '计算', '证明'],
              related: ['thm-important-limit-two', 'note-indeterminate-forms', 'thm-elementary-continuity', 'thm-continuity-composite'],
            },
          ],
        },
        {
          id: 'ch1-10',
          no: '1.10',
          title: '闭区间上连续函数的性质',
          summary: '本节的结果全部依赖“闭区间 + 连续”这两个条件：有界性、最值定理、零点定理、介值定理，以及更强的一致连续性定理（Cantor 定理）。',
          items: [
            {
              id: 'thm-extreme-value',
              kind: 'theorem',
              name: '有界性与最大值最小值定理',
              aka: ['最值定理', '最大值最小值定理', '闭区间上连续函数的有界性'],
              statement: '若函数 <code>f</code> 在闭区间 <code>[a, b]</code> 上连续，则\n1.<b>有界性：</b><code>f</code> 在 <code>[a, b]</code> 上有界，即存在 <code>M > 0</code> 使对一切 <code>x ∈ [a, b]</code> 有 <code>|f(x)| ≤ M</code>；\n2.<b>最值定理：</b><code>f</code> 在 <code>[a, b]</code> 上一定能取到最大值和最小值，即存在 <code>ξ₁, ξ₂ ∈ [a, b]</code>，使得对一切 <code>x ∈ [a, b]</code> 有\n<code>f(ξ₁) ≤ f(x) ≤ f(ξ₂)</code>。\n（其中 <code>f(ξ₂)</code> 是最大值，<code>f(ξ₁)</code> 是最小值。）',
              plain: '这条定理说：<b>在一个有限长的闭区间上画一条不断裂的曲线，这条曲线一定既跑不远（有界），又一定有最高点和最低点。</b>\n两个条件都不能少：\n- <b>必须是闭区间</b>：<code>f(x) = 1/x</code> 在开区间 <code>(0, 1)</code> 上连续，但无界（<code>x</code> 靠近 0 时冲到无穷），也没有最大值。<code>f(x) = x</code> 在 <code>(0, 1)</code> 上连续、有界，但取不到最大值（无限接近 1 却永远不等于 1）。\n- <b>必须连续</b>：<code>f(x) = 1/x（x ≠ 0），f(0) = 0</code> 在 <code>[-1, 1]</code> 上有定义，但 <code>x = 0</code> 处不连续，结果在 <code>[-1, 1]</code> 上无界。\n所以“<b>闭区间 + 连续 ⟹ 有界且有最值</b>”是一句需要两个前提都齐的话，缺一个就崩。',
              why: '这条定理的证明不能只靠 ε-δ 的局部性质，因为局部有界只保证“每一点附近有界”，而 <code>[a, b]</code> 上有无穷多个点，“无穷多个局部界”未必能合成一个整体的界。这就要用到实数系的一条深刻性质——<b>致密性定理（有界数列必有收敛子列）</b>，或者等价的<b>有限覆盖定理</b>。\n证明思路（<b>反证 + 致密性</b>）：\n先证有界性。反设 <code>f</code> 在 <code>[a, b]</code> 上无界，则对每个正整数 <code>n</code>，都存在 <code>xₙ ∈ [a, b]</code> 使 <code>|f(xₙ)| > n</code>。数列 <code>{xₙ}</code> 有界（都在 <code>[a, b]</code> 内），由致密性定理，它有一个收敛子列 <code>x_{n_k} → ξ</code>，且 <code>ξ ∈ [a, b]</code>。因为 <code>f</code> 在 <code>ξ</code> 连续，所以 <code>f(x_{n_k}) → f(ξ)</code>，特别地 <code>{f(x_{n_k})}</code> 有界；但另一方面 <code>|f(x_{n_k})| > n_k → ∞</code>，无界，矛盾。故 <code>f</code> 有界。\n再证取到最值。设 <code>M = sup{ f(x) | x ∈ [a, b] }</code>（由有界性，<code>M</code> 是有限数）。由上确界的定义，对每个 <code>n</code>，存在 <code>xₙ ∈ [a, b]</code> 使 <code>f(xₙ) > M - 1/n</code>。仍由致密性定理取出收敛子列 <code>x_{n_k} → ξ ∈ [a, b]</code>，由连续性 <code>f(x_{n_k}) → f(ξ)</code>；另一方面夹逼给出 <code>f(x_{n_k}) → M</code>。故 <code>f(ξ) = M</code>，即最大值被取到。最小值同理（考虑 <code>-f</code>）。\n<b>关键点在哪？</b>在于“<b>取子列</b>”这一步。有了子列，我们才能把“无穷多个逼近的点”抓住其中一个，然后利用连续性把函数值也逼过去。这正是实数完备性的用法。',
              proof: '<ol><li><b>第一步：证明有界。</b>反设 <code>f</code> 在 <code>[a, b]</code> 上无界，则对每个 <code>n ∈ ℕ₊</code>，存在 <code>xₙ ∈ [a, b]</code> 使 <code>|f(xₙ)| > n</code>。</li><li>数列 <code>{xₙ} ⊆ [a, b]</code> 有界，由致密性定理（有界数列必有收敛子列），存在子列 <code>{x_{n_k}}</code> 与实数 <code>ξ</code>，使 <code>lim(k→∞) x_{n_k} = ξ</code>。又由 <code>a ≤ x_{n_k} ≤ b</code> 及极限的保序性，<code>ξ ∈ [a, b]</code>。</li><li>因为 <code>f</code> 在 <code>ξ</code> 连续，<code>lim(k→∞) f(x_{n_k}) = f(ξ)</code>。由收敛数列的有界性，<code>{f(x_{n_k})}</code> 有界。</li><li>但由第 1 步的构造，<code>|f(x_{n_k})| > n_k</code>，而 <code>n_k ≥ k → ∞</code>，故 <code>{f(x_{n_k})}</code> 无界，与第 3 步矛盾。</li><li>矛盾说明 <code>f</code> 在 <code>[a, b]</code> 上有界。于是数集 <code>S = { f(x) | x ∈ [a, b] }</code> 非空且有界，由确界原理有上确界 <code>M = sup S</code>（有限）。</li><li><b>第二步：证明最大值可达到。</b>由上确界的定义（<code>M</code> 是最小上界），对每个 <code>n ∈ ℕ₊</code>，<code>M - 1/n</code> 不是 <code>S</code> 的上界，故存在 <code>xₙ ∈ [a, b]</code> 使 <code>M - 1/n < f(xₙ) ≤ M</code>。</li><li>同第 2 步，<code>{xₙ}</code> 有界，取收敛子列 <code>x_{n_k} → ξ ∈ [a, b]</code>。由 <code>f</code> 在 <code>ξ</code> 连续，<code>f(x_{n_k}) → f(ξ)</code>。</li><li>另一方面，由 <code>M - 1/n_k < f(x_{n_k}) ≤ M</code> 及 <code>1/n_k → 0</code>，用夹逼准则得 <code>f(x_{n_k}) → M</code>。由极限的唯一性，<code>f(ξ) = M</code>，即 <code>f</code> 在 <code>ξ</code> 取到最大值。</li><li><b>第三步：最小值。</b>对 <code>g = -f</code> 应用第二步，得存在 <code>η ∈ [a, b]</code> 使 <code>-f(η) = sup{ -f(x) } = -inf{ f(x) }</code>，即 <code>f(η) = inf{ f(x) }</code>，是最小值。</li><li>定理证毕。</li></ol>\n<b>过渡说明</b>：第 1 至 4 步与第 6 至 8 步用的是同一个套路——<b>构造一个数列 + 取收敛子列 + 用连续性收网</b>。这套“三连击”是处理闭区间上连续函数问题的万能招式，后面的介值定理、一致连续性定理也是同一路数。第 5 步引入上确界是必要的：我们事先并不知道最大值是多少，只能先取上确界，再想办法证明它被取到。',
              example: '<code>f(x) = x² - 2x</code> 在 <code>[0, 3]</code> 上连续，最小值 <code>f(1) = -1</code>，最大值 <code>f(3) = 3</code>（比较 <code>f(0) = 0</code>、<code>f(3) = 3</code>）。\n<b>开区间失灵的例：</b><code>f(x) = 1/x</code> 在 <code>(0, 1)</code> 上连续但无上界，也无最大值。\n<b>不连续失灵的例：</b><code>f(x) = 1/x（x ≠ 0），f(0) = 0</code> 在 <code>[-1, 1]</code> 上无界。\n<b>有界但取不到最值的例：</b><code>f(x) = x</code> 在 <code>(0, 1)</code> 上，<code>sup f = 1</code> 但无最大值。',
              pitfalls: [
                '把定理用在开区间或半开区间上。',
                '把定理用在有间断点的函数上。',
                '把“有界”与“能取到最值”混为一谈：<code>f(x) = x</code> 在 <code>(0,1)</code> 上有界但无最大值，说明有界不蕴含取到最值，闭区间这个条件不能省。',
                '认为最大值一定在端点处取到。反例：<code>f(x) = x² - 2x</code> 在 <code>[0, 3]</code> 上的最小值在内部点 <code>x = 1</code> 取到。',
              ],
              tags: ['连续', '闭区间', '最值定理', '证明'],
              related: ['def-continuity-point', 'thm-zero-point', 'thm-intermediate-value', 'thm-cantor', 'thm-conv-bounded'],
            },
            {
              id: 'thm-zero-point',
              kind: 'theorem',
              name: '零点定理（根的存在定理）',
              aka: ['零点定理', '根的存在性定理', 'Bolzano 零点定理'],
              statement: '设函数 <code>f</code> 在闭区间 <code>[a, b]</code> 上连续，且 <code>f(a)</code> 与 <code>f(b)</code> 异号（即 <code>f(a) · f(b) < 0</code>），则至少存在一点 <code>ξ ∈ (a, b)</code>，使得\n<code>f(ξ) = 0</code>。',
              plain: '零点定理说的是：<b>如果你从低于海平面的地方出发，走到高于海平面的地方，而且一路都是连续走的（没有坐飞机跳过去），那你一定会在某个时刻正好踩在海平面上。</b>\n更生活化一点：早上 8 点你在 1 楼，8 点 10 分你到了 5 楼，电梯/楼梯是连续上升的，那么这 10 分钟里必然有一刻你恰好在 3 楼。\n这就是“连续”这两个字的全部含义：<b>不可能跳过中间的值。</b>\n它的实际用途是<b>判断方程有没有根</b>。比如要证 <code>x³ - x - 1 = 0</code> 在 <code>(1, 2)</code> 内有根：算 <code>f(1) = -1 < 0</code>，<code>f(2) = 5 > 0</code>，函数连续，异号，故必有根。这个证明只用了两次代入计算，非常省力。',
              why: '证明思路是“<b>二分法 + 区间套</b>”：不断把区间对半切，每次都保留“函数值异号”的那一半。\n具体地：令 <code>a₁ = a</code>，<code>b₁ = b</code>。取中点 <code>c₁ = (a₁ + b₁)/2</code>。若 <code>f(c₁) = 0</code>，已经找到；若 <code>f(c₁)</code> 与 <code>f(a₁)</code> 异号，就令 <code>[a₂, b₂] = [a₁, c₁]</code>；否则令 <code>[a₂, b₂] = [c₁, b₁]</code>。这样得到的每个区间端点处函数值都异号，区间长度每次减半，趋于 0。\n由区间套定理，存在唯一的 <code>ξ</code> 属于所有区间，且 <code>aₙ → ξ</code>、<code>bₙ → ξ</code>。\n最后用连续性收网：因为 <code>f(aₙ) ≤ 0 ≤ f(bₙ)</code>（或反过来，符号按构造固定），而 <code>f</code> 在 <code>ξ</code> 连续，故 <code>f(aₙ) → f(ξ)</code> 且 <code>f(bₙ) → f(ξ)</code>。对 <code>f(aₙ) ≤ 0</code> 取极限得 <code>f(ξ) ≤ 0</code>，对 <code>f(bₙ) ≥ 0</code> 取极限得 <code>f(ξ) ≥ 0</code>。两边一夹，只能 <code>f(ξ) = 0</code>。\n<b>关键在哪？</b>在于构造区间套时始终保住“异号”这个性质——这保证了极限点处函数值既不能正也不能负，只能等于 0。这就像两个人从两侧往中间走，最后必然在中间相遇。\n这个证明还是“二分法求根”的理论依据：它不但说明根存在，还给出了一个可以实际计算的方法。',
              proof: '<ol><li>不妨设 <code>f(a) < 0 < f(b)</code>（若相反，考虑 <code>-f</code>）。</li><li><b>构造区间套。</b>令 <code>a₁ = a</code>，<code>b₁ = b</code>，则 <code>f(a₁) < 0 < f(b₁)</code>。归纳地，设已得 <code>[aₙ, bₙ]</code> 满足 <code>f(aₙ) < 0 < f(bₙ)</code> 且 <code>bₙ - aₙ = (b - a)/2^{n-1}</code>。取中点 <code>c = (aₙ + bₙ)/2</code>。</li><li>若 <code>f(c) = 0</code>，则 <code>ξ = c</code> 即为所求，停止。若 <code>f(c) < 0</code>，令 <code>a_{n+1} = c</code>，<code>b_{n+1} = bₙ</code>；若 <code>f(c) > 0</code>，令 <code>a_{n+1} = aₙ</code>，<code>b_{n+1} = c</code>。两种情形都保持 <code>f(a_{n+1}) < 0 < f(b_{n+1})</code>，且区间长度减半。</li><li>若过程永不停止，则得到一个闭区间套 <code>[a₁, b₁] ⊇ [a₂, b₂] ⊇ …</code>，且 <code>bₙ - aₙ = (b - a)/2^{n-1} → 0</code>。</li><li>由区间套定理，存在唯一 <code>ξ</code> 属于所有 <code>[aₙ, bₙ]</code>，且 <code>lim aₙ = lim bₙ = ξ</code>。因 <code>a ≤ aₙ < bₙ ≤ b</code>，由保序性 <code>ξ ∈ [a, b]</code>；又因每步长度都 <code>> 0</code>，<code>ξ</code> 不可能等于 <code>a</code> 或 <code>b</code> 之外的点，实际上 <code>ξ ∈ (a, b)</code>（除非第一步就取到端点情形，那时 <code>f(a)</code> 或 <code>f(b)</code> 已为 0，但题设异号排除了这种可能）。</li><li>因为 <code>f(aₙ) < 0</code> 对一切 <code>n</code> 成立，且 <code>f</code> 在 <code>ξ</code> 连续，故 <code>f(ξ) = lim f(aₙ) ≤ 0</code>（这里用了极限的保序性：每一项都 <code>< 0</code> 只能推出极限 <code>≤ 0</code>）。</li><li>同理，由 <code>f(bₙ) > 0</code> 得 <code>f(ξ) = lim f(bₙ) ≥ 0</code>。</li><li>两式合起来 <code>0 ≤ f(ξ) ≤ 0</code>，故 <code>f(ξ) = 0</code>。定理得证。</li></ol>\n<b>过渡说明</b>：第 2、3 步是“二分法的算法描述”，它保证每一步都维持“左端为负、右端为正”的不变量。第 5 步用区间套定理取出极限点 <code>ξ</code>。第 6、7 步是收网的关键：注意这里只能得到非严格不等号（<code>≤ 0</code> 与 <code>≥ 0</code>），这正是为什么结果是 <code>= 0</code> 而不是别的东西——这就是“夹”的力量。',
              example: '<b>证方程有根：</b>证明 <code>x³ - 4x² + 1 = 0</code> 在 <code>(0, 1)</code> 内有根。令 <code>f(x) = x³ - 4x² + 1</code>，它是多项式（连续）。<code>f(0) = 1 > 0</code>，<code>f(1) = 1 - 4 + 1 = -2 < 0</code>，异号，故存在 <code>ξ ∈ (0, 1)</code> 使 <code>f(ξ) = 0</code>。\n<b>二分法求近似值：</b>接上例，取中点 <code>0.5</code>，<code>f(0.5) = 0.125 - 1 + 1 = 0.125 > 0</code>，故根在 <code>(0.5, 1)</code>；再取 <code>0.75</code>，<code>f(0.75) ≈ 0.42 - 2.25 + 1 = -0.83 < 0</code>，故根在 <code>(0.5, 0.75)</code>；继续下去可任意逼近。\n<b>注意“异号”不能省：</b><code>f(x) = x² + 1</code> 在 <code>[-1, 1]</code> 上连续且 <code>f(-1) = f(1) = 2 > 0</code>（同号），确实没有实根，与定理不矛盾。',
              pitfalls: [
                '只验证 <code>f(a) · f(b) < 0</code> 就断定“恰有一个根”。定理只保证<b>至少一个</b>根，个数要靠单调性等信息另外讨论。',
                '把结论写成 <code>ξ ∈ [a, b]</code>。其实由异号可知 <code>f(a) ≠ 0</code>、<code>f(b) ≠ 0</code>，所以 <code>ξ</code> 必在开区间 <code>(a, b)</code> 内。',
                '忽略连续性的要求。不连续函数可以“跳”过零点，例如 <code>f(x) = -1（x < 0），1（x ≥ 0）</code>，<code>f(-1) < 0 < f(1)</code> 但无零点。',
                '把零点定理的逆命题当成真的：“若有根则必异号”是错的，<code>f(x) = x²</code> 在 <code>[-1, 1]</code> 上有根（<code>x = 0</code>）但端点同号。',
              ],
              tags: ['连续', '闭区间', '零点定理', '证明'],
              related: ['thm-intermediate-value', 'thm-extreme-value', 'thm-continuity-local', 'thm-nested-intervals'],
            },
            {
              id: 'thm-intermediate-value',
              kind: 'theorem',
              name: '介值定理',
              aka: ['介值定理', '中间值定理'],
              statement: '设函数 <code>f</code> 在闭区间 <code>[a, b]</code> 上连续，且 <code>f(a) ≠ f(b)</code>。则对 <code>f(a)</code> 与 <code>f(b)</code> 之间的任意一个数 <code>c</code>（即 <code>min{ f(a), f(b) } < c < max{ f(a), f(b) }</code>），至少存在一点 <code>ξ ∈ (a, b)</code>，使得\n<code>f(ξ) = c</code>。\n推论：闭区间上的连续函数必能取到其最大值 <code>M</code> 与最小值 <code>m</code> 之间的<b>一切</b>值，即值域是闭区间 <code>[m, M]</code>。',
              plain: '介值定理就是零点定理的“升级版”：零点定理说的是“一定穿过 0 这条水平线”，介值定理说的是“<b>一定穿过任意一条介于两端高度之间的水平线</b>”。\n想象一座山的轮廓是一条连续曲线，山脚高度 10 米，山顶高度 100 米。那么无论你说 37 米、58.5 米还是 99 米，山上必然有某个高度恰好等于它。\n证明方法就是“把水平线拉下来变成 0”：<b>令 <code>g(x) = f(x) - c</code></b>，于是“<code>f(x) = c</code>”就变成了“<code>g(x) = 0</code>”，直接套用零点定理。\n零点定理就是取 <code>c = 0</code> 的特殊情形，所以这两个定理本质上是同一个东西。',
              why: '证明的思路只有一步：<b>平移函数，把目标值降到 0</b>。\n为什么这么做？因为零点定理是我们已经证过的工具，而 <code>f(x) = c</code> 这种“等于某个非零常数”的问题，只要减去 <code>c</code> 就变成了“等于 0”。这个“<b>化归到已知定理</b>”的思路是数学证明中最常用、也最值得学习的一招。\n具体验证一下条件：若 <code>f(a) < c < f(b)</code>，则 <code>g(a) = f(a) - c < 0</code>，<code>g(b) = f(b) - c > 0</code>，异号；并且 <code>g</code> 连续（连续函数减常数仍连续）。于是零点定理适用，存在 <code>ξ ∈ (a, b)</code> 使 <code>g(ξ) = 0</code>，即 <code>f(ξ) = c</code>。\n至于推论（值域是 <code>[m, M]</code>）：由最值定理，<code>m</code>、<code>M</code> 都能取到。任取 <code>c ∈ [m, M]</code>，在取到 <code>m</code> 的点与取到 <code>M</code> 的点构成的子区间上用介值定理，就得到 <code>c</code> 也在值域中。所以值域包含了 <code>[m, M]</code> 中的每一个数，而值域本身又不会超出 <code>[m, M]</code>，两端合起来就是 <code>[m, M]</code>。',
              proof: '<ol><li>不妨设 <code>f(a) < f(b)</code>，并设 <code>f(a) < c < f(b)</code>。</li><li>构造函数 <code>g(x) = f(x) - c</code>（<code>x ∈ [a, b]</code>）。因为 <code>f</code> 在 <code>[a, b]</code> 上连续，常数函数 <code>c</code> 也连续，故由连续函数的减法法则，<code>g</code> 在 <code>[a, b]</code> 上连续。</li><li>计算端点值：<code>g(a) = f(a) - c < 0</code>，<code>g(b) = f(b) - c > 0</code>，即 <code>g(a)</code> 与 <code>g(b)</code> 异号。</li><li>由零点定理，存在 <code>ξ ∈ (a, b)</code> 使 <code>g(ξ) = 0</code>，即 <code>f(ξ) - c = 0</code>，也就是 <code>f(ξ) = c</code>。定理的第一部分得证（<code>f(a) > f(b)</code> 的情形把上述不等号方向翻转即可，或对 <code>-f</code> 使用）。</li><li><b>推论的证明。</b>由最值定理，<code>f</code> 在 <code>[a, b]</code> 上取到最小值 <code>m = f(x₁)</code> 与最大值 <code>M = f(x₂)</code>（<code>x₁, x₂ ∈ [a, b]</code>）。显然值域 <code>f([a,b]) ⊆ [m, M]</code>。</li><li>反过来，任取 <code>c ∈ [m, M]</code>。若 <code>c = m</code> 或 <code>c = M</code>，已由最值定理保证取到。若 <code>m < c < M</code>，不妨设 <code>x₁ < x₂</code>（若 <code>x₂ < x₁</code> 则交换）。则 <code>f</code> 在 <code>[x₁, x₂]</code> 上连续，且 <code>f(x₁) = m < c < M = f(x₂)</code>，由本定理第一部分，存在 <code>ξ ∈ [x₁, x₂] ⊆ [a, b]</code> 使 <code>f(ξ) = c</code>。</li><li>故 <code>[m, M] ⊆ f([a, b])</code>。结合第 5 步得 <code>f([a, b]) = [m, M]</code>，即值域恰为闭区间 <code>[m, M]</code>。</li></ol>\n<b>过渡说明</b>：第 2 至 4 步是“标准的一步化归”，非常干净。第 5 至 7 步是推论的证明，注意其中用到了一个细节——把取到 <code>m</code> 和取到 <code>M</code> 的两个点按大小排好，才能构成一个合法的闭区间去用介值定理。',
              example: '<code>f(x) = x³</code> 在 <code>[1, 2]</code> 上连续且单调增加，<code>f(1) = 1</code>，<code>f(2) = 8</code>，所以 <code>f</code> 在 <code>[1, 2]</code> 上取遍 <code>[1, 8]</code> 中每个值。特别地存在 <code>ξ</code> 使 <code>ξ³ = 5</code>，即 <code>ξ = ∛5</code>，这与开立方的存在性一致。\n<b>证方程有根：</b>证明 <code>x = cos x</code> 有解。令 <code>f(x) = x - cos x</code>，连续。<code>f(0) = -1 < 0</code>，<code>f(1) = 1 - cos 1 ≈ 0.46 > 0</code>，由零点定理（或介值定理取 <code>c = 0</code>）存在 <code>ξ ∈ (0, 1)</code> 使 <code>f(ξ) = 0</code>。\n<b>证不动点：</b>若 <code>f : [0, 1] → [0, 1]</code> 连续，则存在 <code>ξ</code> 使 <code>f(ξ) = ξ</code>。证明：令 <code>g(x) = f(x) - x</code>，则 <code>g(0) = f(0) - 0 ≥ 0</code>，<code>g(1) = f(1) - 1 ≤ 0</code>，用零点定理（若端点恰为 0 则已得解）即得。',
              pitfalls: [
                '把“<code>c</code> 在 <code>f(a)</code> 与 <code>f(b)</code> 之间”理解成“<code>c</code> 在区间 <code>[a, b]</code> 之间”。<code>c</code> 是<b>函数值</b>，不是自变量。',
                '忘记 <code>f(a) ≠ f(b)</code> 的前提。若 <code>f(a) = f(b)</code>，则不存在“之间的数”，定理结论为空。',
                '误以为定理保证了唯一性。介值定理只保证存在性，<code>sin x = 1/2</code> 在 <code>[0, 2π]</code> 上有两个解，并不违反定理。',
                '把结论用在非闭区间或非连续函数上。',
              ],
              tags: ['连续', '闭区间', '介值定理', '证明'],
              related: ['thm-zero-point', 'thm-extreme-value', 'thm-continuity-local', 'thm-cantor'],
            },
            {
              id: 'def-uniform-continuity',
              kind: 'definition',
              name: '一致连续的定义',
              aka: ['一致连续', '一致连续性'],
              statement: '设函数 <code>f</code> 在区间 <code>I</code> 上有定义。若对任意给定的正数 <code>ε</code>，总存在正数 <code>δ</code>，使得对 <code>I</code> 上的<b>任意</b>两点 <code>x₁</code>、<code>x₂</code>，只要 <code>|x₁ - x₂| < δ</code>，就有\n<code>|f(x₁) - f(x₂)| < ε</code>，\n则称函数 <code>f</code> 在区间 <code>I</code> 上<b>一致连续</b>。\n（注意：这里的 <code>δ</code> 只依赖于 <code>ε</code>，不依赖于 <code>x₁</code>、<code>x₂</code> 的具体位置。）',
              plain: '一致连续是比连续更强的要求，差别就在<b>那个 <code>δ</code> 能不能“统一”</b>。\n普通连续：在<b>每一个点 <code>x₀</code></b> 处，你给一个 <code>ε</code>，我都能找到一个 <code>δ</code>。<b>但这个 <code>δ</code> 可以因点而异</b>——在陡峭的地方我给小一点，在平坦的地方我给大一点。\n一致连续：你给一个 <code>ε</code>，我要找一个<b>全区间通用</b>的 <code>δ</code>，不管在区间的哪个角落，只要两点距离小于 <code>δ</code>，函数值就差得小于 <code>ε</code>。\n打个比方（这可能是最好懂的比喻）：\n普通连续像“<b>每块地都能按需要铺路</b>”，各铺各的；\n一致连续像“<b>整条路用同一种规格的路砖</b>”，不管在哪儿，砖的大小都一样。\n反例最能说明差别：<code>f(x) = 1/x</code> 在 <code>(0, 1)</code> 上<b>连续但不一致连续</b>。因为在 <code>x</code> 靠近 0 的地方，函数变化极其剧烈，同样大小的 <code>δ</code> 在那里根本不够用。具体地，取 <code>x₁ = 1/n</code>、<code>x₂ = 1/(n+1)</code>，两者距离 <code>1/(n(n+1)) → 0</code> 可以任意小，但 <code>|f(x₁) - f(x₂)| = |n - (n+1)| = 1</code> 始终是 1。所以无论 <code>δ</code> 多小，总有一对靠得很近的点函数值差 1，取 <code>ε = 1</code> 就不满足一致连续。',
              why: '为什么要有“一致连续”这个概念？因为在很多问题里，<b>我们需要的恰恰是那个“统一的 δ”</b>。\n比如证明“连续函数可积”时，需要把区间切成很多小段，并保证每一小段上函数值的变化都很小。如果 <code>δ</code> 逐点不同，切成有限多段就未必能照顾到所有点（可能有一处的 <code>δ</code> 极小，切得再细也顾不上）。只有存在一个统一的 <code>δ</code>，才能“一刀切”地完成估计。\n<b>一致连续与连续的逻辑关系：</b>一致连续 ⟹ 连续（这是显然的，把 <code>x₂</code> 取成 <code>x₀</code> 就得到普通连续）。但连续 ⇏ 一致连续，上面 <code>1/x</code> 的例子就是证据。\n<b>那什么时候连续能升级为一致连续？</b>答案就是下一节的 Cantor 定理：<b>在闭区间上，连续就是一致连续。</b>闭区间这个条件又一次成了关键。\n<b>证明不一致连续的标准套路：</b>找出两列点 <code>xₙ</code>、<code>yₙ</code> 使 <code>|xₙ - yₙ| → 0</code> 但 <code>|f(xₙ) - f(yₙ)| ≥ ε₀ > 0</code>。这个“<b>取两个序列</b>”的方法是一致连续性反证题的通用武器。',
              example: '<b>一致连续：</b><code>f(x) = x²</code> 在 <code>[0, 1]</code> 上一致连续。因为 <code>|x₁² - x₂²| = |x₁ + x₂||x₁ - x₂| ≤ 2|x₁ - x₂|</code>，取 <code>δ = ε/2</code> 即可（这个 <code>δ</code> 与位置无关）。\n<b>连续但不一致连续：</b><code>f(x) = x²</code> 在 <code>ℝ</code> 上不一致连续（虽然处处连续）。取 <code>xₙ = n</code>，<code>yₙ = n + 1/n</code>，则 <code>|xₙ - yₙ| = 1/n → 0</code>，但 <code>|f(xₙ) - f(yₙ)| = (n + 1/n)² - n² = 2 + 1/n² > 2</code>，取 <code>ε = 2</code> 即可看出不一致连续。\n<b>连续但不一致连续：</b><code>f(x) = sin(1/x)</code> 在 <code>(0, 1)</code> 上（有界但震荡太快，不一致连续）。\n<b>一致连续：</b><code>f(x) = sin x</code> 在 <code>ℝ</code> 上一致连续（用 <code>|sin x₁ - sin x₂| ≤ |x₁ - x₂|</code>，取 <code>δ = ε</code> 即可，与位置无关）。',
              pitfalls: [
                '把一致连续与连续当成一回事。区别在于 <code>δ</code> 是否可以依赖位置。',
                '在定义中把“对任意两点”写成“存在两点”，量词顺序错了定义就完全不同。',
                '以为“有界 + 连续”就能推出一致连续。<code>sin(1/x)</code> 在 <code>(0, 1)</code> 上既有界又连续，但不一致连续。',
                '以为只有闭区间上才可能一致连续。<code>sin x</code> 在 <code>ℝ</code> 上也一致连续，所以开区间/无界区间上也有可能一致连续，只是需要单独验证。',
              ],
              tags: ['连续', '一致连续', '基础概念'],
              related: ['def-continuity-point', 'thm-cantor', 'thm-extreme-value', 'thm-zero-point'],
            },
            {
              id: 'thm-cantor',
              kind: 'theorem',
              name: '一致连续性定理（Cantor 定理）',
              aka: ['Cantor 定理', '一致连续性定理'],
              statement: '若函数 <code>f</code> 在闭区间 <code>[a, b]</code> 上连续，则 <code>f</code> 在 <code>[a, b]</code> 上<b>一致连续</b>。即对任意 <code>ε > 0</code>，存在只依赖于 <code>ε</code> 的 <code>δ > 0</code>，使得对 <code>[a, b]</code> 上任意两点 <code>x₁</code>、<code>x₂</code>，只要 <code>|x₁ - x₂| < δ</code>，就有 <code>|f(x₁) - f(x₂)| < ε</code>。',
              plain: '这条件定理说：<b>在闭区间上，连续自动升级为一致连续。</b>\n通俗地说：只要你在一段<b>有限的、两头都包含的</b>区间上画一条不断裂的曲线，那么整条曲线上的“陡峭程度”总有一个上限——你可以用同一种规格的路砖把整条路铺满。\n为什么闭区间这么关键？因为区间有两个“边界”。在边界附近，函数可能很陡（比如 <code>1/x</code> 在 <code>x = 0</code> 附近），但边界本身被包含进来了，而且区间长度有限，所以“陡”的地方有限，可以被统一照顾。\n如果把闭区间换成开区间 <code>(0, 1)</code>，<code>1/x</code> 这个反例就说明结论会失效——因为 0 这个“最陡的地方”被排除在外，可它偏偏影响了整段。\n[[tip:记住一句话：“闭区间上连续 = 一致连续”，这是把局部性质升级为整体性质的最经典例子。]]',
              why: '证明用<b>反证法 + 致密性定理</b>，思路和“最值定理”如出一辙。\n反设 <code>f</code> 在 <code>[a, b]</code> 上连续但不一致连续。按一致连续的否定，存在某个 <code>ε₀ > 0</code>，使得对每个 <code>δ = 1/n</code>，都能找到两点 <code>xₙ</code>、<code>yₙ ∈ [a, b]</code> 满足\n<code>|xₙ - yₙ| < 1/n</code> 但 <code>|f(xₙ) - f(yₙ)| ≥ ε₀</code>。\n现在关键的一步来了：<code>{xₙ}</code> 有界（在 <code>[a, b]</code> 内），由致密性定理取收敛子列 <code>x_{n_k} → ξ ∈ [a, b]</code>。又因为 <code>|y_{n_k} - ξ| ≤ |y_{n_k} - x_{n_k}| + |x_{n_k} - ξ| < 1/n_k + |x_{n_k} - ξ| → 0</code>，所以 <b><code>y_{n_k}</code> 也收敛到同一个 <code>ξ</code></b>——两个序列被挤到了同一个点上！\n最后用连续性收网：<code>f(x_{n_k}) → f(ξ)</code>，<code>f(y_{n_k}) → f(ξ)</code>，于是\n<code>|f(x_{n_k}) - f(y_{n_k})| → |f(ξ) - f(ξ)| = 0</code>，\n这与 <code>|f(x_{n_k}) - f(y_{n_k})| ≥ ε₀ > 0</code> 矛盾。\n<b>看明白这个证明的妙处了吗？</b>不一致连续意味着“存在靠得无限近、但函数值差得很远的两点”。取子列后，这两点被迫聚到同一个 <code>ξ</code>，而连续性要求函数在 <code>ξ</code> 附近的行为一致，于是矛盾。这就是为什么“闭”这么重要：<b>子列的极限 <code>ξ</code> 必须留在区间内（还得是定义域内的点），连续性才能在那里发挥作用。</b>开区间 <code>(0, 1)</code> 时，<code>1/x</code> 的两列点会聚到 <code>ξ = 0</code>，而 0 不在定义域里，连续性管不着，矛盾不成立。',
              proof: '<ol><li>设 <code>f</code> 在 <code>[a, b]</code> 上连续。反设 <code>f</code> 在 <code>[a, b]</code> 上不一致连续。</li><li>按一致连续定义的否定：存在 <code>ε₀ > 0</code>，使得对任意 <code>δ > 0</code>，都存在 <code>x, y ∈ [a, b]</code> 满足 <code>|x - y| < δ</code> 但 <code>|f(x) - f(y)| ≥ ε₀</code>。特别地，对每个正整数 <code>n</code>，取 <code>δ = 1/n</code>，得到 <code>xₙ, yₙ ∈ [a, b]</code> 使\n<code>|xₙ - yₙ| < 1/n</code>，<code>|f(xₙ) - f(yₙ)| ≥ ε₀</code>。</li><li>数列 <code>{xₙ} ⊆ [a, b]</code> 有界，由致密性定理，存在收敛子列 <code>{x_{n_k}}</code>，设 <code>lim(k→∞) x_{n_k} = ξ</code>。由 <code>a ≤ x_{n_k} ≤ b</code> 及保序性，<code>ξ ∈ [a, b]</code>。</li><li>估计 <code>y_{n_k}</code> 的极限：<code>|y_{n_k} - ξ| ≤ |y_{n_k} - x_{n_k}| + |x_{n_k} - ξ| < 1/n_k + |x_{n_k} - ξ|</code>。由 <code>n_k ≥ k</code> 得 <code>1/n_k ≤ 1/k → 0</code>，且 <code>|x_{n_k} - ξ| → 0</code>，故 <code>|y_{n_k} - ξ| → 0</code>，即 <code>lim(k→∞) y_{n_k} = ξ</code>。</li><li>因为 <code>f</code> 在 <code>ξ ∈ [a, b]</code> 处连续，<code>lim f(x_{n_k}) = f(ξ)</code>，<code>lim f(y_{n_k}) = f(ξ)</code>。</li><li>由极限的减法法则，<code>lim(k→∞) [f(x_{n_k}) - f(y_{n_k})] = f(ξ) - f(ξ) = 0</code>，即 <code>lim |f(x_{n_k}) - f(y_{n_k})| = 0</code>。</li><li>但由第 2 步的构造，<code>|f(x_{n_k}) - f(y_{n_k})| ≥ ε₀ > 0</code> 对一切 <code>k</code> 成立。由保号性（每一项都 <code>≥ ε₀</code>，极限必须 <code>≥ ε₀</code>），应有 <code>lim |f(x_{n_k}) - f(y_{n_k})| ≥ ε₀ > 0</code>，与第 6 步矛盾。</li><li>矛盾说明反设不成立，故 <code>f</code> 在 <code>[a, b]</code> 上一致连续。定理证毕。</li></ol>\n<b>过渡说明</b>：第 2 步把“不一致连续”翻译成两列具体点的存在性，是全证明的枢纽；这里量词的否定必须写准（<code>∃ ε₀, ∀ δ, ∃ x, y</code>）。第 3、4 步是本题最漂亮的地方：<b>先取一列的子列，然后证明另一列被“拉”到同一个极限</b>，这个技巧在处理“两个序列都靠近某点”的题目中反复出现。第 7 步用保号性把“每一项都不小于 <code>ε₀</code>”传到极限，得到矛盾。',
              example: '<b>闭区间上一致连续：</b><code>f(x) = 1/x</code> 在 <code>[1, 2]</code> 上一致连续（因为闭区间 + 连续）。可以直接验证：<code>|1/x₁ - 1/x₂| = |x₁ - x₂|/(x₁x₂) ≤ |x₁ - x₂|</code>，取 <code>δ = ε</code>。\n<b>开区间上失效：</b><code>f(x) = 1/x</code> 在 <code>(0, 1)</code> 上连续但不一致连续（取 <code>xₙ = 1/n</code>、<code>yₙ = 1/(n+1)</code>，距离趋于 0 而函数值差恒为 1）。\n<b>无界区间上也可能一致连续：</b><code>f(x) = sin x</code> 在 <code>ℝ</code> 上一致连续；<code>f(x) = arctan x</code> 在 <code>ℝ</code> 上也一致连续（因为导数有界，变化率被控制住）。',
              pitfalls: [
                '把定理的结论反过来用（一致连续 ⟹ 在某闭区间上连续），单看“一致连续”本身确实推出连续，但推不出“在某个特定闭区间上”。',
                '在开区间上套用本定理。开区间上连续未必一致连续。',
                '证明中取子列后忘记证明另一列 <code>y_{n_k}</code> 也收敛到同一个 <code>ξ</code>。这一步不能省。',
                '量词写错：把一致连续的定义写成 <code>∀ ε, ∀ x₁, x₂, ∃ δ</code>（这样 δ 依赖于点，就退化成普通连续了）。正确的顺序是 <code>∀ ε > 0, ∃ δ > 0, ∀ x₁, x₂</code>。',
              ],
              tags: ['连续', '一致连续', '闭区间', '证明'],
              related: ['def-uniform-continuity', 'def-continuity-point', 'thm-extreme-value', 'thm-zero-point', 'thm-conv-bounded'],
            },
          ],
        },
      ],
    },
  ],
};
