// 同济《高等数学》上册（第八版）· 第5章 定积分
export default {
  id: 'ch5',
  no: 5,
  title: '定积分',
  intro: '前面我们一直在问「某个瞬间变化得多快」，这一章反过来问「把一段变化累加起来，总共是多少」。为了回答这个问题，我们要先给「无限细分再求和」这件事起一个名字——定积分，然后证明一个惊人的事实：这么麻烦的极限，居然可以用求原函数来做（牛顿-莱布尼茨公式）。最后我们还会把积分区间放大到无穷、把被积函数放宽到无界，请出反常积分和 Γ 函数。',
  sections: [
    {
      id: 'ch5-1',
      no: '5.1',
      title: '定积分的概念与性质',
      summary: '用「分割、近似、求和、取极限」把曲边梯形的面积算出来，由此定义定积分，并列出它的全部基本性质。',
      items: [
        {
          id: 'def-definite-integral',
          kind: 'definition',
          name: '定积分的定义',
          aka: ['黎曼和的极限', '黎曼积分'],
          statement: '设函数 <code>f(x)</code> 在闭区间 <code>[a, b]</code> 上有界。用分点\n<code>a = x₀ &lt; x₁ &lt; x₂ &lt; … &lt; xₙ = b</code>\n把 <code>[a, b]</code> 任意分成 <code>n</code> 个小区间，第 <code>i</code> 个小区间的长度记作 <code>Δxᵢ = xᵢ - xᵢ₋₁</code>；再在每个小区间上任取一点 <code>ξᵢ ∈ [xᵢ₋₁, xᵢ]</code>，作黎曼和\n<code>S = Σᵢ₌₁ⁿ f(ξᵢ)Δxᵢ</code>。\n记 <code>λ = max{Δx₁, Δx₂, …, Δxₙ}</code>（最大区间长度）。若当 <code>λ → 0</code> 时 <code>S</code> 总趋于同一个有限数 <code>I</code>，且这个数<b>与区间的分法无关、与点 <code>ξᵢ</code> 的取法无关</b>，就称 <code>f(x)</code> 在 <code>[a, b]</code> 上<b>可积</b>，并称 <code>I</code> 为 <code>f(x)</code> 在 <code>[a, b]</code> 上的<b>定积分</b>，记作\n<code>∫ₐᵇ f(x)dx = I = lim<sub>λ→0</sub> Σᵢ₌₁ⁿ f(ξᵢ)Δxᵢ</code>。\n其中 <code>a</code> 叫积分下限，<code>b</code> 叫积分上限，<code>f(x)</code> 叫被积函数，<code>f(x)dx</code> 叫被积表达式，<code>x</code> 叫积分变量。',
          plain: '你要算一张奇形怪状纸片的面积。办法是：把它剪成很多根很细的竖长条，每一根都当成一个长方形（宽乘高）来算，再把所有长条的答案加起来。\n问题是：剪得越细，结果越准，可谁也不知道「无限细」那一刻到底是多少。于是我们说：当所有长条的宽度都趋于 0 时，这些答案会稳定地逼近某一个数，那就把这个数<b>定义</b>为纸片的面积。\n定积分就是这件事的严格写法。它的名字里虽然有个「积分」，可算出来的东西是一个数，不长不短，就是个数。',
          why: '为什么非要用极限来定义，而不直接说「面积」？因为在定积分出现之前，「曲边图形的面积」本身就是一笔糊涂账。<code>∫ₐᵇ f(x)dx</code> 的高明之处在于：它用「有限个矩形之和」这种我们完全算得清的东西当作替身，让替身去逼近一个极限，从而把「面积」这个概念<b>造出来</b>，而不是假设它已经存在。\n为什么要强调「与分法无关、与取点无关」？因为如果换一种分法就换一个答案，那这个极限只属于某一次具体操作，不能代表图形的固有属性。只有当我们能证明各种分法、各种取点都通向同一个数，这个数才配叫做「这块图形的面积」。这也是后面可积性定理要专门研究的对象。',
          example: '<b>用定义算一次：</b>求 <code>∫₀¹ x dx</code>。\n把 <code>[0, 1]</code> 等分成 <code>n</code> 份，则 <code>Δxᵢ = 1/n</code>，取 <code>ξᵢ = i/n</code>（右端点），黎曼和为\n<code>Sₙ = Σᵢ₌₁ⁿ (i/n)·(1/n) = (1/n²)·Σᵢ₌₁ⁿ i = (1/n²)·n(n+1)/2 = (n+1)/(2n)</code>。\n这是一块三角形，真面积应为 <code>1/2</code>；而 <code>lim<sub>n→∞</sub> (n+1)/(2n) = 1/2</code>，正好吻合。\n换个取法（取左端点 <code>ξᵢ = (i-1)/n</code>）会得到 <code>(n-1)/(2n) → 1/2</code>，答案一样，这正是定义里那句「与取法无关」。\n[[tip:如果函数简单、图形规则，定积分就等于熟知的面积。这里 <code>∫₀¹ x dx</code> 就是底 1、高 1 的三角形面积 <code>1/2</code>。]]',
          pitfalls: [
            '把 <code>∫ₐᵇ f(x)dx</code> 说成「面积」时忘了前提：只有当 <code>f(x) ≥ 0</code> 时它才是面积；<code>f(x) &lt; 0</code> 时它会算出负数。',
            '把定积分当成函数。<code>∫ₐᵇ f(x)dx</code> 里的 <code>x</code> 是哑变量，把它换成 <code>t</code> 结果完全不变：<code>∫ₐᵇ f(x)dx = ∫ₐᵇ f(t)dt</code>。',
            '以为 <code>λ → 0</code> 就是「份数 <code>n → ∞</code>」。等分时两者等价，但一般分法下必须用最大区间长度 <code>λ</code>，因为「份数多」不代表「每份都变窄」。',
            '忘记定义里要求 <code>f</code> 有界。无界函数的黎曼和根本不会趋于有限数，那种情况要靠 5.4 节的瑕积分另行定义。',
          ],
          tags: ['定积分', '黎曼和', '极限', '曲边梯形'],
          related: ['def-integral-necessary-condition', 'thm-integral-sufficient-condition', 'def-integral-geometric-meaning', 'thm-integral-mean-value'],
        },
        {
          id: 'def-integral-necessary-condition',
          kind: 'theorem',
          name: '可积的必要条件：可积必有界',
          statement: '若 <code>f(x)</code> 在 <code>[a, b]</code> 上可积，则 <code>f(x)</code> 在 <code>[a, b]</code> 上必有界。\n换句话说：<b>无界函数一定不可积</b>。但反过来不成立——有界函数未必可积。',
          plain: '一个函数要想在区间上「积分得出来」，它首先得在这个区间上不飞出去。\n想想竖长条：如果某个小区间上函数值能大到没边，那根长条的「高」就是无穷大，乘上宽度还是无穷大，把所有长条加起来当然不可能得到一个正常的数。所以有界是入场券。\n但拿到入场券不等于能进门：有些函数虽然被卡在两条水平线之间，却在区间里跳动得太疯狂，导致长条们怎么加都稳定不下来，比如下面例子里的狄利克雷函数。',
          why: '这个结论的作用是「快速排除」。做题时如果发现被积函数在区间上无界（比如 <code>1/x</code> 在含 0 的区间上），就<b>不能</b>直接说它不可积然后不管了——教材里的定积分定义只覆盖有界函数，这种函数必须改用瑕积分（5.4 节）来讨论。所以这条定理真正教给你的是：<b>先看有界性，再决定用哪套定义</b>。',
          proof: '<b>证明（反证法）。</b>\n设 <code>f(x)</code> 在 <code>[a, b]</code> 上可积，积分值为 <code>I</code>，即 <code>lim<sub>λ→0</sub> Σᵢ f(ξᵢ)Δxᵢ = I</code>。\n<b>第一步：把「可积」翻译成一句关于和式的话。</b>由极限定义，取 <code>ε = 1</code>，存在 <code>δ &gt; 0</code>，使得只要分割满足 <code>λ &lt; δ</code>，无论怎样取点 <code>ξᵢ</code>，都有\n<code>|Σᵢ f(ξᵢ)Δxᵢ - I| &lt; 1</code>，从而 <code>|Σᵢ f(ξᵢ)Δxᵢ| &lt; |I| + 1</code>。\n记 <code>M = |I| + 1</code>，这是一个与分割、与取点都无关的固定数。\n<b>第二步：挑一个长度受控的小区间。</b>取任意满足 <code>λ &lt; δ</code> 的分割（例如等分到足够细），固定这个分割和除第 <code>k</code> 个小区间以外的所有取点，其中第 <code>k</code> 个小区间长度记为 <code>Δx_k</code>。\n<b>第三步：让这一点上的函数值任意跑动。</b>把 <code>ξ_k</code> 在 <code>[x_{k-1}, x_k]</code> 内任取，则和式只随 <code>f(ξ_k)</code> 变化，可以写成\n<code>Σᵢ f(ξᵢ)Δxᵢ = f(ξ_k)Δx_k + C</code>，\n其中 <code>C</code> 是其余各项之和，此时是固定的。于是\n<code>|f(ξ_k)Δx_k + C| &lt; M</code>，即 <code>|f(ξ_k)|Δx_k &lt; M + |C|</code>，\n所以 <code>|f(ξ_k)| &lt; (M + |C|)/Δx_k</code>，右边是一个有限数。\n<b>第四步：得出结论。</b>这说明在被选中的任意小区间上，<code>f</code> 的取值都被同一个常数压住，取遍所有小区间就得到 <code>f</code> 在整个 <code>[a, b]</code> 上有界。矛盾假设不成立，定理得证。',
          example: '看 <code>f(x) = 1/x</code> 在开区间 <code>(0, 1]</code> 上：当 <code>x → 0⁺</code> 时 <code>f(x) → +∞</code>，无界，所以它<b>不是</b>本章意义下可积的函数，<code>∫₀¹ (1/x)dx</code> 这个记号在 5.1 节里无意义。\n到了 5.4 节我们会给它一个补救定义（瑕积分），并且算出 <code>∫₀¹ (1/x)dx</code> 发散。对比 <code>∫₀¹ (1/√x)dx = 2</code> 是收敛的——同为无界，一个救得回来，一个救不回来。',
          pitfalls: [
            '把「可积必有界」记成「有界必可积」，方向搞反。有界只是必要条件。',
            '看到被积函数无界就直接判它发散。无界只说明定积分定义不适用，是否发散要看瑕积分的极限算出来是什么。',
            '在证明里混淆「对任意取点成立」和「存在一组取点成立」。可积说的是前者，这个区别正是证明成立的关键。',
          ],
          tags: ['可积性', '有界', '必要条件'],
          related: ['def-definite-integral', 'thm-integral-sufficient-condition', 'def-improper-unbounded'],
        },
        {
          id: 'thm-integral-sufficient-condition',
          kind: 'theorem',
          name: '可积的充分条件',
          statement: '下列条件中任一条成立，都能保证 <code>f(x)</code> 在 <code>[a, b]</code> 上可积：\n<b>（1）连续性：</b><code>f(x)</code> 在 <code>[a, b]</code> 上连续；\n<b>（2）有限个间断点：</b><code>f(x)</code> 在 <code>[a, b]</code> 上有界，且只有有限个间断点；\n<b>（3）单调性：</b><code>f(x)</code> 在 <code>[a, b]</code> 上单调有界。',
          plain: '什么函数能积分？三类「乖」函数：画出来一笔不断的；虽然断了但只断有限几下的；一路只升或只降的。\n直觉是：定积分算的是「长条之和」，而改掉函数在个别点上的值，只是把某一根或几根长条的高改了改，它们占的分量随着长条变细趋于 0，不影响总和的极限。所以「连续」可以放宽成「有界＋有限个间断点」。\n反过来说，如果函数在每个小区间里都上蹿下跳，一根长条的高取多少完全看运气，和式就永远稳不下来，那就不可积。',
          why: '课本把这些条件叫做「充分」而不叫「充要」，是因为可积函数其实比这三类还要多。历史上黎曼给出过更精细的判定（用振幅之和刻画），后来勒贝格进一步证明：有界函数可积当且仅当它的间断点组成的集合「测度为零」——粗略说就是「间断点虽可能无穷多，但总长度为零」。这条路线超出上册范围，上册只要求你会用上面三类充分条件做判断。',
          proof: '<b>证明（1）连续性 ⇒ 可积。</b>这条最关键，思路是「用一致连续性把每根长条的内部起伏压平」。\n<b>第一步：连续 ⇒ 一致连续。</b>由康托尔定理，闭区间上的连续函数一致连续：任给 <code>ε &gt; 0</code>，存在 <code>δ &gt; 0</code>，使 <code>[a, b]</code> 中任意两点只要距离小于 <code>δ</code>，函数值之差就小于 <code>ε</code>。\n<b>第二步：把区间切得比 δ 更窄。</b>取任一分割使 <code>λ &lt; δ</code>。在每个小区间 <code>[x_{i-1}, x_i]</code> 上，<code>f</code> 连续因而取到最大值 <code>Mᵢ</code> 与最小值 <code>mᵢ</code>，且区间内任意两点距离 ≤ <code>λ &lt; δ</code>，所以 <code>Mᵢ - mᵢ &lt; ε</code>。\n<b>第三步：比较上下和与任意黎曼和。</b>对任意取点 <code>ξᵢ</code> 都有 <code>mᵢ ≤ f(ξᵢ) ≤ Mᵢ</code>，于是<br>任意两个黎曼和 <code>S</code>、<code>S′</code> 满足 <code>|S - S′| ≤ Σᵢ (Mᵢ - mᵢ)Δxᵢ &lt; ε Σᵢ Δxᵢ = ε(b - a)</code>。\n<b>第四步：极限存在。</b>上述不等式说明：当 <code>λ</code> 足够小时，所有黎曼和彼此之差可以任意小，也就是说黎曼和族满足柯西收敛准则。因此无论怎样分、怎样取点，和式都趋于同一个有限数，<code>f</code> 可积。\n<b>（2）的思路：</b>设间断点只有有限个，在每个间断点两侧各挖一个总长可任意小的小区间，剩下的部分上 <code>f</code> 连续。挖掉的那些小区间上，因为 <code>f</code> 有界（设 <code>|f| ≤ K</code>），贡献至多 <code>2K·(挖掉的总长)</code>，可以压到任意小。连续部分用（1）、挖掉部分用有界性，两边合起来就完成估计。\n<b>（3）的思路：</b>设 <code>f</code> 单调不减，则每个小区间上最小值在左端、最大值在右端，<code>Σᵢ (Mᵢ - mᵢ)Δxᵢ = Σᵢ [f(xᵢ) - f(x_{i-1})]Δxᵢ ≤ λ Σᵢ [f(xᵢ) - f(x_{i-1})] = λ[f(b) - f(a)]</code>。\n（这里用了「同一小区间内 <code>Δxᵢ ≤ λ</code>」，以及中间各项两两抵消的裂项求和。）当 <code>λ → 0</code> 时右边趋于 0，同（1）的第三步即知黎曼和趋于唯一极限。',
          example: '<b>连续可积：</b><code>f(x) = sin x / x</code> 在 <code>[0, 1]</code> 上有界（可补充定义 <code>f(0) = 1</code> 使之连续），故可积。\n<b>单调可积：</b><code>f(x) = [x]</code>（取整函数）在 <code>[0, 3]</code> 上单调不减，虽然 <code>x = 1, 2</code> 处有两个跳跃间断点，仍可积。\n<b>反例（有界但不可积）：</b><code>D(x) = 1</code>（当 <code>x</code> 为有理数）、<code>D(x) = 0</code>（当 <code>x</code> 为无理数），在 <code>[0, 1]</code> 上有界。但任意小区间里既有有理数又有无理数，取有理点得 <code>S = 1</code>，取无理点得 <code>S = 0</code>，极限不存在，故不可积。',
          pitfalls: [
            '把充分条件当成必要条件，以为「不连续就可积」。可积函数可以有不可数个间断点，只是上册不讨论。',
            '对单调性条件忘记「有界」。单调函数在闭区间上自动有界，但在开区间上不一定，做判断时要看清区间。',
            '拿连续函数可积去反推「不连续就不可积」，然后误判取整函数的积分不存在。',
          ],
          tags: ['可积性', '连续', '单调', '充分条件'],
          related: ['def-integral-necessary-condition', 'def-definite-integral'],
        },
        {
          id: 'def-integral-geometric-meaning',
          kind: 'definition',
          name: '定积分的几何意义',
          statement: '设 <code>f(x)</code> 在 <code>[a, b]</code> 上可积。则 <code>∫ₐᵇ f(x)dx</code> 等于曲线 <code>y = f(x)</code>、直线 <code>x = a</code>、直线 <code>x = b</code> 与 <code>x</code> 轴所围图形的<b>带符号面积</b>：\n位于 <code>x</code> 轴上方的部分面积取<b>正</b>号，位于 <code>x</code> 轴下方的部分面积取<b>负</b>号。\n特别地，当 <code>f(x) ≥ 0</code> 时，<code>∫ₐᵇ f(x)dx</code> 就是曲边梯形的面积。',
          plain: '把定积分看成「面积账本」：曲线跑到 <code>x</code> 轴上面，就在账本上记一笔收入（正数）；跑到下面，就记一笔支出（负数）；最后算总账。\n所以一个定积分可能是正数、可能是负数、也可能是零。它等于零时并不代表图形没有面积，只代表上面多出来的和下面多出来的正好一样多。\n常见的图形规则时，账本上的数字就等于中学里背过的面积公式。',
          why: '理解「带符号面积」是本章最省力的一个视角。后面很多结论用这个视角一眼就能看穿：比如奇函数在对称区间上的积分为什么是 0——因为它左右两半一个在上面一个在下面，两块图形关于原点中心对称，面积大小相同、符号相反，正好抵消；偶函数为什么等于两倍——因为左右两半形状一样且都在同一侧，面积叠加。',
          example: '<code>∫₀^π sin x dx = [-cos x]₀^π = -cos π + cos 0 = 1 + 1 = 2</code>：在 <code>[0, π]</code> 上 <code>sin x ≥ 0</code>，所以这是拱形下实实在在的面积 <code>2</code>。\n<code>∫_{-π}^{π} sin x dx = 0</code>：<code>[0, π]</code> 上的拱形记 <code>+2</code>，<code>[-π, 0]</code> 上的谷形记 <code>-2</code>，总账为零。\n<code>∫_{-1}^{1} x³ dx = 0</code>：图形一部分在轴上一部分在轴下，抵消为零。注意「积分为 0」和「函数恒为 0」是两码事。',
          pitfalls: [
            '把 <code>∫ₐᵇ f(x)dx</code> 一律读成「面积」，于是在 <code>f(x) &lt; 0</code> 时得出负数却不知哪里错了。',
            '求「几何面积」（不带符号）时直接算定积分。面积应为 <code>∫ₐᵇ |f(x)|dx</code>，需要先按零点分段。',
            '以为「积分上下限越大结果越大」。上下限之下图形可能在轴下方，积分反而更小。',
          ],
          tags: ['几何意义', '面积', '带符号面积'],
          related: ['def-definite-integral', 'note-velocity-distance', 'prop-integral-even-odd'],
        },
        {
          id: 'note-velocity-distance',
          kind: 'note',
          name: '定积分的物理意义：变速直线运动的路程',
          statement: '设物体沿直线运动，速度 <code>v(t)</code> 在 <code>[a, b]</code> 上连续且 <code>v(t) ≥ 0</code>，则物体在时间 <code>[a, b]</code> 内走过的路程为\n<code>s = ∫ₐᵇ v(t)dt</code>。\n若允许 <code>v(t)</code> 取负值（表示反向运动），则 <code>∫ₐᵇ v(t)dt</code> 表示<b>位移</b>，而路程为 <code>∫ₐᵇ |v(t)|dt</code>。',
          plain: '速度表上的数字一刻不停地变，怎么知道一共走了多远？\n把整段时间切成很多个很短很短的小段。每一小段里，速度几乎来不及变，就当成匀速处理：这一小段走的路 ≈ 「此刻的速度 × 这一小段的时长」。把所有小段的路加起来，再让每一段短到趋于 0，和就稳定在一个数上——那就是总路程。\n这和把纸片剪成细条算面积是同一件事，只是一个把图形切竖条，一个把时间切横条。',
          why: '这条意义解释了定积分<b>为什么值得发明</b>。它同时说明「微元法」的套路：<code>ds = v(t)dt</code> 读作「极短时间内的路程近似为速度乘时间」，然后对所有微元积分得到整体。物理上凡是「某量的变化率」与「该量的总量」之间的关系，都可以套这个格式：变力做功 <code>W = ∫ₐᵇ F(x)dx</code>、水压力、转动惯量都是同一个套路。',
          example: '物体速度 <code>v(t) = t</code>（米/秒），时间从 <code>0</code> 到 <code>3</code> 秒。\n用微元法：总路程 <code>s = ∫₀³ t dt = [t²/2]₀³ = 9/2 = 4.5</code> 米。\n验算：这是匀加速运动，初速 0、末速 3，平均速度 <code>(0+3)/2 = 1.5</code>，路程 <code>1.5 × 3 = 4.5</code> 米，一致。\n如果 <code>v(t) = t - 2</code> 在 <code>[0, 3]</code> 上，则位移 <code>∫₀³ (t-2)dt = 9/2 - 6 = -1.5</code>，路程则是 <code>∫₀² (2-t)dt + ∫₂³ (t-2)dt = 2 + 0.5 = 2.5</code>。',
          pitfalls: [
            '把路程和位移混为一谈。只有速度不变号时两者才相等。',
            '忘记单位与变号。速度单位是米/秒时，<code>∫v dt</code> 的单位是米，而不是米/秒。',
            '认为「速度连续」是必须的。其实只要可积即可；速度突变（如碰撞）时积分仍然有意义。',
          ],
          tags: ['物理意义', '路程', '位移', '微元法'],
          related: ['def-integral-geometric-meaning', 'def-definite-integral'],
        },
        {
          id: 'thm-integral-properties',
          kind: 'theorem',
          name: '定积分的线性性质与区间可加性',
          aka: ['定积分的基本运算性质'],
          statement: '设 <code>f(x)</code>、<code>g(x)</code> 在涉及的区间上可积，<code>k</code> 为常数，则：\n<b>（1）线性性质：</b><code>∫ₐᵇ [αf(x) + βg(x)]dx = α∫ₐᵇ f(x)dx + β∫ₐᵇ g(x)dx</code>；特别地 <code>∫ₐᵇ kf(x)dx = k∫ₐᵇ f(x)dx</code>。\n<b>（2）区间可加性：</b>对任意 <code>a, b, c</code> 三者的大小关系不加限制，都有\n<code>∫ₐᵇ f(x)dx = ∫ₐᶜ f(x)dx + ∫꜀ᵇ f(x)dx</code>。\n<b>（3）两个约定：</b><code>∫ₐᵃ f(x)dx = 0</code>；<code>∫ₐᵇ f(x)dx = -∫ᵦᵃ f(x)dx</code>。',
          plain: '这些性质说穿了都是「分钱」的常识。\n线性性质：把两笔账混在一起算，等于分开算再相加；每一笔都翻三倍，总和也翻三倍。\n区间可加性：从北京到广州的总花销，等于北京到武汉的加上武汉到广州的，中间在哪儿停下都行。\n两个约定是刻意规定的：原地不动花了 0 元（<code>∫ₐᵃ = 0</code>）；反着走一遍，账目取相反数（<code>∫ₐᵇ = -∫ᵦᵃ</code>）。这样规定以后，上面那条可加性对 <code>c</code> 在区间外也照样成立，用起来不用分类讨论。',
          why: '为什么区间可加性要允许 <code>c</code> 在 <code>[a, b]</code> 外面？因为在实际计算中（尤其是分段函数的积分、以及后面处理瑕点）经常需要「把区间往外扩或往中间插一刀」，如果性质只在 <code>a &lt; c &lt; b</code> 时成立，每次都得先讨论位置。教材先把 <code>∫ₐᵇ = -∫ᵦᵃ</code> 和 <code>∫ₐᵃ = 0</code> 规定好，就是为了让可加性成为一条无条件通用的公式——这是数学里「用约定换便利」的典型手法。',
          proof: '<b>证明（1）线性性质。</b>在 <code>[a, b]</code> 上任取分割与取点，作黎曼和：\n<code>Σᵢ [αf(ξᵢ) + βg(ξᵢ)]Δxᵢ = α Σᵢ f(ξᵢ)Δxᵢ + β Σᵢ g(ξᵢ)Δxᵢ</code>。\n（这一步只是把有限和按项拆开、把常数提出求和号。）\n令 <code>λ → 0</code>，右边两个和分别趋于 <code>∫ₐᵇ f</code> 与 <code>∫ₐᵇ g</code>（因二者可积），再由极限的四则运算即得结论。\n<b>证明（2）区间可加性（先设 a &lt; c &lt; b）。</b>取 <code>[a, b]</code> 的一个分割，并让 <code>c</code> 成为其中一个分点（若原来的分割没有 <code>c</code>，把 <code>c</code> 补进去，这只会让分割更细，不影响极限）。则从 <code>a</code> 到 <code>b</code> 的所有小区间被 <code>c</code> 切成前后两组：\n<code>Σ_{[a,b]} f(ξᵢ)Δxᵢ = Σ_{[a,c]} f(ξᵢ)Δxᵢ + Σ_{[c,b]} f(ξᵢ)Δxᵢ</code>。\n令 <code>λ → 0</code>，左右两组的极限分别是 <code>∫ₐᶜ f</code> 与 <code>∫꜀ᵇ f</code>，故 <code>∫ₐᵇ f = ∫ₐᶜ f + ∫꜀ᵇ f</code>。\n<b>其余位置关系。</b>若 <code>c</code> 不在 <code>[a, b]</code> 内（例如 <code>c &lt; a &lt; b</code>），上式对 <code>[c, b]</code> 用一次（<code>a</code> 是它的内点）得 <code>∫꜀ᵇ f = ∫꜀ᵃ f + ∫ₐᵇ f</code>，即 <code>∫ₐᵇ f = ∫꜀ᵇ f - ∫꜀ᵃ f = ∫ₐᶜ f + ∫꜀ᵇ f</code>（最后一步用了约定 <code>∫ₐᶜ = -∫꜀ᵃ</code>）。其余情形同理，故可加性无条件成立。',
          example: '<b>拆分计算：</b>对 <code>f(x) = |x|</code> 求 <code>∫_{-1}^{2} |x|dx</code>。因为 <code>|x|</code> 在 <code>x = 0</code> 处换表达式，按可加性拆开：\n<code>∫_{-1}^{0} (-x)dx + ∫₀^{2} x dx = [-(x²/2)]_{-1}^{0} + [x²/2]₀^{2} = 1/2 + 2 = 5/2</code>。\n<b>线性拼接：</b><code>∫₀¹ (3x² - 2x)dx = 3∫₀¹ x²dx - 2∫₀¹ x dx = 3·(1/3) - 2·(1/2) = 1 - 1 = 0</code>。\n验算：原函数 <code>x³ - x²</code>，代入得 <code>(1-1) - (0-0) = 0</code>，一致。',
          pitfalls: [
            '把 <code>∫ₐᵇ [f(x)g(x)]dx</code> 拆成 <code>∫ₐᵇ f · ∫ₐᵇ g</code>。乘法没有这种性质，别把线性性质用过头。',
            '在 <code>∫ₐᵇ f(x)dx = ∫ₐᶜ f + ∫꜀ᵇ f</code> 中要求 <code>c</code> 必须在 <code>[a, b]</code> 内，导致分段时多做无用讨论。',
            '对 <code>∫ₐᵇ f(x)dx = -∫ᵦᵃ f(x)dx</code> 与 <code>∫ₐᵃ = 0</code> 这两个「约定」当成定理去证，浪费时间去讨论「上下限反着写时的分割」。它们是人为规定。',
          ],
          tags: ['定积分性质', '线性', '区间可加性'],
          related: ['def-definite-integral', 'prop-integral-sign-preserving', 'thm-integral-estimation'],
        },
        {
          id: 'prop-integral-sign-preserving',
          kind: 'theorem',
          name: '定积分的保号性',
          aka: ['比较性质'],
          statement: '设 <code>f(x)</code>、<code>g(x)</code> 在 <code>[a, b]</code>（<code>a &lt; b</code>）上可积。\n<b>（1）</b>若 <code>f(x) ≥ 0</code> 对一切 <code>x ∈ [a, b]</code> 成立，则 <code>∫ₐᵇ f(x)dx ≥ 0</code>。\n<b>（2）</b>若 <code>f(x) ≤ g(x)</code> 对一切 <code>x ∈ [a, b]</code> 成立，则 <code>∫ₐᵇ f(x)dx ≤ ∫ₐᵇ g(x)dx</code>。\n<b>（3）</b>若 <code>f(x) ≥ 0</code> 且 <code>f</code> 在 <code>[a, b]</code> 上<b>连续</b>、<code>f(x)</code> 不恒为 0，则 <code>∫ₐᵇ f(x)dx &gt; 0</code>（等号只有在 <code>f</code> 恒为 0 时才能取到）。',
          plain: '（1）就是「非负的东西加起来不会变负」。每一根长条的高都不小于 0，宽是正的，所以每根长条的面积不为负，总和自然不为负。\n（2）是（1）的直接后果：<code>g - f ≥ 0</code>，于是 <code>∫g - ∫f ≥ 0</code>。也就是说，函数大的那个，积分也大。\n（3）稍微细一点：光说「不小于 0」不够狠。如果函数连续、非负、又不是一路贴着 0 走，那就一定在某处真正鼓起来一块，这一块面积是正的，加起来就严格大于 0。',
          why: '保号性是<b>估值和比较</b>的地基：有了它，我们才能用简单的函数去夹住复杂的函数，从而判断积分的大小甚至判断敛散性（5.5 节的比较审敛法就是它推到无穷区间上的产物）。\n注意（3）里「连续」这个条件是必需的。如果只要求可积，函数完全可以在个别点上取正值，而积分仍然是 0——比如把 <code>f(x) = 1</code>（仅当 <code>x = 1/2</code>）而其他地方为 <code>0</code> 的函数积分，这根「针」的面积为 0。所以结论要把连续性写清楚，不能想当然。',
          proof: '<b>证明（1）。</b>设 <code>f(x) ≥ 0</code>。对 <code>[a, b]</code> 的任意分割与任意取点 <code>ξᵢ</code>，每一项都有 <code>f(ξᵢ) ≥ 0</code>、<code>Δxᵢ &gt; 0</code>，所以\n<code>Σᵢ f(ξᵢ)Δxᵢ ≥ 0</code>。\n（有限个非负数之和仍非负。）由可积性，该和式当 <code>λ → 0</code> 时趋于 <code>∫ₐᵇ f(x)dx</code>；再由「极限保号性」（若 <code>Sₙ ≥ 0</code> 且 <code>Sₙ → L</code>，则 <code>L ≥ 0</code>）得到 <code>∫ₐᵇ f(x)dx ≥ 0</code>。\n<b>证明（2）。</b>由 <code>f(x) ≤ g(x)</code> 知 <code>g(x) - f(x) ≥ 0</code>。对 <code>g - f</code> 用（1）得 <code>∫ₐᵇ[g(x) - f(x)]dx ≥ 0</code>，再用线性性质展开：<code>∫ₐᵇ g(x)dx - ∫ₐᵇ f(x)dx ≥ 0</code>，即 <code>∫ₐᵇ f ≤ ∫ₐᵇ g</code>。\n<b>证明（3）（反证法）。</b>已知 <code>f</code> 连续、<code>f ≥ 0</code>、不恒为 0，由（1）已有 <code>I = ∫ₐᵇ f(x)dx ≥ 0</code>。下面排除 <code>I = 0</code>。\n<b>第一步：找到一处为正的函数值。</b>因 <code>f</code> 不恒为 0，存在 <code>x₀ ∈ [a, b]</code> 使 <code>f(x₀) = c &gt; 0</code>。\n<b>第二步：由连续性把它扩展成一小段「有厚度」的正值区域。</b>取 <code>ε = c/2 &gt; 0</code>，由 <code>f</code> 在 <code>x₀</code> 连续，存在 <code>δ &gt; 0</code>，使 <code>|x - x₀| &lt; δ</code> 时 <code>|f(x) - c| &lt; c/2</code>，从而 <code>f(x) &gt; c/2 &gt; 0</code>。\n把这段与 <code>[a, b]</code> 的交记作 <code>[p, q]</code>（长度 <code>h &gt; 0</code>）。\n<b>第三步：在这段上积分是正的。</b>由（2）（在 <code>[p, q]</code> 上比较 <code>f</code> 与常数 <code>c/2</code>）以及 <code>f ≥ 0</code> 在其余部分的积分非负、区间可加性：\n<code>I = ∫ₐᵖ f + ∫ₚ^q f + ∫_q^b f ≥ 0 + (c/2)(q - p) + 0 = c·h/2 &gt; 0</code>。\n<b>第四步：结论。</b>这与 <code>I = 0</code> 矛盾，所以 <code>I &gt; 0</code>。于是 <code>∫ₐᵇ f(x)dx = 0</code> 只能在 <code>f</code> 恒为 0 时发生，得证。',
          example: '<b>比大小：</b>在 <code>[0, 1]</code> 上 <code>x² ≤ x</code>，所以 <code>∫₀¹ x²dx ≤ ∫₀¹ x dx</code>，而 <code>1/3 &lt; 1/2</code>，吻合。\n<b>正性：</b><code>f(x) = x(1-x)</code> 在 <code>[0, 1]</code> 上连续非负且不恒为 0，于是 <code>∫₀¹ x(1-x)dx = 1/2 - 1/3 = 1/6 &gt; 0</code>，严格为正。\n<b>反例（连续性不能丢）：</b>设 <code>f(x) = 0</code>（<code>x ≠ 1/2</code>）而 <code>f(1/2) = 1</code>。它在 <code>[0, 1]</code> 上可积（只有一个间断点），非负、不恒为 0，但 <code>∫₀¹ f(x)dx = 0</code>。可见没有连续性时（3）不成立。',
          pitfalls: [
            '忘记（3）中「连续」的条件，用它去判断带跳跃间断的函数的积分严格为正。',
            '把「<code>f(x) ≥ 0</code> 导致 <code>∫f ≥ 0</code>」反过来用：「<code>∫f ≥ 0</code> 所以 <code>f ≥ 0</code>」是错的。例如 <code>∫₀^π sin x dx = 2 &gt; 0</code>，但 <code>sin x</code> 在 <code>[0, π]</code> 上确实非负——换个例子：<code>f(x) = x - 1/2</code> 在 <code>[0, 1]</code> 上取负值，可 <code>∫₀¹ (x - 1/2)dx = 0</code>。',
            '在反向区间（<code>a &gt; b</code>）上照搬保号性。此时 <code>Δxᵢ &gt; 0</code> 的前提变了，符号会反过来。',
          ],
          tags: ['定积分性质', '保号性', '比较'],
          related: ['thm-integral-properties', 'thm-integral-estimation', 'prop-integral-abs-inequality', 'thm-comparison-infinite'],
        },
        {
          id: 'thm-integral-estimation',
          kind: 'theorem',
          name: '定积分的估值定理',
          statement: '设 <code>f(x)</code> 在 <code>[a, b]</code>（<code>a &lt; b</code>）上可积，且对一切 <code>x ∈ [a, b]</code> 有 <code>m ≤ f(x) ≤ M</code>，则\n<code>m(b - a) ≤ ∫ₐᵇ f(x)dx ≤ M(b - a)</code>。',
          plain: '这是一条「不知道精确值也能估算范围」的性质。\n想象用两块矩形夹住那块曲边图形：矮矩形高 <code>m</code>、宽 <code>b - a</code>，高矩形高 <code>M</code>、宽一样。曲线既然夹在两条水平线之间，它的面积也就夹在这两块矩形的面积之间。\n所以只要你能说出函数的最大值和最小值，就能马上给积分划一个可信的区间，连原函数都不用找。',
          why: '估值定理的用处主要是<b>两步走</b>：一是能对算不出来的积分给出误差可控的范围（比如 <code>∫₀¹ e^{-x²}dx</code> 没有初等原函数）；二是它是积分中值定理的直接前置——把不等式两边同除 <code>b - a</code>，就得到「平均值 <code>∫ₐᵇ f/(b-a)</code> 夹在 <code>m</code> 与 <code>M</code> 之间」，再用连续函数的介值定理就冒出「存在一点等于平均值」。可以说，积分中值定理就是估值定理加介值定理。',
          proof: '<b>证明。</b>\n<b>第一步：把常数看作函数，用比较性质。</b>由 <code>m ≤ f(x) ≤ M</code>，在 <code>[a, b]</code> 上分别把常数函数 <code>m</code> 与 <code>f</code>、<code>f</code> 与常数函数 <code>M</code> 比较，用保号性的第（2）条得\n<code>∫ₐᵇ m dx ≤ ∫ₐᵇ f(x)dx ≤ ∫ₐᵇ M dx</code>。\n<b>第二步：算出两端的常数积分。</b>常数函数在 <code>[a, b]</code> 上的积分为「高 × 宽」：\n<code>∫ₐᵇ m dx = m(b - a)</code>，<code>∫ₐᵇ M dx = M(b - a)</code>。\n（理由：常数函数的黎曼和为 <code>Σᵢ m Δxᵢ = m Σᵢ Δxᵢ = m(b - a)</code>，与分法无关。）\n<b>第三步：合并。</b>把第二步代入第一步即得\n<code>m(b - a) ≤ ∫ₐᵇ f(x)dx ≤ M(b - a)</code>。证毕。',
          example: '<b>估算 <code>∫₀¹ e^{-x²}dx</code>。</b><code>f(x) = e^{-x²}</code> 在 <code>[0, 1]</code> 上单调递减，故 <code>m = e^{-1}</code>（在 <code>x = 1</code> 处）、<code>M = e^{0} = 1</code>（在 <code>x = 0</code> 处），<code>b - a = 1</code>。于是\n<code>1/e ≈ 0.368 ≤ ∫₀¹ e^{-x²}dx ≤ 1</code>。\n（真实值约 <code>0.7468</code>，确实落在区间里。）\n<b>估算 <code>∫₀^{π/2} sin x dx</code>。</b>在 <code>[0, π/2]</code> 上 <code>0 ≤ sin x ≤ 1</code>，得 <code>0 ≤ ∫ ≤ π/2 ≈ 1.571</code>；精确值 <code>[-cos x]₀^{π/2} = 1</code>，符合夹逼。',
          pitfalls: [
            '用区间内某一点的函数值当 <code>m</code> 或 <code>M</code>。必须是整个区间上的最小值与最大值，取错端点会给出错误区间。',
            '忘记乘 <code>(b - a)</code>，直接把 <code>m ≤ ∫f ≤ M</code> 写出来（量纲都不对）。',
            '把这个不等式当成等式去反解积分值。它只给范围，不给精确值。',
          ],
          tags: ['定积分性质', '估值', '不等式'],
          related: ['prop-integral-sign-preserving', 'thm-integral-mean-value', 'thm-integral-properties'],
        },
        {
          id: 'prop-integral-abs-inequality',
          kind: 'theorem',
          name: '定积分的绝对值不等式',
          statement: '设 <code>f(x)</code> 在 <code>[a, b]</code>（<code>a &lt; b</code>）上可积，则 <code>|f(x)|</code> 也可积，并且\n<code>|∫ₐᵇ f(x)dx| ≤ ∫ₐᵇ |f(x)|dx</code>。',
          plain: '一句话：「先取绝对值再积分」不会比「先积分再取绝对值」小。\n直觉是抵消的代价。把 <code>f</code> 直接积分时，正负两半会互相抵消，结果可能很小甚至为 0；而取绝对值以后，负的部分被翻上来变成正的，不再抵消，只会把总和撑大。\n就像记账：一天的净收入可能很少（收支相抵），但一天流水的总额（把每笔都按金额记）一定不小于净收入的绝对值。',
          why: '这条不等式主要干两件事。\n一是<b>控制误差</b>：想说明两个积分很接近，只要说明它们的差的绝对值积分很小即可。\n二是<b>证明收敛</b>：在判断反常积分是否收敛时，「绝对值收敛 ⇒ 原积分收敛」（绝对收敛，见 5.5 节）这条结论正是从这里出发的。因为 <code>|f|</code> 非负，它的积分是单调不减的量，比会被正负抵消的 <code>∫f</code> 好控制得多。',
          proof: '<b>证明。</b>\n<b>第一步：先说明 <code>|f|</code> 可积。</b>已知 <code>f</code> 可积，故 <code>f</code> 有界，设 <code>|f(x)| ≤ K</code>。对任意 <code>x, y</code> 有三角不等式的变形\n<code>||f(x)| - |f(y)|| ≤ |f(x) - f(y)|</code>，\n即取绝对值这个操作不会放大两个函数值之间的距离。因此 <code>f</code> 的「振幅」被 <code>|f|</code> 原样继承：同一小区间上 <code>|f|</code> 的振幅不超过 <code>f</code> 的振幅，<code>|f|</code> 也满足同样的可积性估计，故 <code>|f|</code> 在 <code>[a, b]</code> 上可积。\n<b>第二步：用两边夹。</b>对每个 <code>x ∈ [a, b]</code> 都有 <code>-|f(x)| ≤ f(x) ≤ |f(x)|</code>。对左右两端与中间分别用保号性（比较性质）：\n<code>-∫ₐᵇ |f(x)|dx ≤ ∫ₐᵇ f(x)dx ≤ ∫ₐᵇ |f(x)|dx</code>。\n<b>第三步：翻译成绝对值形式。</b>对任意实数 <code>u</code> 与正数 <code>A</code>，<code>-A ≤ u ≤ A</code> 等价于 <code>|u| ≤ A</code>。取 <code>u = ∫ₐᵇ f(x)dx</code>、<code>A = ∫ₐᵇ |f(x)|dx</code>（由保号性 <code>A ≥ 0</code>），即得\n<code>|∫ₐᵇ f(x)dx| ≤ ∫ₐᵇ |f(x)|dx</code>。证毕。',
          example: '<b>体会「抵消」：</b><code>f(x) = sin x</code> 在 <code>[0, 2π]</code> 上，<code>∫₀^{2π} sin x dx = 0</code>，所以左边是 <code>0</code>；而 <code>∫₀^{2π} |sin x|dx = 4</code>（一个完整周期上下两拱各为 2），右边是 <code>4</code>。不等式 <code>0 ≤ 4</code> 成立，而且差距很大。\n<b>用来判收敛：</b>要说明 <code>∫₁^∞ (sin x / x²)dx</code> 收敛，可先看 <code>|sin x / x²| ≤ 1/x²</code>，而 <code>∫₁^∞ dx/x² = 1</code> 收敛，于是绝对值积分收敛，原积分也收敛。',
          pitfalls: [
            '把不等号方向记反。是 <code>|∫f| ≤ ∫|f|</code>，绝不是 <code>∫|f| ≤ |∫f|</code>。',
            '以为等号总成立。等号成立需要 <code>f</code> 在区间上不变号（或几乎处处不变号）。',
            '用这条不等式去证明「<code>∫|f|</code> 发散 ⇒ <code>∫f</code> 发散」。方向不对，发散性不能这样传。',
          ],
          tags: ['定积分性质', '绝对值', '不等式'],
          related: ['prop-integral-sign-preserving', 'thm-integral-estimation', 'thm-absolute-convergence'],
        },
        {
          id: 'thm-integral-mean-value',
          kind: 'theorem',
          name: '积分中值定理',
          statement: '若 <code>f(x)</code> 在 <code>[a, b]</code> 上连续，则至少存在一点 <code>ξ ∈ [a, b]</code>，使\n<code>∫ₐᵇ f(x)dx = f(ξ)(b - a)</code>，\n即 <code>f(ξ) = (1/(b - a))·∫ₐᵇ f(x)dx</code>。\n右端的量称为 <code>f</code> 在 <code>[a, b]</code> 上的<b>平均值</b>。',
          plain: '先看一串数：如果五个数里最大的是 10、最小的是 0，那平均数一定在 0 到 10 之间。函数也一样——它的「平均值」不可能超出它自己的最大值和最小值。\n积分中值定理进一步说：只要函数是连续的（图像一笔画下去不断开），它就能取到中间的任何高度，所以一定有某一刻的函数值恰好等于平均值。\n换句话说，一块形状不规则的曲边图形，总可以换成一个同样宽、但高度等于「平均高度」的矩形，面积分毫不差。',
          why: '这条定理是<b>把整体信息压缩成一个点的信息</b>的工具。它最著名的用处是证明「变上限积分求导定理」：那里 <code>ΔΦ/Δx</code> 恰好能写成某个中间点 <code>ξ</code> 的函数值 <code>f(ξ)</code>，再让 <code>ξ → x</code> 就得到 <code>Φ′(x) = f(x)</code>。\n它也是「平均」这个概念的正式定义：中学的平均速度、平均温度，到了这里终于有了统一的说法——「平均值＝积分除以区间长度」。要求连续性是因为要动用介值定理；若只有可积性，结论要弱化成 <code>m ≤ (1/(b-a))∫ₐᵇ f ≤ M</code>。',
          proof: '<b>证明。</b>设 <code>f</code> 在 <code>[a, b]</code> 上连续。\n<b>第一步：最值定理给出上下界。</b>闭区间上的连续函数必取到最大值和最小值：存在 <code>xₘ, x_M ∈ [a, b]</code>，使得对一切 <code>x ∈ [a, b]</code> 有\n<code>m = f(xₘ) ≤ f(x) ≤ f(x_M) = M</code>。\n<b>第二步：估值定理给出平均值的范围。</b>对上面的不等式在 <code>[a, b]</code> 上用估值定理：\n<code>m(b - a) ≤ ∫ₐᵇ f(x)dx ≤ M(b - a)</code>。\n由于 <code>b - a &gt; 0</code>，两边同除 <code>b - a</code>：\n<code>m ≤ (1/(b - a))∫ₐᵇ f(x)dx ≤ M</code>。\n记中间这个数为 <code>μ</code>，它就夹在 <code>f</code> 的最小值与最大值之间。\n<b>第三步：介值定理把 μ 变成某个函数值。</b>若 <code>m = M</code>，则 <code>f</code> 恒为常数，取 <code>ξ</code> 为任意内点即可；若 <code>m &lt; M</code>，则 <code>μ ∈ [m, M]</code>，而 <code>f</code> 在闭区间 <code>[a, b]</code> 上连续，由介值定理，<code>f</code> 取遍 <code>[m, M]</code> 上的一切值，特别地存在 <code>ξ ∈ [a, b]</code> 使 <code>f(ξ) = μ</code>。\n<b>第四步：写成乘积形式。</b>把 <code>μ = f(ξ)</code> 代回，得 <code>∫ₐᵇ f(x)dx = f(ξ)(b - a)</code>。证毕。',
          example: '<b>反求中值点：</b><code>f(x) = x²</code> 在 <code>[0, 3]</code> 上。<code>∫₀³ x²dx = 27/3 = 9</code>，平均值 <code>μ = 9/(3-0) = 3</code>。解 <code>ξ² = 3</code> 得 <code>ξ = √3 ≈ 1.732 ∈ [0, 3]</code>，确实存在。\n<b>直接看平均：</b><code>f(x) = sin x</code> 在 <code>[0, π]</code> 上，<code>∫₀^π sin x dx = 2</code>，平均值 <code>2/π ≈ 0.637</code>，而存在 <code>ξ = arcsin(2/π) ≈ 0.690</code> 使 <code>sin ξ = 2/π</code>。\n[[tip:定理只保证「存在」，不告诉你在哪里，也不保证唯一。上例中 <code>[0, π]</code> 上的解就是唯一的。]]',
          pitfalls: [
            '把 <code>ξ</code> 的范围写成开区间 <code>(a, b)</code>。定理的结论允许 <code>ξ</code> 取到端点（常数函数时就必须取端点）。',
            '忽略连续性条件，对只有可积性的函数去解 <code>f(ξ) = μ</code>，可能无解。',
            '把积分中值定理和微分中值定理的 <code>ξ</code> 混为一谈。前者说「面积可换成等面积矩形」，后者说「割线斜率等于某点切线斜率」，是两个不同的存在性结论。',
          ],
          tags: ['定积分', '中值定理', '平均值'],
          related: ['thm-integral-estimation', 'thm-variable-upper-derivative', 'thm-integral-properties'],
        },
        {
          id: 'def-variable-upper-integral',
          kind: 'definition',
          name: '积分上限函数（变上限积分）',
          aka: ['变上限积分', '面积累积函数'],
          statement: '设 <code>f(x)</code> 在 <code>[a, b]</code> 上可积。对每个 <code>x ∈ [a, b]</code>，<code>f</code> 在 <code>[a, x]</code> 上也可积，于是可以定义一个新函数\n<code>Φ(x) = ∫ₐˣ f(t)dt，x ∈ [a, b]</code>。\n称 <code>Φ</code> 为<b>积分上限函数</b>（或变上限积分）。这里积分变量用 <code>t</code> 而不用 <code>x</code>，是为了把「上限这个位置上的自变量」与「被积函数里的哑变量」区分开。',
          plain: '原来 <code>∫ₐᵇ f(x)dx</code> 的上下限都是定死的数，算出来就是一个数。现在把上边那条「收口的线」<code>b</code> 换成一个会滑动的 <code>x</code>：从 <code>a</code> 开始涂色，涂到 <code>x</code>，涂出来的面积就随 <code>x</code> 的移动而变化。\n于是「面积」变成了一个随 <code>x</code> 变化的东西，也就是一个函数 <code>Φ(x)</code>。\n它很好想象：<code>x</code> 往右挪一点点，<code>Φ</code> 就多出窄窄的一条；<code>x</code> 若跑到 <code>a</code> 左边（在允许的区间外），面积就记成负的。',
          why: '为什么值得单独定义？因为它是整个微积分的<b>枢纽</b>。在此之前，「积分」只是求面积的算法；一旦把上限变成变量，积分就变成了一个函数，于是可以问它「导数是几」。答案惊人地简单：导数就是被积函数本身（见下一条定理）。\n这一步把「积分」和「微分」这对看起来毫不相干的操作接在了一起，也顺手解决了「什么样的函数有原函数」这个难题——连续函数的原函数可以直接用 <code>Φ</code> 写出来。可以说，牛顿-莱布尼茨公式、反常积分的处理，全都建立在 <code>Φ</code> 这个对象上。',
          example: '取 <code>f(t) = t²</code>，<code>a = 0</code>：\n<code>Φ(x) = ∫₀ˣ t²dt = [t³/3]₀ˣ = x³/3</code>。\n求导得 <code>Φ′(x) = x²</code>，正好等于被积函数在 <code>x</code> 处的值。<b>这不是巧合</b>，正是下一条定理。\n再看 <code>f(t) = 1</code>：<code>Φ(x) = ∫₀ˣ 1dt = x</code>，求导得 <code>1</code>，也吻合。\n<b>与数值对照：</b><code>Φ(2) = 8/3 ≈ 2.667</code>，而 <code>∫₀² t²dt</code> 就是曲线 <code>t²</code> 在 <code>[0, 2]</code> 下的面积，量级合理。',
          pitfalls: [
            '把上限变量和积分变量混用，写成 <code>Φ(x) = ∫ₐˣ f(x)dx</code>。这在逻辑上自相矛盾（同一个 <code>x</code> 既当固定上限又当变量），必须换成 <code>f(t)dt</code>。',
            '以为 <code>Φ(x)</code> 一定是正的。若 <code>f</code> 取负值，<code>Φ(x)</code> 就会是负的。',
            '把 <code>Φ(x) = ∫ₐˣ f(t)dt</code> 与 <code>∫ₓᵃ f(t)dt</code> 混同。后者等于 <code>-Φ(x)</code>。',
          ],
          tags: ['变上限积分', '积分上限函数'],
          related: ['thm-variable-upper-derivative', 'thm-antiderivative-existence', 'thm-newton-leibniz'],
        },
        {
          id: 'thm-variable-upper-derivative',
          kind: 'theorem',
          name: '变上限积分求导定理',
          aka: ['微积分基本定理（第一部分）', '积分上限函数的导数'],
          statement: '设 <code>f(x)</code> 在 <code>[a, b]</code> 上连续，<code>Φ(x) = ∫ₐˣ f(t)dt</code>，则 <code>Φ(x)</code> 在 <code>[a, b]</code> 上可导，且\n<code>d/dx ∫ₐˣ f(t)dt = f(x)，x ∈ [a, b]</code>。\n即：<b>连续函数的变上限积分是它的一个原函数</b>。\n推论（变下限与复合情形）：\n<code>d/dx ∫ₓᵇ f(t)dt = -f(x)</code>；\n若 <code>u(x)</code>、<code>v(x)</code> 可导，则 <code>d/dx ∫_{v(x)}^{u(x)} f(t)dt = f(u(x))u′(x) - f(v(x))v′(x)</code>。',
          plain: '把「涂色面积」记成 <code>Φ(x)</code>：从 <code>a</code> 涂到 <code>x</code>。现在让 <code>x</code> 往右挪极小的一步 <code>Δx</code>，多涂出来的那一小条几乎是长方形：宽是 <code>Δx</code>，高大约就是该处的函数值 <code>f(x)</code>。\n所以「面积的增加速度」＝「多出那一条的高」＝<code>f(x)</code>。面积的增加速度，正是 <code>Φ</code> 的导数。\n一句话记住：<b>面积攒得多快，取决于曲线现在有多高。</b>曲线高，面积涨得快；曲线在 <code>x</code> 轴下方，面积就在减少。',
          why: '这条定理就是「微积分基本定理」的前一半，它的价值在于<b>把微分和积分接了起来</b>。在此之前，求导和求积分（面积）看起来是两个世界的技术；它告诉我们要研究积分，可以改而去研究一个函数的导数——而导数我们熟。\n证明的思路很值得记：<code>ΔΦ = Φ(x+Δx) - Φ(x)</code> 正好是一个区间上的积分，用积分中值定理可以把它精确地写成 <code>f(ξ)Δx</code>（<code>ξ</code> 在 <code>x</code> 与 <code>x+Δx</code> 之间），于是 <code>ΔΦ/Δx = f(ξ)</code>。当 <code>Δx → 0</code> 时 <code>ξ</code> 被挤向 <code>x</code>，再由 <code>f</code> 的连续性就得到 <code>f(x)</code>。整套证明只用了导数定义、区间可加性和积分中值定理。',
          proof: '<b>证明。</b>设 <code>f</code> 在 <code>[a, b]</code> 上连续，<code>Φ(x) = ∫ₐˣ f(t)dt</code>，取 <code>x ∈ [a, b)</code> 并设 <code>Δx &gt; 0</code> 使 <code>x + Δx ∈ [a, b]</code>（<code>Δx &lt; 0</code> 的情形完全对称，最后统一取极限即可）。\n<b>第一步：算出函数的增量 <code>ΔΦ</code>。</b>由区间可加性，\n<code>ΔΦ = Φ(x + Δx) - Φ(x) = ∫ₐ^{x+Δx} f(t)dt - ∫ₐˣ f(t)dt = ∫ₓ^{x+Δx} f(t)dt</code>。\n这一步把「两个积分的差」变成了「一个小区间上的积分」，是整段证明的关键化简。\n<b>第二步：在小区间上使用积分中值定理。</b><code>f</code> 在 <code>[x, x+Δx]</code> 上连续，故存在 <code>ξ ∈ [x, x+Δx]</code>，使\n<code>∫ₓ^{x+Δx} f(t)dt = f(ξ)·Δx</code>。\n<b>第三步：作差商。</b>因为 <code>Δx ≠ 0</code>，\n<code>ΔΦ/Δx = f(ξ)</code>，\n其中 <code>ξ</code> 位于 <code>x</code> 与 <code>x + Δx</code> 之间，即 <code>|ξ - x| ≤ |Δx|</code>。\n<b>第四步：让 <code>Δx → 0</code> 并利用连续性。</b>当 <code>Δx → 0</code> 时，由夹逼知 <code>ξ → x</code>；又 <code>f</code> 在 <code>x</code> 处连续，所以 <code>f(ξ) → f(x)</code>。于是\n<code>lim<sub>Δx→0</sub> ΔΦ/Δx = lim<sub>Δx→0</sub> f(ξ) = f(x)</code>。\n按导数的定义，左边的极限就是 <code>Φ′(x)</code>，因此 <code>Φ′(x) = f(x)</code>。证毕。\n<b>推论（复合情形）。</b>设 <code>G(u) = ∫ₐᵘ f(t)dt</code>，则 <code>G′(u) = f(u)</code>；把 <code>∫_{v(x)}^{u(x)} f(t)dt = G(u(x)) - G(v(x))</code>（区间可加性）对 <code>x</code> 用复合函数求导法则，即得 <code>f(u(x))u′(x) - f(v(x))v′(x)</code>。',
          example: '<b>直接套公式：</b><code>d/dx ∫₀ˣ √(1 + t²)dt = √(1 + x²)</code>——无需真的求出这个原函数（它很复杂），一步到位。\n<b>上限是函数：</b><code>d/dx ∫₀^{x²} sin t dt = sin(x²)·(x²)′ = 2x sin(x²)</code>。\n<b>上下限都是函数：</b><code>d/dx ∫ₓ^{x²} (1/t)dt = (1/x²)·2x - (1/x)·1 = 2/x - 1/x</code>。\n验算：<code>∫ₓ^{x²}(1/t)dt = ln(x²) - ln x = ln x</code>，求导得 <code>1/x</code>；而上面的结果 <code>2/x - 1/x = 1/x</code>，一致。',
          pitfalls: [
            '忘记上限本身也是函数，漏乘内层导数。写 <code>d/dx ∫₀^{x²} sin t dt = sin(x²)</code> 是错的，应为 <code>2x sin(x²)</code>。',
            '处理下限时忘记负号：<code>d/dx ∫ₓᵇ f(t)dt = -f(x)</code>。',
            '对不连续的被积函数照用此定理。定理要求 <code>f</code> 连续；若只有可积性，<code>Φ</code> 仍然连续，但可能处处不可导。',
            '以为定理中的 <code>x</code> 可以取到区间外。<code>Φ</code> 只在 <code>f</code> 有定义的范围内讨论。',
          ],
          tags: ['变上限积分', '求导', '微积分基本定理'],
          related: ['def-variable-upper-integral', 'thm-newton-leibniz', 'thm-integral-mean-value', 'thm-antiderivative-existence'],
        },
        {
          id: 'thm-antiderivative-existence',
          kind: 'theorem',
          name: '原函数存在定理',
          statement: '若 <code>f(x)</code> 在区间 <code>I</code> 上连续，则 <code>f(x)</code> 在 <code>I</code> 上存在原函数。具体地，取定 <code>a ∈ I</code>，则\n<code>Φ(x) = ∫ₐˣ f(t)dt</code>\n就是 <code>f(x)</code> 在 <code>I</code> 上的一个原函数，即 <code>Φ′(x) = f(x)</code>。因此 <code>f</code> 的全部原函数为 <code>∫ₐˣ f(t)dt + C</code>。\n反之，有第一类间断点的函数在含该点的区间上<b>不存在</b>原函数。',
          plain: '在学不定积分时，我们老是「猜」原函数：看到 <code>x²</code> 想到 <code>x³/3</code>。可要是猜不出来呢？是不是有些函数根本没有原函数？\n这条定理给了一颗定心丸：<b>只要函数连续，原函数一定存在</b>，而且可以直接写出来——就是它的变上限积分。\n所以像 <code>e^{-x²}</code>、<code>sin x / x</code> 这些「积不出初等原函数」的函数，原函数其实是存在的，只是不能用我们熟悉的那些函数写出来而已。',
          why: '这条定理解决了上一章留下的一个逻辑窟窿。不定积分的定义是「求导的逆运算」，可我们从未证明过逆运算一定有解。现在知道了：<b>连续是原函数存在的充分条件</b>。\n它还给出了一个重要的思维方式：与其追问「原函数是什么初等形式」，不如接受「原函数可以就用积分号写出来」。工程上计算 <code>∫₀ˣ e^{-t²}dt</code> 这类「非初等」积分，就是把它当成一个新函数来研究。\n定理后半句提醒你：连续性不可省。如果函数在某点有跳跃或可去间断，变上限积分在该点就不可导，原函数不存在。',
          proof: '<b>证明。</b>这是变上限积分求导定理的直接推论，把每一步重新交代清楚：\n<b>第一步：<code>Φ</code> 有定义。</b>因 <code>f</code> 在 <code>I</code> 上连续，故在任意子区间 <code>[a, x]</code> 上连续，从而可积（5.1 节充分条件），所以 <code>Φ(x) = ∫ₐˣ f(t)dt</code> 对每个 <code>x ∈ I</code> 都有意义。\n<b>第二步：<code>Φ</code> 可导且导数为 <code>f</code>。</b>由变上限积分求导定理，<code>Φ</code> 在 <code>I</code> 上可导且 <code>Φ′(x) = f(x)</code>。\n<b>第三步：按定义得出结论。</b>原函数的定义就是「导数等于 <code>f</code> 的函数」，所以 <code>Φ</code> 是 <code>f</code> 的一个原函数，<code>f</code> 的原函数存在。\n<b>第四步：写出全部原函数。</b>若 <code>F</code> 也是 <code>f</code> 的原函数，则 <code>(F - Φ)′ = f - f = 0</code>，由拉格朗日中值定理的推论知 <code>F - Φ ≡ C</code>（常数），故 <code>F(x) = ∫ₐˣ f(t)dt + C</code>。\n<b>第五步（后半句）。</b>若 <code>f</code> 在 <code>x₀</code> 有第一类间断点（左右极限都存在但不等，或函数值与该极限不等），且设 <code>F′ = f</code>。由导数极限定理，<code>F</code> 在 <code>x₀</code> 可导必在 <code>x₀</code> 连续，且 <code>f(x₀) = F′(x₀) = lim<sub>x→x₀</sub> F′(x) = lim<sub>x→x₀</sub> f(x)</code>（导数极限定理：若 <code>F</code> 在 <code>x₀</code> 连续、在去心邻域内可导且 <code>F′</code> 的极限存在，则该极限等于 <code>F′(x₀)</code>）。\n也就是说 <code>f</code> 必须在 <code>x₀</code> 连续。这与「<code>x₀</code> 是第一类间断点」矛盾，故这样的原函数不存在。',
          example: '<b>非初等原函数照样存在：</b><code>f(x) = e^{-x²}</code> 在 <code>[0, 1]</code> 上连续，所以 <code>Φ(x) = ∫₀ˣ e^{-t²}dt</code> 是它的原函数。虽然 <code>Φ</code> 不能用初等函数表示（这正是概率论里那个著名的「正态积分」），但它是完全合法的函数。\n<b>第一类间断点的反例：</b>取符号函数 <code>f(x) = sgn x</code>，在 <code>x = 0</code> 处跳跃。若存在 <code>F</code> 使 <code>F′ = sgn x</code>，则 <code>F′(0) = 0</code>，但 <code>lim<sub>x→0</sub> F′(x) = lim<sub>x→0</sub> sgn x</code> 不存在，与导数极限定理冲突，故 <code>sgn x</code> 在含 0 的区间上没有原函数。',
          pitfalls: [
            '认为「找不到初等原函数」就是「原函数不存在」。这是两件事：<code>e^{-x²}</code> 的原函数存在，只是非初等。',
            '以为定理对可积函数也成立。只有连续（或更弱的条件下）才保证，反之有第一类间断点就不行。',
            '把 <code>Φ(x) = ∫ₐˣ f(t)dt</code> 中的 <code>a</code> 当成唯一的。换成别的起点只会改变常数 <code>C</code>，都是同一个 <code>f</code> 的原函数。',
          ],
          tags: ['原函数', '存在性', '变上限积分'],
          related: ['thm-variable-upper-derivative', 'def-variable-upper-integral', 'thm-newton-leibniz'],
        },
        {
          id: 'thm-newton-leibniz',
          kind: 'theorem',
          name: '牛顿-莱布尼茨公式',
          aka: ['微积分基本定理（第二部分）', '微积分基本公式'],
          statement: '设 <code>f(x)</code> 在闭区间 <code>[a, b]</code> 上连续，<code>F(x)</code> 是 <code>f(x)</code> 在 <code>[a, b]</code> 上的<b>任意</b>一个原函数，则\n<code>∫ₐᵇ f(x)dx = F(b) - F(a)</code>。\n通常记作 <code>∫ₐᵇ f(x)dx = [F(x)]ₐᵇ = F(x)|ₐᵇ</code>。',
          plain: '算面积本来要「切细条、求极限」，又长又难。这条公式说：<b>不用切了，去找原函数。</b>\n只要能找到一个「导数等于被积函数」的函数 <code>F</code>，把区间的右端点和左端点分别代进去一减，答案就出来了。\n比如算曲线 <code>y = x²</code> 从 0 到 1 下面的面积：找一个求导得 <code>x²</code> 的函数，是 <code>x³/3</code>；代入得 <code>1/3 - 0 = 1/3</code>。整个极限过程被一次减法代替了。',
          why: '这条公式回答了一个非常深刻的问题：<b>为什么「求面积」可以变成「求原函数」？</b>\n关键在于变上限积分求导定理。定义面积函数 <code>Φ(x) = ∫ₐˣ f(t)dt</code>，我们证明了 <code>Φ′(x) = f(x)</code>——也就是说，<b>面积的变化率恰好是被积函数本身</b>。既然 <code>Φ</code> 是 <code>f</code> 的原函数，而 <code>f</code> 的任意两个原函数只差一个常数 <code>C</code>，那么你手头随便哪个原函数 <code>F</code> 都能用：<code>F = Φ + C</code>。\n相减时那个 <code>C</code> 自动消掉了：<code>F(b) - F(a) = Φ(b) - Φ(a) = ∫ₐᵇ f - 0</code>。这就是为什么「随便找一个原函数」都行，也是为什么「不定积分的 +C」在定积分里可以不理它。\n<b>附加常数为什么必须消掉？</b>因为定积分是一个确定的数，不能因为换了个原函数而改变。相减正是消掉任意性的机制。',
          proof: '<b>证明。</b>设 <code>f</code> 在 <code>[a, b]</code> 上连续，<code>F</code> 为 <code>f</code> 的任一原函数。<b>唯一要说明的是 <code>F(b) - F(a)</code> 确实是那个面积，而不是别的数。</b>\n<b>第一步：构造面积函数。</b>令 <code>Φ(x) = ∫ₐˣ f(t)dt</code>。由原函数存在定理，<code>Φ</code> 在 <code>[a, b]</code> 上可导且 <code>Φ′(x) = f(x)</code>。\n<b>第二步：比较两个原函数。</b>因为 <code>F′(x) = f(x) = Φ′(x)</code>，所以 <code>(F - Φ)′(x) = 0</code> 在 <code>[a, b]</code> 上恒成立。\n由拉格朗日中值定理的推论（导数为零的函数必为常数），存在常数 <code>C</code>，使\n<code>F(x) - Φ(x) = C</code>，即 <code>F(x) = Φ(x) + C</code>，<code>x ∈ [a, b]</code>。\n<b>第三步：定出 <code>C</code>。</b>取 <code>x = a</code>：<code>Φ(a) = ∫ₐᵃ f(t)dt = 0</code>，故 <code>C = F(a)</code>。于是 <code>F(x) = Φ(x) + F(a)</code>，即\n<code>Φ(x) = F(x) - F(a)</code>。\n<b>第四步：取 <code>x = b</code> 得结论。</b>\n<code>∫ₐᵇ f(x)dx = Φ(b) = F(b) - F(a)</code>。\n<b>第五步：注意常数 <code>C</code> 无关。</b>若把 <code>F</code> 换成 <code>F + C₀</code>（另一个原函数），则 <code>(F + C₀)(b) - (F + C₀)(a) = F(b) - F(a) + C₀ - C₀ = F(b) - F(a)</code>，结果不变。这正是公式里敢写「任意原函数」的理由。证毕。',
          example: '<b>基本计算：</b><code>∫₀¹ x²dx = [x³/3]₀¹ = 1/3 - 0 = 1/3</code>。\n<b>另一例：</b><code>∫₀³ 2x dx = [x²]₀³ = 9 - 0 = 9</code>；用定义验算：这是底 3、高 6 的三角形，面积为 <code>(1/2)·3·6 = 9</code>，一致。\n<b>再一例（要选对原函数）：</b><code>∫₁^e (1/x)dx = [ln x]₁^e = 1 - 0 = 1</code>。注意 <code>ln|x|</code> 与 <code>ln x</code> 在正区间上一样，取哪个都行。\n<b>用定义验算 <code>∫₀¹ x²dx</code>：</b>等分取右端点，<code>Sₙ = (1/n³)Σi² = (1/n³)·n(n+1)(2n+1)/6 → 2/6 = 1/3</code>，与公式结果吻合。',
          pitfalls: [
            '把上下限代反：写成 <code>F(a) - F(b)</code>。结果会整体差一个负号。',
            '在 <code>[a, b]</code> 上 <code>f</code> 不连续（或有瑕点）时照搬公式。比如 <code>∫_{-1}^{1} (1/x²)dx</code>，被积函数在 <code>x = 0</code> 无界，此时若错误地写成 <code>[-1/x]_{-1}^{1} = -1 - 1 = -2</code> 就荒谬了——被积函数恒正，积分不可能是负数，正确结论是这个瑕积分发散。',
            '忘记 <code>F</code> 必须满足 <code>F′(x) = f(x)</code> 在整个区间上成立。用 <code>ln x</code> 去算跨过 <code>x = 0</code> 的 <code>∫(1/x)dx</code> 就会出错，须改用 <code>ln|x|</code> 并避开原点。',
            '把记号 <code>[F(x)]ₐᵇ</code> 读成 <code>F(b) - F(a)</code> 之外的东西，或在分步计算中提前把括号里的差算错。建议每一步都写清 <code>F(b) - F(a)</code>。',
          ],
          tags: ['牛顿-莱布尼茨公式', '微积分基本定理', '定积分计算'],
          related: ['thm-variable-upper-derivative', 'thm-antiderivative-existence', 'def-definite-integral', 'thm-integral-by-parts'],
        },
        {
          id: 'prop-integral-even-odd',
          kind: 'theorem',
          name: '奇偶函数在对称区间上的定积分',
          statement: '设 <code>f(x)</code> 在 <code>[-a, a]</code> 上连续（<code>a &gt; 0</code>），则：\n<b>（1）偶函数：</b>若 <code>f(-x) = f(x)</code>，则 <code>∫_{-a}^{a} f(x)dx = 2∫₀^{a} f(x)dx</code>。\n<b>（2）奇函数：</b>若 <code>f(-x) = -f(x)</code>，则 <code>∫_{-a}^{a} f(x)dx = 0</code>。',
          plain: '对称区间上的积分可以「偷懒」。\n偶函数的图像关于 <code>y</code> 轴对称，左右两半一模一样，所以算一半再乘 2。\n奇函数的图像关于原点对称，右半边的图形在 <code>x</code> 轴上方，左半边就把它原样翻到了下方。算总账时一上一下正好抵消，答案是 0。\n考试里看到「对称区间 + 奇函数」，直接写 0，一分不丢。',
          why: '这个性质的来源其实就是 5.3 节的换元法，取代换 <code>t = -x</code>（见下面证明），所以它不是一个独立的新结论，而是换元的漂亮应用。\n它的实用价值极高：能省掉大量计算。遇到 <code>∫_{-1}^{1} x³cos x dx</code> 这种看着很吓人的积分，只要能识别出被积函数是奇函数，答案立刻是 0。反过来，如果区间不对称（比如 <code>[0, 1]</code> 或 <code>[-1, 2]</code>），这套办法完全失效——不能强行「补成对称」。',
          proof: '<b>证明。</b>把 <code>∫_{-a}^{a} f(x)dx</code> 用区间可加性在 <code>0</code> 处拆成两段：\n<code>∫_{-a}^{a} f(x)dx = ∫_{-a}^{0} f(x)dx + ∫₀^{a} f(x)dx</code>。\n<b>第一步：处理负半轴那一段。作代换 <code>x = -t</code>。</b>此时 <code>dx = -dt</code>；当 <code>x</code> 从 <code>-a</code> 变到 <code>0</code> 时，<code>t</code> 从 <code>a</code> 变到 <code>0</code>。由定积分换元公式：\n<code>∫_{-a}^{0} f(x)dx = ∫_{a}^{0} f(-t)(-dt) = ∫₀^{a} f(-t)dt</code>。\n（倒数第二步把 <code>dx</code> 换成 <code>-dt</code>，再交换上下限消去一个负号。）\n<b>第二步：代回原式。</b>\n<code>∫_{-a}^{a} f(x)dx = ∫₀^{a} f(-t)dt + ∫₀^{a} f(x)dx</code>。\n把两个积分都写成对 <code>x</code> 的积分（积分变量是哑变量，改名不影响值）：\n<code>∫_{-a}^{a} f(x)dx = ∫₀^{a} [f(-x) + f(x)]dx</code>。\n<b>第三步：代入奇偶性。</b>\n若 <code>f</code> 为偶函数，<code>f(-x) + f(x) = 2f(x)</code>，故 <code>∫_{-a}^{a} f = ∫₀^{a} 2f(x)dx = 2∫₀^{a} f(x)dx</code>；\n若 <code>f</code> 为奇函数，<code>f(-x) + f(x) = 0</code>，故 <code>∫_{-a}^{a} f = ∫₀^{a} 0 dx = 0</code>。证毕。',
          example: '<b>奇函数直接为 0：</b><code>∫_{-π}^{π} sin x dx = 0</code>；<code>∫_{-1}^{1} x³dx = 0</code>；<code>∫_{-1}^{1} x³cos x dx = 0</code>（奇 × 偶 = 奇）。\n<b>偶函数算一半：</b><code>∫_{-π/2}^{π/2} cos x dx = 2∫₀^{π/2} cos x dx = 2[sin x]₀^{π/2} = 2(1 - 0) = 2</code>。\n验算：<code>[sin x]_{-π/2}^{π/2} = 1 - (-1) = 2</code>，一致。\n<b>区间对称但函数非奇非偶：</b><code>∫_{-1}^{1} (x + 1)dx</code>，<code>x</code> 是奇函数部分贡献 0，常数 <code>1</code> 是偶函数贡献 <code>2∫₀¹ 1dx = 2</code>，答案 <code>2</code>。直接算：<code>[x²/2 + x]_{-1}^{1} = (1/2+1) - (1/2-1) = 2</code>。',
          pitfalls: [
            '区间不对称时乱套公式。例如 <code>∫₀^{2} x³dx = 4</code>，不能因为被积函数是奇函数就说答案是 0——对称区间是前提。',
            '判断错奇偶性。<code>x·cos x</code> 是奇函数，<code>x·sin x</code> 是偶函数，<code>sin(x²)</code> 是偶函数，要看准。',
            '对 <code>x³ + x²</code> 这种「一奇一偶」的和，只处理奇的部分而漏算偶的部分。应当拆开分别用性质。',
            '求「曲线围成的几何面积」时用奇函数性质得 0，然后以为面积真的是 0。面积要用 <code>∫|f|</code>，正负不抵消。',
          ],
          tags: ['定积分性质', '奇偶性', '对称区间', '换元'],
          related: ['def-integral-geometric-meaning', 'thm-integral-substitution', 'thm-integral-properties'],
        },
      ],
    },
    {
      id: 'ch5-2',
      no: '5.2',
      title: '微积分基本公式',
      summary: '把积分上限当成变量，证明「变上限积分的导数就是被积函数」，由此得到把定积分计算变成求原函数的牛顿-莱布尼茨公式。',
      items: [
        {
          id: 'thm-substitution-infinite-limit',
          kind: 'theorem',
          name: '积分上限函数的连续性与可导性（前置结论）',
          statement: '设 <code>f(x)</code> 在 <code>[a, b]</code> 上可积，<code>Φ(x) = ∫ₐˣ f(t)dt</code>，则 <code>Φ(x)</code> 在 <code>[a, b]</code> 上<b>连续</b>（这是可积条件下的结论，不需要 <code>f</code> 连续）。\n若进一步设 <code>f(x)</code> 在 <code>[a, b]</code> 上连续，则 <code>Φ(x)</code> 在 <code>[a, b]</code> 上<b>可导</b>且 <code>Φ′(x) = f(x)</code>。',
          plain: '这一条是把「可积」和「连续」两种前提下的结论摆在一起对比。\n结论一：即使被积函数乱七八糟（只要可积），面积函数 <code>Φ</code> 也一定是连续的——因为 <code>x</code> 只动一点点，多涂出来的那一条很窄，面积也只会变一点点，绝不会突然跳。\n结论二：如果被积函数还是连续的，那 <code>Φ</code> 不光连续，还可导，而且导数就是 <code>f</code> 本身。\n排序要记住：可积 <code>&lt;</code> 连续。前提越强，得到的结论越强。',
          why: '这一条的意义在于让你分清「连续性」和「可导性」这两个结论各自需要多强的条件。很多证明只需要连续性（例如证明积分中值定理的某种推广形式、或处理分段函数的拼接），这时候不必要求 <code>f</code> 连续。\n它同时也是一个反例的温床：存在可积但处处不连续的函数（如 <code>f(x) = 1</code> 当 <code>x</code> 为有理数、<code>0</code> 当 <code>x</code> 为无理数时，它在 <code>[0, 1]</code> 上不可积；而相应的变上限积分在可积的前提下仍然连续）。也就是说，<b>积分是一种比求导更「平滑」的运算</b>：它会抹平被积函数的毛刺。',
          proof: '<b>证明（连续性部分）。</b>设 <code>|f(x)| ≤ K</code>（可积必有界）。对 <code>x, x + Δx ∈ [a, b]</code>，由区间可加性\n<code>|Φ(x + Δx) - Φ(x)| = |∫ₓ^{x+Δx} f(t)dt| ≤ |∫ₓ^{x+Δx} |f(t)|dt| ≤ K|Δx|</code>。\n（中间一步用了定积分的绝对值不等式与保号性。）\n令 <code>Δx → 0</code>，右端趋于 0，故 <code>|Φ(x + Δx) - Φ(x)| → 0</code>，即 <code>Φ</code> 在 <code>x</code> 处连续，由 <code>x</code> 任意性得 <code>Φ</code> 在 <code>[a, b]</code> 上连续。\n<b>证明（可导部分）。</b>与变上限积分求导定理的证明完全相同：\n先由区间可加性得 <code>ΔΦ = ∫ₓ^{x+Δx} f(t)dt</code>；再由 <code>f</code> 在小区间上连续、用积分中值定理得 <code>ΔΦ = f(ξ)Δx</code>，其中 <code>ξ</code> 介于 <code>x</code> 与 <code>x + Δx</code> 之间；于是 <code>ΔΦ/Δx = f(ξ)</code>，令 <code>Δx → 0</code> 得 <code>ξ → x</code>，再由 <code>f</code> 连续得 <code>f(ξ) → f(x)</code>，故 <code>Φ′(x) = f(x)</code>。',
          example: '<b>可积但不连续的例子：</b>设 <code>f(t) = 0</code>（<code>t ≠ 1</code>）、<code>f(1) = 5</code>。它在 <code>[0, 2]</code> 上可积（只有一个间断点）。则\n<code>Φ(x) = ∫₀ˣ f(t)dt = 0</code> 对一切 <code>x</code> 成立——因为那根「针」的面积是 0。\n<code>Φ ≡ 0</code> 当然连续；但 <code>Φ′(1) = 0 ≠ 5 = f(1)</code>，说明没有连续性时「<code>Φ′ = f</code>」不成立。\n<b>连续情形：</b><code>f(t) = |t|</code> 在 <code>[-1, 1]</code> 上连续，<code>Φ(x) = ∫₋₁ˣ |t|dt</code>，则 <code>Φ′(x) = |x|</code>，符合定理。',
          pitfalls: [
            '以为「变上限积分一定可导」。连续性只保证 <code>Φ</code> 连续；可导需要 <code>f</code> 本身连续。',
            '在 <code>f</code> 只在个别点被改动的情形下仍然坚称 <code>Φ′ = f</code> 处处成立。积分对个别点的改动「看不见」，但导数看得见。',
            '把这一条当成新的定理去重复证明。它的证明就是前面两条定理的结论合起来。',
          ],
          tags: ['变上限积分', '连续性', '可导性', '可积'],
          related: ['thm-variable-upper-derivative', 'def-variable-upper-integral', 'prop-integral-abs-inequality'],
        },
      ],
    },
    {
      id: 'ch5-3',
      no: '5.3',
      title: '定积分的换元法和分部积分法',
      summary: '把不定积分里的两套技术搬到定积分上，并说清定积分换元必须同步换上下限、算完不必回代的道理。',
      items: [
        {
          id: 'thm-integral-substitution',
          kind: 'theorem',
          name: '定积分的换元公式',
          statement: '设函数 <code>f(x)</code> 在 <code>[a, b]</code> 上连续；函数 <code>x = φ(t)</code> 在 <code>[α, β]</code> 上有连续导数，且当 <code>t</code> 从 <code>α</code> 变到 <code>β</code> 时，<code>φ(t)</code> 从 <code>a</code> 变到 <code>b</code>，即 <code>φ(α) = a</code>、<code>φ(β) = b</code>，且 <code>φ</code> 的值域包含于 <code>[a, b]</code>。则\n<code>∫ₐᵇ f(x)dx = ∫_α^β f(φ(t))φ′(t)dt</code>。',
          plain: '换元就是把题目换个「变量的说法」，让被积函数变简单。\n关键差别：不定积分换完元，算出答案后必须把 <code>t</code> 换回 <code>x</code>；定积分不用，因为上下限也跟着换成 <code>t</code> 的两个端点了，算完直接就是一个数。\n所以口诀是：<b>换元就换限，换限不回代。</b>新下限对应旧下限，新上限对应旧上限，一一配对。',
          why: '为什么可以「只换限不回代」？因为定积分只关心结果那个数，而换元前后的两个积分表示的是同一个数，不是同一个函数。不定积分求的是一族函数，必须回到原变量才算回答完毕。\n从证明的角度看，最省力的办法是「两边分别求导」：把新积分看成关于上限 <code>β</code> 的变上限积分，用复合函数求导得到 <code>f(φ(β))φ′(β)</code>；而旧积分对 <code>b</code> 求导是 <code>f(b)</code>，再由 <code>b = φ(β)</code> 知道两者相等。两个函数导数相同、在 <code>β = α</code> 处值都为 0，所以它们恒等。这套「构造变上限积分再求导」的技巧是证明积分恒等式的标准武器。',
          proof: '<b>证明。</b>由条件，<code>f(φ(t))φ′(t)</code> 在 <code>[α, β]</code> 上连续，两个积分都存在。\n<b>第一步：把 <code>b</code> 看成变量。</b>对任意 <code>u ∈ [α, β]</code>，令 <code>b_u = φ(u)</code>，构造两个变上限积分\n<code>P(u) = ∫ₐ^{φ(u)} f(x)dx，Q(u) = ∫_α^u f(φ(t))φ′(t)dt</code>。\n要证的是 <code>P(β) = Q(β)</code>。\n<b>第二步：分别求导。</b>\n对 <code>P</code> 用变上限积分求导定理（外层），再用复合函数求导（内层）：\n<code>P′(u) = f(φ(u))·φ′(u)</code>。\n对 <code>Q</code> 直接用变上限积分求导定理：<code>Q′(u) = f(φ(u))φ′(u)</code>。\n<b>第三步：两个原函数只差常数。</b>\n<code>(P - Q)′(u) = 0</code> 在 <code>[α, β]</code> 上恒成立，由拉格朗日中值定理的推论，<code>P(u) - Q(u) ≡ C</code>。\n<b>第四步：用 <code>u = α</code> 定出 <code>C</code>。</b>\n<code>P(α) = ∫ₐ^{φ(α)} f(x)dx = ∫ₐ^{a} f(x)dx = 0</code>，<code>Q(α) = ∫_α^α f(φ(t))φ′(t)dt = 0</code>，\n故 <code>C = 0 - 0 = 0</code>，即 <code>P(u) ≡ Q(u)</code> 在整个 <code>[α, β]</code> 上成立。\n<b>第五步：取 <code>u = β</code>。</b>\n<code>∫ₐᵇ f(x)dx = P(β) = Q(β) = ∫_α^β f(φ(t))φ′(t)dt</code>。证毕。',
          example: '<b>根式代换：</b>求 <code>∫₀⁴ dx/(1 + √x)</code>。令 <code>t = √x</code>，则 <code>x = t²</code>、<code>dx = 2t dt</code>；<code>x = 0</code> 时 <code>t = 0</code>，<code>x = 4</code> 时 <code>t = 2</code>。于是\n<code>∫₀⁴ dx/(1+√x) = ∫₀² 2t/(1+t)dt = 2∫₀² (1 - 1/(1+t))dt = 2[t - ln(1+t)]₀² = 2(2 - ln 3) ≈ 1.803</code>。\n这里用的是「先约分再积分」，最后代入的是 <code>t</code> 的上下限，完全不用把 <code>t</code> 换回 <code>x</code>。\n<b>三角代换：</b>求 <code>∫₀^{π/2} cos³x dx</code>。令 <code>t = sin x</code>，<code>dt = cos x dx</code>；<code>x = 0 → t = 0</code>，<code>x = π/2 → t = 1</code>。于是\n<code>∫₀^{π/2} cos³x dx = ∫₀^{π/2} (1 - sin²x)cos x dx = ∫₀¹ (1 - t²)dt = [t - t³/3]₀¹ = 1 - 1/3 = 2/3</code>。',
          pitfalls: [
            '换元后忘记同步换上下限，仍然代 <code>a</code>、<code>b</code>。这是本章最高频的错误，务必每写一次代换就立刻在旁边写下新的两个端点值。',
            '把 <code>dx = φ′(t)dt</code> 写成 <code>dx = φ′(t)</code>，丢掉 <code>dt</code>。微分符号不能省。',
            '代换 <code>x = φ(t)</code> 不满足「值域落在 <code>[a, b]</code> 内」或 <code>φ′</code> 不连续，却仍然套公式。例如在 <code>x = 0</code> 处取 <code>x = t²</code> 去处理含 <code>1/x</code> 的积分，会踩到瑕点。',
            '用 <code>t = √x</code> 这类代换时忽略 <code>t</code> 的取值范围。例如 <code>∫_{-1}^{1}</code> 上取 <code>t = x²</code>，会有两个 <code>t</code> 对应同一个 <code>x</code>，必须把区间拆开再做。',
            '算完仍然画蛇添足地把结果换成 <code>x</code> 的表达式，或者把 <code>t</code> 的上下限与 <code>x</code> 的混用。要明确：<b>要么两边都是 <code>x</code>，要么两边都是 <code>t</code></b>。',
          ],
          tags: ['换元法', '定积分计算', '上下限'],
          related: ['prop-integral-even-odd', 'thm-integral-by-parts', 'thm-newton-leibniz'],
        },
        {
          id: 'thm-integral-by-parts',
          kind: 'theorem',
          name: '定积分的分部积分公式',
          statement: '设 <code>u(x)</code>、<code>v(x)</code> 在 <code>[a, b]</code> 上有连续导数，则\n<code>∫ₐᵇ u dv = [uv]ₐᵇ - ∫ₐᵇ v du</code>，\n写成完整形式即\n<code>∫ₐᵇ u(x)v′(x)dx = [u(x)v(x)]ₐᵇ - ∫ₐᵇ u′(x)v(x)dx</code>，其中 <code>[u(x)v(x)]ₐᵇ = u(b)v(b) - u(a)v(a)</code>。',
          plain: '分部积分是「把难缠的部分甩给对方去处理」的技巧。它和不定积分的分部积分长得几乎一样，只是每一项都在 <code>a</code>、<code>b</code> 上取值后相减。\n口诀还是那句：<b>反对幂指三</b>——反三角、对数、幂函数、指数、三角，谁在前面谁当 <code>u</code>，剩下的当 <code>dv</code>。\n为什么要这样排？因为 <code>u</code> 是要被拿去求导的，排在前面的一般求导后更简单（比如 <code>ln x</code> 求导变成 <code>1/x</code>；<code>x²</code> 求导降一次），而 <code>dv</code> 要被积分，排在后面的一般积分后不会更复杂。',
          why: '定积分的分部积分公式不是新东西，它是<b>不定积分的分部积分公式配一次牛顿-莱布尼茨公式</b>。理解这一点很重要：<b>所有定积分技巧都是「先在不定积分里算出原函数、再代上下限」的包装</b>。\n证明只需两步：对乘积求导法则 <code>(uv)′ = u′v + uv′</code> 在 <code>[a, b]</code> 上积分，左边用牛顿-莱布尼茨公式搬出 <code>[uv]ₐᵇ</code>，右边拆成两个积分，移项即得。这条推导顺手说明了分部积分的本质：它是<b>乘积求导公式的积分版本</b>。',
          proof: '<b>证明。</b>\n<b>第一步：写出乘积求导法则。</b>因为 <code>u</code>、<code>v</code> 都有连续导数，乘积 <code>uv</code> 的导数为\n<code>[u(x)v(x)]′ = u′(x)v(x) + u(x)v′(x)，x ∈ [a, b]</code>。\n<b>第二步：在 <code>[a, b]</code> 上积分。</b>两边都是连续函数，可积，逐项积分（线性性质）：\n<code>∫ₐᵇ [u(x)v(x)]′dx = ∫ₐᵇ u′(x)v(x)dx + ∫ₐᵇ u(x)v′(x)dx</code>。\n<b>第三步：左端用牛顿-莱布尼茨公式。</b><code>[u(x)v(x)]′</code> 的原函数就是 <code>u(x)v(x)</code>，故\n<code>∫ₐᵇ [u(x)v(x)]′dx = [u(x)v(x)]ₐᵇ = u(b)v(b) - u(a)v(a)</code>。\n<b>第四步：移项。</b>把第二个积分搬到左边：\n<code>∫ₐᵇ u(x)v′(x)dx = [u(x)v(x)]ₐᵇ - ∫ₐᵇ u′(x)v(x)dx</code>。\n写成微分形式 <code>du = u′(x)dx</code>、<code>dv = v′(x)dx</code>，就是 <code>∫ₐᵇ u dv = [uv]ₐᵇ - ∫ₐᵇ v du</code>。证毕。',
          example: '<b>幂 × 指数：</b>求 <code>∫₀¹ x eˣ dx</code>。取 <code>u = x</code>、<code>dv = eˣdx</code>，则 <code>du = dx</code>、<code>v = eˣ</code>：\n<code>∫₀¹ x eˣ dx = [x eˣ]₀¹ - ∫₀¹ eˣ dx = (1·e - 0) - [eˣ]₀¹ = e - (e - 1) = 1</code>。\n<b>幂 × 三角（用两次）：</b><code>∫₀^{π} x sin x dx</code>。取 <code>u = x</code>、<code>dv = sin x dx</code>，则 <code>v = -cos x</code>：\n<code>∫₀^{π} x sin x dx = [-x cos x]₀^{π} + ∫₀^{π} cos x dx = (-π·(-1) - 0) + [sin x]₀^{π} = π + 0 = π</code>。\n<b>对数函数：</b><code>∫₁^e ln x dx</code>。取 <code>u = ln x</code>、<code>dv = dx</code>，则 <code>du = dx/x</code>、<code>v = x</code>：\n<code>∫₁^e ln x dx = [x ln x]₁^e - ∫₁^e 1 dx = (e·1 - 0) - (e - 1) = 1</code>。',
          pitfalls: [
            '忘记 <code>[uv]ₐᵇ</code> 这一项，只留下 <code>-∫v du</code>。分部积分是「两项」，缺一不可。',
            '把 <code>[uv]ₐᵇ</code> 代成 <code>u(a)v(a) - u(b)v(b)</code>，符号搞反。',
            '选反了 <code>u</code> 和 <code>dv</code>，导致第二个积分比原来更难。例如对 <code>∫x eˣdx</code> 取 <code>u = eˣ</code>、<code>dv = x dx</code>，会得到含 <code>x²</code> 的更难积分。',
            '需要分部两次时中途丢项或提前代限。建议先把不定积分部分整理完，最后一次性代上下限。',
            '在 <code>∫₀^{π} x sin x dx</code> 这类题目上忘记 <code>v = -cos x</code> 的负号，导致结果符号错。',
          ],
          tags: ['分部积分', '定积分计算'],
          related: ['thm-integral-substitution', 'thm-newton-leibniz', 'def-gamma-function'],
        },
      ],
    },
    {
      id: 'ch5-4',
      no: '5.4',
      title: '反常积分',
      summary: '把积分区间放宽到无穷、把被积函数放宽到无界，用极限来定义这两类新的积分，并给出它们收敛与发散的判别标准。',
      items: [
        {
          id: 'def-improper-infinite',
          kind: 'definition',
          name: '无穷限反常积分（无穷积分）',
          statement: '设 <code>f(x)</code> 在 <code>[a, +∞)</code> 上有定义，且对任意 <code>b &gt; a</code>，<code>f</code> 在 <code>[a, b]</code> 上可积。若极限\n<code>lim<sub>b→+∞</sub> ∫ₐᵇ f(x)dx</code>\n存在（为有限数），则称反常积分 <code>∫ₐ^{+∞} f(x)dx</code> <b>收敛</b>，并把这个极限值规定为该反常积分的值；否则称它<b>发散</b>。\n类似地定义：\n<code>∫_{-∞}^{b} f(x)dx = lim<sub>a→-∞</sub> ∫ₐᵇ f(x)dx</code>；\n<code>∫_{-∞}^{+∞} f(x)dx = ∫_{-∞}^{c} f(x)dx + ∫_{c}^{+∞} f(x)dx</code>（<code>c</code> 任意），当且仅当右边两个积分<b>都收敛</b>时左边的积分才收敛。',
          plain: '普通定积分要求区间是有限的一段。可现实里常常要算「一直算下去」的量：从今天到永远的收益、从地球表面一直延伸到太空的引力。\n办法很朴素：<b>先只算到 <code>b</code> 为止</b>，得到一个普通数；然后问「当 <code>b</code> 越来越大时，这个数会不会稳定下来」。会，就叫收敛，极限值就是答案；不会（越来越大或来回晃），就叫发散。\n所以「反常」并不意味着无法处理，只是说这个积分是「用极限」而不是「用一条有限区间」定义的。',
          why: '为什么不能直接把 <code>+∞</code> 代进牛顿-莱布尼茨公式了事？因为牛顿-莱布尼茨公式的前提是 <code>f</code> 在<b>闭区间</b> <code>[a, b]</code> 上连续，而 <code>[a, +∞)</code> 不是闭区间，「<code>F(+∞)</code>」也没有定义。\n所以必须先用有限区间算出 <code>F(b) - F(a)</code>，再对 <code>b</code> 取极限。写的时候 <code>[F(x)]ₐ^{+∞}</code> 只是一种<b>简写</b>，它的实际含义是 <code>lim<sub>b→∞</sub>[F(x)]ₐ^b</code>。\n同样的理由：<code>∫_{-∞}^{+∞}</code> 必须拆成两段分别算，不能在同一个极限里让两端一起跑——否则会出现「先跑一边再跑另一边」的假答案（见下面陷阱里的 <code>sin x</code> 例子）。',
          example: '<b>收敛的例子：</b><code>∫₁^{+∞} dx/x² = lim<sub>b→+∞</sub> ∫₁^b x^{-2}dx = lim<sub>b→+∞</sub> [-1/x]₁^b = lim<sub>b→+∞</sub> (1 - 1/b) = 1</code>。极限是有限的 <code>1</code>，故收敛，值就是 <code>1</code>。\n<b>发散的例子：</b><code>∫₁^{+∞} dx/x = lim<sub>b→+∞</sub> [ln x]₁^b = lim<sub>b→+∞</sub> ln b = +∞</code>，极限不存在（无限增大），故发散。\n<b>整个实轴：</b><code>∫_{-∞}^{+∞} dx/(1+x²) = [arctan x]_{-∞}^{+∞} = π/2 - (-π/2) = π</code>。严格写应拆成 <code>∫_{-∞}^{0} + ∫₀^{+∞}</code>，两个极限分别是 <code>π/2</code> 和 <code>π/2</code>，都收敛，故总积分收敛于 <code>π</code>。\n[[warn:如果写成 <code>lim<sub>b→+∞</sub> ∫_{-b}^{b} dx/(1+x²)</code>，那是另一种规定（柯西主值），一般不等于反常积分的值。做题时按定义拆开算。]]',
          pitfalls: [
            '直接用 <code>[F(x)]ₐ^{+∞} = F(+∞) - F(a)</code> 而不交代这是极限的简写，遇到振荡函数（如 <code>∫₀^{+∞} sin x dx</code>）就会把发散当成收敛。',
            '把 <code>∫_{-∞}^{+∞}</code> 写成 <code>lim<sub>b→+∞</sub>∫_{-b}^{b}</code>。按定义要两段独立取极限，例如 <code>sin x</code> 在 <code>(-∞, +∞)</code> 上的这个对称极限是 0，但按定义它是发散的。',
            '漏掉「极限是有限数」这一句。极限为 <code>+∞</code> 属于发散，不能算收敛。',
            '把一个收敛积分与一个发散积分相加，然后说「结果发散」却不拆开验证。按定义，<code>∫(f+g)</code> 与 <code>∫f</code>、<code>∫g</code> 的敛散性之间的关系需要逐个判断，不能想当然。',
          ],
          tags: ['反常积分', '无穷限', '收敛', '发散'],
          related: ['def-improper-unbounded', 'thm-p-integral-infinite', 'thm-comparison-infinite', 'thm-absolute-convergence'],
        },
        {
          id: 'thm-p-integral-infinite',
          kind: 'theorem',
          name: '无穷限 p 积分的敛散性',
          statement: '对 <code>p</code> 为常数，反常积分\n<code>∫₁^{+∞} dx/x^p</code>\n当 <code>p &gt; 1</code> 时<b>收敛</b>，且 <code>∫₁^{+∞} dx/x^p = 1/(p - 1)</code>；\n当 <code>p ≤ 1</code> 时<b>发散</b>。',
          plain: '这是一条可以背下来当「尺子」用的结论：<b>次数比 1 大就收敛，小于等于 1 就发散。</b>\n意思是：函数往右跑时衰减得<b>足够快</b>，尾巴上的面积才收得住。<code>1/x²</code>、<code>1/x^{1.5}</code> 都收得住；<code>1/x</code> 就恰好差一点点，收不住；<code>1/√x</code> 衰减太慢，更收不住。\n有意思的是 <code>p = 1</code> 这条分界线：<code>1/x</code> 每往右走 10 倍，函数值只降到十分之一，面积还在缓慢地累积，永远积不完。',
          why: '这条结论是 5.5 节比较审敛法的<b>标准尺子</b>。判断一个复杂的无穷积分是否收敛时，我们通常只关心被积函数在 <code>x → +∞</code> 时的「主项」像不像 <code>1/x^p</code>，然后借用这条公式给出答案。\n为什么分界点恰好在 <code>p = 1</code>？看证明就很清楚：积分 <code>∫₁^b x^{-p}dx</code> 等于 <code>(b^{1-p} - 1)/(1 - p)</code>，收敛与否取决于 <code>b^{1-p}</code> 在 <code>b → +∞</code> 时是否趋于 0，而这只在 <code>1 - p &lt; 0</code>（即 <code>p &gt; 1</code>）时发生。<code>p = 1</code> 时换成了对数 <code>ln b</code>，它无界地增长，所以发散。',
          proof: '<b>证明。</b>按定义，先算有限区间上的定积分，再取极限。分三种情况。\n<b>（i）<code>p ≠ 1</code>。</b>\n<code>∫₁^b x^{-p}dx = [x^{1-p}/(1-p)]₁^b = (b^{1-p} - 1)/(1 - p)</code>。\n此时敛散性由 <code>b^{1-p}</code> 当 <code>b → +∞</code> 时的行为决定：\n若 <code>p &gt; 1</code>，则 <code>1 - p &lt; 0</code>，故 <code>b^{1-p} = 1/b^{p-1} → 0</code>，于是原式趋于 <code>(0 - 1)/(1 - p) = 1/(p - 1)</code>，极限为有限数，积分收敛，值为 <code>1/(p-1)</code>。\n若 <code>p &lt; 1</code>，则 <code>1 - p &gt; 0</code>，故 <code>b^{1-p} → +∞</code>，原式也趋于 <code>+∞</code>，极限不是有限数，积分发散。\n<b>（ii）<code>p = 1</code>。</b>\n<code>∫₁^b dx/x = [ln x]₁^b = ln b → +∞</code>（<code>b → +∞</code>），故发散。\n<b>（iii）合并。</b>（i）（ii）合起来即：<code>p &gt; 1</code> 时收敛，<code>p ≤ 1</code> 时发散。证毕。',
          example: '<code>∫₁^{+∞} dx/x² = 1/(2-1) = 1</code>：<code>p = 2 &gt; 1</code>，收敛（前面算过，吻合）。\n<code>∫₁^{+∞} dx/x^{3} = 1/(3-1) = 1/2</code>：收敛。\n<code>∫₁^{+∞} dx/x = +∞</code>：<code>p = 1</code>，发散。\n<code>∫₁^{+∞} dx/√x = lim<sub>b→∞</sub> 2√b - 2 = +∞</code>：<code>p = 1/2 ≤ 1</code>，发散。\n<code>∫₁^{+∞} dx/x^{1.0001}</code>：<code>p = 1.0001 &gt; 1</code>，收敛！虽然「刚刚超过 1」，但收敛性只关乎是否超过，与超过多少无关。\n[[tip:注意下限是 <code>1</code>（正数）。若下限是 <code>0</code>，另一端会冒出瑕点，须与瑕积分的 p 判别法配合使用。]]',
          pitfalls: [
            '把结论记成「<code>p &gt; 0</code> 收敛」。分界点是 <code>1</code>，不是 <code>0</code>。',
            '把无穷限的结论直接搬到瑕积分上。瑕积分的分界方向<b>相反</b>：<code>∫₀¹ dx/x^p</code> 在 <code>p &lt; 1</code> 时收敛。',
            '用这条定理处理 <code>∫₀^{+∞}</code> 而不检查 <code>x = 0</code> 是否为瑕点。区间两端都要照顾到，收敛必须两端都收敛。',
            '认为「函数趋于 0 就收敛」。<code>1/x</code> 也趋于 0，但积分发散。趋于 0 的速度才是关键。',
          ],
          tags: ['反常积分', 'p 积分', '收敛判别', '标准尺子'],
          related: ['def-improper-infinite', 'thm-comparison-infinite', 'thm-limit-comparison', 'thm-p-integral-unbounded'],
        },
        {
          id: 'def-improper-unbounded',
          kind: 'definition',
          name: '无界函数的反常积分（瑕积分）',
          aka: ['瑕积分'],
          statement: '设 <code>f(x)</code> 在点 <code>a</code> 的任一右邻域内无界，而在任意 <code>[a + ε, b]</code>（<code>ε &gt; 0</code>）上可积，则称 <code>a</code> 为 <b>瑕点</b>，并定义\n<code>∫ₐᵇ f(x)dx = lim<sub>ε→0⁺</sub> ∫_{a+ε}^{b} f(x)dx</code>；\n若右端极限存在且为有限数，称此瑕积分<b>收敛</b>，否则称<b>发散</b>。\n<b>瑕点在右端点 <code>b</code>：</b><code>∫ₐᵇ f(x)dx = lim<sub>ε→0⁺</sub> ∫ₐ^{b-ε} f(x)dx</code>。\n<b>瑕点在内部 <code>c ∈ (a, b)</code>：</b>必须拆成两段\n<code>∫ₐᵇ f(x)dx = ∫ₐᶜ f(x)dx + ∫꜀ᵇ f(x)dx</code>，\n当且仅当<b>两段都收敛</b>时，原瑕积分才收敛（右端两个极限必须分别存在）。',
          plain: '有的函数在某个点附近「冲上天」：比如 <code>1/√x</code> 在 <code>x → 0⁺</code> 时越来越大，根本没有上界。这种点叫瑕点，像是图形上的一道「悬崖」。\n处理办法和无穷限一模一样：<b>从悬崖边上退开一小步 <code>ε</code></b>，先算一段正常的有限积分；再让 <code>ε</code> 趋于 0，看这个数是否稳定下来。稳定就收敛，不稳定就发散。\n所以「无穷限」和「瑕积分」本质上是同一招：<b>先把危险的地方隔开，算出普通积分，再取极限</b>。区别只是危险发生在「很远很远」还是「很近很近」。',
          why: '为什么不能直接用牛顿-莱布尼茨公式？因为公式要求 <code>f</code> 在闭区间 <code>[a, b]</code> 上连续，而瑕点处 <code>f</code> 根本取不到有限值，原函数在那里可能也没有定义（例如 <code>∫dx/x</code> 的原函数 <code>ln|x|</code> 在 <code>0</code> 处无定义）。\n所以判断瑕积分<b>必须先找出瑕点</b>，再用极限绕过去。最危险的做法是「假装没看见瑕点，直接代上下限」——这会算出完全错误的答案（看下面陷阱中的例子）。\n还有一条容易忽略的规矩：如果瑕点在区间<b>内部</b>，必须拆成两段分别取极限。不能像对称主值那样让两边同时靠近，因为那样可能掩盖真正的发散。',
          example: '<b>收敛：</b><code>∫₀¹ dx/√x</code> 在 <code>x = 0</code> 处有瑕点。按定义：\n<code>lim<sub>ε→0⁺</sub> ∫_ε^{1} x^{-1/2}dx = lim<sub>ε→0⁺</sub> [2√x]_ε^{1} = lim<sub>ε→0⁺</sub> (2 - 2√ε) = 2</code>。极限是有限数，故收敛，值为 <code>2</code>。\n<b>发散：</b><code>∫₀¹ dx/x</code> 在 <code>x = 0</code> 处有瑕点：\n<code>lim<sub>ε→0⁺</sub> [ln x]_ε^{1} = lim<sub>ε→0⁺</sub> (0 - ln ε) = +∞</code>，故发散。\n<b>瑕点在内部：</b><code>∫_{-1}^{1} dx/x²</code> 在 <code>x = 0</code> 处有瑕点，须拆成 <code>∫_{-1}^{0} + ∫₀^{1}</code>。<code>∫₀^{1} dx/x² = lim<sub>ε→0⁺</sub>[-1/x]_ε^1 = lim(1/ε - 1) = +∞</code>，已经发散，故整个积分发散。\n[[warn:若错误地直接代 <code>[-1/x]_{-1}^{1} = -2</code>，会得到「负数」，而 <code>1/x² &gt; 0</code> 恒正，这个结果明显荒谬——这正是「没检查瑕点就用牛顿-莱布尼茨公式」的典型翻车现场。]]',
          pitfalls: [
            '不检查瑕点直接套牛顿-莱布尼茨公式。<code>∫_{-1}^{1} dx/x²</code> 是这个错误的经典标本。',
            '瑕点在区间内部时只写一个极限，比如 <code>lim<sub>ε→0</sub>(∫_{-1}^{-ε} + ∫_ε^{1})</code>。按定义两段必须各自收敛，这个对称写法可能给出「0」这类假答案。',
            '忘记写 <code>ε → 0⁺</code>（只在右边靠近）。瑕点从哪一侧靠近要看瑕点在端点还是内部。',
            '把瑕积分和无穷限积分的 p 判据搞混。<code>∫₀¹ dx/x^p</code> 收敛条件是 <code>p &lt; 1</code>，与 <code>∫₁^{∞} dx/x^p</code> 的 <code>p &gt; 1</code> 正好相反。',
            '只找到一个瑕点就以为万事大吉。有的题目在区间两端甚至内部都有瑕点，必须逐个查。',
          ],
          tags: ['反常积分', '瑕积分', '瑕点', '无界函数'],
          related: ['def-improper-infinite', 'thm-p-integral-unbounded', 'def-definite-integral', 'thm-comparison-unbounded'],
        },
        {
          id: 'thm-p-integral-unbounded',
          kind: 'theorem',
          name: '瑕积分的 p 判别法',
          statement: '设 <code>a</code> 为瑕点，则反常积分\n<code>∫ₐᵇ dx/(x - a)^p</code>\n当 <code>p &lt; 1</code> 时<b>收敛</b>（<code>p ≤ 0</code> 时瑕点消失，退化为普通定积分）；当 <code>p ≥ 1</code> 时<b>发散</b>。\n常见形式：<code>∫₀¹ dx/x^p</code> 当 <code>p &lt; 1</code> 时收敛于 <code>1/(1 - p)</code>，当 <code>p ≥ 1</code> 时发散。',
          plain: '这是瑕积分里那把「标准尺子」，和无穷限那把正好<b>相反</b>：<b>次数比 1 小才收敛，大于等于 1 就发散。</b>\n为什么方向相反？想想左端的悬崖。<code>1/x^p</code> 在 <code>x → 0⁺</code> 时冲上天的速度取决于 <code>p</code>：<code>p</code> 越大冲得越猛。<code>1/√x</code>（<code>p = 1/2</code>）虽然也冲，但冲得慢，悬崖下的面积竟然还是有限的（等于 2）；<code>1/x</code>（<code>p = 1</code>）恰好冲得太快，面积就无穷大了。\n一句话对齐记忆：<b>无穷远处要衰减快（<code>p &gt; 1</code>），悬崖附近要上升慢（<code>p &lt; 1</code>）。</b>',
          why: '这条结论与无穷限的 p 积分是同一套计算的两张面孔。把 <code>p</code> 积分的计算过程写清楚，就能看出分界为什么在 <code>p = 1</code>，以及为什么两次的方向不同。\n它的作用同样是当「尺子」：判断复杂瑕积分时，先看瑕点处被积函数「像不像」<code>1/(x-a)^p</code>，再套这条结论。这也是为什么它必须和比较审敛法配合使用。',
          proof: '<b>证明（以瑕点在左端点 <code>x = 0</code>、区间 <code>(0, 1]</code> 为例）。</b>按定义先算有限积分再取极限。\n<b>（i）<code>p ≠ 1</code>。</b>对 <code>0 &lt; ε &lt; 1</code>，\n<code>∫_ε^{1} x^{-p}dx = [x^{1-p}/(1-p)]_ε^{1} = (1 - ε^{1-p})/(1 - p)</code>。\n敛散性由 <code>ε^{1-p}</code> 在 <code>ε → 0⁺</code> 时的行为决定：\n若 <code>p &lt; 1</code>，则 <code>1 - p &gt; 0</code>，故 <code>ε^{1-p} → 0</code>，原式趋于 <code>1/(1 - p)</code>，极限为有限数，收敛。\n若 <code>p &gt; 1</code>，则 <code>1 - p &lt; 0</code>，故 <code>ε^{1-p} = 1/ε^{p-1} → +∞</code>，原式趋于 <code>+∞</code>，发散。\n<b>（ii）<code>p = 1</code>。</b><code>∫_ε^{1} dx/x = [ln x]_ε^{1} = 0 - ln ε = -ln ε → +∞</code>（<code>ε → 0⁺</code>），发散。\n<b>（iii）合并。</b><code>p &lt; 1</code> 时收敛于 <code>1/(1-p)</code>，<code>p ≥ 1</code> 时发散。对一般瑕点 <code>a</code>，作平移 <code>x - a = u</code> 即化归本情形。证毕。',
          example: '<code>∫₀¹ dx/√x = 2</code>：<code>p = 1/2 &lt; 1</code>，收敛（前面算过，吻合）。\n<code>∫₀¹ dx/x^{2/3} = 1/(1 - 2/3) = 3</code>：<code>p = 2/3 &lt; 1</code>，收敛。\n<code>∫₀¹ dx/x = +∞</code>：<code>p = 1</code>，发散。\n<code>∫₀¹ dx/x² = +∞</code>：<code>p = 2 &gt; 1</code>，发散。\n<code>∫₀¹ dx/∛x</code>：<code>p = 1/3 &lt; 1</code>，收敛于 <code>1/(1 - 1/3) = 3/2</code>。\n<b>对照记忆：</b><code>∫₁^{+∞} dx/x² = 1</code> 收敛，而 <code>∫₀¹ dx/x² = +∞</code> 发散——同一个函数 <code>1/x²</code>，在无穷远处乖、在原点附近凶。',
          pitfalls: [
            '把两个 p 判别法的方向记混。建议固定记忆：「无穷远用 <code>p &gt; 1</code>，瑕点用 <code>p &lt; 1</code>」。',
            '忘记瑕点位置，直接用 <code>∫₀¹</code> 的形式。若瑕点在 <code>x = 1</code>，要先作代换把瑕点挪到左端，或直接按右端瑕点的极限定义算。',
            '把 <code>p = 0</code> 也算成反常积分。<code>p = 0</code> 时被积函数是常数 1，没有瑕点，是普通定积分。',
            '在含多个瑕点或同时含无穷限的积分上只用一条判据。必须分段，每段都收敛才整体收敛。',
          ],
          tags: ['反常积分', '瑕积分', 'p 判别法', '标准尺子'],
          related: ['def-improper-unbounded', 'thm-p-integral-infinite', 'thm-comparison-unbounded', 'thm-limit-comparison'],
        },
      ],
    },
    {
      id: 'ch5-5',
      no: '5.5',
      title: '反常积分的审敛法　Γ 函数',
      summary: '用比较的方法判断反常积分是否收敛，并介绍由反常积分定义的 Γ 函数以及它把阶乘推广到正实数的递推公式。',
      items: [
        {
          id: 'thm-comparison-infinite',
          kind: 'theorem',
          name: '无穷限反常积分的比较审敛法',
          statement: '设 <code>f(x)</code>、<code>g(x)</code> 在 <code>[a, +∞)</code> 上连续，且 <code>0 ≤ f(x) ≤ g(x)</code>（<code>x ≥ a</code>）。则：\n<b>（1）</b>若 <code>∫ₐ^{+∞} g(x)dx</code> 收敛，则 <code>∫ₐ^{+∞} f(x)dx</code> 也收敛，且 <code>∫ₐ^{+∞} f(x)dx ≤ ∫ₐ^{+∞} g(x)dx</code>；\n<b>（2）</b>若 <code>∫ₐ^{+∞} f(x)dx</code> 发散，则 <code>∫ₐ^{+∞} g(x)dx</code> 也发散。',
          plain: '比较审敛法就是把「不知道的积分」和「知道的积分」比大小。\n如果被积的函数比一个<b>收敛</b>的函数还小，那它自己的面积当然也收得住 —— 收敛。\n如果被积的函数比一个<b>发散</b>的函数还大，那它自己的面积只会更大，当然也收不住 —— 发散。\n记忆可以概括成：<b>大的收敛，小的必收敛；小的发散，大的必发散。</b>另外两个方向（大的发散、小的收敛）什么也推不出来，因为大的那部分信息不够。\n用的时候通常拿 <code>1/x^p</code> 当比较对象，因为它的敛散性我们已经背下来了。',
          why: '比较审敛法的证明思想是<b>单调有界原理</b>。定义 <code>F(b) = ∫ₐᵇ f(x)dx</code>，因为 <code>f ≥ 0</code>，<code>F</code> 随 <code>b</code> 单调不减。而如果 <code>g</code> 收敛，就有一个统一的「天花板」<code>∫ₐ^{+∞}g</code> 压住 <code>F</code>。单调不减又有上界的数列/函数必有极限，于是 <code>F</code> 收敛。\n为什么一定要 <code>f ≥ 0</code>？因为要让 <code>F</code> 单调，才能用「单调有界必收敛」。如果 <code>f</code> 变号，<code>F</code> 会上下晃，即使有界也未必收敛——这正是后面要引入「绝对收敛」的原因。',
          proof: '<b>证明（1）。</b>由 <code>0 ≤ f(x) ≤ g(x)</code> 及保号性，对任意 <code>b &gt; a</code> 有\n<code>0 ≤ ∫ₐᵇ f(x)dx ≤ ∫ₐᵇ g(x)dx</code>。\n<b>第一步：<code>F(b) = ∫ₐᵇ f</code> 单调不减。</b>对 <code>b₂ &gt; b₁ &gt; a</code>，由区间可加性\n<code>F(b₂) - F(b₁) = ∫_{b₁}^{b₂} f(x)dx ≥ 0</code>（保号性，因 <code>f ≥ 0</code>）。\n<b>第二步：它有上界。</b>设 <code>∫ₐ^{+∞} g(x)dx = G</code> 有限，则由上式 <code>F(b) ≤ ∫ₐᵇ g(x)dx ≤ G</code> 对一切 <code>b</code> 成立。<b>第三步：单调有界必收敛。</b>由单调有界原理，<code>lim<sub>b→+∞</sub> F(b)</code> 存在且有限，即 <code>∫ₐ^{+∞} f(x)dx</code> 收敛。\n<b>第四步：不等式。</b>在 <code>∫ₐᵇ f ≤ ∫ₐᵇ g</code> 中令 <code>b → +∞</code>，由极限保号性得 <code>∫ₐ^{+∞} f ≤ ∫ₐ^{+∞} g</code>。\n<b>证明（2）。</b>这是（1）的逆否命题：若 <code>∫ₐ^{+∞} g</code> 收敛，则由（1）知 <code>∫ₐ^{+∞} f</code> 收敛，与已知 <code>∫ₐ^{+∞} f</code> 发散矛盾。故 <code>∫ₐ^{+∞} g</code> 发散。证毕。\n<b>注：对瑕积分，把上述「<code>b → +∞</code>」换为「<code>ε → 0⁺</code>」即可，理由完全一样（此时 <code>F(ε) = ∫_{a+ε}^{b} f</code> 随 <code>ε</code> 减小而单调不减）。</b>',
          example: '<b>判收敛：</b>判断 <code>∫₁^{+∞} dx/(x³ + 1)</code>。因为 <code>0 ≤ 1/(x³ + 1) ≤ 1/x³</code>（<code>x ≥ 1</code>），而 <code>∫₁^{+∞} dx/x³ = 1/2</code> 收敛，所以原积分收敛。\n<b>判发散：</b>判断 <code>∫₁^{+∞} dx/√(x + 1)</code>。因为 <code>1/√(x+1) ≥ 1/√(2x) = (1/√2)·x^{-1/2}</code>（<code>x ≥ 1</code>），而 <code>∫₁^{+∞} x^{-1/2}dx</code> 因 <code>p = 1/2 ≤ 1</code> 发散，所以原积分发散。\n<b>注意不能用的方向：</b><code>1/(x² + 1) ≤ 1/x²</code>，而 <code>1/x²</code> 收敛——这没问题；但如果拿一个<b>收敛</b>的积分去比较一个<b>更大</b>的函数，什么也推不出来。比如 <code>1/x ≤ 2/x</code>，<code>∫2/x</code> 不存在收敛性可言，不能由此说明什么。',
          pitfalls: [
            '搞错方向，用「大发散 ⇒ 小发散」或「小收敛 ⇒ 大收敛」。只有题目里写的两个方向成立。',
            '忽略前提 <code>f ≥ 0</code>。若函数变号，比较审敛法不能直接用，须先讨论绝对收敛。',
            '比较时只在「部分 <code>x</code>」上验证不等式，却忘了无穷积分关心的是 <code>x → +∞</code> 的整体行为。<code>0 ≤ f ≤ g</code> 需要对充分大的 <code>x</code> 成立（有限个点可以例外），但要写清楚。',
            '拿一个敛散性未知的积分当比较对象，论证原地打转。务必用 <code>p</code> 积分、几何级数这类已知结论。',
          ],
          tags: ['反常积分', '比较审敛法', '收敛判别'],
          related: ['prop-integral-sign-preserving', 'thm-p-integral-infinite', 'thm-limit-comparison', 'def-improper-infinite'],
        },
        {
          id: 'thm-comparison-unbounded',
          kind: 'theorem',
          name: '瑕积分的比较审敛法',
          statement: '设 <code>a</code> 为瑕点，<code>f(x)</code>、<code>g(x)</code> 在 <code>(a, b]</code> 上连续，且 <code>0 ≤ f(x) ≤ g(x)</code>（<code>a &lt; x ≤ b</code>）。则：\n<b>（1）</b>若 <code>∫ₐᵇ g(x)dx</code> 收敛，则 <code>∫ₐᵇ f(x)dx</code> 也收敛；\n<b>（2）</b>若 <code>∫ₐᵇ f(x)dx</code> 发散，则 <code>∫ₐᵇ g(x)dx</code> 也发散。',
          plain: '和无穷限的版本是同一句话，只是把「<code>x</code> 跑向无穷」换成了「<code>x</code> 逼近瑕点」。\n在悬崖附近，如果函数的「冲高」比一个已经确认收敛的对手更温柔，它就收敛；如果比一个确认发散的对手更猛，它就发散。\n比较的对手一般取 <code>1/(x-a)^p</code>，按上一节的 p 判别法：<code>p &lt; 1</code> 收敛，<code>p ≥ 1</code> 发散。',
          why: '证明思路与无穷限版本完全平行，只是把极限过程的变量从「区间右端点 <code>b</code>」换成「退让量 <code>ε</code>」。这也提醒我们：<b>无穷限和瑕积分在数学结构上是一回事</b>，都可以通过一个单调变换把对方变成自己（例如 <code>x = 1/t</code> 就把 <code>∫₁^{+∞}</code> 变成 <code>∫₀¹</code> 上的瑕积分）。\n理解了这层对应关系，两条比较审敛法、两条 p 判别法其实只需要记一套。',
          proof: '<b>证明（1）。</b>对 <code>0 &lt; ε &lt; b - a</code>，由 <code>0 ≤ f ≤ g</code> 与保号性得\n<code>0 ≤ ∫_{a+ε}^{b} f(x)dx ≤ ∫_{a+ε}^{b} g(x)dx</code>。\n令 <code>F(ε) = ∫_{a+ε}^{b} f(x)dx</code>。<b>第一步：<code>F</code> 随 <code>ε</code> 减小而单调不减。</b>若 <code>0 &lt; ε₂ &lt; ε₁</code>，则\n<code>F(ε₂) - F(ε₁) = ∫_{a+ε₂}^{a+ε₁} f(x)dx ≥ 0</code>，因为 <code>f ≥ 0</code>。\n<b>第二步：上界。</b>设 <code>∫ₐᵇ g(x)dx = G</code> 收敛（有限），则 <code>F(ε) ≤ ∫_{a+ε}^{b} g(x)dx ≤ G</code> 对一切 <code>ε &gt; 0</code> 成立。\n<b>第三步：单调有界必收敛。</b>当 <code>ε → 0⁺</code> 时 <code>F(ε)</code> 单调不减且有上界 <code>G</code>，故极限存在且有限，即 <code>∫ₐᵇ f(x)dx</code> 收敛。\n<b>证明（2）。</b>逆否命题：若 <code>∫ₐᵇ g</code> 收敛则由（1）得 <code>∫ₐᵇ f</code> 收敛，与已知矛盾。故 <code>∫ₐᵇ g</code> 发散。证毕。',
          example: '<b>判收敛：</b>判断 <code>∫₀¹ dx/(√x + x)</code> 是否收敛。当 <code>x → 0⁺</code> 时 <code>√x + x ~ √x</code>，故 <code>1/(√x + x) ~ 1/√x = x^{-1/2}</code>。严格地：<code>0 ≤ 1/(√x + x) ≤ 1/√x</code>（因 <code>x &gt; 0</code> 时 <code>√x + x ≥ √x</code>）。由 <code>∫₀¹ x^{-1/2}dx = 2</code> 收敛，得原积分收敛。\n<b>判发散：</b>判断 <code>∫₀¹ dx/(x^{3/2} + x^{1/2})</code>。当 <code>x → 0⁺</code> 时分母 <code>~ x^{1/2}</code>，所以被积函数 <code>~ x^{-1/2}</code>……这里要小心：<code>x^{3/2} ≤ x^{1/2}</code>，故 <code>x^{3/2} + x^{1/2} ≤ 2x^{1/2}</code>，从而 <code>1/(x^{3/2}+x^{1/2}) ≥ 1/(2√x) = (1/2)x^{-1/2}</code>，而 <code>∫₀¹ (1/2)x^{-1/2}dx = 1</code> 收敛，这只能说明它「不小于一个收敛的量」——结论什么也得不到！<b>这正是比较审敛法的方向限制。</b>此题须改用极限审敛法或直接计算。',
          pitfalls: [
            '忘记「<code>f</code>、<code>g</code> 都不小于 0」。瑕积分里被积函数可能在靠近瑕点时变号（如 <code>sin(1/x)/x</code>），必须先讨论绝对值。',
            '比较的对象选错量级，导致不等式方向不利于判断。例如想证发散，却找出一个「更大且收敛」的对手——这什么也证明不了。',
            '只比较靠近瑕点的主项而忽略系数的严格性，直接写「<code>~</code>」就下结论。当题目要求严格证明时，要写成明确的不等式并说明依据。',
            '瑕点在右端点或内部时照抄左端点的写法。要把区间或变量作相应调整，或先平移/翻转把瑕点挪到左端。',
          ],
          tags: ['反常积分', '瑕积分', '比较审敛法'],
          related: ['thm-comparison-infinite', 'thm-p-integral-unbounded', 'def-improper-unbounded', 'thm-limit-comparison'],
        },
        {
          id: 'thm-limit-comparison',
          kind: 'theorem',
          name: '反常积分的极限审敛法',
          statement: '<b>无穷限情形：</b>设 <code>f(x)</code> 在 <code>[a, +∞)</code>（<code>a &gt; 0</code>）上连续且 <code>f(x) ≥ 0</code>。\n<b>（1）</b>若存在 <code>p &gt; 1</code> 使 <code>lim<sub>x→+∞</sub> x^p f(x) = c</code>（<code>0 ≤ c &lt; +∞</code>），则 <code>∫ₐ^{+∞} f(x)dx</code> 收敛；特别地，若 <code>lim<sub>x→+∞</sub> x^p f(x) = 0</code> 且 <code>p &gt; 1</code>，则收敛。\n<b>（2）</b>若存在 <code>p ≤ 1</code> 使 <code>lim<sub>x→+∞</sub> x^p f(x) = c</code>（<code>0 &lt; c ≤ +∞</code>），则 <code>∫ₐ^{+∞} f(x)dx</code> 发散；特别地，若 <code>lim<sub>x→+∞</sub> x f(x) = d &gt; 0</code>（或 <code>= +∞</code>），则发散。\n<b>瑕积分情形：</b>设 <code>a</code> 为瑕点，<code>f(x) ≥ 0</code> 在 <code>(a, b]</code> 上连续。若存在 <code>p &lt; 1</code> 使 <code>lim<sub>x→a⁺</sub> (x - a)^p f(x) = c</code> 为有限数，则 <code>∫ₐᵇ f(x)dx</code> 收敛；若存在 <code>p ≥ 1</code> 使该极限为 <code>0 &lt; c ≤ +∞</code>，则发散。',
          plain: '比较审敛法要求你亲手写出一个不等式，有时挺麻烦。极限审敛法是它的「偷懒版」：不用找不等式，只要算出「被积函数乘上 <code>x^p</code> 后会不会稳定下来」。\n如果乘上 <code>x^p</code>（<code>p &gt; 1</code>）之后结果有界，说明 <code>f</code> 衰减得不比 <code>1/x^p</code> 慢，收敛；如果乘上 <code>x^p</code>（<code>p ≤ 1</code>）之后还不趋于 0，说明 <code>f</code> 衰减得不够快，发散。\n注意条件的方向：<b>要判收敛就得找一个比 1 大的 <code>p</code>；要判发散就得找一个不超过 1 的 <code>p</code>。</b>选错 <code>p</code> 会得到无用的结论。',
          why: '它的本质是比较审敛法加上「极限保号性」：<code>lim x^p f(x) = c &gt; 0</code> 意味着当 <code>x</code> 足够大时 <code>x^p f(x)</code> 被夹在 <code>c/2</code> 与 <code>2c</code> 之间，于是 <code>f</code> 与 <code>1/x^p</code> 是<b>同量级</b>的，两者同敛散；<code>c = 0</code> 则说明 <code>f</code> 比 <code>1/x^p</code> 还小，可以收敛。\n所以真正好用的形式是：<b>把被积函数的主项提出来，看它像 <code>1/x^p</code> 还是像 <code>1/(x-a)^p</code></b>，<code>p</code> 取多少由主项决定，然后直接套用。',
          proof: '<b>证明（无穷限，情形（1）中 <code>c &gt; 0</code> 的部分）。</b>设 <code>lim<sub>x→+∞</sub> x^p f(x) = c</code>，其中 <code>p &gt; 1</code>、<code>0 &lt; c &lt; +∞</code>。\n<b>第一步：把极限翻译成不等式。</b>取 <code>ε = c/2 &gt; 0</code>，存在 <code>X &gt; a</code>，使 <code>x &gt; X</code> 时 <code>|x^p f(x) - c| &lt; c/2</code>，即\n<code>0 ≤ f(x) &lt; (3c/2)·(1/x^p)</code>。\n<b>第二步：与 <code>p</code> 积分比较。</b>因为 <code>p &gt; 1</code>，由无穷限 p 判别法，<code>∫_X^{+∞} (3c/2)x^{-p}dx</code> 收敛。对 <code>f</code> 与这个函数用比较审敛法，得 <code>∫_X^{+∞} f(x)dx</code> 收敛。\n<b>第三步：补上有限段。</b>由区间可加性 <code>∫ₐ^{+∞} f = ∫ₐ^{X} f + ∫_X^{+∞} f</code>，第一项是普通定积分（有限数），第二项已证收敛，故原积分收敛。\n<b>情形 <code>c = 0</code>：</b>同理取 <code>X</code> 使 <code>x &gt; X</code> 时 <code>x^p f(x) &lt; 1</code>，即 <code>f(x) &lt; x^{-p}</code>，其余步骤相同。\n<b>情形（2）。</b>设 <code>lim<sub>x→+∞</sub> x^p f(x) = c &gt; 0</code>，<code>p ≤ 1</code>。由极限保号性，存在 <code>X</code> 使 <code>x &gt; X</code> 时 <code>x^p f(x) &gt; c/2</code>，即 <code>f(x) &gt; (c/2)x^{-p}</code>。因 <code>p ≤ 1</code>，<code>∫_X^{+∞}(c/2)x^{-p}dx</code> 发散，由比较审敛法的（2）得 <code>∫_X^{+∞} f</code> 发散，从而原积分发散。\n<b>瑕积分情形。</b>把上述论证中的 <code>x → +∞</code> 换成 <code>x → a⁺</code>、把 <code>x^p</code> 换成 <code>(x-a)^p</code>，并把 <code>p</code> 积分判据换成瑕积分的版本（<code>p &lt; 1</code> 收敛、<code>p ≥ 1</code> 发散），逐字重复上述三步即可。',
          example: '<b>无穷限、判收敛：</b><code>∫₁^{+∞} dx/(x² + 1)</code>。当 <code>x → +∞</code> 时 <code>x²·1/(x²+1) → 1</code>，取 <code>p = 2 &gt; 1</code>、<code>c = 1</code>，由情形（1）知收敛。\n<b>无穷限、判发散：</b><code>∫₁^{+∞} dx/√(x³ + 1)</code>。取 <code>p = 3/2</code>，则 <code>x^{3/2}/√(x³+1) = 1/√(1 + x^{-3}) → 1</code>，<code>p = 3/2 &gt; 1</code>，故<b>收敛</b>。\n换一题：<code>∫₁^{+∞} dx/√(x(x+1))</code>。分母 <code>~ x</code>，取 <code>p = 1</code>：<code>x/√(x(x+1)) = 1/√(1 + 1/x) → 1</code>，<code>p = 1 ≤ 1</code>、<code>c = 1 &gt; 0</code>，故<b>发散</b>。\n<b>瑕积分、判收敛：</b><code>∫₀¹ dx/√(1 - x²)</code> 在 <code>x = 1</code> 处有瑕点。令 <code>t = 1 - x</code>，被积函数 <code>~ 1/√(2t)</code>，取 <code>p = 1/2 &lt; 1</code>，极限为有限数 <code>1/√2</code>，故收敛。（实际值可用换元 <code>x = sin θ</code> 算出为 <code>π/2</code>。）',
          pitfalls: [
            '选错 <code>p</code> 的档位。想证收敛却取了 <code>p ≤ 1</code>，或想证发散却取了 <code>p &gt; 1</code>，得到的结论无效（不是「相反」，而是「什么也没说」）。',
            '只算出极限值为 0 就说发散。对情形（2）要求 <code>c &gt; 0</code>；<code>c = 0</code> 说明 <code>f</code> 更小，反而是收敛的信号。',
            '漏掉非负条件 <code>f ≥ 0</code>。极限审敛法是建立在比较审敛法之上的，前提必须满足。',
            '多个瑕点或无穷限与瑕点同时出现时只用一次判据。要把积分拆开，每段单独判断，全部收敛才算收敛。',
            '把「极限审敛法」与「比值审敛法」混淆。前者是反常积分的判别法，后者是数项级数的判别法。',
          ],
          tags: ['反常积分', '极限审敛法', '收敛判别'],
          related: ['thm-comparison-infinite', 'thm-comparison-unbounded', 'thm-p-integral-infinite', 'thm-p-integral-unbounded'],
        },
        {
          id: 'thm-absolute-convergence',
          kind: 'theorem',
          name: '反常积分的绝对收敛',
          statement: '设 <code>f(x)</code> 在 <code>[a, +∞)</code> 上连续（瑕积分情形完全类似）。若 <code>∫ₐ^{+∞} |f(x)|dx</code> 收敛，则 <code>∫ₐ^{+∞} f(x)dx</code> 也收敛，此时称 <code>∫ₐ^{+∞} f(x)dx</code> <b>绝对收敛</b>。\n若 <code>∫ₐ^{+∞} f(x)dx</code> 收敛而 <code>∫ₐ^{+∞} |f(x)|dx</code> 发散，则称 <code>∫ₐ^{+∞} f(x)dx</code> <b>条件收敛</b>。\n特别地，当 <code>f(x) ≥ 0</code> 时，收敛与绝对收敛等价。',
          plain: '被积函数一会儿正一会儿负时，积分可能因为正负抵消而「勉强算得出来」，但它离开抵消就收不住。\n绝对收敛的意思是：<b>把负的部分都翻成正的，面积仍然有限</b>——这才是真正稳的收敛。条件收敛则是「靠抵消才收住」，比较脆。\n实用上，绝对收敛是个好用的<b>工具</b>：因为 <code>|f| ≥ 0</code>，可以放心地用比较审敛法去判断 <code>∫|f|</code>，一旦判定它收敛，就顺带得到了 <code>∫f</code> 收敛。\n但反过来不行：绝对值的积分发散，原积分也可能收敛（条件收敛）。',
          why: '为什么要专门引入这个概念？因为前面所有审敛法都要求被积函数非负，而工程与理论中大量出现变号的积分（<code>sin x/x</code>、<code>cos x/x²</code> 之类）。绝对收敛把「变号问题」转化成了「非负问题」，让我们能继续使用比较法和极限审敛法。\n它和数项级数的绝对收敛是同一套逻辑，证明手法也一样：把 <code>f</code> 拆成正部和负部，再分别用比较法夹住。可以这样理解：<b>绝对收敛是「结构上」的收敛，条件收敛是「运气上」的收敛。</b>',
          proof: '<b>证明（无穷限情形）。</b>设 <code>∫ₐ^{+∞}|f(x)|dx</code> 收敛。\n<b>第一步：制造一个非负函数。</b>对每个 <code>x</code> 有\n<code>0 ≤ f(x) + |f(x)| ≤ 2|f(x)|</code>。\n（左边是因为 <code>|f| ≥ -f</code>；右边是因为 <code>f ≤ |f|</code>。）\n<b>第二步：对非负函数用比较审敛法。</b>由 <code>∫ₐ^{+∞} 2|f(x)|dx = 2∫ₐ^{+∞}|f(x)|dx</code> 收敛，而 <code>f + |f|</code> 非负且不超过它，故\n<code>∫ₐ^{+∞} [f(x) + |f(x)|]dx</code> 收敛。\n<b>第三步：用线性性质分离出 <code>f</code>。</b>由\n<code>f(x) = [f(x) + |f(x)|] - |f(x)|</code>，\n两个已收敛的反常积分之差也收敛（线性性质对收敛的反常积分同样成立，因为极限的四则运算成立），故 <code>∫ₐ^{+∞} f(x)dx</code> 收敛。\n<b>第四步：非负情形。</b>若 <code>f ≥ 0</code>，则 <code>|f| = f</code>，两个积分是同一个，故收敛与绝对收敛等价。\n<b>瑕积分情形。</b>把上述论证中的 <code>∫ₐ^{+∞}</code> 全部替换为 <code>∫ₐᵇ</code>（<code>a</code> 为瑕点），并把 <code>b → +∞</code> 的极限过程换成 <code>ε → 0⁺</code>，逐字重复即可，因为两类反常积分的收敛都归结为「单调有界」或「极限存在」。证毕。',
          example: '<b>绝对收敛：</b><code>∫₁^{+∞} (sin x)/x² dx</code>。因为 <code>|sin x/x²| ≤ 1/x²</code>，而 <code>∫₁^{+∞} dx/x² = 1</code> 收敛，由比较审敛法 <code>∫₁^{+∞}|sin x/x²|dx</code> 收敛，故原积分绝对收敛（因而收敛）。\n<b>条件收敛：</b><code>∫₁^{+∞} (sin x)/x dx</code> 收敛（可用狄利克雷判别法证明），但 <code>∫₁^{+∞}|sin x|/x dx</code> 发散。所以它是条件收敛的。这提醒我们：<b>不能因为绝对值积分发散就断言原积分发散。</b>\n<b>非负情形：</b><code>∫₁^{+∞} dx/x²</code>，被积函数非负，收敛即绝对收敛。',
          pitfalls: [
            '把「绝对收敛 ⇒ 收敛」反着用，认为「<code>∫|f|</code> 发散 ⇒ <code>∫f</code> 发散」。<code>sin x/x</code> 就是反例。',
            '在判断 <code>∫f</code> 的收敛性时直接用比较审敛法，却忘记 <code>f</code> 不满足非负条件。正确流程是：先看 <code>∫|f|</code>。',
            '把「条件收敛」与「发散」混为一谈。条件收敛是收敛的一种。',
            '只对无穷限情形记住这条结论，处理瑕积分时忘记它同样成立。',
          ],
          tags: ['反常积分', '绝对收敛', '条件收敛'],
          related: ['prop-integral-abs-inequality', 'thm-comparison-infinite', 'def-improper-infinite', 'def-gamma-function'],
        },
        {
          id: 'def-gamma-function',
          kind: 'definition',
          name: 'Γ 函数',
          aka: ['Gamma 函数', '伽马函数'],
          statement: '对 <code>x &gt; 0</code>，定义\n<code>Γ(x) = ∫₀^{+∞} e^{-t} t^{x-1} dt</code>。\n可以证明：对每个 <code>x &gt; 0</code>，这个反常积分都收敛（<code>x ≥ 1</code> 时 <code>t = 0</code> 不是瑕点；<code>0 &lt; x &lt; 1</code> 时 <code>t = 0</code> 是瑕点，由瑕积分 p 判别法 <code>p = 1 - x &lt; 1</code> 收敛），因此 <code>Γ</code> 在 <code>(0, +∞)</code> 上有定义。\n它最重要的性质是递推公式（见下一条）：<code>Γ(x + 1) = xΓ(x)</code>。',
          plain: '<code>Γ</code> 函数是「阶乘的升级版」。\n阶乘 <code>n!</code> 只对 <code>1, 2, 3, …</code> 这些整数有意义，「<code>2.5!</code>」在中学里根本没法算。可是很多公式（比如概率论里的分布）需要把阶乘用在任意正数上。\n<code>Γ</code> 函数就是用积分造出来的那个「连续版阶乘」：它的定义里没有阶乘，只有一个从 0 到无穷的积分，但它在整数点上的取值恰好就是阶乘。\n可以把它记成：「<code>Γ</code> 把「整数上的乘法累加」换成了「连续上的积分累加」，于是阶乘被抹平成了光滑曲线。',
          why: '为什么要用这样一个积分来定义？因为阶乘的核心特征是递推关系 <code>n! = n·(n-1)!</code>，而积分恰好天然地提供这种结构：<b>分部积分会把 <code>t^{x-1}</code> 的指数降下来，同时把一个 <code>x</code> 提到积分号外面</b>（下一项的证明就是这件事）。于是「每一步降一阶、乘一个数」的机制被完美复刻。\n另一个理由是：指数 <code>x - 1</code> 的选取（而不是 <code>x</code>）是为了让 <code>Γ(n+1) = n!</code> 这个漂亮的对应关系成立。若定义成 <code>∫₀^{+∞}e^{-t}t^{x}dt</code>，那就只能得到 <code>Γ(n) = (n-1)!</code>，不够顺手。\n<code>Γ</code> 函数在上册里主要作为反常积分的应用和计算工具出现，在概率统计、数论、复分析里则是绝对的主角。',
          example: '<b>算 Γ(1)：</b><code>Γ(1) = ∫₀^{+∞} e^{-t}dt = lim<sub>b→+∞</sub>[-e^{-t}]₀^b = 0 - (-1) = 1</code>。\n<b>算 Γ(2)：</b>直接算：<code>Γ(2) = ∫₀^{+∞} t e^{-t}dt</code>，分部积分（取 <code>u = t</code>、<code>dv = e^{-t}dt</code>，则 <code>v = -e^{-t}</code>）：\n<code>Γ(2) = [-t e^{-t}]₀^{+∞} + ∫₀^{+∞} e^{-t}dt = 0 + 1 = 1</code>。\n这和 <code>1! = 1</code> 一致。用递推公式更快：<code>Γ(2) = 1·Γ(1) = 1</code>。\n<b>算 Γ(3)：</b><code>Γ(3) = 2Γ(2) = 2 = 2!</code>。\n<b>算 Γ(4)：</b><code>Γ(4) = 3Γ(3) = 6 = 3!</code>。规律浮现。\n<b>半整数（上册只要求会用递推）：</b><code>Γ(1/2) = ∫₀^{+∞} e^{-t}t^{-1/2}dt = √π</code>（作代换 <code>t = u²</code> 后化为概率积分 <code>2∫₀^{+∞}e^{-u²}du = √π</code>，后者在二重积分一章计算）。于是 <code>Γ(3/2) = (1/2)Γ(1/2) = √π/2</code>。',
          pitfalls: [
            '把定义写成 <code>t^{x}</code> 而不是 <code>t^{x-1}</code>，结果递推关系变成 <code>Γ(x+1) = (x+1)Γ(x)</code>，全乱套。',
            '讨论 <code>x ≤ 0</code> 的情形。定义式只对 <code>x &gt; 0</code> 收敛；<code>x = 0</code> 时 <code>t^{-1}</code> 在 0 附近的积分发散，<code>Γ(0)</code> 无定义。',
            '忘记它是由反常积分定义的，直接对 <code>∫₀^{+∞}</code> 用牛顿-莱布尼茨公式。必须先算 <code>∫₀^{b}</code> 再取 <code>b → +∞</code>。',
            '以为 <code>Γ(x)</code> 对一切实数都有定义。它只在 <code>(0, +∞)</code> 上由这个积分直接定义；负数的值要用递推式「反向」延拓（如 <code>Γ(x) = Γ(x+1)/x</code>），在非正整数处仍然无定义。',
          ],
          tags: ['Γ 函数', '反常积分', '阶乘推广'],
          related: ['thm-gamma-recursion', 'thm-absolute-convergence', 'thm-integral-by-parts', 'thm-p-integral-unbounded'],
        },
        {
          id: 'thm-gamma-recursion',
          kind: 'theorem',
          name: 'Γ 函数的递推公式',
          statement: '对任意 <code>x &gt; 0</code>，有\n<code>Γ(x + 1) = xΓ(x)</code>。\n由此可得：对任意正整数 <code>n</code>，<code>Γ(n + 1) = n!</code>，即\n<code>Γ(n+1) = n(n-1)(n-2)…2·1 = n!</code>，其中 <code>Γ(1) = 1</code>。',
          plain: '这条公式说的是：<b><code>Γ</code> 函数每往前走一步，就多乘一个数。</b>\n从 1 走到 2，乘 1；走到 3，乘 2；走到 4，乘 3……走到底就正好是 <code>1 × 2 × 3 × …</code>，也就是阶乘。\n所以 <code>Γ</code> 函数就是把阶乘那条「一步一乘」的楼梯，铺成了可以斜着走的斜坡：整数台阶上站着的还是原来的阶乘值，台阶之间也能落脚了（比如 <code>Γ(1/2) = √π</code>）。',
          why: '为什么分部积分会正好造出「乘一个数、指数降一阶」的效果？关键看两项：\n一是<b>指数降阶</b>：<code>t^{(x+1)-1} = t^x</code> 求导后变成 <code>x·t^{x-1}</code>——那个因子 <code>x</code> 就是我们要的「乘数」，指数也从 <code>x</code> 降到了 <code>x - 1</code>。\n二是<b>边界项消失</b>：<code>t^x e^{-t}</code> 在 <code>t = 0</code> 处因为 <code>x &gt; 0</code> 而等于 0，在 <code>t → +∞</code> 处因为指数衰减快于多项式增长而趋于 0。正是这个「两头都归零」让分部积分剩下的只有那个积分本身，从而得到干净的递推式。\n这也解释了为什么定义里必须用 <code>e^{-t}</code>：换一个衰减不够快的因子（比如 <code>1/(1+t)</code>），边界项就不会消失，递推关系也就不成立了。',
          proof: '<b>证明。</b>设 <code>x &gt; 0</code>。按定义\n<code>Γ(x + 1) = ∫₀^{+∞} e^{-t} t^{x} dt</code>，\n这是一个反常积分，须先算有限区间上的积分再取极限。\n<b>第一步：截断为有限积分。</b>对 <code>b &gt; 0</code>、<code>0 &lt; ε &lt; b</code>，记\n<code>I(ε, b) = ∫_ε^{b} t^{x} e^{-t} dt</code>。\n（取 <code>ε &gt; 0</code> 是为了在 <code>x</code> 可能小于 1 时避开 <code>t = 0</code> 处的瑕点，让分部积分有合法的舞台。）\n<b>第二步：分部积分。</b>取 <code>u = t^{x}</code>、<code>dv = e^{-t}dt</code>，则 <code>du = x t^{x-1}dt</code>、<code>v = -e^{-t}</code>，由定积分的分部积分公式：\n<code>I(ε, b) = [-t^{x} e^{-t}]_ε^{b} + x ∫_ε^{b} t^{x-1} e^{-t} dt = (ε^{x}e^{-ε} - b^{x}e^{-b}) + x ∫_ε^{b} t^{x-1}e^{-t}dt</code>。\n<b>第三步：让 b → +∞，处理右端边界项。</b>因为指数函数增长快于任何幂函数，<code>lim<sub>b→+∞</sub> b^{x}e^{-b} = 0</code>。\n（严格地说，对 <code>b ≥ 1</code> 有 <code>b^{x}e^{-b} ≤ b^{N}e^{-b}</code>，其中 <code>N</code> 为不小于 <code>x</code> 的整数，而 <code>b^{N}e^{-b} → 0</code>。）\n<b>第四步：让 ε → 0⁺，处理左端边界项。</b>因为 <code>x &gt; 0</code>，<code>lim<sub>ε→0⁺</sub> ε^{x}e^{-ε} = 0 · 1 = 0</code>。\n（这一步正是需要 <code>x &gt; 0</code> 的地方；若 <code>x = 0</code>，<code>ε^{0}e^{-ε} → 1</code>，边界项不消失。）\n<b>第五步：把两个极限合起来。</b>由已知 <code>Γ(x+1) = lim<sub>ε→0⁺, b→+∞</sub> I(ε, b)</code> 收敛，且两个边界项都趋于 0，于是\n<code>Γ(x + 1) = 0 + x·lim<sub>ε→0⁺, b→+∞</sub> ∫_ε^{b} t^{x-1}e^{-t}dt = x Γ(x)</code>。\n（最后一步用了 <code>Γ(x) = ∫₀^{+∞}t^{x-1}e^{-t}dt</code> 的定义。）\n<b>第六步：导出阶乘形式。</b>由 <code>Γ(1) = 1</code> 与递推式，对正整数 <code>n</code> 反复使用：\n<code>Γ(n+1) = nΓ(n) = n(n-1)Γ(n-1) = … = n(n-1)…2·1·Γ(1) = n!</code>。证毕。',
          example: '<b>用递推算整数点：</b><code>Γ(5) = 4Γ(4) = 4·3·Γ(3) = 4·3·2·Γ(2) = 4·3·2·1·Γ(1) = 24 = 4!</code>。\n<b>用递推算半整数：</b>已知 <code>Γ(1/2) = √π</code>，则\n<code>Γ(3/2) = (1/2)Γ(1/2) = √π/2 ≈ 0.886</code>；\n<code>Γ(5/2) = (3/2)Γ(3/2) = (3/2)(√π/2) = 3√π/4 ≈ 1.329</code>。\n验算 <code>Γ(5/2)</code> 的另一条路：<code>Γ(5/2) = (3/2)(1/2)Γ(1/2) = (3/4)√π</code>，与上面一致。\n<b>反向使用（求 <code>Γ(-1/2)</code>）：</b>由 <code>Γ(x+1) = xΓ(x)</code> 取 <code>x = -1/2</code>，得 <code>Γ(1/2) = (-1/2)Γ(-1/2)</code>，故 <code>Γ(-1/2) = -2Γ(1/2) = -2√π</code>。这是延拓，不是原定义直接给出的。',
          pitfalls: [
            '把递推方向记反，写成 <code>Γ(x+1) = (x+1)Γ(x)</code>。正确是 <code>Γ(x+1) = xΓ(x)</code>。',
            '在 <code>x ≤ 0</code> 时使用该公式。证明中的边界项 <code>ε^{x}e^{-ε}</code> 只有当 <code>x &gt; 0</code> 时才趋于 0。',
            '把 <code>Γ(n+1) = n!</code> 记成 <code>Γ(n) = n!</code>。正确的对应是「<code>Γ</code> 的自变量比阶乘的自变量大 1」：<code>Γ(4) = 3! = 6</code>，<code>Γ(5) = 4! = 24</code>。',
            '在计算 <code>Γ(1)</code> 时忘记它是反常积分，直接写 <code>[-e^{-t}]₀^{+∞} = -1</code>。正确是 <code>0 - (-1) = 1</code>，符号别丢。',
            '以为 <code>Γ(x)</code> 恒大于 0。它在 <code>(0, +∞)</code> 上确实为正，但延拓到负数区间后会出现负值（如 <code>Γ(-1/2) = -2√π</code>）。',
          ],
          tags: ['Γ 函数', '递推公式', '阶乘', '分部积分'],
          related: ['def-gamma-function', 'thm-integral-by-parts', 'thm-p-integral-unbounded'],
        },
      ],
    },
  ],
};
