// 同济《高等数学》上册（第八版）· 第6章 定积分的应用
export default {
  id: 'ch6',
  no: 6,
  title: '定积分的应用',
  intro: '前面我们学会了怎么算出定积分的值，这一章要回答一个更实在的问题：这玩意儿到底能用来干什么。\n答案几乎无处不在：一块奇形怪状的地有多大、一个萝卜（旋转体）有多重、一段弯曲的铁丝有多长、把整池水抽干要做多少功、水坝闸门承受多大的推力——全都能用一个定积分算出来。\n这一章真正要掌握的不是十几个公式，而是一套统一的思路：<b>微元法</b>。学会它，公式忘光了也能自己推出来。',
  sections: [
    {
      id: 'ch6-1',
      no: '6.1',
      title: '定积分的元素法（微元法）',
      summary: '把一个大问题切成无数个“小到可以偷懒”的碎片，每片上用最简单的公式，再积分加回去。',
      items: [
        {
          id: 'fml-element-method-steps',
          kind: 'formula',
          name: '元素法（微元法）的一般步骤',
          aka: ['微元法', '元素法'],
          statement: '若待求量 <code>Q</code> 与区间 <code>[a, b]</code> 有关，且 <code>Q</code> 对该区间具有可加性，则可按两步求 <code>Q</code>：\n<b>第一步（找微元）</b>：在 <code>[a, b]</code> 上任取小区间 <code>[x, x + dx]</code>，求出这一小段上 <code>Q</code> 的近似值 <code>ΔQ ≈ dQ = f(x) dx</code>，且要求误差是 <code>dx</code> 的高阶无穷小，即 <code>ΔQ − f(x)dx = o(dx)</code>；\n<b>第二步（积分求和）</b>：把微元在 <code>[a, b]</code> 上积分，得 <code>Q = ∫ₐᵇ f(x) dx</code>。',
          plain: '微元法就是“把弯曲的东西切成无数小片，每片小到可以当成直的、方的、不变的东西”。\n比如要量一条弯弯曲曲的山路有多长，直接量量不出来。但你把它切成一小段一小段，每一小段短得几乎像直线，用勾股定理一算，再把所有小段长度加起来——就差不多了。切得越细越准，细到极限就是精确值。\n又比如算一个萝卜的体积：整根萝卜形状不规则，没有现成公式。但把它横着切成无数薄片，每一片薄得像个圆饼，圆饼体积好学（底面积乘厚度），把所有圆饼加起来就是萝卜的体积。',
          why: '为什么这样“偷懒”最后能得到精确答案？关键在于两条：\n<b>第一，误差是高阶无穷小，加不起来。</b>把一小段曲线当成直线，误差大约是 <code>(Δx)²</code> 那一档的量级（曲线只在这一小段里弯了那么一点点）。当 <code>Δx → 0</code> 时它比 <code>Δx</code> 跑得更快。整段分成 <code>n</code> 份后，每份误差约 <code>C(Δx)²</code>，总误差约 <code>n·C(Δx)²</code>，而 <code>n</code> 与 <code>Δx</code> 大致成反比，所以总误差与 <code>Δx</code> 同阶，趋于零。如果每片的误差只是 <code>Δx</code> 的同阶量，<code>n</code> 份加起来就是常数，永远消不掉，那就不行了。\n<b>第二，“以直代曲、以不变代变”是把非线性问题化成线性问题的唯一办法。</b>我们天生只会算“矩形面积 = 长 × 宽”“圆饼体积 = 底面积 × 高”这种常量乘法，不会直接算变力做功、变速运动。微元法做的事就是：在一小段上假装函数值不变（就取作 <code>f(x)</code>），用常量公式算，再让 <code>dx</code> 趋于零，把这份“假装”的代价抹平。\n[[tip:判断微元写对没有，做一个量纲检查：<code>dQ</code> 的单位必须与被求量 <code>Q</code> 一致。求体积时微元必须是“面积 × 长度”，求功时必须是“力 × 长度”。对不上就说明微元写错了。]]',
          proof: '推导（为什么“近似和”的极限就是真值）：\n<b>1. 严格化近似。</b>把 <code>[a, b]</code> 任意分成 <code>n</code> 个小区间，第 <code>i</code> 段长为 <code>Δxᵢ</code>，该段上真值为 <code>ΔQᵢ</code>。按条件有 <code>ΔQᵢ = f(ξᵢ)Δxᵢ + o(Δxᵢ)</code>。\n<b>2. 求和。</b><code>Q = ΣΔQᵢ = Σf(ξᵢ)Δxᵢ + Σo(Δxᵢ)</code>。\n<b>3. 取极限。</b>第一项按定积分定义趋于 <code>∫ₐᵇ f(x) dx</code>（因为 <code>f</code> 连续因而可积）。第二项：由 <code>o(Δxᵢ)</code> 的含义，当各段足够短时 <code>|o(Δxᵢ)| ≤ εΔxᵢ</code>，于是 <code>|Σo(Δxᵢ)| ≤ εΣΔxᵢ = ε(b − a)</code>，令 <code>ε → 0</code> 即知这一项趋于 0。\n<b>4. 结论。</b><code>Q = ∫ₐᵇ f(x) dx</code>，且与区间的分法、<code>ξᵢ</code> 的取法都无关。\n这说明微元法不是新方法，它就是“分割、近似、求和、取极限”四步被压缩成了一行式子。',
          example: '用微元法求由 <code>y = x</code>、<code>x = 0</code>、<code>x = 2</code> 与 <code>x</code> 轴围成的三角形面积（初中就会算，用来看清流程）。\n<b>第一步</b>：在 <code>[0, 2]</code> 上取小区间 <code>[x, x + dx]</code>。这一竖条又细又窄，上端弯曲可以忽略，把它当成高为 <code>x</code>、宽为 <code>dx</code> 的小矩形，得 <code>dS = x dx</code>（面积单位是长度乘长度，量纲正确）。\n<b>第二步</b>：<code>S = ∫₀² x dx = [x²/2]₀² = 2</code>。\n验算：底 2、高 2 的三角形面积是 <code>2 × 2 ÷ 2 = 2</code>，一致。',
          pitfalls: [
            '把微元写成“整段的精确值除以 n”，那得到只是近似和，误差不会自动消失。',
            '选错积分变量：横着切用 <code>dx</code>，就必须把上下边界写成 <code>x</code> 的函数；若写成 <code>y</code> 的函数却不换元，公式立刻失效。',
            '对不可加的量用微元法：弧长可加（分段加得总长），但“某点的斜率”不可加，不能对它积分。',
            '不做量纲检查，写出 <code>dV = 2πx dx</code> 这类少了一个长度因子的错误微元。',
          ],
          tags: ['定积分', '微元法', '元素法'],
          related: ['def-definite-integral', 'thm-newton-leibniz', 'fml-area-cartesian', 'fml-volume-washer'],
        },
        {
          id: 'note-element-method-dx-notation',
          kind: 'note',
          name: '为什么微元可以直接写成 dx 而不是 Δx',
          statement: '在微元法中我们直接写 <code>dQ = f(x) dx</code>，而不是先写 <code>ΔQ ≈ f(x)Δx</code> 再令 <code>Δx → 0</code>。这是合法的简写：因为 <code>ΔQ − f(x)Δx = o(Δx)</code>，在取极限的意义下两者不可区分。',
          plain: '这就像用正多边形逼近圆：正 96 边形的周长已经和圆周长很接近了，但你真正想要的是“边数无限多”的那个结果。<code>dx</code> 就是那个“无限小的一小段”，它不是某个真实存在的数，而是一个记号，代表“让 <code>Δx</code> 趋于 0 的这个过程”。\n所以看到 <code>dV = πf(x)²dx</code>，不要理解成“先乘出一个小体积，再把它积分”，而应理解成“这是小薄片体积的表达式，最后统一积分求和”。',
          why: '把“找微元”和“做积分”拆成两个独立动作，写公式时不必每一步都挂一个极限号，思维负担小很多。这就是元素法能被当作操作流程来教的原因：找微元是建模，积分是计算，两件事分开做，各管一段。',
          example: '对 <code>y = x²</code> 与 <code>x</code> 轴、<code>x = 3</code> 围成的图形，看一小段的误差：<code>ΔS</code> 与 <code>x²Δx</code> 之差约为 <code>x(Δx)²</code>，是 <code>(Δx)²</code> 量级，属于 <code>Δx</code> 的高阶无穷小，所以写成 <code>dS = x²dx</code> 没问题。积分得 <code>S = 3³ ÷ 3 = 9</code>。',
          pitfalls: [
            '把 <code>dx</code> 当成一个具体的“很小很小的正数”去做代数运算（约掉、移项、开平方），容易得出荒谬结论。',
            '在需要二阶近似的场合（如旋转曲面面积）只保留一阶项，导致结果偏小。',
          ],
          tags: ['微元法', '微分记号'],
          related: ['fml-element-method-steps', 'fml-surface-area-revolution'],
        },
      ],
    },
    {
      id: 'ch6-2',
      no: '6.2',
      title: '定积分在几何学上的应用',
      summary: '面积、体积、弧长、旋转曲面面积，四类几何量用同一套微元法全部拿下。',
      items: [
        {
          id: 'fml-area-cartesian',
          kind: 'formula',
          name: '直角坐标下平面图形的面积（上下曲线型）',
          aka: ['面积公式', 'X 型区域面积'],
          statement: '设 <code>f(x)</code>、<code>g(x)</code> 在 <code>[a, b]</code> 上连续，且 <code>f(x) ≥ g(x)</code>，则由 <code>y = f(x)</code>（上边界）、<code>y = g(x)</code>（下边界）与直线 <code>x = a</code>、<code>x = b</code> 所围图形的面积为\n<code>S = ∫ₐᵇ [f(x) − g(x)] dx</code>。\n若上下关系不固定，则应写成 <code>S = ∫ₐᵇ |f(x) − g(x)| dx</code>，或先求交点再分段积分。',
          plain: '“面积 = 长 × 宽”这句小学知识到这里只剩一个小麻烦：宽好办，就是 <code>dx</code>；长呢？在一个竖直窄条里，上面那条曲线和下面那条曲线的高度差几乎不变，所以这个窄条就是个长方形，长等于“上面减下面”，宽等于 <code>dx</code>。\n把无数个这样的窄条拼起来，就是整个图形的面积。所以公式里一定是<b>上面的减下面的</b>，顺序反了就会算出负数——在面积题里，负数永远是错误信号。',
          why: '为什么必须“上减下”？因为定积分算的是带符号的代数面积：曲线在 <code>x</code> 轴下方时，积分贡献为负。如果写成 <code>g − f</code>，每个窄条的贡献都是负的，总和就是真实面积的相反数。\n记住一句话：<b>定积分给你的是带符号面积，几何面积要的是绝对值</b>。当你不确定谁在上面时，先解方程求交点、画草图、把区间切开，逐段判断。',
          proof: '推导：\n<b>1. 找微元。</b>取 <code>[x, x + dx]</code>，对应窄条的上下边界高度分别为 <code>f(x)</code>、<code>g(x)</code>，因 <code>f − g ≥ 0</code>，窄条面积近似为矩形面积 <code>dS = [f(x) − g(x)] dx</code>。误差来自曲线在这一小段内的弯曲，量级为 <code>(dx)²</code>，是 <code>dx</code> 的高阶无穷小。\n<b>2. 可加性。</b>图形被竖线分割后各块面积之和等于总面积，满足可加性，因此可用微元法。\n<b>3. 积分。</b><code>S = ∫ₐᵇ [f(x) − g(x)] dx</code>，由牛顿-莱布尼茨公式，求出原函数 <code>F − G</code> 后代入上下限即得。\n<b>4. 与 <code>x</code> 轴情形的统一。</b>若下边界就是 <code>x</code> 轴，即 <code>g(x) ≡ 0</code>，公式退化为 <code>S = ∫ₐᵇ f(x) dx</code>。这时才要求 <code>f(x) ≥ 0</code>；若 <code>f</code> 变号，必须取绝对值或分段处理。',
          example: '求 <code>y = x²</code> 与 <code>y = x</code> 围成的图形面积。\n<b>第 1 步，求交点定上下限</b>：令 <code>x² = x</code>，得 <code>x = 0</code> 或 <code>x = 1</code>，所以 <code>a = 0</code>，<code>b = 1</code>。\n<b>第 2 步，判断谁在上</b>：取中点 <code>x = 1/2</code>，<code>y = x</code> 给 <code>1/2</code>，<code>y = x²</code> 给 <code>1/4</code>，所以直线在上，<code>f − g = x − x²</code>。\n<b>第 3 步，积分</b>：<code>S = ∫₀¹ (x − x²) dx = [x²/2 − x³/3]₀¹ = 1/2 − 1/3 = 1/6</code>。\n结果 <code>1/6</code> 与图像相符：这个小弓形确实很小。',
          pitfalls: [
            '没判断谁在上面就直接算 <code>∫(f − g)dx</code>，若 <code>g</code> 在上会得到负面积。',
            '上下限直接抄题目给的数，忘了它们应该是两条曲线的交点横坐标。',
            '图形左右分叉（同一个 <code>x</code> 对应两段区域）时仍强行按 <code>x</code> 积分，此时应改为按 <code>y</code> 积分：<code>S = ∫_c^d [φ(y) − ψ(y)] dy</code>（右减左）。',
            '遇到 <code>x</code> 轴下方的图形忘记取绝对值，答案出负号。',
          ],
          tags: ['面积', '定积分应用', '几何应用'],
          related: ['fml-area-polar', 'fml-element-method-steps', 'fml-volume-disk'],
        },
        {
          id: 'fml-area-polar',
          kind: 'formula',
          name: '极坐标下平面图形的面积（曲边扇形）',
          aka: ['极坐标面积', '扇形面积公式'],
          statement: '设曲线由极坐标方程 <code>r = r(θ)</code> 给出，<code>r(θ)</code> 在 <code>[α, β]</code> 上连续。则由曲线 <code>r = r(θ)</code> 与两条射线 <code>θ = α</code>、<code>θ = β</code> 围成的曲边扇形面积为\n<code>S = (1/2)∫_α^β r(θ)² dθ</code>。',
          plain: '极坐标下的“窄条”不再是长方形，而是一个<b>很窄的扇形</b>（像切得很薄的一牙西瓜）。圆心角很小的一牙西瓜近似等于一个三角形：底是弧长 <code>r dθ</code>，高是半径 <code>r</code>，面积就是 <code>1/2 × 底 × 高 = (1/2)·r·dθ·r = (1/2)r² dθ</code>。\n把所有小扇形加起来就得到面积。注意这里多出来的那个 <code>1/2</code>，正是“三角形而不是矩形”留下的痕迹。',
          why: '为什么是 <code>r²</code> 而不是 <code>r</code>？因为当 <code>θ</code> 增加一点点时，扫过的不是一段长度，而是一小块面；这一小块的长和高<b>都</b>与 <code>r</code> 成正比（长约为 <code>r dθ</code>，高约为 <code>r</code>），所以面积里出现 <code>r²</code>。用微元的眼光看：<code>dS</code> 必须与 <code>dθ</code> 成正比（这是“对角度可加”的要求），而唯一能与 <code>dθ</code> 配出面积量纲的组合就是 <code>r²dθ</code>。\n这也告诉我们：<b>极坐标面积公式里不会有 <code>1/2</code> 以外的常数，也不会出现 <code>dr</code></b>。如果推出来带 <code>dr</code>，说明微元找错了。',
          proof: '推导：\n<b>1. 分割。</b>用射线 <code>θ = θ₀ &lt; θ₁ &lt; … &lt; θₙ = β</code> 把整个区域分成 <code>n</code> 个窄扇形，第 <code>i</code> 个的圆心角为 <code>Δθᵢ</code>。\n<b>2. 近似。</b>在第 <code>i</code> 个窄扇形里任取角 <code>θᵢ</code>，因为 <code>r(θ)</code> 连续、<code>Δθᵢ</code> 又很小，半径可近似看作常数 <code>r(θᵢ)</code>。用圆扇形面积公式得 <code>ΔSᵢ ≈ (1/2) r(θᵢ)² Δθᵢ</code>。误差量级仍是 <code>(Δθᵢ)²</code>，是 <code>Δθᵢ</code> 的高阶无穷小。\n<b>3. 求和取极限。</b><code>S = lim Σ (1/2) r(θᵢ)² Δθᵢ = (1/2)∫_α^β r(θ)² dθ</code>。\n<b>4. 与直角坐标公式的对照。</b>直角坐标的一条窄条是“长 <code>y</code>、宽 <code>dx</code>”，极坐标的一牙西瓜是“长 <code>r dθ</code>、高 <code>r</code>”，两者其实都是“底乘高”：微元一个是 <code>y dx</code>，一个是 <code>r·r dθ</code>。差别只在于用哪个量做自变量。',
          example: '求心形线 <code>r = a(1 + cos θ)</code>（<code>a &gt; 0</code>）所围图形的面积。\n<b>定限</b>：曲线封闭一圈对应 <code>θ</code> 从 <code>0</code> 到 <code>2π</code>，所以 <code>α = 0</code>，<code>β = 2π</code>。\n<b>展开</b>：<code>r² = a²(1 + cos θ)² = a²(1 + 2cos θ + cos²θ)</code>，再用 <code>cos²θ = (1 + cos 2θ)/2</code>，得 <code>r² = a²(3/2 + 2cos θ + (1/2)cos 2θ)</code>。\n<b>积分</b>：<code>S = (1/2)a²∫₀^{2π} (3/2 + 2cos θ + (1/2)cos 2θ) dθ</code>。其中 <code>∫₀^{2π}cos θ dθ = 0</code>，<code>∫₀^{2π}cos 2θ dθ = 0</code>，只剩第一项：<code>S = (1/2)a² · (3/2) · 2π = (3/2)πa²</code>。\n验算：心形线围的面积比半径 <code>a</code> 的圆（<code>πa²</code>）大一些，<code>1.5πa²</code> 符合直觉。',
          pitfalls: [
            '忘掉 <code>1/2</code>，把 <code>∫r²dθ</code> 当面积，结果整整大一倍。',
            '积分上下限乱写：要按曲线“真正扫过一遍”的角度范围定限，而不是永远写 <code>0</code> 到 <code>2π</code>。例如心形线的一半图形只需 <code>0</code> 到 <code>π</code>。',
            '把极坐标方程直接代入直角坐标公式 <code>∫y dx</code> 使用，却不做换元。',
            '两曲线围成的区域（如 <code>r₁(θ) ≤ r ≤ r₂(θ)</code>）忘记用 <code>r₂² − r₁²</code>，只算了外圈。',
          ],
          tags: ['面积', '极坐标', '曲边扇形'],
          related: ['fml-area-cartesian', 'fml-arc-length-polar', 'fml-element-method-steps'],
        },
        {
          id: 'fml-volume-disk',
          kind: 'formula',
          name: '旋转体的体积（圆盘法）',
          aka: ['圆盘法', '切片法', '绕 x 轴旋转'],
          statement: '设 <code>f(x)</code> 在 <code>[a, b]</code> 上连续且 <code>f(x) ≥ 0</code>。由曲线 <code>y = f(x)</code>、直线 <code>x = a</code>、<code>x = b</code> 与 <code>x</code> 轴围成的图形绕 <code>x</code> 轴旋转一周所成旋转体的体积为\n<code>V = π∫ₐᵇ f(x)² dx</code>。\n若绕的是 <code>y</code> 轴且图形由 <code>x = φ(y)</code>（<code>c ≤ y ≤ d</code>）界定，则把变量换成 <code>y</code>：<code>V = π∫_c^d φ(y)² dy</code>。',
          plain: '求体积＝“把萝卜切成无数薄片，每片算成一个圆饼，再加起来”。\n薄片厚度是 <code>dx</code>，圆饼的半径就是这条曲线到旋转轴的距离 <code>f(x)</code>，圆饼面积就是 <code>πf(x)²</code>，所以一片的体积是 <code>πf(x)²dx</code>。积分就是把无数片圆饼叠起来。',
          why: '为什么要“先切片再积分”，而不能直接对整段用圆台公式？因为各处的半径 <code>f(x)</code> 在变，整段是个曲面体，没有初中公式可用。切片是唯一能把“变化的半径”变成“暂时不变的半径”的方法：一片薄到可以忽略半径的变化，就能用最熟的圆柱公式。\n这也解释了公式里为什么是 <code>f(x)²</code> 而不是 <code>f(x)</code>：一片的<b>面积</b>与半径的平方成正比，再把面积乘上厚度才是体积。',
          proof: '推导：\n<b>1. 找微元。</b>在 <code>[x, x + dx]</code> 上，把这一小段旋转体近似当作底面半径 <code>f(x)</code>、高 <code>dx</code> 的薄圆柱。体积微元 <code>dV = πf(x)² dx</code>。\n<b>2. 误差估计。</b>真实的薄片是圆台形，半径从 <code>f(x)</code> 变到 <code>f(x + dx) ≈ f(x) + f′(x)dx</code>，两者之差与 <code>dx</code> 同阶，所以圆台与圆柱的体积差只有 <code>dx</code> 的<b>二阶</b>量级，是 <code>dx</code> 的高阶无穷小。这一步很关键：<b>半径的一阶误差只造成体积的二阶误差</b>，因为体积是长度的三次方，而厚度方向已经被 <code>dx</code> 占去了一个一阶。\n<b>3. 积分。</b><code>V = ∫ₐᵇ πf(x)² dx = π∫ₐᵇ f(x)² dx</code>。\n<b>4. 球体积的验证（最漂亮的一次自检）。</b>取半径 <code>R</code> 的球，由 <code>y = √(R² − x²)</code>（<code>−R ≤ x ≤ R</code>）绕 <code>x</code> 轴旋转而成。则 <code>V = π∫_{−R}^{R} (R² − x²) dx = π[R²x − x³/3]_{−R}^{R} = π(2R³ − 2R³/3) = 4πR³/3</code>，正是课本上的球体积公式。用积分推出一个初中就背过的公式，说明这套方法是对的。',
          example: '求 <code>y = x²</code>（<code>0 ≤ x ≤ 1</code>）与 <code>x</code> 轴围成的图形绕 <code>x</code> 轴旋转所成体积。\n<b>定限</b>：图形在 <code>x</code> 方向从 <code>0</code> 到 <code>1</code>，所以 <code>a = 0</code>，<code>b = 1</code>。\n<b>套公式</b>：<code>V = π∫₀¹ (x²)² dx = π∫₀¹ x⁴ dx = π[x⁵/5]₀¹ = π/5</code>。\n<b>再算一个球</b>：半径 <code>R</code> 的球体积 <code>= π∫_{−R}^{R}(R² − x²)dx = 4πR³/3</code>。当 <code>R = 3</code> 时得 <code>36π</code>，与 <code>4π·27/3 = 36π</code> 一致。',
          pitfalls: [
            '把 <code>πf(x)²</code> 错写成 <code>(πf(x))²</code> 或 <code>πf(x)</code>，前者多一个 <code>π</code>，后者变成“周长积分”，量纲都不对。',
            '旋转轴不是 <code>x</code> 轴时照抄公式：绕 <code>y = c</code> 旋转，半径应是 <code>f(x) − c</code>，即 <code>V = π∫ₐᵇ [f(x) − c]² dx</code>。',
            '遇到中空（转起来中间被挖空）的情形仍用圆盘法，实际应该用垫圈法减掉内圈。',
            '把“面积”和“体积”两个量的要求混起来：算面积必须取绝对值，算体积时 <code>f(x)²</code> 会自动变正。',
          ],
          tags: ['体积', '旋转体', '圆盘法'],
          related: ['fml-volume-washer', 'fml-volume-shell', 'fml-volume-cross-section', 'fml-area-cartesian'],
        },
        {
          id: 'fml-volume-washer',
          kind: 'formula',
          name: '旋转体的体积（垫圈法）',
          aka: ['垫圈法', '中空旋转体'],
          statement: '设 <code>f(x) ≥ g(x) ≥ 0</code> 在 <code>[a, b]</code> 上连续。由 <code>y = f(x)</code>、<code>y = g(x)</code> 与 <code>x = a</code>、<code>x = b</code> 围成的图形绕 <code>x</code> 轴旋转一周，所得中空旋转体的体积为\n<code>V = π∫ₐᵇ [f(x)² − g(x)²] dx</code>。\n这里 <code>f</code> 是外半径，<code>g</code> 是内半径，得到的是一个“垫圈”（中间有洞的圆环饼）。',
          plain: '如果图形不贴着旋转轴，转起来就会在中间留个洞，像一枚垫圈或一个甜甜圈。\n算体积也简单：先按外圈算一个大圆饼的体积，再减掉中间那个洞（小圆饼）的体积——但要注意是<b>半径的平方相减</b>，不能把半径直接相减再平方。',
          why: '为什么不能写成 <code>π∫(f − g)²dx</code>？因为那是“半径差”的圆饼，几何上根本不是我们转出来的形状。转出来的实心部分，每一片的截面积是 <code>πf² − πg² = π(f² − g²)</code>，是“大圆减小圆”，不是一个“小圆”。\n这就是初学最容易掉进去的坑：<b>遇到减法，先算各自的平方再减，而不是先减再平方</b>。可以自己代 <code>f = 2</code>、<code>g = 1</code> 试一下：正确值 <code>π(4 − 1) = 3π</code>，错误值 <code>π(1)² = π</code>，差了 3 倍。',
          proof: '推导：\n<b>1. 看一片的截面。</b>在 <code>[x, x + dx]</code> 处，图形是 <code>g(x)</code> 到 <code>f(x)</code> 的一段竖条。它绕 <code>x</code> 轴转一圈，扫出一个圆环：外半径 <code>f(x)</code>，内半径 <code>g(x)</code>。\n<b>2. 截面面积。</b>圆环面积 <code>= πf(x)² − πg(x)² = π[f(x)² − g(x)²]</code>。\n<b>3. 体积微元。</b>薄片厚度 <code>dx</code>，所以 <code>dV = π[f(x)² − g(x)²] dx</code>。\n<b>4. 积分求和。</b><code>V = π∫ₐᵇ [f(x)² − g(x)²] dx</code>。\n<b>5. 顺便验证球体积（另一种切法）。</b>把半径 <code>R</code> 的球看成由 <code>y = √(R² − x²)</code> 与 <code>y = 0</code> 围成的半圆绕 <code>x</code> 轴旋转而成，这里 <code>g(x) ≡ 0</code>，垫圈法直接退化成圆盘法，仍得 <code>4πR³/3</code>。这说明两个公式是一致的，垫圈法只是圆盘法的一般版。',
          example: '求圆 <code>x² + y² = R²</code> 绕直线 <code>y = R</code>（圆的上切线）旋转一周所得旋转体（一个“苹果形”体）的体积。\n<b>分析</b>：这个旋转体是实心的，但用水平切片看，每一片都是圆环：外半径 <code>R + √(R² − x²)</code>，内半径 <code>R − √(R² − x²)</code>。\n<b>用垫圈法</b>：<code>V = π∫_{−R}^{R}[(R + √(R² − x²))² − (R − √(R² − x²))²]dx</code>。\n展开：两项之差 <code>= 4R√(R² − x²)</code>。\n所以 <code>V = 4πR∫_{−R}^{R} √(R² − x²) dx</code>。而 <code>∫_{−R}^{R}√(R² − x²)dx</code> 正是半径为 <code>R</code> 的半圆面积 <code>= πR²/2</code>。\n<b>结果</b>：<code>V = 4πR · πR²/2 = 2π²R³</code>。结果干净得令人愉快，可以用来记这个经典题。',
          pitfalls: [
            '把公式写成 <code>π∫(f − g)²dx</code>，结果严重偏小。',
            '内外半径搞反（不减反而加），体积变大。',
            '内半径不是 <code>g(x)</code> 而是到轴的距离：绕 <code>y = c</code> 旋转时内外半径都要减去 <code>c</code> 再取绝对值。',
            '图形跨过旋转轴时套用本公式（此时内半径会出现负数），应该改用柱壳法。',
          ],
          tags: ['体积', '旋转体', '垫圈法'],
          related: ['fml-volume-disk', 'fml-volume-shell', 'fml-element-method-steps'],
        },
        {
          id: 'fml-volume-shell',
          kind: 'formula',
          name: '旋转体的体积（柱壳法）',
          aka: ['柱壳法', '薄壳法', '圆筒法'],
          statement: '设 <code>f(x) ≥ 0</code> 在 <code>[a, b]</code>（<code>a ≥ 0</code>）上连续。由 <code>y = f(x)</code>、<code>x = a</code>、<code>x = b</code> 与 <code>x</code> 轴围成的图形绕 <code>y</code> 轴旋转一周，所得旋转体的体积为\n<code>V = 2π∫ₐᵇ x f(x) dx</code>。\n一般地，绕 <code>y</code> 轴旋转时，柱壳法的自变量是 <code>x</code>，被积函数里出现“周长 <code>2πx</code> 乘高 <code>f(x)</code>”。',
          plain: '前面是“把萝卜横着切片”，柱壳法是“把萝卜竖着剥皮”。\n想象一棵树的年轮，或者一卷卫生纸：它由无数层薄薄的圆筒套起来。每一层圆筒的周长是 <code>2πx</code>（<code>x</code> 是它到中心的距离），高是 <code>f(x)</code>，厚度是 <code>dx</code>。把这一层摊平，就是一张长 <code>2πx</code>、高 <code>f(x)</code>、厚 <code>dx</code> 的薄板，体积是 <code>2πx f(x) dx</code>。把所有薄壳套起来就是总体积。',
          why: '既然已有圆盘/垫圈法，为什么还要柱壳法？因为<b>有些题用切片法算不出来或特别麻烦</b>。典型例子：求 <code>y = x²</code> 与 <code>y = x</code> 围成的图形绕 <code>y</code> 轴旋转。用切片法要横着切，得把边界解成 <code>x = √y</code> 的形式，还要按 <code>y</code> 分段处理上边界是直线还是抛物线；而柱壳法一段就搞定。\n选择标准很简单：<b>看哪个方向的边界更容易写成函数</b>。绕 <code>y</code> 轴转时，柱壳法对 <code>x</code> 积分，避开了“求反函数”这个麻烦。',
          proof: '推导：\n<b>1. 找微元。</b>取 <code>[x, x + dx]</code>，这个竖条绕 <code>y</code> 轴旋转，得到厚度为 <code>dx</code>、半径为 <code>x</code>、高为 <code>f(x)</code> 的薄圆筒。\n<b>2. 展开成薄板。</b>把圆筒沿一条母线剪开摊平，长是底面周长 <code>2πx</code>，高是 <code>f(x)</code>，厚是 <code>dx</code>。要注意：外圈周长是 <code>2π(x + dx)</code>，内圈是 <code>2πx</code>，展开后的真实体积介于两者之间，差别只有 <code>dx</code> 的二阶量级，可忽略。因此 <code>dV = 2πx f(x) dx</code>。\n<b>3. 积分。</b><code>V = 2π∫ₐᵇ x f(x) dx</code>。\n<b>4. 与圆盘法的等价性验证。</b>用两种方法算同一个球：\n圆盘法：<code>V = π∫_{−R}^{R}(R² − x²)dx = 4πR³/3</code>。\n柱壳法：把上半圆 <code>y = √(R² − x²)</code> 绕 <code>y</code> 轴旋转（<code>0 ≤ x ≤ R</code>）：<code>V = 2π∫₀^R x√(R² − x²) dx</code>。令 <code>u = R² − x²</code>，<code>du = −2x dx</code>，得 <code>V = π∫₀^{R²}√u du = π·(2/3)R³ = 4πR³/3</code>，完全一致。\n[[tip:两种方法算出同一个体积，是检验自己是否套错公式的最好办法。题目不限制你用哪种，选算得动的那种。]]',
          example: '求由 <code>y = x²</code>、<code>y = 0</code>、<code>x = 1</code> 围成的图形绕 <code>y</code> 轴旋转所得体积。\n<b>柱壳法</b>：<code>V = 2π∫₀¹ x·x² dx = 2π∫₀¹ x³ dx = 2π[x⁴/4]₀¹ = π/2</code>。\n<b>切片法验算</b>：图形绕 <code>y</code> 轴旋转后，<code>y</code> 从 <code>0</code> 到 <code>1</code>，每一层截面的外半径固定为 <code>1</code>，内半径是 <code>x = √y</code>，截面是圆环，面积 <code>π(1 − y)</code>。于是 <code>V = π∫₀¹(1 − y)dy = π[y − y²/2]₀¹ = π/2</code>。两法一致。',
          pitfalls: [
            '忘记 <code>2π</code>，只算成 <code>∫x f(x)dx</code>（那是“面积矩”，不是体积）。',
            '半径写错：绕 <code>y</code> 轴转半径是 <code>x</code>；绕 <code>x = c</code> 转则半径是 <code>|x − c|</code>，写成 <code>2π∫(x − c)f(x)dx</code> 时要小心符号。',
            '图形有一部分在 <code>y</code> 轴左边（<code>x &lt; 0</code>）时直接套用，得到负体积，应取 <code>|x|</code>。',
            '柱壳法和圆盘法用混：柱壳法对 <code>x</code> 积分、被积函数含 <code>x</code>；圆盘法对外层变量积分、被积函数含半径的平方。',
          ],
          tags: ['体积', '旋转体', '柱壳法'],
          related: ['fml-volume-disk', 'fml-volume-washer', 'fml-element-method-steps'],
        },
        {
          id: 'fml-volume-cross-section',
          kind: 'formula',
          name: '平行截面面积已知的立体体积',
          aka: ['截面法', '已知截面面积求体积'],
          statement: '设一立体夹在平面 <code>x = a</code> 与 <code>x = b</code> 之间，且对每个 <code>x ∈ [a, b]</code>，用垂直于 <code>x</code> 轴的平面去截它所得截面的面积 <code>A(x)</code> 是已知的连续函数，则该立体的体积为\n<code>V = ∫ₐᵇ A(x) dx</code>。\n特别地，若截面是半径 <code>f(x)</code> 的圆，就得到旋转体体积 <code>V = π∫ₐᵇ f(x)² dx</code>。',
          plain: '这是所有体积公式的“祖师爷”。它说的道理朴素到不能再朴素：<b>体积就是把一片片截面积沿着厚度方向加起来</b>。\n就像一摞切片面包：只要知道每一片面包的面积和厚度，整袋面包的体积就是所有片加起来。至于每片面包是什么形状，公式根本不关心。',
          why: '为什么它能把旋转体、棱锥、斜切圆柱全都管住？因为它只要求“你能算出每个位置的截面积”，对截面形状毫无限制。这正体现了微元法的一般性：<b>旋转体体积公式只是它的一个特例</b>。\n更有价值的是反过来用：当你记不住该用圆盘法还是垫圈法时，回到这一条，问自己“垂直于某轴的截面是什么形状、面积是多少”，答案自然浮现。',
          proof: '推导：\n<b>1. 找微元。</b>取 <code>[x, x + dx]</code>，这段立体像一个很薄的“片”。因为 <code>A(x)</code> 连续，这一片内各处的截面面积几乎都是 <code>A(x)</code>，所以可当成底面积 <code>A(x)</code>、高 <code>dx</code> 的柱体，<code>dV = A(x) dx</code>。\n<b>2. 误差。</b><code>A</code> 在这一小段内的变化量与 <code>dx</code> 同阶，造成的体积误差是 <code>dx</code> 的二阶量，是高阶无穷小。\n<b>3. 积分。</b><code>V = ∫ₐᵇ A(x) dx</code>。\n<b>4. 用它验证两个老朋友。</b>\n球：取截面面积 <code>A(x) = π(R² − x²)</code>，得 <code>V = 4πR³/3</code>。\n正棱锥（底面积 <code>S</code>、高 <code>h</code>）：截面是与底面相似的多边形，相似比为 <code>(h − x)/h</code>，所以 <code>A(x) = S(h − x)²/h²</code>，于是 <code>V = ∫₀^h S(1 − x/h)² dx = S/3 · h</code>，正好是初中背的锥体体积公式。',
          example: '一立体以半径 <code>R</code> 的圆为底，用垂直于底面某条直径的平面去截，截面都是等腰直角三角形（直角边落在底面内），求体积。\n<b>建系</b>：把底圆放成 <code>x² + y² = R²</code>，用垂直于 <code>x</code> 轴的平面截。\n<b>求截面面积</b>：在 <code>x</code> 处，平面与底圆相交出一条弦，长度是 <code>2√(R² − x²)</code>。截面是等腰直角三角形，直角边长就是这条弦，所以 <code>A(x) = (1/2)·[2√(R² − x²)]² = 2(R² − x²)</code>。\n<b>积分</b>：<code>V = ∫_{−R}^{R} 2(R² − x²) dx = 2[R²x − x³/3]_{−R}^{R} = 2 · (4R³/3) = 8R³/3</code>。\n验算量纲：结果是长度的三次方，正确；且随 <code>R</code> 增大而增大，符合直觉。',
          pitfalls: [
            '截面不垂直于 <code>x</code> 轴时仍然照搬 <code>∫A(x)dx</code>，此时薄片的厚度不再是 <code>dx</code>，需要用投影关系折算。',
            '求截面面积时把弦长、边长、对角线搞混，例如把等腰直角三角形的斜边当成了直角边。',
            '忘记截面面积本身也依赖 <code>x</code>，把它当成常数提了出来。',
          ],
          tags: ['体积', '截面法', '几何应用'],
          related: ['fml-volume-disk', 'fml-element-method-steps', 'fml-volume-washer'],
        },
        {
          id: 'fml-arc-length-cartesian',
          kind: 'formula',
          name: '平面曲线的弧长（直角坐标）',
          aka: ['弧长公式', '直角坐标弧长'],
          statement: '设 <code>y = f(x)</code> 在 <code>[a, b]</code> 上有一阶连续导数，则曲线从 <code>x = a</code> 到 <code>x = b</code> 的一段弧长为\n<code>s = ∫ₐᵇ √(1 + y′²) dx</code>，其中 <code>y′ = f′(x)</code>。\n公式也常写成 <code>s = ∫ₐᵇ √(1 + f′(x)²) dx</code>。',
          plain: '量一段弯曲绳子的长度，最笨也最有效的办法：把它切成很多很短的小段，每小段几乎就是直的，用勾股定理量出这一小段，再加起来。\n一小段在横方向走了 <code>dx</code>，在竖方向走了 <code>dy</code>，那它的长度就是 <code>√(dx² + dy²)</code>——这就是勾股定理。把 <code>dy = y′dx</code> 代进去，提出 <code>dx</code>，就得到 <code>√(1 + y′²)dx</code>。积分就是把这些小段全加起来。',
          why: '这是全章最值得理解的一个公式，因为它完美展示了“以直代曲”的威力，也暴露了它的边界。\n<b>为什么允许把曲线当成直线？</b>在一段足够短的弧上，曲线的弯曲量是 <code>(dx)²</code> 级别（切线方向几乎没变）。直线是弦，弧比弦长一点点，这个差值也是二阶量。分成 <code>n</code> 段后总差值趋于零，所以极限就是精确弧长。\n<b>为什么必须平方相加、不能简单相加？</b>因为“走的路程”是向量长度，横向位移和纵向位移按勾股定理合成，不是相加。如果曲线先往右再往上，你走的路比“横着走一段再竖着走一段”短——这正是三角形两边之和大于第三边。\n[[warn:弧长公式要求导数存在且连续（曲线要光滑）。若曲线有尖角，尖角处不可导，必须把尖角作为分段点，分几段分别积分，不能整段硬套公式。]]',
          proof: '推导（<code>√(1 + y′²)</code> 是怎么来的）：\n<b>1. 分割并近似。</b>在曲线 <code>y = f(x)</code> 上取一小段，端点横坐标为 <code>x</code> 与 <code>x + Δx</code>，对应纵坐标之差 <code>Δy = f(x + Δx) − f(x)</code>。把这一小段曲线近似看作直线段（连接两端点的弦），由勾股定理，其长度 <code>Δs ≈ √(Δx² + Δy²)</code>。\n<b>2. 化成对 x 的表达式。</b>提出 <code>Δx</code>：<code>Δs ≈ √(1 + (Δy/Δx)²)·Δx</code>。\n<b>3. 取极限。</b>由导数定义 <code>Δy/Δx → y′</code>，并注意到弦长与弧长之差的极限为 0，得弧长微元\n<code>ds = √(1 + y′²) dx</code>。\n<b>4. 积分求和。</b><code>s = ∫ₐᵇ √(1 + y′²) dx</code>。\n<b>5. 一个能一眼看出对错的特例。</b>若 <code>f(x) = mx + c</code> 是直线，则 <code>y′ = m</code> 为常数，<code>s = √(1 + m²)·(b − a)</code>，这正是直线段的长度公式（水平距离 <code>b − a</code>，竖直距离 <code>m(b − a)</code>，开方后提出 <code>b − a</code>），完全吻合。',
          example: '求抛物线 <code>y = x²</code> 从 <code>x = 0</code> 到 <code>x = 1</code> 的弧长。\n<b>求导</b>：<code>y′ = 2x</code>，所以 <code>√(1 + y′²) = √(1 + 4x²)</code>。\n<b>定限</b>：<code>0</code> 到 <code>1</code>。\n<b>积分</b>：<code>s = ∫₀¹ √(1 + 4x²) dx</code>。用代换 <code>2x = tan t</code>，<code>2dx = sec²t dt</code>，得\n<code>s = (1/2)∫ sec³t dt = (1/4)[sec t · tan t + ln(sec t + tan t)]</code>（上下限随代换变：<code>x = 0</code> 对应 <code>t = 0</code>；<code>x = 1</code> 对应 <code>tan t = 2</code>）。\n代回 <code>tan t = 2x</code>、<code>sec t = √(1 + 4x²)</code>，得原函数 <code>(1/4)[2x√(1 + 4x²) + ln(2x + √(1 + 4x²))]</code>。\n代入 <code>0</code> 到 <code>1</code>：<code>s = (1/4)[2√5 + ln(2 + √5)] ≈ (1/4)(4.4721 + 1.4436) ≈ 1.479</code>。\n<b>合理性检查</b>：弦长是 <code>√2 ≈ 1.414</code>，弧比弦长，<code>1.479 &gt; 1.414</code>，合理；而且曲线只弯了一点，弧长只比弦长大一点点，也合理。',
          pitfalls: [
            '把公式写成 <code>∫(1 + y′)dx</code> 或 <code>∫√(1 + y′)dx</code>，忘了平方。',
            '忘了根号外面还要乘 <code>dx</code>：正确形式是 <code>√(1 + y′²) dx</code>，只有 <code>1 + y′²</code> 在根号内。',
            '积分上下限用了 <code>y</code> 的范围（应该用 <code>x</code> 的范围）。',
            '曲线有尖角或竖直切线（如 <code>y = x<sup>2/3</sup></code> 在 <code>x = 0</code>）时不分段，导致积分算不出来或算错。',
            '把弧长和“两点间距离”混为一谈，直接用弦长公式。',
          ],
          tags: ['弧长', '几何应用', '勾股定理'],
          related: ['fml-arc-length-parametric', 'fml-arc-length-polar', 'fml-surface-area-revolution', 'fml-element-method-steps'],
        },
        {
          id: 'fml-arc-length-parametric',
          kind: 'formula',
          name: '平面曲线的弧长（参数方程）',
          aka: ['参数方程弧长'],
          statement: '设曲线由参数方程 <code>x = x(t)</code>、<code>y = y(t)</code>（<code>α ≤ t ≤ β</code>）给出，且 <code>x′(t)</code>、<code>y′(t)</code> 连续、不同时为零，则弧长为\n<code>s = ∫_α^β √(x′(t)² + y′(t)²) dt</code>。',
          plain: '还是勾股定理，只不过现在“一小步”是用时间（或角度）来计量的。\n参数走得快，横向和纵向就各自走得快：横向速度是 <code>x′(t)</code>，纵向速度是 <code>y′(t)</code>，合速度就是 <code>√(x′² + y′²)</code>。速度乘时间 <code>dt</code> 就是这一小段路程，加起来就是全路程。\n这跟物理里“速率对时间积分得路程”是同一个道理。',
          why: '为什么参数方程反而更“本质”？因为直角坐标公式有个隐含要求：曲线必须能写成单值函数 <code>y = f(x)</code>。但圆、椭圆、摆线这些曲线做不到——一个 <code>x</code> 可能对应两个 <code>y</code>。参数方程 <code>(x(t), y(t))</code> 用第三个变量把曲线“捋直”了，一个 <code>t</code> 只对应一个点，所以适用范围更广，而且形式对称、干净。\n其实直角坐标公式只是它的特例：取参数 <code>t = x</code>，则 <code>x′ = 1</code>，公式立刻变成 <code>∫√(1 + y′²)dx</code>。',
          proof: '推导：\n<b>1. 找微元。</b><code>t</code> 增到 <code>t + dt</code> 时，位移的两个分量为 <code>dx = x′(t)dt</code>、<code>dy = y′(t)dt</code>。\n<b>2. 用勾股定理合成长度。</b>小段近似为直线，<code>ds = √(dx² + dy²) = √(x′(t)²dt² + y′(t)²dt²) = √(x′(t)² + y′(t)²) dt</code>。\n<b>3. 积分。</b><code>s = ∫_α^β √(x′(t)² + y′(t)²) dt</code>。\n<b>4. 圆的验证。</b>圆 <code>x = R cos t</code>、<code>y = R sin t</code>（<code>0 ≤ t ≤ 2π</code>）：<code>x′ = −R sin t</code>，<code>y′ = R cos t</code>，于是 <code>√(x′² + y′²) = √(R² sin²t + R² cos²t) = R</code>（常数！）。\n所以 <code>s = ∫₀^{2π} R dt = 2πR</code>，正是圆周长公式。这一步特别漂亮：<b>用微积分推出了圆周长公式，同时说明被积函数是常数意味着曲线各处“走得一样快”，这正是圆的性质。</b>',
          example: '求摆线（旋轮线）<code>x = a(t − sin t)</code>、<code>y = a(1 − cos t)</code> 的一拱（<code>0 ≤ t ≤ 2π</code>）的弧长。\n<b>求导</b>：<code>x′ = a(1 − cos t)</code>，<code>y′ = a sin t</code>。\n<b>化简被积函数</b>：<code>x′² + y′² = a²[(1 − cos t)² + sin²t] = a²[1 − 2cos t + cos²t + sin²t] = a²(2 − 2cos t) = 2a²(1 − cos t)</code>。\n用半角公式 <code>1 − cos t = 2sin²(t/2)</code>，得 <code>x′² + y′² = 4a²sin²(t/2)</code>，所以 <code>√(x′² + y′²) = 2a sin(t/2)</code>（在 <code>0 ≤ t ≤ 2π</code> 上 <code>sin(t/2) ≥ 0</code>，可直接去掉绝对值）。\n<b>积分</b>：<code>s = ∫₀^{2π} 2a sin(t/2) dt = 2a · [−2cos(t/2)]₀^{2π} = 2a·(2 + 2) = 8a</code>。\n<b>验算</b>：摆线一拱的“宽度”是 <code>2πa ≈ 6.28a</code>，“高度”是 <code>2a</code>，弧长 <code>8a</code> 介于两者之间且明显大于宽度，符合视觉上“拱起又落下”的实际形状。这是课本的经典结果。',
          pitfalls: [
            '漏乘 <code>dt</code>：写成 <code>∫√(x′² + y′²)</code> 就失去了“长度”的量纲。',
            '积分上下限用了 <code>x</code> 的范围，忘了要先由参数方程把 <code>x</code> 的范围换算成 <code>t</code> 的范围。',
            '开方后忘记加绝对值：如摆线题里的 <code>√(4a²sin²(t/2)) = 2a|sin(t/2)|</code>，只有当 <code>sin(t/2) ≥ 0</code> 时才能直接写成 <code>2a sin(t/2)</code>。',
            '曲线自交或来回往返时（如 <code>x = t²</code>），同一段几何弧被重复走过，需要按实际几何范围重新定限，否则算出的是“走过的路程”而不是“弧长”。',
          ],
          tags: ['弧长', '参数方程', '几何应用'],
          related: ['fml-arc-length-cartesian', 'fml-arc-length-polar', 'fml-surface-area-revolution'],
        },
        {
          id: 'fml-arc-length-polar',
          kind: 'formula',
          name: '平面曲线的弧长（极坐标）',
          aka: ['极坐标弧长'],
          statement: '设曲线由极坐标方程 <code>r = r(θ)</code>（<code>α ≤ θ ≤ β</code>）给出，<code>r(θ)</code> 有一阶连续导数，则弧长为\n<code>s = ∫_α^β √(r(θ)² + r′(θ)²) dθ</code>。',
          plain: '极坐标里的一小步，可以看成“斜着走”。\n一步里身体发生了两件事：一是<b>转了角度</b>，于是沿着圆弧划过了 <code>r dθ</code> 这么长；二是<b>半径变了</b>，于是又沿半径方向走了 <code>dr</code>。这两个方向互相垂直（一个沿着圆弧，一个沿着半径），所以合起来还是勾股定理：<code>ds = √((r dθ)² + dr²)</code>。把 <code>dr = r′dθ</code> 代入、提出 <code>dθ</code>，就得到 <code>√(r² + r′²)dθ</code>。',
          why: '这个公式最容易记错，因为根号里是 <code>r² + r′²</code>——<code>r</code> 本身带平方，但它对应的是“转角度”的贡献，不是导数。记法：<b>“转角度方向的 <code>r</code>”和“半径变化方向的 <code>r′</code>”地位相同，都出一次平方。</b>\n用特例检验最有效：若曲线是圆 <code>r ≡ R</code>，则 <code>r′ = 0</code>，公式给 <code>s = ∫R dθ = R(β − α)</code>，正是圆弧长公式。若曲线是射线 <code>θ ≡ 常数</code>，则 <code>s = ∫|r′|dθ</code>，即径向长度的累加，也对。两个极端都对，公式就靠得住。',
          proof: '推导（两条路，都走一遍）：\n<b>路一，从参数方程转化。</b>极坐标其实是一种特殊的参数方程：<code>x = r cos θ</code>、<code>y = r sin θ</code>，参数是 <code>θ</code>。求导得\n<code>x′ = r′cos θ − r sin θ</code>，<code>y′ = r′sin θ + r cos θ</code>。\n计算 <code>x′² + y′²</code>：交叉项恰好抵消，得\n<code>x′² + y′² = (r′²cos²θ − 2rr′sin θcos θ + r²sin²θ) + (r′²sin²θ + 2rr′sin θcos θ + r²cos²θ) = r′² + r²</code>。\n代入参数方程弧长公式 <code>s = ∫√(x′² + y′²)dθ</code>，立刻得到 <code>s = ∫_α^β √(r² + r′²) dθ</code>。\n<b>路二，直接用微元（更直观）。</b>角度增 <code>dθ</code> 时，径向长度变化 <code>dr = r′dθ</code>，垂直于径向的弧向长度为 <code>r dθ</code>。两者垂直，勾股定理给 <code>ds = √((r dθ)² + (dr)²) = √(r² + r′²) dθ</code>，与路一结果一致。\n<b>用圆检验。</b><code>r ≡ R</code> 得 <code>r′ = 0</code>，<code>s = R(β − α)</code>；取 <code>α = 0</code>、<code>β = 2π</code> 得 <code>2πR</code>，圆周长，正确。',
          example: '求心形线 <code>r = a(1 + cos θ)</code>（<code>a &gt; 0</code>）的全长。\n<b>求导</b>：<code>r′ = −a sin θ</code>。\n<b>化简</b>：<code>r² + r′² = a²[(1 + cos θ)² + sin²θ] = a²(2 + 2cos θ) = 4a²cos²(θ/2)</code>，所以 <code>√(r² + r′²) = 2a|cos(θ/2)|</code>。\n<b>定限并分段</b>：曲线封闭一圈对应 <code>θ</code> 从 <code>0</code> 到 <code>2π</code>。在 <code>[0, π]</code> 上 <code>cos(θ/2) ≥ 0</code>；在 <code>[π, 2π]</code> 上 <code>cos(θ/2) ≤ 0</code>。\n<b>积分</b>：<code>s = ∫₀^π 2a cos(θ/2)dθ + ∫_π^{2π} (−2a cos(θ/2))dθ = 2a[2sin(θ/2)]₀^π − 2a[2sin(θ/2)]_π^{2π} = 4a + 4a = 8a</code>。\n<b>验算</b>：心形线围成的面积是 <code>(3/2)πa² ≈ 4.71a²</code>，周长 <code>8a</code>。取 <code>a = 1</code>，曲线最宽处横跨 <code>4</code>，周长 <code>8</code> 是宽度的两倍，对于这个胖胖的、带个凹坑的形状，很合理。',
          pitfalls: [
            '<code>r</code> 忘记平方，写成 <code>√(r + r′²)</code>；或写成 <code>√(r² + r′)</code>，两者都错。',
            '把被积函数和极坐标面积公式搞混：面积是 <code>(1/2)∫r²dθ</code>（没有根号），弧长是 <code>∫√(r² + r′²)dθ</code>（有根号）。',
            '<code>r(θ) &lt; 0</code> 的区间没处理。极坐标允许 <code>r</code> 取负（点画在相反方向的射线上），此时 <code>r²</code> 仍然是正的，弧长公式依然成立，所以不要以为负半径是错误。',
            '<code>√(cos²(θ/2))</code> 直接开成 <code>cos(θ/2)</code> 而不加绝对值，会算出一个偏小甚至为 0 的荒谬结果。',
          ],
          tags: ['弧长', '极坐标', '几何应用'],
          related: ['fml-arc-length-parametric', 'fml-arc-length-cartesian', 'fml-area-polar', 'fml-surface-area-revolution'],
        },
        {
          id: 'fml-surface-area-revolution',
          kind: 'formula',
          name: '旋转曲面的面积',
          aka: ['旋转体侧面积', '旋转曲面面积'],
          statement: '设 <code>f(x) ≥ 0</code> 在 <code>[a, b]</code> 上有连续导数。曲线 <code>y = f(x)</code>（<code>a ≤ x ≤ b</code>）绕 <code>x</code> 轴旋转一周所得旋转曲面的面积为\n<code>S侧 = 2π∫ₐᵇ f(x)√(1 + f′(x)²) dx</code>。\n若曲线由参数方程 <code>x = x(t)</code>、<code>y = y(t)</code>（<code>α ≤ t ≤ β</code>，<code>y(t) ≥ 0</code>）给出，则\n<code>S侧 = 2π∫_α^β y(t)√(x′(t)² + y′(t)²) dt</code>。',
          plain: '把曲面的皮“竖着切成一条条细带”，每条细带摊平后就是一个长方形：宽是这一小段曲线的长度 <code>ds</code>，长是它转一圈划出的周长 <code>2πf(x)</code>。\n长方形的面积就是 <code>2πf(x)·ds</code>，把所有细带加起来就是整个侧面积。注意这里用的是“曲线弧长 <code>ds</code>”而不是 <code>dx</code>——这是本题的关键，也是错得最多的地方。',
          why: '为什么<b>不能</b>用 <code>2π∫f(x)dx</code>？这是本章最深刻的一个坑。\n因为斜着的那一小段曲线，比它的水平投影 <code>dx</code> 要长。如果按 <code>dx</code> 算，你算的是把这条斜带“压扁”后的面积，会系统性偏小。就像卷一个纸筒：斜着卷和水平着卷，用的纸不一样多。\n更准确地说：<b>切出来的一小条圆台侧面，其面积 = π(r₁ + r₂)·母线长。</b>当 <code>dx → 0</code> 时 <code>r₁ → r₂ → f(x)</code>，所以 <code>π(r₁ + r₂) → 2πf(x)</code>，而母线长就是弧长微元 <code>ds</code>。因此 <code>dS = 2πf(x) ds</code>。\n[[warn:这里的误差分析比前面更严格：如果用 <code>dx</code> 代替 <code>ds</code>，误差是 <code>dx</code> 的<b>一阶</b>量（因为 <code>ds − dx ≈ (y′²/2)dx</code>），<code>n</code> 段加起来不会消失，结果就是错的。这与体积公式可以容忍半径的一阶误差形成鲜明对比——所以体积用 <code>dx</code> 没问题，面积必须用 <code>ds</code>。]]',
          proof: '推导（用圆台侧面积，不用“薄圆柱”）：\n<b>1. 分割。</b>把曲线分成 <code>n</code> 小段，第 <code>i</code> 段两端点横坐标为 <code>x_{i−1}</code>、<code>xᵢ</code>，纵坐标为 <code>y_{i−1}</code>、<code>yᵢ</code>。\n<b>2. 用弦代替弧，得到圆台。</b>用弦代替这一小段弧，它绕 <code>x</code> 轴旋转出的曲面是一个<b>圆台侧面</b>（不是圆柱侧面），两底半径分别是 <code>y_{i−1}</code> 和 <code>yᵢ</code>，母线长是弦长 <code>√(Δxᵢ² + Δyᵢ²)</code>。\n<b>3. 用圆台侧面积公式。</b>圆台侧面积 <code>= π(r₁ + r₂)·l</code>，所以\n<code>ΔSᵢ = π(y_{i−1} + yᵢ)·√(Δxᵢ² + Δyᵢ²)</code>。\n<b>4. 化简微元。</b>因 <code>y</code> 连续，<code>y_{i−1} + yᵢ ≈ 2y(ξᵢ)</code>，故\n<code>ΔSᵢ ≈ 2πy(ξᵢ)√(Δxᵢ² + Δyᵢ²) = 2π y(ξᵢ)√(1 + (Δyᵢ/Δxᵢ)²) Δxᵢ</code>。\n<b>5. 取极限。</b>因 <code>Δyᵢ/Δxᵢ → y′</code>，于是\n<code>S侧 = lim Σ 2π y(ξᵢ)√(1 + y′(ξᵢ)²) Δxᵢ = 2π∫ₐᵇ f(x)√(1 + f′(x)²) dx</code>。\n<b>6. 球面积的验证（必做自检）。</b>半径 <code>R</code> 的球面由 <code>y = √(R² − x²)</code>（<code>−R ≤ x ≤ R</code>）绕 <code>x</code> 轴旋转而成。<code>y′ = −x/√(R² − x²)</code>，于是\n<code>√(1 + y′²) = √(1 + x²/(R² − x²)) = √(R²/(R² − x²)) = R/√(R² − x²) = R/y</code>。\n代入公式，<code>S = 2π∫_{−R}^{R} y · (R/y) dx = 2π∫_{−R}^{R} R dx = 2πR · 2R = 4πR²</code>，正是球面积公式。这一验证特别有价值：它说明“用 <code>ds</code> 而不是 <code>dx</code>”是必需的——若用 <code>dx</code> 就会算出 <code>2π∫y dx = π²R²</code> 这种莫名其妙的数，与 <code>4πR²</code> 相差甚远。',
          example: '求 <code>y = x²</code> 在 <code>0 ≤ x ≤ 1</code> 上的一段绕 <code>x</code> 轴旋转所得曲面面积。\n<b>准备</b>：<code>f(x) = x²</code>，<code>f′(x) = 2x</code>，<code>√(1 + f′²) = √(1 + 4x²)</code>。\n<b>套公式</b>：<code>S = 2π∫₀¹ x²√(1 + 4x²) dx</code>。\n<b>计算</b>：令 <code>2x = tan t</code>，则 <code>2dx = sec²t dt</code>，<code>√(1 + 4x²) = sec t</code>，<code>x² = tan²t/4</code>，于是\n<code>S = 2π∫ x²·sec t·dx = 2π∫ (tan²t/4)·sec t·(sec²t/2)dt = (π/4)∫ tan²t·sec³t dt</code>。\n用 <code>tan²t = sec²t − 1</code> 拆开后逐项积分（<code>∫sec³t dt</code> 与 <code>∫sec⁵t dt</code> 都是标准积分），再代回上下限 <code>x = 0 → t = 0</code>、<code>x = 1 → tan t = 2</code>，可得数值 <code>S ≈ 5.33</code>。\n<b>合理性检查</b>：这一段旋转后像个小喇叭，底端（<code>x = 1</code> 处）半径是 <code>1</code>，圆周长 <code>2π ≈ 6.28</code>，而曲线长度约 <code>1.479</code>。曲面面积应与“周长乘长度”同量级，<code>5.33</code> 与 <code>6.28 × 1.479 ≈ 9.3</code> 同量级，且因半径从 0 渐增而更小，合理。',
          pitfalls: [
            '把 <code>√(1 + f′²)dx</code> 简化成 <code>dx</code>，漏掉弧长因子——这是本节第一大错误。',
            '把 <code>2πf(x)</code> 写成 <code>πf(x)²</code>（那是截面面积，不是周长）。',
            '绕 <code>y</code> 轴旋转时半径应是到 <code>y</code> 轴的距离 <code>x</code>，公式变成 <code>S = 2π∫x√(1 + y′²)dx</code>，别照抄 <code>f(x)</code>。',
            '曲线穿过旋转轴（<code>f</code> 变号）时不分段、不取绝对值，导致曲面重叠部分被算重或算负。',
            '用参数方程时漏乘参数微元：应为 <code>2π∫y(t)√(x′² + y′²)dt</code>，不是 <code>2π∫y dt</code>。',
          ],
          tags: ['旋转曲面', '侧面积', '几何应用'],
          related: ['fml-arc-length-cartesian', 'fml-arc-length-parametric', 'fml-volume-disk', 'fml-element-method-steps'],
        },
      ],
    },
    {
      id: 'ch6-3',
      no: '6.3',
      title: '定积分在物理学上的应用',
      summary: '变力做功、水压力、引力、平均值——物理量的微元，几乎都是“某个强度 × 一个小几何量”。',
      items: [
        {
          id: 'fml-work-variable-force',
          kind: 'formula',
          name: '变力沿直线做功',
          aka: ['变力做功', '功的积分公式'],
          statement: '设物体在变力 <code>F(x)</code> 的作用下沿 <code>x</code> 轴由 <code>x = a</code> 移动到 <code>x = b</code>，且力的方向与运动方向一致、<code>F(x)</code> 在 <code>[a, b]</code> 上连续，则力所做的功为\n<code>W = ∫ₐᵇ F(x) dx</code>。\n更一般地，若力是位置的函数而位移是曲线的，只有在力与位移同方向且大小只依赖位置时才能这样写。',
          plain: '初中学过“功 = 力 × 距离”，但那个公式只在力不变时成立。\n拉弹簧就是典型：刚开始很轻松，越拉越费劲，力一直在变。怎么算总功？把它切成很多很多小段，每一小段里弹簧几乎没被继续拉长，所以力几乎不变——这一小段就退回初中公式，功是 <code>F(x)dx</code>。把所有小段加起来就是总功。\n就像爬一段忽陡忽缓的坡：每一小步的坡度可以看成不变，一步的“代价”算得出来，全程代价就是所有小步之和。',
          why: '为什么“力不变”这个条件这么要命？因为初中的乘法 <code>W = Fs</code> 本质上是“力在位移上的累积”，它要求被累积的量在过程中是常数。一旦力在变，乘法就失效了——你不能拿“平均力”去乘，因为平均力的位置你并不知道在哪里取。\n微元法解决的正是这件事：<b>把“变”限制在一个无限小的范围内，就变成“不变”</b>。这跟求曲边梯形面积是同一个套路：整段的“高”在变，但一小段内的“高”几乎不变，于是每一小段都是一个矩形。\n[[tip:功的单位是焦耳（牛·米），所以微元必须写成“力 × 长度”，也就是 <code>F(x)dx</code>。若你写出的微元里只有一个长度因子，比如 <code>F(x)</code> 本身，那它其实是力不是功。]]',
          proof: '推导：\n<b>1. 分割。</b>把区间 <code>[a, b]</code> 分成 <code>n</code> 个小段，各分点 <code>a = x₀ &lt; x₁ &lt; … &lt; xₙ = b</code>，第 <code>i</code> 段长 <code>Δxᵢ</code>。\n<b>2. 近似。</b>当 <code>Δxᵢ</code> 很小时，物体在第 <code>i</code> 段内位移很小，力 <code>F</code> 的变化也很小，可近似看作常力。由常力做功公式，这一段做功 <code>ΔWᵢ ≈ F(ξᵢ)Δxᵢ</code>（<code>ξᵢ</code> 为这一段内任一点）。\n<b>3. 求和。</b><code>W ≈ Σ F(ξᵢ)Δxᵢ</code>。\n<b>4. 取极限。</b>令各段长度的最大值趋于 0，右边按定积分定义趋于 <code>∫ₐᵇ F(x) dx</code>，即 <code>W = ∫ₐᵇ F(x) dx</code>。误差项的分析与曲边梯形面积完全一样：<code>F</code> 在一小段内的变化量是 <code>dx</code> 的一阶量，乘以 <code>dx</code> 后成为二阶量，是 <code>dx·dx</code> 的高阶无穷小，求和后消失。\n<b>5. 与初中公式的相容性检验。</b>若 <code>F</code> 为常数 <code>F₀</code>，则 <code>W = ∫ₐᵇ F₀ dx = F₀(b − a)</code>，正好退回“力 × 距离”。这说明新公式是老公式的推广，不是另起炉灶。',
          example: '三个必会算的例子。\n<b>例 1（变力沿直线）</b>：<code>F(x) = x²</code>（牛），物体从 <code>x = 0</code> 移到 <code>x = 3</code>（米），求功。\n<code>W = ∫₀³ x² dx = [x³/3]₀³ = 9</code>（焦耳）。\n<b>例 2（弹簧）</b>：由胡克定律 <code>F = kx</code>，把弹簧从自然长度拉伸 <code>s</code> 需做功\n<code>W = ∫₀^s kx dx = (1/2)ks²</code>。这就是弹性势能公式的来源。\n<b>例 3（抽水）</b>：把底面半径 <code>1</code> 米、高 <code>2</code> 米的圆柱形水桶抽空（水从桶口溢出），求需做多少功。<b>关键是把功写成“重量 × 提升高度”</b>：以水面为原点、<code>x</code> 轴向下，深度 <code>x</code> 处厚 <code>dx</code> 的一层水，体积是 <code>π·1²·dx = π dx</code>（立方米），重量是 <code>ρgπ dx</code>（<code>ρ = 1000</code> 千克每立方米，<code>g ≈ 9.8</code>），它到桶口的高度就是 <code>x</code> 米。\n所以 <code>dW = ρgπ x dx</code>，<code>W = ∫₀² ρgπ x dx = ρgπ[x²/2]₀² = 2ρgπ ≈ 2 × 1000 × 9.8 × π ≈ 61575</code>（焦耳）。\n<b>验算</b>：水的总质量是 <code>1000 × π × 2 = 2000π</code> 千克，重心在桶深度的中间 <code>1</code> 米处，所以做功等于 <code>2000π × 9.8 × 1 ≈ 61575</code> 焦耳，与积分结果一致。这个“重心验证法”是检查抽水做功题的最好办法。',
          pitfalls: [
            '力的单位与位移单位不统一（如力用千克力、位移用米），必须先化成国际单位再积分。',
            '力的表达式里用的是“剩余长度”还是“总长度”搞混。例如弹簧从已拉伸 1 米处再拉 1 米，积分下限是 <code>1</code> 而不是 <code>0</code>。',
            '抽水做功题的提升高度取错：应取“该层水到出口的高度”，取成“到水面的高度”或“到桶底的高度”都会错。',
            '忘记“水的密度 <code>ρ</code> 与重力加速度 <code>g</code>”只出现一次：重量是 <code>ρgV</code>，漏掉 <code>g</code> 就把质量当成了重量。',
            '坐标系方向任意设，但设完忘了 <code>dx</code> 的符号，导致积分限反向、结果出负号。',
          ],
          tags: ['物理应用', '做功', '变力'],
          related: ['fml-water-pressure', 'fml-average-value', 'fml-element-method-steps'],
        },
        {
          id: 'fml-water-pressure',
          kind: 'formula',
          name: '液体对平面薄板的静压力',
          aka: ['水压力', '静水压力'],
          statement: '设一平面薄板铅直地浸没在液体中。取 <code>x</code> 轴铅直向下、原点在液面上，薄板所占区域对应 <code>x ∈ [a, b]</code>，在深度 <code>x</code> 处薄板的水平宽度为 <code>w(x)</code>（连续）。又设液体密度为 <code>ρ</code>，重力加速度为 <code>g</code>，则液体对薄板一侧的总压力为\n<code>P = ρg ∫ₐᵇ x·w(x) dx</code>。\n若薄板的上边缘不在液面处，而是从深度 <code>a</code> 延伸到 <code>b</code>，公式形式不变，仍为 <code>P = ρg∫ₐᵇ x w(x) dx</code>。',
          plain: '水越深，压强越大——这是游泳时耳朵被压得疼的原因。压强公式是 <code>p = ρgx</code>（<code>x</code> 是深度）。\n所以一块竖着的闸门，上面受力小、下面受力大。怎么算总推力？还是老办法：把闸门横着切成很多条很窄的水平细条。一条细条很窄，它上下的深度几乎一样，压强就可以当成不变的，于是这一条受的力 = 压强 × 面积 = <code>ρgx · w(x)dx</code>。把所有细条加起来，就是总压力。\n关键是：<b>深度 <code>x</code> 是“从水面往下量”的距离</b>，不是从闸门顶部量的距离。',
          why: '为什么压强可以乘面积，但深度不能取整个板的平均值直接乘总面积？其实可以，但前提是你知道“平均压强”发生在哪个深度——那正是图形形心的深度。用积分做的好处是不用先找出形心在哪，直接一刀切到底。\n为什么公式里必然出现 <code>x</code> 这个因子？因为压强与深度成正比，这是液体静力学的基本事实（同一深度各方向压强相等，只与深度有关）。<b>深度是水压问题的灵魂</b>：同一个形状的闸门，装在水下 1 米和装在水下 10 米，受力差 10 倍。\n[[warn:如果题目给的坐标系原点不在液面上，必须先把深度换成“水面到该点的距离”。很多错误都发生在把坐标值直接当深度用。]]',
          proof: '推导：\n<b>1. 找微元。</b>在深度 <code>x</code> 处取宽为 <code>dx</code> 的水平细条（对应区间 <code>[x, x + dx]</code>）。因 <code>dx</code> 很小，这条上各点的深度都近似为 <code>x</code>。\n<b>2. 求这一条的面积。</b>水平宽度为 <code>w(x)</code>，厚度为 <code>dx</code>，所以 <code>dA = w(x) dx</code>，单位是平方米。\n<b>3. 求这一条上的压强。</b>由液体静压强公式 <code>p = ρgx</code>，单位是帕斯卡（牛每平方米）。\n<b>4. 乘出压力微元。</b>压力 = 压强 × 面积，所以\n<code>dP = p · dA = ρgx · w(x) dx</code>。\n量纲检查：<code>ρgx</code> 是压强（牛每平方米），乘上 <code>w(x)dx</code>（平方米）后是牛顿，正是力的单位，微元写对了。\n<b>5. 积分。</b><code>P = ρg∫ₐᵇ x w(x) dx</code>。误差分析同前：压强在一条细条内的变化是 <code>dx</code> 的一阶量，乘面积后成二阶量，求和后消失。\n<b>6. 用“形心法”交叉验证。</b>总压力也等于“液面处压强”乘“面积”乘“形心深度”，即 <code>P = ρg·x̄·A</code>，其中 <code>x̄</code> 是薄板图形的形心深度、<code>A</code> 是面积。而 <code>∫x w(x)dx</code> 恰好就是图形对液面的静矩，二者完全一致——这说明积分法与力学结论自洽。',
          example: '半径为 <code>R</code> 的半圆形闸门，直径边在液面上、弧朝下，求水对闸门一侧的压力。\n<b>建系</b>：<code>x</code> 轴向下，原点取在圆心（也就是液面上）。半圆的方程为 <code>x² + y² = R²</code>，只需 <code>x ≥ 0</code> 的下半部分，<code>x</code> 从 <code>0</code> 到 <code>R</code>。\n<b>求宽度</b>：深度 <code>x</code> 处的水平宽度是 <code>w(x) = 2√(R² − x²)</code>。\n<b>套公式</b>：<code>P = ρg∫₀^R x·2√(R² − x²) dx = 2ρg∫₀^R x√(R² − x²) dx</code>。\n<b>积分</b>：令 <code>u = R² − x²</code>，<code>du = −2x dx</code>，则\n<code>∫₀^R x√(R² − x²) dx = (1/2)∫₀^{R²} √u du = (1/2)·(2/3)R³ = R³/3</code>。\n<b>结果</b>：<code>P = 2ρg · R³/3 = (2/3)ρgR³</code>。\n<b>验算（形心法）</b>：半圆的面积是 <code>πR²/2</code>，半圆的形心到直径的距离是 <code>4R/(3π)</code>，所以\n<code>P = ρg · (4R/(3π)) · (πR²/2) = ρg · 2R³/3 = (2/3)ρgR³</code>，与积分结果完全一致。这个例子建议反复练，它把积分法和形心法绑在了一起。',
          pitfalls: [
            '深度取错：把坐标值直接当成深度，而坐标系原点并不在液面上。',
            '宽度函数写错：深度 <code>x</code> 处的宽度要由几何关系解出来（如圆里是 <code>2√(R² − x²)</code>），不是常数。',
            '闸门有一部分露出液面时仍对整个高度积分，应该只对浸没的那一段积分。',
            '忘记水的密度 <code>ρ = 1000</code> 千克每立方米，或忘记乘 <code>g ≈ 9.8</code>，结果差了近万倍。',
            '题目问“闸门两侧的压力差”时只算了一侧，忘记液体可能两面都有。',
          ],
          tags: ['物理应用', '水压力', '静压强'],
          related: ['fml-work-variable-force', 'fml-gravitation', 'fml-element-method-steps'],
        },
        {
          id: 'fml-gravitation',
          kind: 'formula',
          name: '两物体间的引力',
          aka: ['引力公式', '万有引力积分'],
          statement: '设一细杆（或细棒）位于 <code>x</code> 轴上的区间 <code>[a, a + l]</code>，线密度为常数 <code>μ</code>；另有一质量为 <code>m</code> 的质点位于原点（或位于轴上某点）。则由万有引力定律，用微元法可求得杆与质点间的引力大小为\n<code>F = Gμm∫_a^{a+l} (1/x²) dx = Gμm(1/a − 1/(a + l))</code>，方向指向杆。\n更一般地，引力公式为 <code>dF = G·(m·dm)/(r²)</code>，其中 <code>dm</code> 是被吸引物体（或被吸引的微元）的质量，<code>r</code> 是它与质点之间的距离。',
          plain: '万有引力定律说：两个物体之间的引力是 <code>G·m₁m₂/r²</code>，其中 <code>r</code> 是它们的距离。\n但当其中一个物体是<b>一根长长的杆</b>时，杆上不同位置到质点的距离 <code>r</code> 都不一样，近端拉得狠、远端拉得轻，不能用一次乘法算完。\n于是还是切：把杆切成很多很多小段，每小段很短，可以当成一个质点，它和外部那个质点的引力就能用公式算了。把所有小段的引力加起来，就是整根杆的引力。',
          why: '这里用微元法要特别小心一件事：<b>力是矢量，能不能直接把大小加起来？</b>只有当所有小段产生的引力方向都相同时才可以。本题中杆在 <code>x</code> 轴上、质点也在 <code>x</code> 轴上，每小段的引力方向都沿着杆的方向，方向一致，所以可以代数相加。\n如果杆不在质点的连线上（例如质点悬在杆的正上方，或者是一根斜杆），方向就各不相同了，必须把每个微元的力分解到坐标轴上，分别积分（<code>F_x = ∫dF_x</code>，<code>F_y = ∫dF_y</code>），绝不可以直接叠加大小。\n[[warn:引力问题里最常见的错误就是“只算大小不算方向”，在共线的简单情形下侥幸对了，一换到不共线就全错。判断准则：微元力方向是否恒定。]]',
          proof: '推导：\n<b>1. 建系与分割。</b>取质点 <code>m</code> 在原点，杆沿 <code>x</code> 轴从 <code>x = a</code> 到 <code>x = a + l</code>（<code>a &gt; 0</code>）。在 <code>x</code> 处取长为 <code>dx</code> 的一小段。\n<b>2. 求微元质量。</b>线密度为 <code>μ</code>，所以这一小段的质量 <code>dm = μ dx</code>。\n<b>3. 用万有引力定律写出这一小段的引力。</b>它的距离是 <code>x</code>，所以\n<code>dF = G·(m·dm)/x² = Gμm·(1/x²) dx</code>，方向沿 <code>x</code> 轴向原点。\n<b>4. 方向一致性判断。</b>所有微元的引力都沿同一直线同方向，故可直接积分大小：\n<code>F = Gμm∫_a^{a+l} (1/x²) dx = Gμm[−1/x]_a^{a+l} = Gμm(1/a − 1/(a + l))</code>。\n<b>5. 合理性检验。</b>\n若杆缩成一个点（<code>l → 0</code>），则 <code>F → 0</code>，因为杆的质量趋于零，正确。\n若把杆整个质量 <code>M = μl</code> 全部放在 <code>x = a</code> 处，引力应为 <code>GmM/a² = Gμm l/a²</code>。而我们的结果是 <code>Gμm(1/a − 1/(a + l)) = Gμm·l/(a(a + l))</code>。由于 <code>a(a + l) &gt; a²</code>，所以结果<b>小于</b>“质量集中在最近端”的值——这非常合理：杆的质量分散开了，远端拉得更弱，所以总引力一定小于假设全部集中在最近处。这个比较是判断答案有没有算反的好方法。',
          example: '一根长为 <code>l</code>、线密度为 <code>μ</code> 的均匀细杆，与一个质量为 <code>m</code> 的质点在一条直线上，质点距杆的近端为 <code>a</code>。取 <code>a = l = 1</code>、<code>Gμm = 1</code>（用无量纲单位），则\n<code>F = 1 · (1/1 − 1/2) = 1/2</code>。\n<b>对照检查</b>：若把杆的全部质量 <code>μ·1 = μ</code> 都当作集中在最近端（距离 1），引力是 <code>1</code>；若当作集中在杆的中点（距离 1.5），引力是 <code>1/2.25 ≈ 0.444</code>。而正确值 <code>0.5</code> 落在 <code>0.444</code> 与 <code>1</code> 之间，且更靠近中点那个估计，符合“质量分散、越远越弱”的直觉。\n<b>再看一个极限</b>：取 <code>a = 1</code>、<code>l = 100</code>（很长的杆），<code>F = 1 − 1/101 ≈ 0.990</code>。杆再长下去，<code>F</code> 也只能趋近于 <code>Gμm/a = 1</code>，不会超过它。这说明<b>无限长杆的引力是有限的</b>——因为 <code>1/x²</code> 趋于零足够快，远端的贡献可以忽略。这个结论值得记住。',
          pitfalls: [
            '把 <code>1/x²</code> 里的 <code>x</code> 写成“杆长”或“距离的一半”，其实它是微元到质点所在位置的实际距离。',
            '积分上下限写反：应是 <code>a</code> 到 <code>a + l</code>，不是 <code>0</code> 到 <code>l</code>（除非质点恰在杆端）。',
            '微元力方向不恒定时仍直接对大小积分，必须先按坐标轴分解。',
            '忘记 <code>dm = μ dx</code>（线密度）或 <code>dm = σ dA</code>（面密度）、<code>dm = ρ dV</code>（体密度），一味当成质点。',
          ],
          tags: ['物理应用', '引力', '万有引力'],
          related: ['fml-water-pressure', 'fml-element-method-steps', 'fml-work-variable-force'],
        },
        {
          id: 'fml-average-value',
          kind: 'formula',
          name: '连续函数的平均值（积分平均值定理）',
          aka: ['函数平均值', '积分中值定理'],
          statement: '设 <code>f(x)</code> 在 <code>[a, b]</code> 上连续，则 <code>f</code> 在 <code>[a, b]</code> 上的平均值为\n<code>ȳ = (1/(b − a))∫ₐᵇ f(x) dx</code>。\n并且存在 <code>ξ ∈ [a, b]</code>，使得 <code>ȳ = f(ξ)</code>，即 <code>∫ₐᵇ f(x) dx = f(ξ)(b − a)</code>。这就是积分中值定理。\n在物理中常用它表示平均功率：若功率 <code>P(t)</code> 连续，则 <code>[0, T]</code> 上的平均功率 <code>P̄ = (1/T)∫₀^T P(t) dt</code>。',
          plain: '算术平均数是“把几个数加起来除以个数”，比如三次考试 80、90、100 分，平均 90 分。\n如果数据不是几个数，而是一条连续的曲线（每时每刻都有一个值），那么“加起来”就变成积分，“个数”就变成总长度 <code>b − a</code>。所以平均值就是 <code>∫f(x)dx ÷ (b − a)</code>。\n面积题的角度更好懂：<code>∫ₐᵇ f(x)dx</code> 是曲线下的面积，<code>b − a</code> 是底边宽度，面积除以宽度就是“<b>把这块地推平成矩形后的高度</b>”。这个高度就是平均值。',
          why: '为什么平均值公式里要除以 <code>b − a</code>？因为不除就变成了“总和”，而平均值必须与自变量的量纲无关。举个例子：把区间从 <code>[0, 1]</code> 拉长到 <code>[0, 10]</code>，如果函数值一直是 5，那么积分变成 50，但平均值显然还是 5——只有除以长度才能得到 5。\n积分中值定理的几何意义也非常直白：<b>任何一块曲边梯形的面积，都能找到一个等底等面积的矩形。</b>这个矩形的“高”就是 <code>f(ξ)</code>，它就是平均值。这一步之所以成立，靠的是连续函数介值定理：<code>f</code> 取得最大值 <code>M</code> 与最小值 <code>m</code>，而 <code>ȳ</code> 介于 <code>m</code> 与 <code>M</code> 之间，连续函数必在某点取到它。\n[[tip:平均功率、平均电流、平均速度都可以这样做。例如交变电流 <code>i = I₀ sin(ωt)</code>，一个周期内的平均值为 0（正负抵消），所以工程上更关心“有效值”（均方根），那个要积分 <code>i²</code>，与这里不同。]]',
          proof: '推导：\n<b>1. 用最值夹逼。</b>因 <code>f</code> 在闭区间 <code>[a, b]</code> 上连续，故取到最小值 <code>m</code> 与最大值 <code>M</code>，即 <code>m ≤ f(x) ≤ M</code> 对一切 <code>x ∈ [a, b]</code> 成立。\n<b>2. 积分。</b>由定积分的保序性，<code>m(b − a) ≤ ∫ₐᵇ f(x) dx ≤ M(b − a)</code>。\n<b>3. 除以区间长度。</b>因 <code>b − a &gt; 0</code>，得\n<code>m ≤ (1/(b − a))∫ₐᵇ f(x) dx ≤ M</code>，即 <code>m ≤ ȳ ≤ M</code>。\n<b>4. 用介值定理取到。</b><code>ȳ</code> 落在 <code>f</code> 的最小值与最大值之间，而连续函数的值域是区间，所以必有 <code>ξ ∈ [a, b]</code> 使 <code>f(ξ) = ȳ</code>。\n<b>5. 整理成中值定理的形式。</b><code>∫ₐᵇ f(x) dx = f(ξ)(b − a)</code>。\n<b>6. 与前面公式的关系。</b>注意这里的 <code>ξ</code> 一般<b>不是</b>区间中点，也不是形心位置，它只是“存在”。用它做理论推导很方便，但具体算出 <code>ξ</code> 需要解方程。',
          example: '求 <code>f(x) = sin x</code> 在 <code>[0, π]</code> 与 <code>[0, 2π]</code> 上的平均值。\n<b>[0, π]</b>：<code>ȳ = (1/π)∫₀^π sin x dx = (1/π)[−cos x]₀^π = (1/π)(1 + 1) = 2/π ≈ 0.637</code>。\n检查：<code>sin x</code> 在 <code>[0, π]</code> 上最大是 1，最小值取在端点处为 0，平均值 <code>0.637</code> 落在 <code>[0, 1]</code> 内，合理。\n<b>[0, 2π]</b>：<code>ȳ = (1/(2π))∫₀^{2π} sin x dx = (1/(2π))[−cos x]₀^{2π} = (1/(2π))(1 − 1) = 0</code>。\n解释：正半周与负半周完全对称，互相抵消，平均值为 0。这正是“交流电平均值为零”的数学原因。\n<b>再看一个量纲例子</b>：若 <code>f</code> 是速度（米每秒），<code>∫ₐᵇ f dx</code> 的单位取决于 <code>x</code>。只有在“速度对时间积分得路程，再除以时间得平均速度”这种配套用法里，公式才有物理意义。所以用平均值公式前一定要想清楚：<code>x</code> 是什么物理量。\n<b>平均功率的例子</b>：某电机功率 <code>P(t) = 2 + cos t</code>（千瓦），求 <code>[0, 2π]</code> 小时的平均功率。\n<code>P̄ = (1/(2π))∫₀^{2π}(2 + cos t)dt = (1/(2π))(4π + 0) = 2</code>（千瓦）。这与“常数部分 2、波动部分平均为 0”的直觉完全吻合。',
          pitfalls: [
            '忘记除以 <code>b − a</code>，把积分值直接当成平均值（量纲和数值都不对）。',
            '把 <code>ȳ</code> 误认为一定等于区间中点处的函数值 <code>f((a + b)/2)</code>。中值定理只保证“存在”，不保证“在中点”。',
            '把平均值的 <code>ξ</code> 当成了形心的横坐标。形心用的是 <code>∫x f(x)dx / ∫f(x)dx</code>，与积分中值定理的 <code>ξ</code> 是两回事。',
            '在交流电路中用算术平均代替有效值：<code>sin</code> 的平均值是 0，但有效值（均方根）不是 0，两者用途完全不同。',
          ],
          tags: ['物理应用', '平均值', '积分中值定理'],
          related: ['fml-work-variable-force', 'thm-newton-leibniz', 'def-definite-integral'],
        },
        {
          id: 'ex-pumping-work',
          kind: 'note',
          name: '抽水做功题的标准做法（重力做功视角）',
          statement: '把液体从容器中抽到出口所做的功，等于“液体的重量”乘以“重心被提升的高度”，即\n<code>W = ρg·V·h̄</code>，其中 <code>V</code> 是液体体积，<code>h̄</code> 是液体重心到出口的高度。\n这与用积分 <code>W = ρg∫₀^H (H − x)A(x) dx</code> 的结果完全一致（<code>x</code> 为距容器底的高度，<code>H</code> 为液面到出口的高度，<code>A(x)</code> 为 <code>x</code> 处的水平截面积）。',
          plain: '抽水做功可以这样想：把整池水看成一堆水，每一滴水都要被搬到高处。搬到最高处的水最费劲，本来就在出口的水几乎不用费劲。\n平均下来，“等效于把整池水一次性搬到它的<b>重心</b>所在的高度”。就像搬一堆砖：如果砖叠成一摞，你不需要为最上面那块跑冤枉路——整堆砖的搬运代价等于把整堆砖的质量集中到重心上搬一次。\n所以有两种算法：一种老老实实切成薄层一层层积分，另一种直接算“总重量 × 重心高度”。两个答案必须一样，后者可以用来验算。',
          why: '为什么要专门提这个方法？因为它是最快的验算工具，也是理解功的概念的最好角度。\n积分法（微观）适合计算复杂形状：每一层水的重量是 <code>ρgA(x)dx</code>，它要被提升 <code>(H − x)</code>，所以 <code>dW = ρg(H − x)A(x)dx</code>。\n重心法（宏观）适合快速检查：<code>W = ρgVh̄</code>，只要形状规则（圆柱、长方体、球缺），重心位置很好找。\n两者一致的深层原因是：功的积分本质上就是“质量对位置的加权平均”，这正是重心的定义。所以 <b>重力做功问题里，重心就是那个“等效提升高度”</b>。',
          proof: '推导（证明两种算法等价）：\n<b>1. 积分表达式。</b>以容器底为原点、<code>x</code> 轴向上，液面高度为 <code>h₀</code>，出口高度为 <code>H</code>（<code>H ≥ h₀</code>）。在高度 <code>x</code> 处厚 <code>dx</code> 的一层水，体积 <code>dV = A(x)dx</code>，重量 <code>ρgA(x)dx</code>，需提升的高度是 <code>(H − x)</code>。于是\n<code>W = ρg∫₀^{h₀}(H − x)A(x) dx = ρg[H∫₀^{h₀}A(x)dx − ∫₀^{h₀}xA(x)dx]</code>。\n<b>2. 认出两个积分。</b>其中 <code>V = ∫₀^{h₀}A(x)dx</code> 是水的总体积；而 <code>∫₀^{h₀}xA(x)dx</code> 是水对容器底的静矩，<b>重心的定义</b>就是 <code>x̄ = ∫₀^{h₀}xA(x)dx ÷ V</code>。\n<b>3. 代入。</b><code>W = ρg(HV − x̄V) = ρgV(H − x̄)</code>。注意 <code>H − x̄</code> 正是重心到出口的高度 <code>h̄</code>，所以 <code>W = ρgVh̄</code>，与重心法完全一致。\n<b>4. 几个常用重心。</b>均匀圆柱（水面高 <code>h₀</code>）：重心在 <code>h₀/2</code>。\n半球形容器（半径 <code>R</code>、装满，球心在下）：重心的球坐标位置是 <code>3R/8</code>（从球心向上度量）。用这些值可以秒算答案。',
          example: '把半径为 <code>R</code> 的半球形容器（平面朝上、曲面朝下）中装满的水抽到容器口，求所需做的功。\n<b>建系（第一步，最重要）</b>：设球心在原点、<code>x</code> 轴向上。水装满整个下半球，所以最低点在 <code>x = −R</code>，容器口所在的水平面是 <code>x = 0</code>，水面恰在容器口。<code>x</code> 的范围是 <code>[−R, 0]</code>。\n<b>写截面与提升高度</b>：高度 <code>x</code> 处水面的圆半径是 <code>√(R² − x²)</code>，所以水平截面面积 <code>A(x) = π(R² − x²)</code>。这一层水要提到 <code>x = 0</code> 处，提升高度是 <code>0 − x = −x</code>（因为 <code>x</code> 是负的，所以这是个正数）。\n<b>列积分</b>：<code>W = ρg∫_{−R}^{0} (−x)·π(R² − x²) dx</code>。\n<b>积分</b>：令 <code>u = R² − x²</code>，则 <code>du = −2x dx</code>，即 <code>(−x)dx = du/2</code>。当 <code>x = −R</code> 时 <code>u = 0</code>；当 <code>x = 0</code> 时 <code>u = R²</code>。于是\n<code>W = ρgπ∫₀^{R²} (1/2)u du = ρgπ·(1/2)·[u²/2]₀^{R²} = (1/4)πρgR⁴</code>。\n<b>验算（重心法，非常划算）</b>：水的体积 <code>V = (2/3)πR³</code>。半球的重心到球心的距离是 <code>3R/8</code>，对下半球来说重心在球心<b>下方</b> <code>3R/8</code> 处，即 <code>x = −3R/8</code>。\n重心被提升到 <code>x = 0</code>，提升高度 <code>h̄ = 3R/8</code>。\n所以 <code>W = ρgVh̄ = ρg·(2/3)πR³·(3R/8) = (1/4)πρgR⁴</code>，与积分结果完全一致。\n[[tip:这个例子最值得学的是<b>建系这一步</b>：先画图、定原点、把“提升高度”写成坐标的表达式，再动手积分。抽水题出错绝大多数都出在这一步，而不是积分本身。用重心法验算则是极划算的保险。]]',
          pitfalls: [
            '提升高度写成“到水面的距离”而不是“到出口的距离”。',
            '坐标系原点选错后，圆半径的表达式中 <code>x</code> 的符号出错（如把 <code>R² − x²</code> 写成 <code>x² − R²</code>）。',
            '容器未装满时仍按“装满”计算体积和重心。',
            '把水的密度和重力加速度混算：重量是 <code>ρgV</code>，只写 <code>ρV</code> 得到的是质量。',
            '当容器形状不规则（如台形、球缺）时找不到重心就放弃验算，其实可以用积分算重心，<code>x̄ = ∫xA(x)dx ÷ ∫A(x)dx</code>。',
          ],
          tags: ['物理应用', '做功', '重心', '抽水'],
          related: ['fml-work-variable-force', 'fml-water-pressure', 'fml-average-value'],
        },
    // @@NEXT@@
      ],
    },
  ],
};
