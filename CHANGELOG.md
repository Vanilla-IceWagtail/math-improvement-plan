# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

> 版本说明：`0.x` 表示**接口与内容仍可能变动**。当前内容仍在持续填充
> （部分知识点配套题不足 3 道），因此首个公开版本定为 `0.1.0` 而不是 `1.0.0`。

## [0.1.2] — 2026-09-21

修复一个让"看答案"完全失效的交互 bug。

### 修复

- **做到不会的题时点「不会，直接看答案」，答案不显示**（只能去错题本才看得到）。

  原因是"当前题"一直是**推导**出来的 —— 每次渲染都去找「第一道尚未提交的题」。
  于是任何一次提交（作答或点"不会，直接看答案"）都会让该题立刻变成"已提交"，
  下一次渲染就直接跳到下一题，**答案与解析根本来不及看**。
  错题本之所以正常，是因为它按记录渲染，不依赖"当前题"。

  改法：引入显式游标 `ui.curIndex`，**只有用户点「下一题」时游标才前进**。
  顺带修正：进度条会把"当前题"标出来（即使它已提交）；点「先跳过」后主动前进；
  最后一题点「下一题」正确进入结果页而不越界。

### 测试

- 冒烟测试新增一节 14 项断言，专门覆盖"答题后能看到答案"：
  「不会，直接看答案」路径、正常提交路径、游标推进、最后一题边界。
  断言总数 62 → **76**。
- 顺带修好了测试垫片的一个缺陷：`querySelector` 不支持后代选择器
  （`.modal-head h2` 匹配失败会让 `openModal` 抛异常），使弹窗相关路径在
  测试中跑不通。现在支持按后代组合符匹配。

## [0.1.1] — 2026-09-21

修复一个影响阅读的渲染 bug。

### 修复

- **HTML 实体被二次转义，导致 `ε &gt; 0` 显示成字面量**。
  内容文件里作者手写的 HTML 实体（实测 `&gt;` 265 处、`&lt;` 240 处，合计 **505 处**）
  在渲染时 `&` 被再次转义：
  `作者写 ε &gt; 0` → `ε &amp;gt; 0` → 浏览器显示字面的 `ε &gt; 0`，
  既看不懂也不符合教科书表达。
  现在渲染管线会**先把实体解码回真字符**（`&gt;` → `>`），再统一做唯一一次 HTML 转义。
  新顺序：`decodeEntities → protectMath → escapeKeepAllowed → expandMath → blocks`。
  安全性不受影响（白名单只有 `code/b/i/sup/sub/br/em/strong` 等无害标签，
  用实体无法注入脚本，已加断言覆盖）。
  同时 `docs/content-spec.md` 写明：**直接写 `>` 和 `<`，不要用 HTML 实体**。

## [0.1.0] — 2026-09-21

首个公开版本。线上地址：https://vanilla-icewagtail.github.io/math-improvement-plan/

### 新增

**组题组（版块一）**
- 按知识点出题：选 1~3 个定义/定理，围绕它们组一条难度阶梯
- 四档难度：入门 → 基础 → 提高 → 挑战，做完一档进入下一档
- 也可按章节出题，或全书混合
- 题量三档：小份 6 题 / 中份 8 题 / 大份 12 题
- 支持按题型筛选（选择 / 填空 / 判断 / 计算 / 证明）
- 相同 seed 生成相同题组，可复现、可分享
- 答完给出各难度档正确率、薄弱知识点，并一键"围绕薄弱点再来一组"
- 做错的题**自动**进错题本

**错题本（版块二）**
- 四个标签页：待复习 / 错题 / 收藏 / 已掌握
- 艾宾浩斯间隔复习：5 分钟 → 30 分钟 → 12 小时 → 1 天 → 2 天 → 4 天 → 7 天 → 15 天 → 30 天 → 60 天
- 答对升一档，答错降一档；第 1 档答错则留在第 1 档（5 分钟后马上再来）
- 打开网站时到期自动弹窗提醒，带提示音；20 分钟内不重复弹
- 遮盖式复习卡：先自己回忆 → 显示答案 → 自评「记住了 / 忘了」，支持键盘操作
- 内置遗忘曲线示意图，解释间隔是怎么定的
- 按章节分组、导出 JSON、打印 / 存 PDF

**教材定理定义详细解（版块三）**
- 目录层层点开：章 → 节 → 定义 / 定理
- 每个条目 6 个板块：严谨说法 / 说人话 / 为什么要这样想 / 证明 / 最小例子 / 易错点
- 定义与定理用不同颜色标签区分
- 全文搜索（名称、别名、陈述、解释、证明都能搜）
- 按"只看定义 / 只看定理 / 只看收藏 / 只看错题本"筛选
- 收录同济《高等数学》上册（第八版）全 7 章，共 **204 条**
  （64 定义 / 142 定理 / 39 公式 / 31 补充），其中 142 个定理全部附完整证明

**数学记号排版**
- 极限号渲染成**教科书样式**：`lim` 与趋近条件上下居中，而不是写成 `lim(x→0)`
- 自动识别四种写法：`lim(x→0)` / `lim[x→0]` / `lim_{n→∞}` / `lim<sub>x→a</sub>`
- 支持单侧极限 `lim(x→0<sup>+</sup>)`
- 前置式 `lim f(x)` 保持并列（教科书里这类极限不与表达式下标化）
- 答题框上方提供**常用符号插入栏**：`< > ≤ ≥ ≠ → ∞ ε δ Δ π ∫ √ ∈ ± ′`
  （中文输入法下打不出 `<` 时的替代方案，点一下插入到光标处）

**通用**
- 每个定义、定理、题目右上角两个可点亮图标：⭐ 收藏、📕 加入错题本
- 深色 / 浅色 / 跟随系统主题
- 快捷键：`1` `2` `3` 切版块，`/` 搜索，复习时空格显示答案
- 数据只存本地 localStorage，可导出 / 导入 JSON 备份
- 打印样式表，错题本与教材条目可直接打印或存 PDF
- 手机端底部导航

**题库与内容**
- 内置 194 道原创题目（CC-BY-4.0），覆盖全部 7 章，每章都有四档难度分布
- `data/concept-aliases.js` 知识点别名表：题库与教材命名不一致时自动对齐

**实时题库（可选）**
- 「数据与设置」里可填外部 JSON 地址，实时拉取合并
- 带字段校验，格式不对的题会被跳过并报告
- `scripts/fetch-open-bank.mjs` 开放许可题库转换管道
- 不内置、不抓取任何第三方题库

**在线站点与工程**
- GitHub Pages 自动部署：CI 通过后才发布，站点只含 `index.html`/`styles`/`js`/`data`
- GitHub Actions CI（Node 20 与 22 双版本矩阵）：零依赖校验 → 内容校验 →
  数学排版测试 → 端到端冒烟测试 → README 事实核对 → 社区文件校验
- `scripts/serve.mjs` 零依赖静态服务器
- `scripts/validate-content.mjs` 内容校验器（id 唯一性、必填字段、引用完整性、富文本标记、LaTeX 泄漏检测）
- `scripts/test-math.mjs` 数学排版测试（单元用例 + 全部内容字符串回归）
- `scripts/smoke-test.mjs` 端到端冒烟测试（62 项断言，Node + 轻量 DOM 垫片）
- `scripts/verify-readme.mjs` README 事实核对（88 项断言：条目计数、每章题目分布、
  题组/题目/复习示例、导出 JSON 结构、许可与 CI 配置、引用的文件路径）
- `scripts/verify-community.mjs` 社区文件校验（32 项断言：issue 表单结构、
  标签引用、行为准则与安全策略）
- `scripts/ci-actions.mjs` CI 所用 Actions 的版本清单（防止退回 deprecated 版本）
- `tools/ghp.ps1` / `tools/ghp.cmd` 给 gh 临时注入代理的包装脚本
  （不改动系统环境变量）
- `npm run check` 串起全部五项检查，全部通过才返回 0
- 开源配套：BSD 3-Clause（代码）+ CC-BY-4.0（内容）、README、CONTRIBUTING、
  行为准则（Contributor Covenant 3.0）、安全策略、issue 表单、PR 模板、
  15 个中文化 issue 标签
- `docs/content-spec.md` 内容数据规范、`docs/legal.md` 版权与合规说明

### 修复

- **错题本答错的回退规则与设计意图不符**：原实现在 `phase = 0`（刚加入错题本）时答错会
  跳到 `phase = 1`，导致下次复习要等 30 分钟；而代码注释与用户预期都是"5 分钟后再来一遍"。
  现改为答对升一档、答错降一档、第 1 档答错留在第 1 档。
  修正后各档位行为：`0→0`（5 分钟）、`1→0`、`3→2`（12 小时）、`9→8`、`10→9`。
  `nextDuePreview()` 里的同一处逻辑也已同步修正。
- **README 里的条目分类计数写错**：文档曾写作"54 定义 / 102 定理 / 23 公式 / 25 补充"，
  而数据文件里一直是 **64 / 142 / 39 / 31**（总数 204 不变，是文档写错了）。
  为防止这类漂移，新增 `npm run verify-readme` 把文档断言纳入自动校验。
- **LICENSE 被 GitHub 识别成 "Other"**：原因是文件末尾附加了中文范围说明，
  干扰了许可证的全文匹配。现 `LICENSE` 只保留 BSD 3-Clause 官方纯正文，
  范围说明移到 `LICENSE-SCOPE.md`。
- **CI 的 actions 使用了 node20 运行时**：`actions/checkout@v4` 与
  `actions/setup-node@v4` 会触发 Node.js 20 deprecated 警告，已升到 v5。
- **JS 与 HTML 注入面**：渲染管线改为"全量 HTML 转义 + 白名单标签还原"，
  并确认导入的外部题库无法注入脚本。

### 变更

- **代码许可由 MIT 改为 BSD 3-Clause**（`LICENSE`、`package.json` 的 `license` 字段、
  README 与 `docs/legal.md` 已同步）。BSD 3-Clause 与 MIT 一样允许自由使用、修改、
  分发和商用，额外多一条限制：**不得用本项目的名义或贡献者的名字为衍生产品背书**；
  同时要求二进制分发时在文档中复现版权声明与免责声明。
  注意：`data/` 目录下的**原创内容**仍然走 CC-BY-4.0，不受此次变更影响。

### 技术特点

零依赖、零构建、零后端。没有 npm 包、没有框架、没有 CDN。
图标是内联 SVG，公式用 Unicode + `<code>`，音效用 Web Audio 合成。
除 `file://` 外任何静态服务器都能跑，也可直接部署到 GitHub Pages。

### 已知限制

- **内容仍在填充**：194 道题摊到 200+ 个知识点上，**143 个知识点配套题少于 3 道**，
  组题阶梯可能出现档位跳空。`npm run validate` 的备注区会列出这些知识点。
- **填空题判分是字符串比对**（已做全角/半角、括号、空白的归一化），
  **不做法等价性判断**。`1/3` 与 `0.333` 这类差异会被判错，
  可能被误记进错题本，手动移出即可。
- **复习间隔是固定表**，不根据个人答题速度或正确率自适应。
- **每日目标设置只保存、未参与统计**（预留给后续版本）。
- **只内置一本教材**（同济《高等数学》上册）。加新书不需要改代码，
  在 `data/books/registry.js` 登记即可。
- **视觉未经自动化验证**：无头浏览器在开发环境中不可用，
  功能由 62 项端到端断言覆盖，但配色与排版需要人工过目。
- **不等式 `a<b>c` 会被当成加粗标签**（`<b>` 是内容白名单标签）。
  请写作 `a < b`（两侧留空格）。
