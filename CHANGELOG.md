# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 变更

- **代码许可由 MIT 改为 BSD 3-Clause**（`LICENSE`、`package.json` 的 `license` 字段、
  README 与 `docs/legal.md` 已同步）。
  BSD 3-Clause 与 MIT 一样允许自由使用、修改、分发和商用，额外多一条限制：
  **不得用本项目的名义或贡献者的名字为衍生产品背书**；同时要求二进制分发时
  在文档中复现版权声明与免责声明。
  注意：`data/` 目录下的**原创内容**仍然走 CC-BY-4.0，不受此次变更影响。

## [1.0.0] — 首个可用版本

### 新增

**组题组（版块一）**
- 按知识点出题：选 1~3 个定义/定理，围绕它们组一条难度阶梯
- 四档难度：入门 → 基础 → 提高 → 挑战，做完一档进入下一档
- 也可按章节出题，或全书混合
- 题量三档：小份 6 题 / 中份 8 题 / 大份 12 题
- 支持按题型筛选（选择 / 填空 / 判断 / 计算 / 证明）
- 相同 seed 生成相同题组，可复现、可分享
- 答完给出各难度档正确率、薄弱知识点，并一键"围绕薄弱点再来一组"
- 做错的题**自动**进错题本

**错题本（版块二）**
- 四个标签页：待复习 / 错题 / 收藏 / 已掌握
- 艾宾浩斯间隔复习：5 分钟 → 30 分钟 → 12 小时 → 1 天 → 2 天 → 4 天 → 7 天 → 15 天 → 30 天 → 60 天
- 答对进下一档，答错打回上一档并记一次卡壳
- 打开网站时到期自动弹窗提醒，带提示音；20 分钟内不重复弹
- 遮盖式复习卡：先自己回忆 → 显示答案 → 自评「记住了 / 忘了」，支持键盘操作
- 内置遗忘曲线示意图，解释间隔是怎么定的
- 按章节分组、导出 JSON、打印 / 存 PDF

**教材定理定义详细解（版块三）**
- 目录层层点开：章 → 节 → 定义 / 定理
- 每个条目 6 个板块：严谨说法 / 说人话 / 为什么要这样想 / 证明 / 最小例子 / 易错点
- 定义与定理用不同颜色标签区分
- 全文搜索（名称、别名、陈述、解释、证明都能搜）
- 按"只看定义 / 只看定理 / 只看收藏 / 只看错题本"筛选
- 收录同济《高等数学》上册（第八版）全 7 章，共 204 条（54 定义 / 102 定理 / 23 公式 / 25 补充）

**通用**
- 每个定义、定理、题目右上角两个可点亮图标：⭐ 收藏、📕 加入错题本
- 深色 / 浅色 / 跟随系统主题
- 快捷键：`1` `2` `3` 切版块，`/` 搜索，复习时空格显示答案
- 数据只存本地 localStorage，可导出 / 导入 JSON 备份
- 打印样式表，错题本与教材条目可直接打印或存 PDF
- 手机端底部导航

**题库与内容**
- 内置 194 道原创题目（CC-BY-4.0），覆盖全部 7 章，每章都有四档难度分布
- `data/concept-aliases.js` 知识点别名表：题库与教材命名不一致时自动对齐

**实时题库（可选）**
- 「数据与设置」里可填外部 JSON 地址，实时拉取合并
- 带字段校验，格式不对的题会被跳过并报告
- `scripts/fetch-open-bank.mjs` 开放许可题库转换管道
- 不内置、不抓取任何第三方题库

**开发工具**
- `scripts/serve.mjs` 零依赖静态服务器
- `scripts/validate-content.mjs` 内容校验器（id 唯一性、必填字段、引用完整性、富文本标记、LaTeX 泄漏检测）
- `scripts/smoke-test.mjs` 端到端冒烟测试（44 项断言，Node + 轻量 DOM 垫片）
- `docs/content-spec.md` 内容数据规范
- `docs/legal.md` 版权与合规说明
- `scripts/verify-readme.mjs` README 事实核对（59 项断言：条目计数、每章题目分布、
  题组/题目/复习示例、导出 JSON 结构、引用的文件路径是否存在）
- `npm run check` 串起「内容校验 + 冒烟测试 + README 核对」三项，全部通过才返回 0

### 修复

- **错题本答错的回退规则与设计意图不符**：原实现在 `phase = 0`（刚加入错题本）时答错会
  跳到 `phase = 1`，导致下次复习要等 30 分钟；而代码注释与用户预期都是"5 分钟后再来一遍"。
  现改为：**答对升一档，答错降一档；第 1 档答错则留在第 1 档（5 分钟后马上再来）**。
  修正后各档位行为：`0→0`（5 分钟）、`1→0`、`3→2`（12 小时）、`9→8`、`10→9`。
  `nextDuePreview()` 里的同一处逻辑也已同步修正。
- **README 中的条目分类计数写错**：原写作"54 定义 / 102 定理 / 23 公式 / 25 补充"，
  实际为 **64 定义 / 142 定理 / 39 公式 / 31 补充**（总数 204 不变）。
  为防止这类漂移，新增 `npm run verify-readme` 把文档断言纳入自动校验。

### 技术特点

零依赖、零构建、零后端。没有 npm 包、没有框架、没有 CDN。
图标是内联 SVG，公式用 Unicode + `<code>`，音效用 Web Audio 合成。
`file://` 之外任何静态服务器都能跑，也可直接部署到 GitHub Pages。

### 已知限制

- 填空题判分是经过归一化的字符串比对，不做法等价性判断；
  写法差异大的正确答案可能被误判为错（会记进错题本，不影响学习，但需要手动移出）。
- 复习间隔是固定表，不根据个人答题速度自适应。
- 内置题库目前只覆盖《高等数学》上册，且题目总量偏少；
  部分知识点配套题不足 3 道（`npm run validate` 会列出来）。
