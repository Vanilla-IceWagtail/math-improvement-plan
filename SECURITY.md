# 安全策略

## 这个项目的安全模型（先读这一节）

本项目是一个**纯静态、零后端、零账号**的网页应用，这会显著缩小攻击面：

| 特性 | 安全含义 |
| --- | --- |
| 没有服务器、没有数据库 | 不存在服务器被入侵、SQL 注入、越权访问这类问题 |
| 没有账号、没有登录 | 不存在密码泄露、会话劫持、凭据填充 |
| 没有遥测、没有第三方脚本 | 没有 CDN 被投毒、供应链脚本窃取数据的风险 |
| 学习数据只存浏览器 localStorage | 数据不出本机；但也意味着**清空浏览器数据等于丢失进度**，请定期「导出全部数据」备份 |
| 零 npm 依赖 | 没有依赖漏洞（`npm audit` 级别的问题）可言 |

因此真正值得关注的只有**一个方向**：**注入**。也就是"恶意内容通过渲染管线变成可执行脚本"。

## 支持的范围

| 项目 | 状态 |
| --- | --- |
| GitHub Pages 上的版本（`main` 分支最新） | ✅ 接受报告 |
| 本地 `npm start` 跑起来的版本 | ✅ 接受报告 |
| 旧的、已过时的 commit | ❌ 请先确认在最新 `main` 上可复现 |
| 第三方题库来源本身的安全性 | ❌ 不在本项目范围（属于你引入的外部内容） |

## 报告漏洞

**请不要用公开 issue 报告安全问题**，那会让问题在被修复前就暴露给所有人。

请用以下任一私密渠道：

1. **GitHub 私密漏洞报告**（推荐）
   打开 https://github.com/Vanilla-IceWagtail/math-improvement-plan/security/advisories/new
   填入复现步骤。只有维护者能看到。
2. **邮件**：`q1433223123123@163.com`

报告里如果能有这些信息会很有帮助：

- 受影响的版本 / commit
- 复现步骤（越具体越好，最好有最小可复现的输入）
- 影响是什么（能执行脚本？能读取数据？能伪造界面？）
- 如果有 PoC 或截图，一并附上

## 响应时间

| 阶段 | 目标 |
| --- | --- |
| 确认收到 | 7 天内 |
| 初步判断与影响评估 | 14 天内 |
| 修复并发布 | 视严重程度而定，会持续同步进展 |

这是个人维护的开源项目，没有 7×24 值班。但每一个有效报告都会被认真对待，
修复后会在 [CHANGELOG](CHANGELOG.md) 里说明。

## 已知的、按设计接受的行为（不算漏洞）

为了避免浪费你的时间，以下是我们**已知且有意为之**的，不算安全问题：

1. **导入的题库内容会被当作 HTML 渲染。**
   渲染管线在 `js/text.js`：先对内容做**全量 HTML 转义**，然后只把白名单标签
   （`code / b / i / sup / sub / br / em / strong`）还原回来。事件属性（`onclick` 等）、
   `<script>`、`<img>`、`<iframe>` 都不在白名单里，因此**无法注入脚本**。
   如果某个导入的题库在页面上显示出奇怪的排版，那是格式问题，不是漏洞。

2. **「实时题库」会请求你在设置里填写的地址。**
   这是功能本身。请只填你自己信任（或你自己搭建）的地址。

3. **localStorage 可以被本机用户或浏览器扩展读取。**
   这是浏览器的设计，任何纯前端应用都一样。请不要把敏感信息存进本站。

4. **没有内容安全策略（CSP）响应头。**
   GitHub Pages 不允许自定义响应头。如果你需要更严格的隔离，
   请自行部署一份并加上 CSP。

5. **页面里出现"数学错误"或"错别字"。**
   这是**内容问题**，不是安全问题。请提普通 issue，或用「内容纠错」模板。

## 修复后的致谢

如果你愿意，我们会在 CHANGELOG 里写明感谢。
若你希望保持匿名，我们也完全尊重。
